# 主壳精简：模块轨 + 顶栏瘦身 + ⌘K Implementation Plan（绿色项③a）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实施。`- [ ]` 复选框跟踪。

**Goal:** 给主壳左侧加一条 ~46px 的**模块轨**（按权限显示的一级模块图标，当前模块高亮，悬停显示模块名，点击进该模块入口路由）——补上"目前没有常驻一级模块切换控件"的空缺，给低频用户/管理者空间感与可发现性；顶栏**瘦身**（把刷新/全屏收进用户下拉）；**核验 ⌘K 命令面板**（现有 `GlobalSearch` 已绑 Ctrl+K，确认其可搜索/跳转任意页，必要时补模块跳转项）。

**Architecture:** 新建 `ModuleRail.vue` 作为 `.app-main` 的首个 flex 子项（`SmartSidebar` 之前）。数据 = `MODULE_TABS`(app.ts) 过滤 `permissionStore.getModuleVisibility(权限)`（workhub 恒显）；active 由 `appStore.currentModule` 决定；点击 `router.push(mod.route)`。顶栏把刷新/全屏移入用户下拉菜单。`layout.scss` 调 `.app-main` 与 `.topbar-context` 的宽度耦合（新增 `--rail-width`）。

**Tech Stack:** Vue 3 + Pinia + Ant Design Vue（`a-tooltip`）。**前端无单测**，验证 `cd web && npm run build` + 预览；样式禁裸 hex（令牌 + 过 stylelint）；`vue-tsc` 基线红不当门禁。

---

## 关键坐标（行号以实际为准，先读相关文件）

- `web/src/layouts/MainLayout.vue`（82 行）：`.app-main`(L12) 含 `<SmartSidebar />`(L14) + `.content-area`(L17)。**模块轨插在 L14 之前**。
- `web/src/styles/layout.scss`：`.app-main` L11-16（`display:flex;flex:1`）、`.topbar-context` 宽 `calc(var(--sidebar-width) - 29px)` L37。
- `web/src/layouts/TopBar.vue`（729 行）：刷新按钮 L87-89（`refreshPage()`→`orgContextStore.triggerPageRefresh()`）、全屏按钮 L90-93（`toggleFullscreen()`）、用户下拉 a-dropdown L94-114（菜单项：个人设置 L106 / 系统管理 L107-109(受 `hasAdminPermission`) / 退出 L111）。
- `web/src/stores/app.ts`：`MODULE_TABS` L16-34（17 项，`{ code, name, route, icon?, alwaysShow?, noHomePage? }`）；`useAppStore` `currentModule`(L39，默认 'workhub') / `setCurrentModule`(L55) / `isWorkhub`(L60)。
- `web/src/stores/permission.ts`：`getModuleVisibility(permissions)` L355-367（返回 `Record<模块key, boolean>`，覆盖 `MODULE_CONFIG` 的 16 个 key，**不含 workhub**）；`hasAdminAccess` L483。
- `web/src/stores/user.ts`：`userStore.permissions`(string[])。
- `MODULE_TABS` 17 项 code/icon：workhub/HomeOutlined、finance/AccountBookOutlined、cardflow/ApartmentOutlined、crm/UserOutlined、hr/TeamOutlined、dormitory/HomeOutlined、vehicle/CarOutlined、contract/FileTextOutlined、supplier/ShopOutlined、points/TrophyOutlined、task/CheckSquareOutlined、express/SendOutlined、insurance/SafetyOutlined、quality/SafetyCertificateOutlined、conference/ScheduleOutlined、report/BarChartOutlined、system/SettingOutlined。
- ⌘K：`web/src/components/GlobalSearch.vue` + `useCommandPalette()`；顶栏搜索框 L69-80（`openGlobalSearch()`→`useCommandPalette().open()`，含 Ctrl+K/⌘K kbd 提示）。

---

## Task 1: 新建 ModuleRail.vue

**Files:** Create `web/src/layouts/ModuleRail.vue`

- [ ] **Step 1: 创建组件**

```vue
<template>
  <nav class="module-rail" aria-label="模块导航">
    <a-tooltip
      v-for="mod in visibleModules"
      :key="mod.code"
      :title="mod.name"
      placement="right"
    >
      <button
        type="button"
        class="module-rail__item"
        :class="{ 'is-active': mod.code === appStore.currentModule }"
        @click="goModule(mod)"
      >
        <component :is="iconOf(mod)" class="module-rail__icon" />
      </button>
    </a-tooltip>
  </nav>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRouter } from 'vue-router'
import {
  HomeOutlined, AccountBookOutlined, ApartmentOutlined, UserOutlined,
  TeamOutlined, CarOutlined, FileTextOutlined, ShopOutlined, TrophyOutlined,
  CheckSquareOutlined, SendOutlined, SafetyOutlined, SafetyCertificateOutlined,
  ScheduleOutlined, BarChartOutlined, SettingOutlined, AppstoreOutlined,
} from '@ant-design/icons-vue'
import { useAppStore, MODULE_TABS, type ModuleTab } from '@/stores/app'
import { usePermissionStore } from '@/stores/permission'
import { useUserStore } from '@/stores/user'

const router = useRouter()
const appStore = useAppStore()
const permissionStore = usePermissionStore()
const userStore = useUserStore()

const iconMap: Record<string, any> = {
  HomeOutlined, AccountBookOutlined, ApartmentOutlined, UserOutlined,
  TeamOutlined, CarOutlined, FileTextOutlined, ShopOutlined, TrophyOutlined,
  CheckSquareOutlined, SendOutlined, SafetyOutlined, SafetyCertificateOutlined,
  ScheduleOutlined, BarChartOutlined, SettingOutlined,
}
function iconOf(mod: ModuleTab) {
  return (mod.icon && iconMap[mod.icon]) || AppstoreOutlined
}

const visibleModules = computed(() => {
  const vis = permissionStore.getModuleVisibility(userStore.permissions) as Record<string, boolean>
  return MODULE_TABS.filter((m) => m.code === 'workhub' || vis[m.code])
})

function goModule(mod: ModuleTab) {
  appStore.setCurrentModule(mod.code)
  router.push(mod.route)
}
</script>

<style scoped>
.module-rail {
  flex: 0 0 var(--rail-width, 46px);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 8px 0;
  background: var(--sidebar-bg);
  border-right: 1px solid var(--border);
  overflow-y: auto;
}
.module-rail__item {
  width: 34px;
  height: 34px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  border-radius: var(--radius-md);
  background: transparent;
  color: var(--text-2);
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
}
.module-rail__item:hover {
  background: var(--sidebar-item-hover);
  color: var(--text-1);
}
.module-rail__item.is-active {
  color: var(--color-primary);
  background: var(--bg-muted);
}
.module-rail__icon {
  font-size: 18px;
}
</style>
```
（`ModuleTab` 类型从 `app.ts` 导出；若未导出则改为本地 `import type` 或 inline 类型。`--sidebar-bg`/`--sidebar-item-hover`/`--text-1/2`/`--border`/`--bg-muted`/`--color-primary`/`--radius-md` 均为既有令牌。`getModuleVisibility` 若签名要求传参就传 `userStore.permissions`，内部实际用 menus 不影响。）

- [ ] **Step 2: 构建验证** — `cd web && npm run build` 成功。

- [ ] **Step 3: Commit** — `git add web/src/layouts/ModuleRail.vue` → `feat(shell): 新增左侧模块轨 ModuleRail(按权限显示一级模块)`（末加 Co-Authored-By）。

---

## Task 2: 挂载模块轨 + 布局对齐

**Files:**
- Modify: `web/src/layouts/MainLayout.vue`
- Modify: `web/src/styles/layout.scss`

- [ ] **Step 1: MainLayout 挂载模块轨**

在 `.app-main`(L12) 内、`<SmartSidebar />`(L14) 之前插入 `<ModuleRail />`，并 import：
```vue
<div class="app-main">
  <ModuleRail />
  <SmartSidebar />
  <div class="content-area"> ... </div>
</div>
```
```ts
import ModuleRail from './ModuleRail.vue'
```

- [ ] **Step 2: layout.scss 顶栏左段对齐**

新增 `--rail-width: 46px`（与组件默认一致，建议加在 `:root` 或 layout.scss 顶部已有变量处）。把 `.topbar-context` 的宽度从 `calc(var(--sidebar-width) - 29px)` 改为：
```scss
.topbar-context {
  width: calc(var(--rail-width, 46px) + var(--sidebar-width) - 29px);
}
```
（让顶栏左段宽度 = 模块轨 + 侧栏，保持品牌/折叠按钮与下方左侧总宽对齐。`.app-main` 是 flex，`ModuleRail` 自带 `flex:0 0 var(--rail-width)`，无需改 `.app-main`。）

- [ ] **Step 3: 构建 + 预览结构**

Run: `cd web && npm run build` 成功。
（预览见 Task 4；本步只确认 build 与无样式报错。）

- [ ] **Step 4: Commit** — `git add web/src/layouts/MainLayout.vue web/src/styles/layout.scss` → `feat(shell): 主壳挂载模块轨 + 顶栏左段宽度对齐`。

---

## Task 3: 顶栏瘦身（刷新/全屏移入用户下拉）

**Files:** Modify `web/src/layouts/TopBar.vue`

- [ ] **Step 1: 删除独立的刷新/全屏按钮**

删除模板里刷新按钮(L87-89)与全屏按钮(L90-93)两个独立按钮。**保留** `refreshPage()`、`toggleFullscreen()` 两个方法（改由下拉菜单触发）。

- [ ] **Step 2: 在用户下拉菜单中加两项**

在用户 a-dropdown 的菜单(L94-114)里，"个人设置"上方或合适位置加：
```html
<a-menu-item key="refresh" @click="refreshPage">
  <template #icon><ReloadOutlined /></template>
  刷新页面
</a-menu-item>
<a-menu-item key="fullscreen" @click="toggleFullscreen">
  <template #icon><FullscreenOutlined /></template>
  全屏
</a-menu-item>
<a-menu-divider />
```
确保 `ReloadOutlined`/`FullscreenOutlined`（或文件原本用于刷新/全屏的图标名）已 import；若原按钮用的是别的图标组件，复用之。

- [ ] **Step 3: 构建验证** — `cd web && npm run build` 成功，无 `refreshPage`/`toggleFullscreen` 未引用告警导致的问题。

- [ ] **Step 4: Commit** — `git add web/src/layouts/TopBar.vue` → `refactor(shell): 顶栏瘦身-刷新/全屏移入用户下拉`。

---

## Task 4: ⌘K 命令面板核验

**Files:** 仅核验，必要时 Modify `web/src/components/GlobalSearch.vue`

- [ ] **Step 1: 读 `GlobalSearch.vue` + `useCommandPalette`**，确认：⌘K/Ctrl+K 能打开；面板支持**搜索并跳转任意页/菜单**（不只是搜数据）。

- [ ] **Step 2:**
  - 若 `GlobalSearch` 已是"可跳转任意页"的命令面板（消费 `getModuleMenuGroups`/路由）→ 本任务**无需改代码**，仅在汇报中确认其能力；可顺手确认顶栏搜索框 kbd 提示为 `⌘K`/`Ctrl+K`（L79）显示正确。
  - 若它只搜数据、不能跳页 → 在汇报中**停下来说明**，把"扩成命令面板"作为单独决策交回（不在本绿色项擅自扩）。

- [ ] **Step 3:** 若有改动则 commit `refactor(shell): ⌘K 命令面板入口核验`；否则跳过。

---

## Task 5: 端到端预览验证

**Files:** 无。**注意**：主树并发会话/共享库/端口占用时预览不可信，待环境空闲。

- [ ] 起前后端，登录进主页。核对：
  - 左侧模块轨出现，只显示**当前用户有权限**的模块；当前模块图标高亮；悬停显示模块名；点击跳到该模块入口、轨高亮跟随。
  - 顶栏不再有独立刷新/全屏按钮；用户下拉里有"刷新页面"/"全屏"且生效。
  - ⌘K 打开命令面板、能跳页。
  - 模块轨 + 侧栏 + 内容三段对齐，顶栏左段不错位。
- [ ] 截图留证。

---

## Self-Review
- **Spec 覆盖**：模块轨 = Task 1+2；顶栏瘦身 = Task 3；⌘K = Task 4（核验为主，不擅自扩）。✅
- **占位符**：无；"以实际为准"点（ModuleTab 导出、令牌名、图标名、getModuleVisibility 签名）均给定位法。✅
- **一致性**：`--rail-width` 在组件(默认46)与 layout.scss(.topbar-context 计算)两处一致；active 用 `appStore.currentModule`，与 MainLayout 既有 `updateCurrentModuleFromRoute` 同源。✅
- **风险**：顶栏左段对齐是视觉细节，预览时重点看；`getModuleVisibility` 不含 workhub 已特判恒显。

## 范围外（后续/红黄色项）
- 模块轨悬停展开二级子页 flyout、可折叠、拖拽排序。
- ⌘K 从"搜索"升级为"执行命令"（红色项）。
- 顶栏前进/后退、页签、公告的进一步精简（按需，本计划只动刷新/全屏）。
