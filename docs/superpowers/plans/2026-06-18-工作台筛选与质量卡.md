# 工作台首页：合并来源筛选 + 统一质量卡 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把工作台首页的同维度双来源筛选合并为单一多选 chip，并把两个撞名的质量部件统一为分域两行的「质量」卡 + 超时门控的紧急条。

**Architecture:** 新增 `useQualitySummary` 模块级单例 composable 作两域质量摘要的单一真源（一处轮询），由右栏质量卡与中栏紧急条共享；来源筛选去掉复选框行，chip 直接驱动 `hub.filters.value.sources`（多选）。

**Tech Stack:** Vue 3 `<script setup>` + Ant Design Vue + Pinia/composable 单例；令牌 SCSS 变量。

## 验证方式（本仓库特例）

本前端**无单元测试运行器，且约定不引入测试框架**（见记忆「前端 type-check 基线红」）。因此每个任务以以下替代 TDD：

- **编译门禁**：`cd web && npm run build`（vite/esbuild，会因改动文件的真实语法/导入/模板错误而失败；不会被 baseline 类型红淹没）。
- **样式门禁**（仅改 `<style>` 的任务）：`cd web && npm run lint:style`（stylelint，强制无裸 hex）。
- **手动验证**：用 restart-dev / preview 工作流在浏览器核对行为（每个任务列出具体核对点）。

所有命令默认在 `web/` 目录执行。提交信息统一以 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 结尾。

---

### Task 1: 新增 useQualitySummary（两域质量摘要单一真源）

**Files:**
- Create: `web/src/composables/useQualitySummary.ts`

- [ ] **Step 1: 写 composable**

```ts
/**
 * useQualitySummary — 工作台质量摘要单一真源
 *
 * 聚合两个不同子系统的质量摘要，供右栏「质量」卡与中栏紧急条共享：
 *   - 异常域：getQualityDashboardStats() → 通用异常框架 (/quality/exceptions)
 *   - 数据域：getWorkHubQualitySummary() → 数据质量中心 (/express/quality-center)
 *
 * 模块级单例 + 引用计数：首个消费组件挂载时拉取并启动 5 分钟轮询，
 * 最后一个卸载时停止，避免两组件各自轮询导致口径分叉与重复请求。
 */
import { ref, computed, onMounted, onUnmounted } from 'vue'
import { getQualityDashboardStats } from '@/api/quality'
import { getWorkHubQualitySummary } from '@/api/qualityCenter'

interface DomainSummary {
  pending: number
  overdue: number
}

const exception = ref<DomainSummary>({ pending: 0, overdue: 0 })
const dataQuality = ref<DomainSummary>({ pending: 0, overdue: 0 })
const loading = ref(false)

const hasOverdue = computed(
  () => exception.value.overdue > 0 || dataQuality.value.overdue > 0
)

let timer: ReturnType<typeof setInterval> | null = null
let refCount = 0

async function refresh() {
  loading.value = true
  try {
    const [exRes, dqRes] = await Promise.allSettled([
      getQualityDashboardStats(),
      getWorkHubQualitySummary(),
    ])
    if (exRes.status === 'fulfilled' && exRes.value) {
      const v = exRes.value as any
      exception.value = { pending: v.pendingCount ?? 0, overdue: v.overdueCount ?? 0 }
    }
    if (dqRes.status === 'fulfilled' && dqRes.value) {
      const v = dqRes.value as any
      dataQuality.value = { pending: v.pendingTotal ?? 0, overdue: v.overdueWarning ?? 0 }
    }
  } finally {
    loading.value = false
  }
}

export function useQualitySummary() {
  onMounted(() => {
    refCount++
    if (refCount === 1) {
      refresh()
      timer = setInterval(refresh, 5 * 60 * 1000)
    }
  })
  onUnmounted(() => {
    refCount--
    if (refCount <= 0) {
      refCount = 0
      if (timer) {
        clearInterval(timer)
        timer = null
      }
    }
  })
  return { exception, dataQuality, loading, hasOverdue, refresh }
}
```

- [ ] **Step 2: 编译门禁**

Run: `cd web && npm run build`
Expected: 构建成功（新文件可被解析；此任务尚无消费方，仅验证语法/导入正确）。

- [ ] **Step 3: Commit**

```bash
git add web/src/composables/useQualitySummary.ts
git commit -m "feat(workhub): 新增 useQualitySummary 两域质量摘要单一真源(单例+引用计数轮询)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 右栏 QualitySummaryCard 重构为分域两行「质量」卡

**Files:**
- Modify: `web/src/views/workhub/QualitySummaryCard.vue`（整文件替换 script+template+style）

- [ ] **Step 1: 替换 `<script setup>`**

将文件顶部 `<script setup lang="ts"> … </script>` 整段替换为：

```ts
<script setup lang="ts">
/**
 * QualitySummaryCard.vue — 工作台右栏「质量」卡
 *
 * 按域两行展示，数据来自共享的 useQualitySummary 单一真源：
 *   - 异常处理 → /quality/exceptions
 *   - 运单与仓储数据 → /express/quality-center/dashboard
 */
import { useRouter } from 'vue-router'
import {
  WarningOutlined,
  AlertOutlined,
  DatabaseOutlined,
  RightOutlined,
} from '@ant-design/icons-vue'
import { useQualitySummary } from '@/composables/useQualitySummary'

const router = useRouter()
const { exception, dataQuality, loading } = useQualitySummary()

function goExceptions() {
  router.push('/quality/exceptions')
}
function goDataQuality() {
  router.push('/express/quality-center/dashboard')
}
</script>
```

- [ ] **Step 2: 替换 `<template>`**

将整段 `<template> … </template>` 替换为：

```html
<template>
  <a-spin :spinning="loading" size="small">
    <div class="quality-card">
      <div class="card-header">
        <WarningOutlined class="card-header__icon" />
        <span class="card-header__title">质量</span>
      </div>

      <div class="quality-row" @click="goExceptions">
        <AlertOutlined class="row-icon" />
        <div class="row-main">
          <div class="row-title">异常处理</div>
          <div class="row-sub">通用异常框架</div>
        </div>
        <div class="row-metrics">
          <span class="metric" :class="{ muted: exception.pending === 0 }">待处理 {{ exception.pending }}</span>
          <span class="metric metric--minor" :class="{ danger: exception.overdue > 0 }">· 超时 {{ exception.overdue }}</span>
        </div>
        <RightOutlined class="row-arrow" />
      </div>

      <div class="quality-row" @click="goDataQuality">
        <DatabaseOutlined class="row-icon" />
        <div class="row-main">
          <div class="row-title">运单与仓储数据</div>
          <div class="row-sub">数据质量中心</div>
        </div>
        <div class="row-metrics">
          <span class="metric" :class="{ muted: dataQuality.pending === 0 }">待处理 {{ dataQuality.pending }}</span>
          <span class="metric metric--minor" :class="{ danger: dataQuality.overdue > 0 }">· 超时 {{ dataQuality.overdue }}</span>
        </div>
        <RightOutlined class="row-arrow" />
      </div>
    </div>
  </a-spin>
</template>
```

- [ ] **Step 3: 替换 `<style scoped>`**

将整段 `<style scoped> … </style>` 替换为（全部用令牌、无裸 hex）：

```css
<style scoped>
.quality-card {
  background: var(--bg-card);
  border-bottom: 1px solid var(--border);
}
.card-header {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 14px 20px 10px;
}
.card-header__icon {
  font-size: 14px;
  color: var(--biz-quality);
}
.card-header__title {
  font-size: 15px;
  font-weight: 600;
  color: var(--text-1);
}
.quality-row {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 20px;
  border-top: 1px solid var(--border);
  cursor: pointer;
  transition: background 0.2s;
}
.quality-row:hover {
  background: var(--bg-muted);
}
.row-icon {
  font-size: 18px;
  color: var(--text-3);
  flex-shrink: 0;
}
.row-main {
  flex: 1;
  min-width: 0;
}
.row-title {
  font-size: 13px;
  color: var(--text-1);
}
.row-sub {
  font-size: 12px;
  color: var(--text-3);
}
.row-metrics {
  display: flex;
  align-items: baseline;
  gap: 4px;
  flex-shrink: 0;
}
.metric {
  font-size: 13px;
  color: var(--text-1);
}
.metric.muted {
  color: var(--text-3);
}
.metric--minor {
  font-size: 12px;
  color: var(--text-3);
}
.metric--minor.danger {
  color: var(--color-danger);
}
.row-arrow {
  font-size: 11px;
  color: var(--text-3);
  flex-shrink: 0;
}
</style>
```

- [ ] **Step 4: 编译 + 样式门禁**

Run: `cd web && npm run build && npm run lint:style`
Expected: 构建成功；stylelint 对本文件无 error（无裸 hex）。

- [ ] **Step 5: Commit**

```bash
git add web/src/views/workhub/QualitySummaryCard.vue
git commit -m "feat(workhub): 右栏质量卡改为分域两行(异常处理/运单与仓储数据)接 useQualitySummary

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 中栏 QualityAlertBar 改为超时门控

**Files:**
- Modify: `web/src/views/workhub/QualityAlertBar.vue`（替换 script+template，保留原 `<style scoped>`）

- [ ] **Step 1: 替换 `<script setup>`**

将整段 `<script setup lang="ts"> … </script>` 替换为：

```ts
<script setup lang="ts">
/**
 * QualityAlertBar.vue — 中栏全宽紧急条
 *
 * 仅当任一质量域存在超时项时显示（真正紧急），数据来自共享 useQualitySummary。
 * 点击「查看」跳转异常列表。
 */
import { useRouter } from 'vue-router'
import { WarningOutlined } from '@ant-design/icons-vue'
import { useQualitySummary } from '@/composables/useQualitySummary'

const router = useRouter()
const { exception, dataQuality, hasOverdue } = useQualitySummary()

function goToExceptions() {
  router.push('/quality/exceptions')
}
</script>
```

- [ ] **Step 2: 替换 `<template>`**

将整段 `<template> … </template>` 替换为：

```html
<template>
  <div v-if="hasOverdue" class="quality-alert-bar">
    <div class="alert-left">
      <WarningOutlined class="alert-icon" />
      <span class="alert-title">质量预警</span>
    </div>
    <div class="alert-center">
      <span v-if="exception.overdue > 0">异常 <span class="alert-num">{{ exception.overdue }}</span> 项已超时</span>
      <span v-if="exception.overdue > 0 && dataQuality.overdue > 0" class="alert-dot">·</span>
      <span v-if="dataQuality.overdue > 0">运单数据 <span class="alert-num">{{ dataQuality.overdue }}</span> 项已超时</span>
    </div>
    <div class="alert-right" @click="goToExceptions">
      查看 →
    </div>
  </div>
</template>
```

> 保留原文件的 `<style scoped lang="scss"> … </style>` 不变（`.quality-alert-bar/.alert-*` 类仍适用）。

- [ ] **Step 3: 编译门禁**

Run: `cd web && npm run build`
Expected: 构建成功。

- [ ] **Step 4: Commit**

```bash
git add web/src/views/workhub/QualityAlertBar.vue
git commit -m "feat(workhub): 质量紧急条改为 overdue 门控并分域显示超时数,接 useQualitySummary

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 合并来源筛选（WorkHubCenter）

**Files:**
- Modify: `web/src/views/workhub/WorkHubCenter.vue`

> 删除本地 `activeSource`、`sourceOptions`、`onSourcesChange`；`toggleSourceFilter` 改多选增删；chip 高亮派生自 `hub.filters.value.sources`；chip 改 `<button aria-pressed>`；删除「来源」复选框行。

- [ ] **Step 1: 删除 `activeSource` ref**

删除这一行（约 `:51`）：

```ts
const activeSource = ref<string>('')
```

- [ ] **Step 2: 删除 `sourceOptions` 常量**

删除这一块（约 `:64-71`）：

```ts
const sourceOptions = [
  { label: 'OA', value: 'oa' },
  { label: '质量', value: 'quality' },
  { label: '任务', value: 'task' },
  { label: '运单', value: 'datacenter' },
  { label: '合同', value: 'contract' },
  { label: '积分', value: 'points' },
]
```

- [ ] **Step 3: 改写 `toggleSourceFilter` 为多选 + 新增 `isSourceActive`**

将原 `toggleSourceFilter`（约 `:258-266`）整段替换为：

```ts
function isSourceActive(key: string) {
  return (hub.filters.value.sources as string[]).includes(key)
}

function toggleSourceFilter(key: string) {
  const current = hub.filters.value.sources as string[]
  const next = current.includes(key)
    ? current.filter((k) => k !== key)
    : [...current, key]
  hub.setFilter('sources', next as WorkItem['source'][])
}
```

- [ ] **Step 4: 删除 `onSourcesChange`**

删除这一块（约 `:268-272`）：

```ts
function onSourcesChange() {
  activeSource.value = ''
  hub.fetchItems(true)
  hub.fetchStats()
}
```

- [ ] **Step 5: 从 `handleResetFilters` 移除 `activeSource`**

将 `handleResetFilters`（约 `:291-295`）替换为：

```ts
function handleResetFilters() {
  dateRangeValue.value = undefined
  hub.resetFilters()
}
```

- [ ] **Step 6: 统计栏 chip 改 `<button aria-pressed>` + 多选高亮**

将模板中的统计栏（约 `:509-522`）替换为：

```html
<!-- 来源筛选（带计数的统计 chip，多选） -->
<div class="stats-bar">
  <button
    v-for="stat in statsItems"
    :key="stat.key"
    type="button"
    class="stat-item"
    :class="{ active: isSourceActive(stat.key) }"
    :aria-pressed="isSourceActive(stat.key)"
    :style="{ '--stat-color': stat.color }"
    @click="toggleSourceFilter(stat.key)"
  >
    <component :is="stat.icon" class="stat-icon" />
    <span class="stat-label">{{ stat.label }}</span>
    <span class="stat-count" v-if="stat.count > 0">{{ stat.count }}</span>
  </button>
</div>
```

- [ ] **Step 7: 删除筛选栏里的「来源」复选框组**

删除筛选栏中这一块（约 `:553-560`）：

```html
<div class="filter-group">
  <span class="filter-label">来源：</span>
  <a-checkbox-group
    v-model:value="hub.filters.value.sources"
    :options="sourceOptions"
    @change="onSourcesChange"
  />
</div>
```

保留同一 `filter-row` 内的优先级下拉、时间范围、重置、多选按钮不变。

- [ ] **Step 8: `.stat-item` 增加 button 重置样式**

在 `<style scoped lang="scss">` 的 `.stat-item { … }` 规则内（约 `:900` 起）追加这三行（消除 `<button>` 默认字体/对齐/外观差异）：

```scss
  font-family: inherit;
  text-align: left;
  appearance: none;
```

- [ ] **Step 9: 编译 + 样式门禁**

Run: `cd web && npm run build && npm run lint:style`
Expected: 构建成功；stylelint 对本文件无 error。

- [ ] **Step 10: Commit**

```bash
git add web/src/views/workhub/WorkHubCenter.vue
git commit -m "feat(workhub): 合并来源筛选为多选 chip,删复选框行,chip 改 button 修高亮 desync

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 集成手动验证（preview）

**Files:** 无（仅验证）

- [ ] **Step 1: 启动开发服务**

用 restart-dev 技能或 `scripts/dev` 拉起前后端（端口 9000/9001），登录进入工作台 `/workhub`。

- [ ] **Step 2: 验证来源筛选（Task 4）**

核对点：
- 连点「审批」「运单」两个 chip → 二者高亮、Feed 与计数按多源过滤；再点「审批」取消 → 仅「运单」生效。
- chip 高亮与实际过滤一致（验证旧 desync 已消除）。
- 点「重置」→ 所有 chip 取消高亮、列表恢复。
- 「来源」复选框行已不存在；优先级/时间/重置/多选仍在。
- 键盘 Tab 可聚焦 chip、Enter/Space 可切换（aria-pressed 随之变化）。

- [ ] **Step 3: 验证质量卡 + 紧急条（Task 2/3）**

核对点：
- 右栏「质量」卡两行：异常处理 / 运单与仓储数据，各显示「待处理 N · 超时 M」，0 时灰显；点击分别跳 `/quality/exceptions`、`/express/quality-center/dashboard`。
- 当两域均无超时（overdue=0）→ 中栏紧急条**不出现**（即使有待处理）。
- 构造/等待 overdue>0 → 中栏紧急条出现并按域列出超时数。
- 打开浏览器网络面板：质量相关请求只在进入页面与每 5 分钟各一组，**不再出现两组件各自重复轮询**。

- [ ] **Step 4: 截图留证**

用 preview 工作流截图质量卡与筛选区，附在完成说明里。

---

## Self-Review

**Spec coverage：**
- 设计 A（合并筛选）→ Task 4（chip 多选/删复选框/删 activeSource/修 desync/chip 改 button）✓
- 设计 B 之 useQualitySummary 单一真源 → Task 1 ✓
- 设计 B 之质量卡分域两行 → Task 2 ✓
- 设计 B 之紧急条 overdue 门控 → Task 3 ✓
- 验证（含"仅一处轮询""行为变化")→ Task 5 ✓

**Placeholder scan：** 无 TBD/TODO；每个代码步骤均含完整代码。

**Type consistency：** `useQualitySummary` 暴露 `exception/dataQuality/loading/hasOverdue/refresh`，Task 2 用 `exception/dataQuality/loading`、Task 3 用 `exception/dataQuality/hasOverdue`，名称一致；`toggleSourceFilter`/`isSourceActive` 在 Task 4 内自洽，`hub.setFilter('sources', …)` 与 `useWorkHub` 签名一致。

**已知风险（见 spec）：** 两接口字段命名不同已在 Task 1 归一（pendingCount/overdueCount vs pendingTotal/overdueWarning）；紧急条隐藏"待处理未超时"项为预期行为变化，PR 描述需点明。
