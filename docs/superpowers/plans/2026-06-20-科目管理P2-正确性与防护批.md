# 科目管理 P2 · 正确性与防护批 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复科目管理页 `/finance/accounts` 的 8 项正确性与防护缺陷——启停开关字段错绑(F1)、读接口越权(F2)、创建/更新输入校验(F6)、辅助核算前缀误命中(F16)、死代码(F18)、固定列失效(F11)、期初按钮语义错位(F13)、误导齿轮图标(F14)。

**Architecture:** 后端逐项 TDD（xUnit + InMemory DbContext，命令带 `--arch x64`）改 `AccountService`/`AccountController`/`RequireAccountSetPermissionAttribute`；前端改 `AccountManage.vue` 走 `vite build` + 用户 live 验证（本前端无测试运行器）。任务按依赖排序：先 F18 定下 4 参构造，再各后端项，最后前端。

**Tech Stack:** .NET 8 / EF Core / xUnit；Vue3 + Ant Design Vue + Pinia。

**对应 spec:** `docs/superpowers/specs/2026-06-20-科目管理P2-正确性与防护批-design.md`

**收尾约定:** 本批提交保持本地、不推远程（master 有并发会话在途，与近期各批一致）。每次 commit 结尾按仓库规范加 `Co-Authored-By` 尾注。

---

## File Structure

**后端（修改）**
- `src/STOTOP.Module.Finance/Services/AccountService.cs` — F18 删死代码/构造瘦身、F6 创建+更新校验、F16 精确匹配、F2 `GetByIdAsync` 加账套校验
- `src/STOTOP.Module.Finance/Services/Interfaces/IAccountService.cs` — F2 `GetByIdAsync` 签名加 `accountSetId`
- `src/STOTOP.Module.Finance/Controllers/AccountController.cs` — F2 5 读接口加权限特性、`GetById` 加 `[FromHeader]`
- `src/STOTOP.Module.Finance/Filters/RequireAccountSetPermissionAttribute.cs` — F2 过滤器构造改 `params`（OR 语义）

**后端测试（新建/修改）**
- 修改 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceToggleStatusTests.cs` — F18 构造 5→4 参
- 修改 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceInitialBalanceTests.cs` — F18 构造 5→4 参
- 新建 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByAuxTypeTests.cs` — F16
- 新建 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceValidationTests.cs` — F6
- 新建 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByIdScopeTests.cs` — F2

**前端（修改）**
- `web/src/views/finance/AccountManage.vue` — F1 开关字段+失败刷新、F11 scroll、F13 期初按钮、F14 去齿轮图标

---

## Task 1: F18 死代码清理 + 同步测试构造

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/AccountService.cs:1,17,19-38`
- Modify: `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceToggleStatusTests.cs:10-15`
- Modify: `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceInitialBalanceTests.cs:11-15`

**为什么先做：** F18 改 `AccountService` 构造签名（去掉 `IHttpContextAccessor`），两个现有测试的 `CreateService` 必须从 5 参改 4 参，否则整个测试项目编译失败。先做这一项，后续新测试直接用 4 参构造。

- [ ] **Step 1: 删除 AccountService 死代码与构造参数**

`src/STOTOP.Module.Finance/Services/AccountService.cs` 顶部删除第 1 行 `using Microsoft.AspNetCore.Http;`。

把第 11-38 行（字段声明 + 构造 + `GetCurrentOrgId`）替换为：

```csharp
public class AccountService : IAccountService
{
    private readonly IRepository<FinAccount> _accountRepository;
    private readonly IRepository<FinAccountBalance> _balanceRepository;
    private readonly IRepository<FinVoucherEntry> _voucherEntryRepository;
    private readonly ChangeTrackingService _changeTrackingService;

    public AccountService(
        IRepository<FinAccount> accountRepository,
        IRepository<FinAccountBalance> balanceRepository,
        IRepository<FinVoucherEntry> voucherEntryRepository,
        ChangeTrackingService changeTrackingService)
    {
        _accountRepository = accountRepository;
        _balanceRepository = balanceRepository;
        _voucherEntryRepository = voucherEntryRepository;
        _changeTrackingService = changeTrackingService;
    }

    public async Task<List<AccountTreeDto>> GetTreeAsync(string? category = null, long accountSetId = 0)
```

（即：删除 `_httpContextAccessor` 字段、构造参数 `IHttpContextAccessor httpContextAccessor` 与其赋值、以及 `GetCurrentOrgId()` 方法整体；`GetTreeAsync` 之后内容不动。）

- [ ] **Step 2: 同步两个测试的 CreateService 为 4 参**

`AccountServiceToggleStatusTests.cs` 第 10-15 行改为：

```csharp
    private static AccountService CreateService(STOTOP.Infrastructure.Data.STOTOPDbContext db)
        => new AccountService(
            new Repository<FinAccount>(db),
            new Repository<FinAccountBalance>(db),
            new Repository<FinVoucherEntry>(db),
            null!);
```

`AccountServiceInitialBalanceTests.cs` 第 11-15 行改为：

```csharp
    private static AccountService CreateService(STOTOP.Infrastructure.Data.STOTOPDbContext db)
        => new AccountService(
            new Repository<FinAccount>(db),
            new Repository<FinAccountBalance>(db),
            null!, null!);
```

- [ ] **Step 3: 编译并跑现有测试确认未破坏**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64`
Expected: 编译通过，原有 9 个测试（Toggle 5 + InitialBalance 4）全部 PASS。

- [ ] **Step 4: 确认无死代码残留**

Run: `rg -n "GetCurrentOrgId|_httpContextAccessor|IHttpContextAccessor" src/STOTOP.Module.Finance/Services/AccountService.cs`
Expected: 无输出（全部已删除）。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/AccountService.cs tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceToggleStatusTests.cs tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceInitialBalanceTests.cs
git commit -m "refactor(finance): 删科目服务 GetCurrentOrgId/_httpContextAccessor 死代码(F18)+测试构造同步4参"
```

---

## Task 2: F16 GetByAuxType 精确匹配（TDD）

**Files:**
- Test: `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByAuxTypeTests.cs`（新建）
- Modify: `src/STOTOP.Module.Finance/Services/AccountService.cs:487`

- [ ] **Step 1: 写失败测试**

新建 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByAuxTypeTests.cs`：

```csharp
using STOTOP.Infrastructure.Repositories;
using STOTOP.Module.Finance.Entities;
using STOTOP.Module.Finance.Services;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Accounts;

public class AccountServiceGetByAuxTypeTests
{
    private static AccountService CreateService(STOTOP.Infrastructure.Data.STOTOPDbContext db)
        => new AccountService(
            new Repository<FinAccount>(db),
            new Repository<FinAccountBalance>(db),
            new Repository<FinVoucherEntry>(db),
            null!);

    [Fact]
    public async Task GetByAuxType_exact_match_not_prefix()
    {
        await using var db = TestDbContextFactory.Create(nameof(GetByAuxType_exact_match_not_prefix));
        db.Set<FinAccount>().AddRange(
            new FinAccount { FID = 1, FCode = "1122", FName = "应收账款", FCategory = "流动资产", FBalanceDirection = "借", FLevel = 1, FParentId = 0, FIsLeaf = 1, FAccountSetId = 1, FEnableStatus = 1, FAuxiliary = "dept,customer" },
            new FinAccount { FID = 2, FCode = "1601", FName = "固定资产", FCategory = "非流动资产", FBalanceDirection = "借", FLevel = 1, FParentId = 0, FIsLeaf = 1, FAccountSetId = 1, FEnableStatus = 1, FAuxiliary = "department" }
        );
        await db.SaveChangesAsync();
        var service = CreateService(db);

        var result = await service.GetByAuxTypeAsync("dept", 1);

        Assert.Single(result);            // 改前 Contains("dept") 会误命中 "department" → 2 条
        Assert.Equal("1122", result[0].Code);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~AccountServiceGetByAuxTypeTests"`
Expected: FAIL — `Assert.Single() Failure: The collection contained 2 items`（子串匹配误命中 `department`）。

- [ ] **Step 3: 改为精确匹配**

`src/STOTOP.Module.Finance/Services/AccountService.cs` 第 487 行：

```csharp
                && a.FAuxiliary.Contains(auxType)
```

改为：

```csharp
                && ("," + a.FAuxiliary + ",").Contains("," + auxType + ",")
```

（首尾补逗号后整项匹配；上一行 `a.FAuxiliary != null` 守卫仍在，短路保证不会拼接 null。）

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~AccountServiceGetByAuxTypeTests"`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByAuxTypeTests.cs src/STOTOP.Module.Finance/Services/AccountService.cs
git commit -m "fix(finance): GetByAuxType 改逗号精确匹配,消辅助核算前缀误命中(F16)"
```

---

## Task 3: F6 创建/更新输入校验（TDD）

**Files:**
- Test: `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceValidationTests.cs`（新建）
- Modify: `src/STOTOP.Module.Finance/Services/AccountService.cs`（CreateAsync 第 122 行后、UpdateAsync 第 204 行后）

- [ ] **Step 1: 写失败测试**

新建 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceValidationTests.cs`：

```csharp
using STOTOP.Infrastructure.Repositories;
using STOTOP.Module.Finance.Dtos;
using STOTOP.Module.Finance.Entities;
using STOTOP.Module.Finance.Services;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Accounts;

public class AccountServiceValidationTests
{
    private static AccountService CreateService(STOTOP.Infrastructure.Data.STOTOPDbContext db)
        => new AccountService(
            new Repository<FinAccount>(db),
            new Repository<FinAccountBalance>(db),
            new Repository<FinVoucherEntry>(db),
            null!);

    [Fact]
    public async Task Create_rejects_empty_name()
    {
        await using var db = TestDbContextFactory.Create(nameof(Create_rejects_empty_name));
        var service = CreateService(db);
        var req = new CreateAccountRequest { Code = "1001", Name = "  ", Category = "流动资产", BalanceDirection = "借", ParentId = 0 };

        var ex = await Assert.ThrowsAsync<InvalidOperationException>(() => service.CreateAsync(req, 1));
        Assert.Contains("名称", ex.Message);
    }

    [Fact]
    public async Task Create_rejects_invalid_direction()
    {
        await using var db = TestDbContextFactory.Create(nameof(Create_rejects_invalid_direction));
        var service = CreateService(db);
        var req = new CreateAccountRequest { Code = "1001", Name = "库存现金", Category = "流动资产", BalanceDirection = "x", ParentId = 0 };

        var ex = await Assert.ThrowsAsync<InvalidOperationException>(() => service.CreateAsync(req, 1));
        Assert.Contains("方向", ex.Message);
    }

    [Fact]
    public async Task Create_succeeds_with_valid_input()
    {
        await using var db = TestDbContextFactory.Create(nameof(Create_succeeds_with_valid_input));
        var service = CreateService(db);
        var req = new CreateAccountRequest { Code = "1001", Name = "库存现金", Category = "流动资产", BalanceDirection = "借", ParentId = 0 };

        var dto = await service.CreateAsync(req, 1);

        Assert.Equal("1001", dto.Code);
        Assert.Equal("借", dto.BalanceDirection);
    }

    [Fact]
    public async Task Update_rejects_empty_name()
    {
        await using var db = TestDbContextFactory.Create(nameof(Update_rejects_empty_name));
        db.Set<FinAccount>().Add(new FinAccount { FID = 1, FCode = "1001", FName = "库存现金", FCategory = "流动资产", FBalanceDirection = "借", FLevel = 1, FParentId = 0, FIsLeaf = 1, FAccountSetId = 1, FEnableStatus = 1 });
        await db.SaveChangesAsync();
        var service = CreateService(db);

        var ex = await Assert.ThrowsAsync<InvalidOperationException>(() => service.UpdateAsync(1, new UpdateAccountRequest { Name = "" }));
        Assert.Contains("名称", ex.Message);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~AccountServiceValidationTests"`
Expected: `Create_rejects_empty_name`/`Create_rejects_invalid_direction`/`Update_rejects_empty_name` FAIL（当前未校验，不抛异常）；`Create_succeeds_with_valid_input` PASS。

- [ ] **Step 3: 给 CreateAsync 加校验**

`src/STOTOP.Module.Finance/Services/AccountService.cs`，在类别校验块（第 119-122 行 `if (string.IsNullOrWhiteSpace(request.Category) ...) { throw ... }`）**之后**插入：

```csharp
        if (string.IsNullOrWhiteSpace(request.Name))
        {
            throw new InvalidOperationException("科目名称不能为空");
        }

        if (request.BalanceDirection != "借" && request.BalanceDirection != "贷")
        {
            throw new InvalidOperationException("科目余额方向只能为「借」或「贷」");
        }
```

- [ ] **Step 4: 给 UpdateAsync 加 Name 校验**

同文件 `UpdateAsync`，在第 204 行 `if (account == null) return null;` **之后**、记录 `oldSnapshot` **之前**插入（务必放在此处，确保空名在触达 `_changeTrackingService` 前就抛出）：

```csharp
        if (string.IsNullOrWhiteSpace(request.Name))
        {
            throw new InvalidOperationException("科目名称不能为空");
        }
```

- [ ] **Step 5: 跑测试确认全部通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~AccountServiceValidationTests"`
Expected: 4 个测试全部 PASS。

- [ ] **Step 6: Commit**

```bash
git add tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceValidationTests.cs src/STOTOP.Module.Finance/Services/AccountService.cs
git commit -m "feat(finance): 创建/更新科目加 Name 非空+余额方向白名单校验(F6)"
```

---

## Task 4: F2-① 过滤器 OR + GetById 账套校验（TDD）

**Files:**
- Modify: `src/STOTOP.Module.Finance/Filters/RequireAccountSetPermissionAttribute.cs:15-20,65-76`
- Modify: `src/STOTOP.Module.Finance/Services/Interfaces/IAccountService.cs:9`
- Modify: `src/STOTOP.Module.Finance/Services/AccountService.cs:93-97`
- Modify: `src/STOTOP.Module.Finance/Controllers/AccountController.cs:85-94`
- Test: `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByIdScopeTests.cs`（新建）

**顺序说明：** 先把过滤器改成支持多权限码（`params`），否则 Step 4 给 `GetById` 标 `[RequireAccountSetPermission(SubjectView, SubjectEdit)]` 两参会编译失败。

- [ ] **Step 1: 过滤器构造改 params（OR 语义）**

`src/STOTOP.Module.Finance/Filters/RequireAccountSetPermissionAttribute.cs` 第 15-20 行：

```csharp
    private readonly string _permissionCode;

    public RequireAccountSetPermissionAttribute(string permissionCode)
    {
        _permissionCode = permissionCode;
    }
```

改为：

```csharp
    private readonly string[] _permissionCodes;

    public RequireAccountSetPermissionAttribute(params string[] permissionCodes)
    {
        _permissionCodes = permissionCodes;
    }
```

同文件第 65-76 行（取 authService 后的单权限校验）：

```csharp
        var authService = context.HttpContext.RequestServices.GetRequiredService<IAccountSetAuthorizationService>();
        var hasPermission = await authService.HasPermissionAsync(userId, accountSetId, _permissionCode);

        if (!hasPermission)
        {
            context.Result = new ObjectResult(new { code = 403, message = $"无账套操作权限：{_permissionCode}" })
            {
                StatusCode = 403
            };
            return;
        }
```

改为（任一权限命中即放行）：

```csharp
        var authService = context.HttpContext.RequestServices.GetRequiredService<IAccountSetAuthorizationService>();
        var hasPermission = false;
        foreach (var code in _permissionCodes)
        {
            if (await authService.HasPermissionAsync(userId, accountSetId, code))
            {
                hasPermission = true;
                break;
            }
        }

        if (!hasPermission)
        {
            context.Result = new ObjectResult(new { code = 403, message = $"无账套操作权限：{string.Join("/", _permissionCodes)}" })
            {
                StatusCode = 403
            };
            return;
        }
```

- [ ] **Step 2: 写失败测试（GetById 账套校验）**

新建 `tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByIdScopeTests.cs`：

```csharp
using STOTOP.Infrastructure.Repositories;
using STOTOP.Module.Finance.Entities;
using STOTOP.Module.Finance.Services;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Accounts;

public class AccountServiceGetByIdScopeTests
{
    private static AccountService CreateService(STOTOP.Infrastructure.Data.STOTOPDbContext db)
        => new AccountService(
            new Repository<FinAccount>(db),
            new Repository<FinAccountBalance>(db),
            new Repository<FinVoucherEntry>(db),
            null!);

    [Fact]
    public async Task GetById_returns_account_when_accountset_matches()
    {
        await using var db = TestDbContextFactory.Create(nameof(GetById_returns_account_when_accountset_matches));
        db.Set<FinAccount>().Add(new FinAccount { FID = 1, FCode = "1001", FName = "库存现金", FCategory = "流动资产", FBalanceDirection = "借", FLevel = 1, FParentId = 0, FIsLeaf = 1, FAccountSetId = 1, FEnableStatus = 1 });
        await db.SaveChangesAsync();
        var service = CreateService(db);

        var dto = await service.GetByIdAsync(1, 1);

        Assert.NotNull(dto);
        Assert.Equal("1001", dto!.Code);
    }

    [Fact]
    public async Task GetById_returns_null_when_accountset_mismatch()
    {
        await using var db = TestDbContextFactory.Create(nameof(GetById_returns_null_when_accountset_mismatch));
        db.Set<FinAccount>().Add(new FinAccount { FID = 1, FCode = "1001", FName = "库存现金", FCategory = "流动资产", FBalanceDirection = "借", FLevel = 1, FParentId = 0, FIsLeaf = 1, FAccountSetId = 1, FEnableStatus = 1 });
        await db.SaveChangesAsync();
        var service = CreateService(db);

        var dto = await service.GetByIdAsync(1, 2); // 用账套2查账套1的科目 → 应视为不存在

        Assert.Null(dto);
    }
}
```

- [ ] **Step 3: 跑测试确认失败（编译错误也算失败）**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~AccountServiceGetByIdScopeTests"`
Expected: 编译失败 —— `GetByIdAsync` 当前只有 `(long id)` 单参签名，测试调用 `GetByIdAsync(1, 1)` 无法编译。这驱动 Step 4 改签名。

- [ ] **Step 4: 改接口、service、controller**

`src/STOTOP.Module.Finance/Services/Interfaces/IAccountService.cs` 第 9 行：

```csharp
    Task<AccountDto?> GetByIdAsync(long id);
```

改为：

```csharp
    Task<AccountDto?> GetByIdAsync(long id, long accountSetId);
```

`src/STOTOP.Module.Finance/Services/AccountService.cs` 第 93-97 行：

```csharp
    public async Task<AccountDto?> GetByIdAsync(long id)
    {
        var account = await LoadAccountAsync(id);
        return account == null ? null : MapToDto(account);
    }
```

改为：

```csharp
    public async Task<AccountDto?> GetByIdAsync(long id, long accountSetId)
    {
        var account = await LoadAccountAsync(id);
        // 科目 id 全局唯一、LoadAccount 不按账套过滤；校验科目所属账套==请求账套，
        // 不符按"不存在"返回，避免跨账套读取单条科目详情(F2)。
        if (account == null || account.FAccountSetId != accountSetId) return null;
        return MapToDto(account);
    }
```

`src/STOTOP.Module.Finance/Controllers/AccountController.cs` 第 85-94 行：

```csharp
    [HttpGet("{id}")]
    public async Task<ApiResult<AccountDto>> GetById(long id)
    {
        var result = await _accountService.GetByIdAsync(id);
        if (result == null)
        {
            return ApiResult<AccountDto>.Fail("科目不存在");
        }
        return ApiResult<AccountDto>.Success(result);
    }
```

改为：

```csharp
    [HttpGet("{id}")]
    [RequireAccountSetPermission(AccountSetPermissions.SubjectView, AccountSetPermissions.SubjectEdit)]
    public async Task<ApiResult<AccountDto>> GetById(long id, [FromHeader(Name = "X-AccountSet-Id")] long accountSetId = 0)
    {
        var result = await _accountService.GetByIdAsync(id, accountSetId);
        if (result == null)
        {
            return ApiResult<AccountDto>.Fail("科目不存在");
        }
        return ApiResult<AccountDto>.Success(result);
    }
```

- [ ] **Step 5: 跑测试确认通过 + 全量编译**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64`
Expected: 编译通过；`AccountServiceGetByIdScopeTests` 2 例 PASS；此前所有测试仍 PASS（确认 `GetByIdAsync` 签名变更未漏改其它调用点）。

- [ ] **Step 6: Commit**

```bash
git add src/STOTOP.Module.Finance/Filters/RequireAccountSetPermissionAttribute.cs src/STOTOP.Module.Finance/Services/Interfaces/IAccountService.cs src/STOTOP.Module.Finance/Services/AccountService.cs src/STOTOP.Module.Finance/Controllers/AccountController.cs tests/STOTOP.Module.Finance.Tests/Accounts/AccountServiceGetByIdScopeTests.cs
git commit -m "feat(finance): 账套权限过滤器支持任一权限(OR)+GetById按请求头账套校验防越权(F2-1)"
```

---

## Task 5: F2-② 读接口加账套权限特性

**Files:**
- Modify: `src/STOTOP.Module.Finance/Controllers/AccountController.cs:22,32,76,161`

**说明：** `GetById` 已在 Task 4 加特性。本任务给其余 4 个读接口加 `SubjectView 或 SubjectEdit`（编辑权蕴含查看权，避免只配了 Edit 的非管理员被锁在外；管理员由过滤器放行）。这 4 个接口都带 `accountSetId` query 参数或由前端 `X-AccountSet-Id` 头提供，过滤器可取到账套。无纯单测（过滤器走 HttpContext/DI），靠编译 + 后续 live 验证。

- [ ] **Step 1: GetTree 加特性**

`AccountController.cs` 第 22 行 `[HttpGet("tree")]` 下一行插入：

```csharp
    [RequireAccountSetPermission(AccountSetPermissions.SubjectView, AccountSetPermissions.SubjectEdit)]
```

- [ ] **Step 2: GetSelectorList 加特性**

第 32 行 `[HttpGet]`（GetSelectorList 上方）下一行插入同一行特性：

```csharp
    [RequireAccountSetPermission(AccountSetPermissions.SubjectView, AccountSetPermissions.SubjectEdit)]
```

- [ ] **Step 3: GetByAuxType 加特性**

第 76 行 `[HttpGet("by-aux-type")]` 下一行插入：

```csharp
    [RequireAccountSetPermission(AccountSetPermissions.SubjectView, AccountSetPermissions.SubjectEdit)]
```

- [ ] **Step 4: GetInitialBalances 加特性**

第 161 行 `[HttpGet("initial-balances")]` 下一行插入：

```csharp
    [RequireAccountSetPermission(AccountSetPermissions.SubjectView, AccountSetPermissions.SubjectEdit)]
```

- [ ] **Step 5: 编译 + 全量测试**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64`
Expected: 编译通过，全部测试 PASS。

- [ ] **Step 6: 确认 5 个读接口都已挂特性**

Run: `rg -n "RequireAccountSetPermission\(AccountSetPermissions\.SubjectView" src/STOTOP.Module.Finance/Controllers/AccountController.cs`
Expected: 5 行（GetTree/GetSelectorList/GetByAuxType/GetInitialBalances/GetById）。

- [ ] **Step 7: Commit**

```bash
git add src/STOTOP.Module.Finance/Controllers/AccountController.cs
git commit -m "feat(finance): 科目5个读接口加账套权限校验(SubjectView或Edit),堵跨账套越权读(F2-2)"
```

---

## Task 6: F1 启停开关字段错绑 + 失败刷新（前端）

**Files:**
- Modify: `web/src/views/finance/AccountManage.vue:108,355,392,793-802`

**说明：** 本前端无测试运行器，走 `vite build` + 用户 live 验证。这是 P1 功能 bug：开关绑 `isEnabled`（后端实为 `enableStatus`）导致开关恒显示"关"，并旁路停用二次确认。

- [ ] **Step 1: 模板开关 :checked 改字段**

`web/src/views/finance/AccountManage.vue` 第 108 行：

```vue
              :checked="record.isEnabled"
```

改为：

```vue
              :checked="record.enableStatus"
```

- [ ] **Step 2: 列定义 dataIndex/key 改字段**

第 355 行：

```js
  { title: '启/停用', dataIndex: 'isEnabled', key: 'isEnabled', width: 100, align: 'center' as const, fixed: 'right' as const },
```

改为：

```js
  { title: '启/停用', dataIndex: 'enableStatus', key: 'enableStatus', width: 100, align: 'center' as const, fixed: 'right' as const },
```

- [ ] **Step 3: AccountNode interface 改字段**

第 392 行：

```ts
  isEnabled: boolean
```

改为：

```ts
  enableStatus: boolean
```

- [ ] **Step 4: doToggleStatus 失败也刷新**

第 793-802 行：

```js
async function doToggleStatus(row: any, val: boolean) {
  try {
    await toggleAccountStatus(row.id)
    message.success(val ? '已启用' : '已停用')
    loadData()
  } catch (error) {
    // 失败原因由请求拦截器统一弹后端消息（如"该科目已被凭证引用，无法停用"）
    console.error('状态切换失败:', error)
  }
}
```

改为：

```js
async function doToggleStatus(row: any, val: boolean) {
  try {
    await toggleAccountStatus(row.id)
    message.success(val ? '已启用' : '已停用')
  } catch (error) {
    // 失败原因由请求拦截器统一弹后端消息（如"该科目已被凭证引用，无法停用"）
    console.error('状态切换失败:', error)
  } finally {
    loadData() // 成功失败都刷新，保证开关视觉与后端真实状态一致
  }
}
```

- [ ] **Step 5: 确认无 isEnabled 残留**

Run: `rg -n "isEnabled|enableStatus" web/src/views/finance/AccountManage.vue`
Expected: 仅出现 `enableStatus`（模板:108、列定义:355、interface:392），无 `isEnabled` 残留。

- [ ] **Step 6: 前端构建**

Run (in `web/`): `npm run build`
Expected: 构建成功（vue-tsc 基线告警可忽略，关注无新增报错、vite 打包完成）。

- [ ] **Step 7: Commit**

```bash
git add web/src/views/finance/AccountManage.vue
git commit -m "fix(finance-web): 科目启停开关绑错字段 isEnabled→enableStatus+失败也刷新,修开关恒关与旁路停用确认(F1)"
```

- [ ] **Step 8: 用户 live 验证（执行者提示用户）**

提示用户在已登录的 9001 会话验证：① 开关如实反映各科目启停状态（不再全显"关"）；② 停用"启用中"的科目时弹出二次确认；③ 停用被凭证引用的科目时后端拒绝、开关回弹为"开"。

---

## Task 7: F11 固定列 + F13 期初按钮 + F14 齿轮图标（前端）

**Files:**
- Modify: `web/src/views/finance/AccountManage.vue:8,11,14,45-55,101-104`

- [ ] **Step 1: F11 给科目表加 scroll.x**

`web/src/views/finance/AccountManage.vue` 第 52 行 `:pagination="false"` 下一行插入：

```vue
        :scroll="{ x: 'max-content' }"
```

（让 `accountColumns` 中 `fixed:'left'`/`fixed:'right'` 的操作列与启停列真正吸附。）

- [ ] **Step 2: F13 期初视图隐藏 新增/删除**

第 8 行：

```vue
        <a-button v-if="accountSetStore.hasAccountSetPermission(AccountSetPermissions.SubjectEdit)" type="primary" @click="handleAdd">
```

改为：

```vue
        <a-button v-if="currentCategory !== 'initial' && accountSetStore.hasAccountSetPermission(AccountSetPermissions.SubjectEdit)" type="primary" @click="handleAdd">
```

第 11 行：

```vue
        <a-button v-if="accountSetStore.hasAccountSetPermission(AccountSetPermissions.SubjectEdit)" @click="handleBatchDelete" :disabled="selectedRows.length === 0">
```

改为：

```vue
        <a-button v-if="currentCategory !== 'initial' && accountSetStore.hasAccountSetPermission(AccountSetPermissions.SubjectEdit)" @click="handleBatchDelete" :disabled="selectedRows.length === 0">
```

- [ ] **Step 3: F13 期初视图隐藏"更多"下拉**

第 14 行：

```vue
        <a-dropdown :trigger="['click']">
```

改为：

```vue
        <a-dropdown v-if="currentCategory !== 'initial'" :trigger="['click']">
```

- [ ] **Step 4: F14 去掉单位列齿轮图标**

第 101-104 行：

```vue
          <template v-if="column.dataIndex === 'unit'">
            <span v-if="record.unit">{{ record.unit }}<SettingOutlined class="unit-icon" /></span>
            <span v-else>-</span>
          </template>
```

改为：

```vue
          <template v-if="column.dataIndex === 'unit'">
            <span v-if="record.unit">{{ record.unit }}</span>
            <span v-else>-</span>
          </template>
```

- [ ] **Step 5: 清理不再使用的 SettingOutlined import 与 .unit-icon 样式**

Run: `rg -n "SettingOutlined|unit-icon" web/src/views/finance/AccountManage.vue`
- 若 `SettingOutlined` 仅剩 import 行（无其它使用），从该文件的图标 import 语句中删除 `SettingOutlined`。
- 若 `.unit-icon` 仅剩 `<style>` 中的样式定义（模板已无引用），删除该样式块。
- 删除后再次运行上面的 `rg`，Expected: 无输出。

- [ ] **Step 6: 前端构建**

Run (in `web/`): `npm run build`
Expected: 构建成功，无新增报错（尤其确认删除 `SettingOutlined` 后无"未定义组件/未使用导入"类报错）。

- [ ] **Step 7: Commit**

```bash
git add web/src/views/finance/AccountManage.vue
git commit -m "fix(finance-web): 科目表固定列补 scroll.x(F11)+期初视图隐藏新增删除更多(F13)+去单位列误导齿轮图标(F14)"
```

- [ ] **Step 8: 用户 live 验证（执行者提示用户）**

提示用户在 9001 会话验证：① 列宽溢出时操作列/启停开关吸附两端；② 切到"期初值"视图时不再显示 新增/删除/更多；③ 单位列不再有齿轮图标。

---

## 完成后（全批终审）

按用户工作流，本批 7 个 task 全部完成后：
- [ ] 跑全量后端测试：`dotnet test tests/STOTOP.Module.Finance.Tests --arch x64`，确认全绿（原 9 + 新增 F16 1 + F6 4 + F2 2 = 16 例）。
- [ ] 前端 `npm run build`（web/）通过。
- [ ] 全特性整体复审（对照 spec §7 验收 checklist 逐条核对，含 F2 误伤抽验：只配 SubjectEdit 的非管理员能读科目页），印证 [[holistic-review-after-subagent-tasks]]。
- [ ] 保持本地提交，不推远程。

---

## Self-Review（计划自检结果）

- **Spec 覆盖**：F1→Task6；F2→Task4+Task5；F6→Task3；F16→Task2；F18→Task1；F11/F13/F14→Task7。spec §2 的 8 项全部有对应 task。✓
- **占位扫描**：无 TBD/TODO；每个代码步骤均给出完整 old/new 代码与精确命令。✓
- **类型/签名一致性**：`GetByIdAsync(long id, long accountSetId)` 在 IAccountService(Task4 Step4)、AccountService(Task4 Step4)、AccountController(Task4 Step4)、测试(Task4 Step2) 四处一致；过滤器 `params string[] permissionCodes` 与 controller 两参用法(Task4/5)一致；前端 `enableStatus` 在模板/列定义/interface 三处一致。✓
- **依赖顺序**：Task1(构造瘦身)先于所有新测试；Task4 过滤器 params 先于 Task4/Task5 的两参特性用法。✓
