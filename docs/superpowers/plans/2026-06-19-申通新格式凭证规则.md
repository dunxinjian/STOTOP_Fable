# 申通新格式(5.7后)交易明细凭证规则 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增一条独立链路(STG 加2列 + 导入规则3130 + 流程2331 + 自动凭证规则3131),处理申通 2026-05-07 后新格式交易明细,与现有规则21(旧格式)并存,旧链零改动。

**Architecture:** 全 additive。复用 `STG申通总部交易明细` 表 + 加 `F费用收付类型`/`F进出港标识` 两可空列;新文件靠列头(`fullColumnIdentifier`)在 `BatchTriggerService` 第一轮精确匹配自动路由到新流程,旧链不受影响。凭证规则3131=rulesBased v2、164组、借贷+对手220201+三维辅助、createDraft占位1901。映射定稿见 `docs/superpowers/specs/2026-06-19-申通新格式映射草案.md`,config 由 build 脚本生成(已存 `docs/superpowers/specs/build-申通新格式/`)。

**Tech Stack:** .NET 10 / EF Core / SQL Server(测试库 47.105.65.51/stotop);CardFlowSeeder 迁移(纯 ADO ExecSql,版本严格 +1);Python(校验/连库 dbq.py);xUnit(--arch x64)。

**关键约定(实现必读):**
- 迁移版本严格连续 +1、不可改已执行版本;STG 前缀表存量库不自动建/改列,必须显式 ALTER。
- C# 逐字字符串 `@"..."`:**只把双引号 `"` 转成 `""`**,单引号与大括号不转义(ExecSql 走纯 ADO)。
- 启动后端即对测试库 live 跑迁移(MigrationRunner),**Task 5 启动后端 = live 追平**,无需另写 live SQL。
- 现有最大 FID:规则3129/流程2329/版本2329/节点6891。本计划用:导入规则**3130**、凭证规则**3131**、流程**2331**、版本**2331**、节点**6892-6896**(均已确认空闲)。
- 账套2 关键 fid:对手 220201=700125、占位 1901=700044;辅助 express_brand ST=16、business_direction OUT=13/IN=12/CMB=14、4网点 outlet=320288/320319/321426/321992。

---

## File Structure

- `src/STOTOP.Module.CardFlow/Entities/StgShentongHqTx.cs` — 加 2 个属性。
- `src/STOTOP.Module.CardFlow/Configurations/StgShentongHqTxConfiguration.cs` — 加 2 个列映射。
- `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs` — 注册 V54-V57 + 4 个 MigrateVxx 方法(在 `MigrateV53` 之后、`Migrate` 方法的 steps 列表末尾)。
- `docs/superpowers/specs/build-申通新格式/` — 已含 `rule3130_sqlescaped.txt`(1.3K)/`rule3131_sqlescaped.txt`(210K)/`rule3131-build.py`/`migrations-build.py`/`final_mapping.json`。
- 验证:无新增测试文件(端到端走真实导入);可选临时探针。

---

## Task 1: STG 表加 2 列(实体 + 配置 + MigrateV54)

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Entities/StgShentongHqTx.cs:38`
- Modify: `src/STOTOP.Module.CardFlow/Configurations/StgShentongHqTxConfiguration.cs:44`
- Modify: `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs:75`(注册)+ 在 `MigrateV53` 方法后插入 `MigrateV54`

- [ ] **Step 1: 实体加 2 属性**

在 `StgShentongHqTx.cs` 末尾属性 `F科目编码` 之后(第 38 行后)加:

```csharp
    public string? F费用收付类型 { get; set; }
    public string? F进出港标识 { get; set; }
```

- [ ] **Step 2: 配置加 2 列映射**

在 `StgShentongHqTxConfiguration.cs` 的 `F科目编码` 映射后(第 44 行后)加:

```csharp
        builder.Property(e => e.F费用收付类型).HasColumnName("F费用收付类型").HasMaxLength(50);
        builder.Property(e => e.F进出港标识).HasColumnName("F进出港标识").HasMaxLength(20);
```

- [ ] **Step 3: 加 MigrateV54 方法**

在 `CardFlowSeeder.cs` 的 `MigrateV53` 方法(约 16927 行起)结束的 `}` 之后,插入:

```csharp
    private static void MigrateV54(STOTOPDbContext ctx)
    {
        if (!SeederHelper.IsSqlServer(ctx)) return;
        // 申通新格式新增列:F费用收付类型 / F进出港标识(STG前缀表不自动改列,显式 ALTER + COL_LENGTH 守卫)
        ExecSql(ctx, @"
        IF COL_LENGTH(N'STG申通总部交易明细', N'F费用收付类型') IS NULL
            ALTER TABLE [STG申通总部交易明细] ADD [F费用收付类型] NVARCHAR(50) NULL;
        ");
        ExecSql(ctx, @"
        IF COL_LENGTH(N'STG申通总部交易明细', N'F进出港标识') IS NULL
            ALTER TABLE [STG申通总部交易明细] ADD [F进出港标识] NVARCHAR(20) NULL;
        ");
    }
```

- [ ] **Step 4: 注册 V54**

在 `CardFlowSeeder.cs:75`(`new(53, ...MigrateV53),` 之后)加:

```csharp
            new(54, "申通新格式:STG申通总部交易明细加 F费用收付类型/F进出港标识 两列 (2026-06-19)", MigrateV54),
```

- [ ] **Step 5: 编译验证**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj -v minimal`
Expected: `生成成功` / 0 Error(允许既有 warning)。

- [ ] **Step 6: 提交**

```bash
git add src/STOTOP.Module.CardFlow/Entities/StgShentongHqTx.cs src/STOTOP.Module.CardFlow/Configurations/StgShentongHqTxConfiguration.cs src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs
git commit -m "feat(cardflow): 申通新格式 STG 加收付类型/进出港标识两列(MigrateV54)"
```

---

## Task 2: 导入规则 3130(MigrateV55)

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs`(`MigrateV54` 后加 `MigrateV55` + 注册)
- Source: `docs/superpowers/specs/build-申通新格式/rule3130_sqlescaped.txt`(已转义 config)

- [ ] **Step 1: 加 MigrateV55 方法**

在 `MigrateV54` 方法之后插入下面方法。`N'...'` 内的 config 直接用本块内容(已 SQL 转义,与 `rule3130_sqlescaped.txt` 一致):

```csharp
    private static void MigrateV55(STOTOPDbContext ctx)
    {
        if (!SeederHelper.IsSqlServer(ctx)) return;
        // 申通新格式导入规则 3130(excelInput;列头自动路由靠 fullColumnIdentifier/columnIdentifier)
        ExecSql(ctx, @"
        SET IDENTITY_INSERT [CF自动插件_规则] ON;
        IF NOT EXISTS (SELECT 1 FROM [CF自动插件_规则] WHERE [FID] = 3130)
        INSERT INTO [CF自动插件_规则] ([FID], [F组织ID], [F类型编码], [F规则名称], [F规则配置JSON], [F状态], [F说明], [F并发戳], [F创建时间])
        VALUES (3130, 192, N'excelInput', N'申通总部交易明细导入规则(新格式5.7后)',
        N'{""targetTable"":""STG申通总部交易明细"",""outputMode"":""stg"",""headerRow"":1,""dataStartRow"":2,""columnMapping"":[{""excelColumn"":""会计日期"",""dbColumn"":""F业务日期""},{""excelColumn"":""扣付日期"",""dbColumn"":""F记账日期""},{""excelColumn"":""网点名称"",""dbColumn"":""F网点名称""},{""excelColumn"":""网点编码"",""dbColumn"":""F网点编号""},{""excelColumn"":""交易类型"",""dbColumn"":""F费用类型""},{""excelColumn"":""费用名称"",""dbColumn"":""F费用名称""},{""excelColumn"":""费用编码"",""dbColumn"":""F科目编码""},{""excelColumn"":""费用收付类型"",""dbColumn"":""F费用收付类型""},{""excelColumn"":""业务摘要"",""dbColumn"":""F业务摘要""},{""excelColumn"":""发生额(收入)"",""dbColumn"":""F发生额收入""},{""excelColumn"":""发生额(支出)"",""dbColumn"":""F发生额支出""},{""excelColumn"":""余额"",""dbColumn"":""F余额""},{""excelColumn"":""进/出港标识"",""dbColumn"":""F进出港标识""},{""excelColumn"":""关联单号"",""dbColumn"":""F运单编号""}],""decimalFields"":[""F发生额收入"",""F发生额支出"",""F余额""],""keyFields"":[""会计日期"",""网点编码"",""业务摘要"",""费用名称"",""费用编码"",""费用收付类型"",""发生额(收入)"",""发生额(支出)"",""余额""],""fullColumnIdentifier"":""会计日期,扣付日期,结算日期,网点名称,网点编码,交易类型,费用名称,费用编码,费用收付类型,业务摘要,发生额(收入),发生额(支出),余额,进/出港标识,联系人,关联单号,银行账号"",""columnIdentifier"":""会计日期,费用编码,费用收付类型,进/出港标识"",""totalRowDetection"":{""enabled"":true,""containsKeywords"":[""合计"",""总计""],""emptyFields"":[""会计日期"",""网点编码"",""余额""]},""crossBatchDedupEnabled"":false,""batchSplit"":{""enabled"":false}}',
        1, N'申通5.7后新格式交易明细Excel导入(会计日期/费用编码/费用收付类型/进出港标识)', REPLACE(NEWID(),'-',''), GETDATE());
        SET IDENTITY_INSERT [CF自动插件_规则] OFF;
        ");
    }
```

- [ ] **Step 2: 注册 V55**

`new(54, ...),` 之后加:

```csharp
            new(55, "申通新格式:导入规则3130(excelInput) (2026-06-19)", MigrateV55),
```

- [ ] **Step 3: 编译**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj -v minimal`
Expected: 生成成功。

- [ ] **Step 4: 提交**

```bash
git add src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs
git commit -m "feat(cardflow): 申通新格式导入规则3130(MigrateV55)"
```

---

## Task 3: 自动凭证规则 3131(MigrateV56,164组,config 机器生成)

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs`(`MigrateV55` 后加 `MigrateV56` + 注册)
- Source(已 SQL 转义,210K,**不可手写,从文件粘**): `docs/superpowers/specs/build-申通新格式/rule3131_sqlescaped.txt`

> config 由 `rule3131-build.py` 从 `final_mapping.json` + 4 个真实数据文件生成(166 映射→164组,自检 0 借贷异常/0 编码不匹配)。如需重生:`python docs/superpowers/specs/build-申通新格式/rule3131-build.py` 再 `python .../migrations-build.py`,产物在 `Temp/gen/`。

- [ ] **Step 1: 加 MigrateV56 方法骨架**

在 `MigrateV55` 之后插入(先用占位 `«PASTE»`,下一步替换):

```csharp
    private static void MigrateV56(STOTOPDbContext ctx)
    {
        if (!SeederHelper.IsSqlServer(ctx)) return;
        // 申通新格式自动凭证规则 3131(rulesBased v2;164组;借贷+对手220201+三维辅助;config 由 build_rule3131.py 生成)
        ExecSql(ctx, @"
        SET IDENTITY_INSERT [CF自动插件_规则] ON;
        IF NOT EXISTS (SELECT 1 FROM [CF自动插件_规则] WHERE [FID] = 3131)
        INSERT INTO [CF自动插件_规则] ([FID], [F组织ID], [F类型编码], [F规则名称], [F规则配置JSON], [F状态], [F说明], [F并发戳], [F创建时间])
        VALUES (3131, 192, N'AutoVoucher', N'Tai申通总部交易明细生成凭证规则(新格式5.7后)',
        N'«PASTE»',
        1, N'申通5.7后新格式凭证规则:164组,品牌版科目,createDraft占位1901(700044)', REPLACE(NEWID(),'-',''), GETDATE());
        SET IDENTITY_INSERT [CF自动插件_规则] OFF;
        ");
    }
```

- [ ] **Step 2: 用生成的转义 config 替换 `«PASTE»`**

把 `«PASTE»` 整体替换为 `docs/superpowers/specs/build-申通新格式/rule3131_sqlescaped.txt` 的全部内容(它已是 SQL 转义后的 JSON,直接落在两个 `'` 之间)。验证替换后该行以 `N'{""mode"":""rulesBased""` 开头、以 `}'` 结尾。

- [ ] **Step 3: 注册 V56**

`new(55, ...),` 之后加:

```csharp
            new(56, "申通新格式:自动凭证规则3131(164组品牌版) (2026-06-19)", MigrateV56),
```

- [ ] **Step 4: 编译**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj -v minimal`
Expected: 生成成功(若报字符串未闭合,说明粘贴漏字符,重粘 rule3131_sqlescaped.txt 全文)。

- [ ] **Step 5: 提交**

```bash
git add src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs
git commit -m "feat(cardflow): 申通新格式自动凭证规则3131-164组(MigrateV56)"
```

---

## Task 4: 流程 2331 + 版本 2331 + 节点 6892-6896(MigrateV57)

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs`(`MigrateV56` 后加 `MigrateV57` + 注册)

- [ ] **Step 1: 加 MigrateV57 方法**

在 `MigrateV56` 之后插入:

```csharp
    private static void MigrateV57(STOTOPDbContext ctx)
    {
        if (!SeederHelper.IsSqlServer(ctx)) return;
        // 申通新格式流程三件套:流程2331 + 版本2331 + 节点6892-6896(链:导入3130→质量→凭证3131→批次汇总→确认)
        ExecSql(ctx, @"
        SET IDENTITY_INSERT [CF卡片流程] ON;
        IF NOT EXISTS (SELECT 1 FROM [CF卡片流程] WHERE [FID] = 2331)
        INSERT INTO [CF卡片流程] ([FID],[F乐观锁],[F创建人ID],[F创建时间],[F可发起角色JSON],[F描述],[F更新时间],[F标题模板],[F流程名称],[F流程组ID],[F流程编码],[F状态],[F组织ID],[F编号模板],[F触发配置JSON],[F账套ID],[F匹配规则],[F是否模板])
        VALUES (2331, NULL, 1, GETDATE(), NULL, N'申通5.7后新格式交易明细导入:Excel导入(规则3130)→质量→自动凭证(规则3131)→批次汇总→确认', GETDATE(), NULL, N'申通总部交易明细导入(新格式)', NULL, N'PL_ST_HQ_TX_V2', N'published', 192, NULL, N'{""type"":""fileUpload""}', NULL, NULL, 0);
        SET IDENTITY_INSERT [CF卡片流程] OFF;

        SET IDENTITY_INSERT [CF流程版本] ON;
        IF NOT EXISTS (SELECT 1 FROM [CF流程版本] WHERE [FID] = 2331)
        INSERT INTO [CF流程版本] ([FID],[F创建人ID],[F创建时间],[F卡片SchemaJSON],[F发布时间],[F明细SchemaJSON],[F是否当前版本],[F流程定义ID],[F流程设置JSON],[F版本号],[F状态])
        VALUES (2331, 1, GETDATE(), NULL, GETDATE(), NULL, 1, 2331, NULL, 1, N'published');
        SET IDENTITY_INSERT [CF流程版本] OFF;

        SET IDENTITY_INSERT [CF流程节点] ON;
        IF NOT EXISTS (SELECT 1 FROM [CF流程节点] WHERE [FID] = 6892)
        INSERT INTO [CF流程节点] ([FID],[F流程版本ID],[F排序号],[F节点名称],[F类型],[F处理粒度],[F审批模式],[F插件注册ID],[F插件规则ID])
        VALUES (6892, 2331, 1, N'Excel导入解析', N'auto', N'batch', N'single', 1, 3130);
        IF NOT EXISTS (SELECT 1 FROM [CF流程节点] WHERE [FID] = 6893)
        INSERT INTO [CF流程节点] ([FID],[F流程版本ID],[F排序号],[F节点名称],[F类型],[F处理粒度],[F审批模式],[F插件注册ID],[F插件规则ID])
        VALUES (6893, 2331, 2, N'质量分析', N'auto', N'batch', N'single', 3, NULL);
        IF NOT EXISTS (SELECT 1 FROM [CF流程节点] WHERE [FID] = 6894)
        INSERT INTO [CF流程节点] ([FID],[F流程版本ID],[F排序号],[F节点名称],[F类型],[F处理粒度],[F审批模式],[F插件注册ID],[F插件规则ID])
        VALUES (6894, 2331, 3, N'自动凭证', N'auto', N'batch', N'single', 5, 3131);
        IF NOT EXISTS (SELECT 1 FROM [CF流程节点] WHERE [FID] = 6895)
        INSERT INTO [CF流程节点] ([FID],[F流程版本ID],[F排序号],[F节点名称],[F类型],[F处理粒度],[F审批模式],[F插件注册ID],[F插件规则ID])
        VALUES (6895, 2331, 4, N'批次汇总', N'auto', N'batch', N'single', 14, NULL);
        IF NOT EXISTS (SELECT 1 FROM [CF流程节点] WHERE [FID] = 6896)
        INSERT INTO [CF流程节点] ([FID],[F流程版本ID],[F排序号],[F节点名称],[F类型],[F处理粒度],[F审批模式],[F插件注册ID],[F插件规则ID])
        VALUES (6896, 2331, 5, N'确认通知', N'human', N'card', N'single', NULL, NULL);
        SET IDENTITY_INSERT [CF流程节点] OFF;
        ");
    }
```

- [ ] **Step 2: 注册 V57**

`new(56, ...),` 之后加:

```csharp
            new(57, "申通新格式:流程2331+版本2331+节点6892-6896 (2026-06-19)", MigrateV57),
```

- [ ] **Step 3: 编译**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj -v minimal`
Expected: 生成成功。

- [ ] **Step 4: 提交**

```bash
git add src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs
git commit -m "feat(cardflow): 申通新格式流程2331三件套(MigrateV57)"
```

---

## Task 5: 落库 + 端到端实测(测试库)

> 启动后端会跑 MigrationRunner 把 V54-V57 应用到测试库 live(= live 追平)。然后用真实 4 文件跑通导入→凭证。

- [ ] **Step 1: 重启后端(应用迁移到 live)**

用 restart-dev 技能或 `scripts/dev` 启动后端(端口 9000)。等待启动日志含 `CardFlow ... V57 ... Success`(或无迁移报错)。

- [ ] **Step 2: 校验迁移落库**

Run:
```bash
python Temp/dbq.py "SELECT COL_LENGTH(N'STG申通总部交易明细',N'F费用收付类型') c1, COL_LENGTH(N'STG申通总部交易明细',N'F进出港标识') c2; SELECT FID FROM CF自动插件_规则 WHERE FID IN (3130,3131); SELECT FID,F状态,F是否当前版本 FROM CF流程版本 WHERE FID=2331; SELECT COUNT(*) FROM CF流程节点 WHERE F流程版本ID=2331;"
```
Expected: c1/c2 非 NULL;规则 3130、3131 都在;版本 2331 published 当前版本=1;节点数=5。

- [ ] **Step 3: 校验路由(新文件命中流程2331 而非2257)**

写临时脚本调 `BatchTriggerService.MatchFlowDefinitionsAsync` 或直接核对:新文件 17 列(`会计日期,扣付日期,结算日期,网点名称,网点编码,交易类型,费用名称,费用编码,费用收付类型,业务摘要,发生额(收入),发生额(支出),余额,进/出港标识,联系人,关联单号,银行账号`)排序后 == 规则3130 `fullColumnIdentifier` 排序;旧规则20 列集不同。可用 SQL 取两规则的 fullColumnIdentifier 比对。

- [ ] **Step 4: 上传 4 个新文件(正规登录)**

登录 `admin/admin123` + 请求头 `X-Org-Context:192` → POST `/api/auth/login` 拿 token → 对每个文件 POST `/api/cardflow/import/upload`(`E:\STOTOP_Fable\Taicang\申通总部交易明细新-20260507开始\` 下 4 个 .xls)。记录返回的 batchId。

- [ ] **Step 5: 校验导入(R1 解析器门禁 + 行数)**

Run:
```bash
python Temp/dbq.py "SELECT COUNT(*) 行数, COUNT(DISTINCT F网点编号) 网点 FROM STG申通总部交易明细 WHERE F批次ID IN (<batchIds>); SELECT TOP 5 F科目编码,F费用名称,F费用收付类型,F进出港标识,F发生额收入,F发生额支出 FROM STG申通总部交易明细 WHERE F批次ID IN (<batchIds>);"
```
Expected: 行数 ≈ 15679(4网点,允许合计行被跳过的小差异);F费用收付类型/F进出港标识 有值(证明南郊/浏河 inlineStr 流式文件也被 MiniExcel 完整读出,无丢列/丢行)。

- [ ] **Step 6: 校验凭证(借贷平衡/品牌版/覆盖率/三维辅助/createDraft)**

Run:
```bash
python Temp/dbq.py "
SELECT COUNT(DISTINCT v.FID) 凭证数 FROM FIN凭证 v WHERE v.F数据作用域ID LIKE N'<batchId>|%';
-- 借贷平衡:每张凭证借=贷
SELECT SUM(CASE WHEN F方向=N'借' THEN F金额 ELSE -F金额 END) 净额 FROM FIN凭证分录 e JOIN FIN凭证 v ON e.F凭证ID=v.FID WHERE v.F数据作用域ID LIKE N'<batchId>|%';
-- 科目全品牌版(账套2 600/700段)
SELECT COUNT(*) 非品牌版分录 FROM FIN凭证分录 e JOIN FIN凭证 v ON e.F凭证ID=v.FID WHERE v.F数据作用域ID LIKE N'<batchId>|%' AND e.F科目ID NOT BETWEEN 700000 AND 700999;
-- 待补录草稿(资金类充值/取款)
SELECT COUNT(*) 待补录 FROM FIN凭证 WHERE F来源 LIKE N'%费用支出%' AND F备注 LIKE N'%待补录%' AND FStatus=0;
"
```
Expected: 凭证数 >0;净额=0(全平衡);非品牌版分录=0;待补录草稿存在(资金类未匹配→createDraft)。
辅助核算抽查:取若干分录确认 outlet=网点编号、express_brand=ST、business_direction 落 OUT/IN/CMB(与映射一致)。

> ⚠️ 字段名以真实表为准(`FIN凭证`/`FIN凭证分录`/`F数据作用域ID`/`F方向`/`F金额`/`F科目ID`),Step 6 SQL 列名需先用 `INFORMATION_SCHEMA.COLUMNS` 核对再跑(参 shentong-hq-tx-import-test 记忆里旧实测的字段)。

- [ ] **Step 7: 撤销测试批次(清理)**

对每个 batchId:`DELETE /api/cardflow/import/batches/{id}?mode=delete&force=true`,确认 STG 行软删 + 凭证按作用域删。

- [ ] **Step 8: 提交实测记录**

把实测结论(行数/凭证数/平衡/覆盖率/三维)写入 `docs/superpowers/specs/2026-06-19-申通新格式映射草案.md` 末尾「端到端实测」节并提交。

---

## Task 6: 全量测试 + 全特性整体审

- [ ] **Step 1: CardFlow 测试**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj --arch x64 -v minimal`
Expected: 全绿(允许既有 skip)。

- [ ] **Step 2: Finance 测试**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests/STOTOP.Module.Finance.Tests.csproj --arch x64 -v minimal`
Expected: 全绿。

- [ ] **Step 3: 全特性整体 Opus 审 + 对抗式验证**

按 [[holistic-review-after-subagent-tasks]]:跑多并行 Workflow 审视全特性(路由不串旧链/迁移幂等/规则3131 config 落库后 ValidateConfig 通过/借贷平衡/辅助三维/createDraft/旧规则21与流程2257零改动),对抗式验证关键断言。修复发现项。

- [ ] **Step 4: 终审提交**

```bash
git add -A && git commit -m "test(cardflow): 申通新格式端到端实测+整体审通过"
```

---

## Self-Review(已核对)

- **Spec 覆盖**:A路由(Task5 Step3 验证)/B STG列(Task1)/C导入规则3130(Task2)/D流程2331(Task4)/E凭证规则3131(Task3)/F映射(已定稿 final.json)/G落库迁移(Task1-4)+live(Task5)→ 全覆盖。
- **占位扫描**:V56 的 `«PASTE»` 是明确的"粘贴生成文件"指令(非 TODO),已给精确来源文件与校验。无其它占位。
- **FID/类型一致**:导入3130/凭证3131/流程2331/版本2331/节点6892-6896 全程一致;辅助 fid(220201=700125/1901=700044/ST=16)与映射 final.json 一致;`accountId` 用 fid(引擎只认 fid)。
- **风险**:R1 解析器(已单测预验、Task5 Step5 终验)/R3 金额方向矛盾(映射已用主额列口径解决)/路由(Task5 Step3)。
