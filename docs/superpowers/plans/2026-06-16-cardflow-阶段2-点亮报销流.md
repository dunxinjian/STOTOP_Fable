# CardFlow 阶段2：点亮费用报销流 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现。步骤用 `- [ ]` 复选框跟踪。
> 所有 commit 末尾追加：`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`；**绝不 push**。

**Goal:** 让 CardFlow 第一次端到端跑通——新增 `FIsTemplate` 列修好"从模板创建"恒空、seed 一条 FOrgId=0 费用报销全局模板（发起→部门负责人→财务→完成，人工审批）、补 InMemory + SQL Server 两种端到端测试、加 `cardflow.apply` 一级发起入口。

**Architecture:** 纯增量。schema 无 EF Migrations——新列靠"实体属性 + EF Configuration 映射"（新库 `DatabaseMigrator.CreateMissingTables` 自动建）+ `CardFlowSeeder` 版本化幂等 SQL（存量库补列、灌种子）。两段迁移分离：**V21 加列、V22 灌模板**（保证 ALTER 先于 INSERT）。测试照搬 `FlowEngineReturnToStageTests` 的 InMemory 服务级集成模式。

**Tech Stack:** .NET（C#，`STOTOP.Module.CardFlow` + `STOTOP.WebAPI`）、xUnit + EFCore.InMemory、Vue（仅触发动作种子，无前端组件改动）、Windows / PowerShell。

---

## 关键现状事实（已逐字核实，写代码照此）

- **CardFlowSeeder**：`src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs`。执行 SQL 用 `ExecSql(ctx, sql)`（内部 `SeederHelper.ExecuteRawSql`，纯 ADO，JSON 大括号无需转义）。每个 `MigrateVN` 开头 `if (!SeederHelper.IsSqlServer(ctx)) return;`。`Migrate()` 里有 `steps` 列表，**当前最大版本 = 20**。C# verbatim string 内 JSON 双引号写作 `""`。
- **InMemory 测试不会执行 seed 迁移**（被 `IsSqlServer` 守卫挡掉），故 Task 1/3/4 的迁移代码只需"编译通过 + 既有 96 测试仍绿"，真实 SQL 执行由 Task 6 的 SQL 冒烟（Skip 保护）与人工启动验收覆盖。
- **可用 FID**：CF卡片流程/CF流程版本 现最大 2261 → 新模板用 **流程 2262 / 版本 2263**；CF流程节点现最大 5030 → 新节点用 **5031（部门）/ 5032（财务）**。
- **WorkflowSeeder**：`src/STOTOP.WebAPI/Data/Seeders/WorkflowSeeder.cs`。其 `MigrateV1` 的插入被 `IF NOT EXISTS (SELECT 1 FROM [WF触发动作])`（**整表为空才插**）包裹，V1 已执行过；新增 `cardflow.apply` **必须用新的 MigrateV2**、按 `F标识` 幂等单插。该文件用 `ctx.Database.ExecuteSqlRaw(@"...")`（无 ExecSql 辅助）。
- **`cardflow.apply` 路由**：`/cardflow/home`（routes.ts:869 已存在 name `CardFlowHome`，"我要发起"集成在该页内）。
- **E2E**：`FlowEngineService` 构造 25 参（22 必填 + 3 可选）；`SubmitAsync(long cardId, long operatorId)`；`ApproveAsync(long cardId, long operatorId, ApproveRequest request)`。`CreateEngine` 与所有 `Fake*`/`DbTodoService` 在 `FlowEngineReturnToStageTests.cs` 内是 `private sealed`，**新测试文件需自带一份副本**。

---

## 文件结构

**修改：**
- `src/STOTOP.Module.CardFlow/Entities/CfFlowDefinition.cs`（加 `FIsTemplate` 属性）
- `src/STOTOP.Module.CardFlow/Configurations/CfFlowDefinitionConfiguration.cs`（加列映射）
- `src/STOTOP.Module.CardFlow/Dtos/Responses.cs`（`FlowDefinitionDto` 加 `IsTemplate`）
- `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs`（`GetTemplatesAsync` 过滤 + `SaveAsTemplateAsync` 两分支置标记 + `MapToDto`）
- `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs`（注册并实现 `MigrateV21` 加列、`MigrateV22` 灌模板）
- `src/STOTOP.WebAPI/Data/Seeders/WorkflowSeeder.cs`（注册并实现 `MigrateV2` 加 `cardflow.apply`）

**新建：**
- `tests/STOTOP.Module.CardFlow.Tests/Approval/ExpenseApprovalE2ETests.cs`（InMemory 服务级 E2E）
- `tests/STOTOP.Module.CardFlow.Tests/Smoke/SqlServerSchemaSmokeTests.cs`（SQL Server 冒烟，Skip 保护）

---

## 通用验证命令

- 编译：`dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj` → 预期 `Build succeeded` + 0 Error（含既有 CS0618 警告无妨）。
- 单测：`dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj` → 预期 `Passed!`，Failed: 0（Task 5 后通过数 = 96 + 新增）。
- 注：seed 迁移在 InMemory 下被 `IsSqlServer` 守卫跳过，故测试不执行真实 SQL。

---

## Task 1：新增 `FIsTemplate` 列（实体 + 映射 + MigrateV21 加列）

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Entities/CfFlowDefinition.cs`
- Modify: `src/STOTOP.Module.CardFlow/Configurations/CfFlowDefinitionConfiguration.cs`
- Modify: `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs`

- [ ] **Step 1：实体加属性**

在 `CfFlowDefinition.cs` 的 `FMatchPattern` 属性之后、`FCreatorId` 之前，加：
```csharp
    /// <summary>是否为可克隆的流程模板（"从模板创建"列表来源）</summary>
    public bool FIsTemplate { get; set; } = false;
```

- [ ] **Step 2：EF Configuration 加列映射**

在 `CfFlowDefinitionConfiguration.cs` 的 `builder.Property(e => e.FMatchPattern)...` 行之后加：
```csharp
        builder.Property(e => e.FIsTemplate).HasColumnName("F是否模板").HasDefaultValue(false);
```

- [ ] **Step 3：注册 MigrateV21**

在 `CardFlowSeeder.cs` 的 `steps` 列表末尾（`new(20, ...)` 之后）加一行：
```csharp
            new(21, "CF卡片流程新增 F是否模板 列 (2026-06-16)", MigrateV21),
```

- [ ] **Step 4：实现 MigrateV21（幂等加列）**

在 `CardFlowSeeder.cs` 类内（与其它 `MigrateVN` 并列，建议放文件末尾最后一个 `MigrateVN` 之后）加：
```csharp
    private static void MigrateV21(STOTOPDbContext ctx)
    {
        if (!SeederHelper.IsSqlServer(ctx)) return;

        ExecSql(ctx, @"
        IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.COLUMNS
            WHERE TABLE_NAME = N'CF卡片流程' AND COLUMN_NAME = N'F是否模板')
        ALTER TABLE [CF卡片流程] ADD [F是否模板] bit NOT NULL CONSTRAINT [DF_CF卡片流程_F是否模板] DEFAULT 0;
        ");
    }
```

- [ ] **Step 5：编译 + 单测验证**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj`
Expected: `Build succeeded`，0 Error。
Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj`
Expected: `Passed!`，Failed: 0（仍 96 通过）。

- [ ] **Step 6：Commit**
```powershell
git add "src/STOTOP.Module.CardFlow/Entities/CfFlowDefinition.cs" `
        "src/STOTOP.Module.CardFlow/Configurations/CfFlowDefinitionConfiguration.cs" `
        "src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs"
git commit -m @'
feat(cardflow): CfFlowDefinition 新增 FIsTemplate 列（实体+映射+MigrateV21 幂等加列）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 2：模板发现口径修复（GetTemplatesAsync + SaveAsTemplateAsync + DTO）

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Dtos/Responses.cs`
- Modify: `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs`

- [ ] **Step 1：DTO 加字段**

在 `Responses.cs` 的 `FlowDefinitionDto` 类内，`DraftVersion` 属性之后加：
```csharp
    public bool IsTemplate { get; set; }
```

- [ ] **Step 2：`GetTemplatesAsync` 改过滤口径 + 投影**

在 `FlowDefinitionService.cs` 的 `GetTemplatesAsync`：
把
```csharp
            .Where(x => x.FOrgId == 0 && x.FStatus == "published")
```
改为
```csharp
            .Where(x => x.FIsTemplate && x.FStatus == "published")
```
并在该方法的 `Select(x => new FlowDefinitionDto { ... })` 投影里，`AccountSetId = x.FAccountSetId` 之后加：
```csharp
                IsTemplate = x.FIsTemplate,
```

- [ ] **Step 3：`SaveAsTemplateAsync` 两分支置标记**

在 `SaveAsTemplateAsync` 的 **update 分支**（`existingTemplate != null`），`existingTemplate.FStatus = "published";` 之后加：
```csharp
            existingTemplate.FIsTemplate = true;
```
在 **else 分支**（新建模板），`templateDef.FStatus = "published";` 之后加：
```csharp
            templateDef.FIsTemplate = true;
```

- [ ] **Step 4：`MapToDto` 加映射**

在 `MapToDto` 的 `AccountSetId = entity.FAccountSetId` 之后加：
```csharp
            IsTemplate = entity.FIsTemplate,
```

- [ ] **Step 5：编译 + 单测验证**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj` → `Build succeeded`，0 Error。
Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj` → `Passed!`，Failed: 0。

- [ ] **Step 6：Commit**
```powershell
git add "src/STOTOP.Module.CardFlow/Dtos/Responses.cs" `
        "src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs"
git commit -m @'
feat(cardflow): 模板发现改按 FIsTemplate 过滤（修"从模板创建"恒空）+ SaveAsTemplate 置标记

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 3：费用报销 FOrgId=0 全局模板种子（MigrateV22）

> 用独立的 V22（晚于 V21 加列），保证 ALTER 先执行、列已存在再 INSERT。三段 `ExecSql` 各自幂等。

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs`

- [ ] **Step 1：注册 MigrateV22**

在 `steps` 列表末尾（`new(21, ...)` 之后）加：
```csharp
            new(22, "费用报销 FOrgId=0 全局模板种子 (2026-06-16)", MigrateV22),
```

- [ ] **Step 2：实现 MigrateV22（流程 + 版本 + 2 节点，幂等）**

在 `CardFlowSeeder.cs` 类内（紧接 `MigrateV21` 之后）加。schema JSON 照搬已验证的 FYBS（FID1347）内容：
```csharp
    private static void MigrateV22(STOTOPDbContext ctx)
    {
        if (!SeederHelper.IsSqlServer(ctx)) return;

        // 1) 流程定义（FOrgId=0，F是否模板=1，published）
        ExecSql(ctx, @"
        IF NOT EXISTS (SELECT 1 FROM [CF卡片流程] WHERE [FID] = 2262)
        BEGIN
            SET IDENTITY_INSERT [CF卡片流程] ON;
            INSERT INTO [CF卡片流程] ([FID],[F乐观锁],[F创建人ID],[F创建时间],[F可发起角色JSON],[F描述],[F更新时间],[F标题模板],[F流程名称],[F流程组ID],[F流程编码],[F状态],[F组织ID],[F编号模板],[F触发配置JSON],[F账套ID],[F匹配规则],[F是否模板])
            VALUES (2262, NULL, 0, GETDATE(), NULL, N'费用报销审批模板（部门负责人→财务）', GETDATE(), N'{发起人}-费用报销-{金额}元', N'费用报销（模板）', NULL, N'FYBS_TEMPLATE', N'published', 0, N'FYBSTPL-{yyyy}{MM}{dd}-{seq}', NULL, NULL, NULL, 1);
            SET IDENTITY_INSERT [CF卡片流程] OFF;
        END
        ");

        // 2) 流程版本（current、published，复用 FYBS 扁平 schema）
        ExecSql(ctx, @"
        IF NOT EXISTS (SELECT 1 FROM [CF流程版本] WHERE [FID] = 2263)
        BEGIN
            SET IDENTITY_INSERT [CF流程版本] ON;
            INSERT INTO [CF流程版本] ([FID],[F创建人ID],[F创建时间],[F卡片SchemaJSON],[F发布时间],[F明细SchemaJSON],[F是否当前版本],[F流程定义ID],[F流程设置JSON],[F版本号],[F状态])
            VALUES (2263, 0, GETDATE(), N'[{""key"":""applicant"",""label"":""申请人"",""type"":""user"",""required"":true},{""key"":""department"",""label"":""部门"",""type"":""department"",""required"":true},{""key"":""amount"",""label"":""报销金额"",""type"":""amount"",""required"":true,""source"":""detailSum""},{""key"":""category"",""label"":""费用类别"",""type"":""select"",""required"":true,""options"":[""差旅费"",""办公费"",""招待费"",""交通费"",""通讯费"",""其他""]},{""key"":""description"",""label"":""报销说明"",""type"":""textarea"",""required"":true},{""key"":""attachments"",""label"":""附件"",""type"":""attachment"",""required"":false}]', GETDATE(), N'[{""key"":""expenseDate"",""label"":""费用日期"",""type"":""date"",""required"":true},{""key"":""expenseType"",""label"":""费用类型"",""type"":""select"",""required"":true,""options"":[""差旅费"",""办公费"",""招待费"",""交通费"",""通讯费"",""住宿费"",""餐费"",""其他""]},{""key"":""description"",""label"":""费用说明"",""type"":""text"",""required"":true},{""key"":""amount"",""label"":""金额"",""type"":""amount"",""required"":true},{""key"":""invoiceNo"",""label"":""发票号"",""type"":""text"",""required"":false}]', 1, 2262, NULL, 1, N'published');
            SET IDENTITY_INSERT [CF流程版本] OFF;
        END
        ");

        // 3) 两个人工节点（线性，靠排序号推进；fixedUsers→管理员占位）
        ExecSql(ctx, @"
        IF NOT EXISTS (SELECT 1 FROM [CF流程节点] WHERE [FID] = 5031)
        BEGIN
            SET IDENTITY_INSERT [CF流程节点] ON;
            INSERT INTO [CF流程节点] ([FID],[F流程版本ID],[F节点键],[F排序号],[F节点名称],[F类型],[F处理粒度],[F审批模式],[F插件注册ID],[F插件规则ID],[F处理人策略],[F处理人配置JSON])
            VALUES (5031, 2263, N'tpl_expense_dept', 1, N'部门负责人审批', N'human', N'card', N'single', NULL, NULL, N'fixedUsers', N'{""users"":[{""userId"":1,""userName"":""管理员""}],""fallback"":{""mode"":""fixedUsers"",""users"":[{""userId"":1,""userName"":""管理员""}]}}');
            INSERT INTO [CF流程节点] ([FID],[F流程版本ID],[F节点键],[F排序号],[F节点名称],[F类型],[F处理粒度],[F审批模式],[F插件注册ID],[F插件规则ID],[F处理人策略],[F处理人配置JSON])
            VALUES (5032, 2263, N'tpl_expense_finance', 2, N'财务审批', N'human', N'card', N'single', NULL, NULL, N'fixedUsers', N'{""users"":[{""userId"":1,""userName"":""管理员""}],""fallback"":{""mode"":""fixedUsers"",""users"":[{""userId"":1,""userName"":""管理员""}]}}');
            SET IDENTITY_INSERT [CF流程节点] OFF;
        END
        ");
    }
```

- [ ] **Step 3：编译 + 单测验证**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj` → `Build succeeded`，0 Error。
Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj` → `Passed!`，Failed: 0（InMemory 不执行该 SQL）。

- [ ] **Step 4：Commit**
```powershell
git add "src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs"
git commit -m @'
feat(cardflow): seed 费用报销全局模板（FOrgId=0/FIsTemplate=1，部门负责人→财务）MigrateV22

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 4：WF 触发动作 `cardflow.apply`（WorkflowSeeder MigrateV2）

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/WorkflowSeeder.cs`

- [ ] **Step 1：注册 MigrateV2**

在 `WorkflowSeeder.Migrate` 的 steps 列表里，`new(1, "WF触发动作种子数据", MigrateV1),` 之后加：
```csharp
            new(2, "新增 cardflow.apply 发起审批入口 (2026-06-16)", MigrateV2),
```

- [ ] **Step 2：实现 MigrateV2（按 F标识 幂等单插）**

在 `WorkflowSeeder` 类内、`MigrateV1` 方法之后加：
```csharp
    private static void MigrateV2(STOTOPDbContext ctx)
    {
        if (!SeederHelper.IsSqlServer(ctx)) return;

        ctx.Database.ExecuteSqlRaw(@"
IF NOT EXISTS (SELECT 1 FROM [WF触发动作] WHERE [F标识] = 'cardflow.apply')
    INSERT INTO [WF触发动作] ([F组织ID],[F标识],[F名称],[F图标],[F模块],[F路由],[F类别],[F权限码],[F排序],[F描述]) VALUES
    (0, 'cardflow.apply', N'发起审批', 'AuditOutlined', 'cardflow', '/cardflow/home', 'apply', NULL, 41, N'发起一条卡片审批流程（如费用报销）');
");
    }
```

- [ ] **Step 3：编译验证**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj` → `Build succeeded`，0 Error。

- [ ] **Step 4：Commit**
```powershell
git add "src/STOTOP.WebAPI/Data/Seeders/WorkflowSeeder.cs"
git commit -m @'
feat(cardflow): 新增 cardflow.apply 发起审批一级入口（WorkflowSeeder MigrateV2）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 5：InMemory 服务级 E2E 测试

> 照搬 `FlowEngineReturnToStageTests` 的 `CreateEngine` + Fake 副本。两个测试：(A) 多级审批生命周期（部门→财务→完成，**用已验证的"seed 首节点 active + 两次 ApproveAsync"模式**，保证可靠）；(B) `SubmitAsync` 从 draft 拉起第一节点。

**Files:**
- Create: `tests/STOTOP.Module.CardFlow.Tests/Approval/ExpenseApprovalE2ETests.cs`

- [ ] **Step 1：写测试文件（含 CreateEngine + Fake 副本）**

创建 `ExpenseApprovalE2ETests.cs`，内容：
```csharp
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Logging.Abstractions;
using STOTOP.Core.Models;
using STOTOP.Module.CardFlow.AutoPlugin;
using STOTOP.Module.CardFlow.Dtos;
using STOTOP.Module.CardFlow.Entities;
using STOTOP.Module.CardFlow.Models.Approval;
using STOTOP.Module.CardFlow.Services;
using STOTOP.Module.CardFlow.Services.Interfaces;
using STOTOP.Module.Finance.Dtos;
using STOTOP.Module.Finance.Services.Interfaces;
using STOTOP.Module.System.Entities;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Approval;

public class ExpenseApprovalE2ETests
{
    [Fact]
    public async Task ExpenseFlow_DeptThenFinance_Approve_Completes()
    {
        using var db = TestDbContextFactory.Create(nameof(ExpenseFlow_DeptThenFinance_Approve_Completes));

        // 用户：部门负责人 21、财务 31
        db.Set<SysUser>().AddRange(
            new SysUser { FID = 21, FName = "部门经理" },
            new SysUser { FID = 31, FName = "财务" });

        // 版本 + 两节点（部门排序1 → 财务排序2）
        db.Set<CfFlowVersion>().Add(new CfFlowVersion
        {
            FID = 2263, FFlowDefinitionId = 2262, FStatus = "published", FIsCurrentVersion = true
        });
        db.Set<CfStageDefinition>().AddRange(
            new CfStageDefinition
            {
                FID = 5031, FFlowVersionId = 2263, FSortOrder = 1, FStageName = "部门负责人审批",
                FType = "human", FApprovalMode = "single", FAssigneeStrategy = "fixedUsers",
                FAssigneeConfigJson = """{"users":[{"userId":21,"userName":"部门经理"}]}"""
            },
            new CfStageDefinition
            {
                FID = 5032, FFlowVersionId = 2263, FSortOrder = 2, FStageName = "财务审批",
                FType = "human", FApprovalMode = "single", FAssigneeStrategy = "fixedUsers",
                FAssigneeConfigJson = """{"users":[{"userId":31,"userName":"财务"}]}"""
            });

        // 卡片：起步停在第一节点（部门）active —— 等价于"已发起"
        var card = new CfCard
        {
            FID = 9100, FFlowDefinitionId = 2262, FFlowVersionId = 2263,
            FTitle = "费用报销", FStatus = "active", FInitiatorId = 99, FInitiatorName = "发起人",
            FCurrentStageInstanceId = 9201, FCurrentRound = 1, FOrgId = 1, FDataJson = "{}"
        };
        db.Set<CfStageInstance>().Add(new CfStageInstance
        {
            FID = 9201, FCardId = 9100, FStageDefinitionId = 5031, FStageName = "部门负责人审批",
            FType = "human", FApprovalMode = "single", FRound = 1, FStatus = "active"
        });
        db.Set<CfStageAssignee>().Add(new CfStageAssignee
        {
            FID = 9301, FStageInstanceId = 9201, FUserId = 21, FUserName = "部门经理",
            FStatus = "pending", FAssignedTime = DateTime.Now
        });
        db.Set<CfTodoItem>().Add(new CfTodoItem
        {
            FID = 9401, FCardId = 9100, FStageInstanceId = 9201, FHandlerId = 21,
            FHandlerName = "部门经理", FStatus = "pending", FOrgId = 1
        });
        db.Set<CfCard>().Add(card);
        await db.SaveChangesAsync();

        var engine = CreateEngine(db);

        // 部门负责人批准 → 推进到财务节点
        await engine.ApproveAsync(9100, 21, new ApproveRequest { Comment = "同意" });
        var afterDept = await db.Set<CfCard>().FindAsync(9100L);
        Assert.Equal("active", afterDept!.FStatus);
        var financeStage = db.Set<CfStageInstance>().Single(s => s.FCardId == 9100 && s.FStageDefinitionId == 5032);
        Assert.Equal("active", financeStage.FStatus);

        // 财务批准 → 完成
        await engine.ApproveAsync(9100, 31, new ApproveRequest { Comment = "同意" });
        var done = await db.Set<CfCard>().FindAsync(9100L);
        Assert.Equal("completed", done!.FStatus);
    }

    [Fact]
    public async Task ExpenseFlow_Submit_FromDraft_ActivatesFirstStage()
    {
        using var db = TestDbContextFactory.Create(nameof(ExpenseFlow_Submit_FromDraft_ActivatesFirstStage));

        db.Set<SysUser>().Add(new SysUser { FID = 21, FName = "部门经理" });
        db.Set<CfFlowVersion>().Add(new CfFlowVersion
        {
            FID = 2263, FFlowDefinitionId = 2262, FStatus = "published", FIsCurrentVersion = true
        });
        db.Set<CfStageDefinition>().Add(new CfStageDefinition
        {
            FID = 5031, FFlowVersionId = 2263, FSortOrder = 1, FStageName = "部门负责人审批",
            FType = "human", FApprovalMode = "single", FAssigneeStrategy = "fixedUsers",
            FAssigneeConfigJson = """{"users":[{"userId":21,"userName":"部门经理"}]}"""
        });
        db.Set<CfCard>().Add(new CfCard
        {
            FID = 9500, FFlowDefinitionId = 2262, FFlowVersionId = 2263,
            FTitle = "费用报销", FStatus = "draft", FInitiatorId = 99, FInitiatorName = "发起人",
            FCurrentRound = 1, FOrgId = 1, FDataJson = "{}"
        });
        await db.SaveChangesAsync();

        var engine = CreateEngine(db);
        await engine.SubmitAsync(9500, 99);

        var card = await db.Set<CfCard>().FindAsync(9500L);
        Assert.Equal("active", card!.FStatus);
        var firstStage = db.Set<CfStageInstance>().Single(s => s.FCardId == 9500 && s.FStageDefinitionId == 5031);
        Assert.Equal("active", firstStage.FStatus);
    }

    private static FlowEngineService CreateEngine(STOTOP.Infrastructure.Data.STOTOPDbContext db)
    {
        var services = new ServiceCollection();
        var provider = services.BuildServiceProvider();
        var orchestration = new OrchestrationEngineService(db, NullLogger<OrchestrationEngineService>.Instance);

        return new FlowEngineService(
            db,
            new FakeNumberSequenceService(),
            new FakeCardSchemaService(),
            new ApprovalModeHandler(),
            new SequentialApprovalRuntime(),
            new ReturnToStageRuntime(),
            new StageConfigParser(),
            new StageFieldAccessService(),
            new StageActionPolicyService(),
            new ConditionEvaluator(),
            new ApproverResolver(db),
            new FakeBudgetOccupationService(),
            new DbTodoService(db),
            new FakeNotificationDispatcher(),
            new AutoPluginFactory(provider),
            provider,
            provider.GetRequiredService<IServiceScopeFactory>(),
            orchestration,
            new FakeBatchNotifier(),
            new FakeBatchLifecycleService(),
            NullLogger<FlowEngineService>.Instance);
    }
}
```

> ⚠️ Fake 类（`FakeNumberSequenceService`/`FakeCardSchemaService`/`FakeBudgetOccupationService`/`FakeNotificationDispatcher`/`FakeBatchNotifier`/`FakeBatchLifecycleService`/`DbTodoService`）当前是 `FlowEngineReturnToStageTests` 的 `private sealed`，**新文件访问不到**。Step 2 处理。

- [ ] **Step 2：让 Fake 类可复用**

打开 `tests/STOTOP.Module.CardFlow.Tests/Approval/FlowEngineReturnToStageTests.cs`，找到文件内这 7 个 `private sealed class`（约 969-1052 行）。把它们**整体移动**到新文件 `tests/STOTOP.Module.CardFlow.Tests/Approval/FlowEngineTestFakes.cs`，改为 `internal sealed class`（去掉 `private`），命名空间 `STOTOP.Module.CardFlow.Tests.Approval`，并补齐所需 using（参照 FlowEngineReturnToStageTests.cs 顶部）。
原 `FlowEngineReturnToStageTests.cs` 删除这些类的定义（它仍能用，因同命名空间 `internal` 可见）；其 `CreateEngine` 不变。
> 这样两个测试文件共享同一份 Fake，避免重复定义冲突（同命名空间内不能有两个同名类）。

- [ ] **Step 3：跑新测试验证通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj --filter "FullyQualifiedName~ExpenseApprovalE2ETests"`
Expected: `Passed!`，2 passed。
若 `ExpenseFlow_Submit_FromDraft_ActivatesFirstStage` 因 `SubmitAsync` 需要额外种子字段而失败：按报错补齐该测试的 Arrange（如补 `FCurrentStageInstanceId` 之外的必填项），保持断言不变；不得改业务代码。

- [ ] **Step 4：跑全量确认无回归**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj`
Expected: `Passed!`，Failed: 0，通过数 = 96 + 2 = 98。

- [ ] **Step 5：Commit**
```powershell
git add tests/STOTOP.Module.CardFlow.Tests/Approval/ExpenseApprovalE2ETests.cs `
        tests/STOTOP.Module.CardFlow.Tests/Approval/FlowEngineTestFakes.cs `
        tests/STOTOP.Module.CardFlow.Tests/Approval/FlowEngineReturnToStageTests.cs
git commit -m @'
test(cardflow): 费用报销审批 InMemory E2E（部门→财务→完成 + Submit 拉起）+ 抽出共享 Fake

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 6：SQL Server 冒烟测试（Skip 保护）

> 默认 Skip，仅当设了环境变量 `STOTOP_TEST_CONNECTION` 才连真实 SQL Server，验证建表后 `[CF卡片流程]` 含 `[F是否模板]` 列。不进常规 CI。

**Files:**
- Create: `tests/STOTOP.Module.CardFlow.Tests/Smoke/SqlServerSchemaSmokeTests.cs`

- [ ] **Step 1：写冒烟测试**

创建 `SqlServerSchemaSmokeTests.cs`：
```csharp
using Microsoft.EntityFrameworkCore;
using STOTOP.Infrastructure.Data;
using STOTOP.Module.CardFlow.Entities;
using STOTOP.Module.System.Entities;
using STOTOP.Module.Finance.Entities;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Smoke;

public class SqlServerSchemaSmokeTests
{
    private static string? Conn => Environment.GetEnvironmentVariable("STOTOP_TEST_CONNECTION");

    [SkippableFact]
    public async Task EnsureCreated_Builds_FIsTemplate_Column()
    {
        Skip.If(string.IsNullOrWhiteSpace(Conn), "未设 STOTOP_TEST_CONNECTION，跳过 SQL Server 冒烟");

        STOTOPDbContext.RegisterModuleAssembly(typeof(CfCard).Assembly);
        STOTOPDbContext.RegisterModuleAssembly(typeof(SysUser).Assembly);
        STOTOPDbContext.RegisterModuleAssembly(typeof(FinVoucher).Assembly);

        var options = new DbContextOptionsBuilder<STOTOPDbContext>()
            .UseSqlServer(Conn)
            .Options;
        await using var db = new STOTOPDbContext(options);

        await db.Database.EnsureCreatedAsync();

        var exists = await db.Database
            .SqlQueryRaw<int>(
                "SELECT COUNT(*) AS [Value] FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = N'CF卡片流程' AND COLUMN_NAME = N'F是否模板'")
            .SingleAsync();

        Assert.True(exists > 0, "建库后 [CF卡片流程] 应包含 [F是否模板] 列");
    }
}
```
> `[SkippableFact]` + `Skip.If` 来自 `Xunit.SkippableFact` 包。若测试项目未引用该包，Step 2 处理；若已引用则跳过 Step 2。

- [ ] **Step 2：确保 SkippableFact 可用**

Run（确认是否已引用）：用 Grep 在 `tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj` 搜 `SkippableFact`。
- 若**未命中**：在该 csproj 的 `<ItemGroup>` 里加 `<PackageReference Include="Xunit.SkippableFact" Version="1.4.13" />`，然后 `dotnet restore tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj`。
- 若已命中：无需改 csproj。
- 备选（不想加包）：把 `[SkippableFact]` 改为 `[Fact]`，方法首行改为 `if (string.IsNullOrWhiteSpace(Conn)) return;`（未设连接串即空跑通过），删掉 `Skip.If` 那行与 `using` 无关项。

- [ ] **Step 3：验证（默认 Skip 路径）**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj --filter "FullyQualifiedName~SqlServerSchemaSmokeTests"`
Expected: 测试被 Skip（或空跑通过），无 Failed。
Run（全量）: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj`
Expected: `Passed!`，Failed: 0（冒烟计为 Skipped）。

- [ ] **Step 4：Commit**
```powershell
git add tests/STOTOP.Module.CardFlow.Tests/Smoke/SqlServerSchemaSmokeTests.cs `
        tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj
git commit -m @'
test(cardflow): SQL Server 冒烟——验证建库含 F是否模板 列（Skip 保护，需 STOTOP_TEST_CONNECTION）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 7：收尾验证

**Files:** 无（仅验证）

- [ ] **Step 1：WebAPI 整体编译**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj`
Expected: `Build succeeded`，0 Error（既有 CS0618 警告无妨）。

- [ ] **Step 2：全量单测**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj`
Expected: `Passed!`，Failed: 0，Passed: 98（96 既有 + 2 新 E2E），Skipped: 1（SQL 冒烟）。

- [ ] **Step 3：（可选）SQL Server 冒烟实跑**

若手头有可写的 SQL Server：
```powershell
$env:STOTOP_TEST_CONNECTION = "<可写测试库连接串>"
dotnet test tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj --filter "FullyQualifiedName~SqlServerSchemaSmokeTests"
Remove-Item Env:\STOTOP_TEST_CONNECTION
```
Expected: 1 passed（确认建库含 F是否模板 列）。无 SQL Server 则跳过此步。

- [ ] **Step 4：人工验收清单（记录到收尾 commit 或回复）**

启动应用后逐项确认（开发库需先跑过 MigrateV21/V22）：
1. "从模板创建"列表返回"费用报销（模板）"（证明 FIsTemplate 过滤生效、恒空已修）。
2. 克隆该模板到当前组织 → 生成 FOrgId=本组织 的流程。
3. 克隆后的流程出现在"我要发起"，可填表发起。
4. 发起的卡片走 部门负责人审批 → 财务审批 → 完成。
5. "发起审批"入口（WF触发动作 cardflow.apply）可见、指向 /cardflow/home。

- [ ] **Step 5：（可选）收尾 commit**
```powershell
git commit --allow-empty -m @'
chore(cardflow): 阶段2 点亮报销流收尾——编译+单测(98 passed/1 skipped)通过

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
'@
```

---

## 自检（spec 覆盖 / 占位符 / 类型一致性）

- **spec 覆盖**：组件1（FIsTemplate 列）→ Task 1；组件1（GetTemplates/SaveAsTemplate/DTO）→ Task 2；组件2（报销模板种子）→ Task 3；组件3（cardflow.apply）→ Task 4；组件4 InMemory E2E → Task 5、SQL 冒烟 → Task 6；验证/验收 → Task 7。spec 第7节 I1（路由=/cardflow/home）、I3（FID 2262/2263/5031-5032）已落实；I2（Submit）在 Task 5 用"proven 模式 + 独立 Submit 测试"覆盖；I4（字段 type 照搬 FYBS）已落实。
- **占位符**：无 TBD/TODO；所有 SQL、列名、FID、签名均为实测确值。
- **类型一致性**：`FIsTemplate`(实体)/`F是否模板`(列名)/`IsTemplate`(DTO) 全程一致；版本 FID 2263 在种子与 E2E 测试中一致；节点 FID 5031/5032、版本 2263、流程 2262 三者引用关系一致；`ApproveAsync(cardId, operatorId, ApproveRequest)`、`SubmitAsync(cardId, operatorId)` 与实测签名一致。

---

## 完成后

阶段2 落地、作者复核"从模板创建 + 端到端审批跑通"后，进入**阶段3（收敛重复）**：5 套条件评估→1 / 3 套字段模型→1 / 处理器三代→1 / 恢复·撤销各一份 / `RowMapper` / `BatchJobKind`·`ValidateRowBuiltin` 等活文件死成员清理 / 前端内嵌编辑器 Tab 冻结。
