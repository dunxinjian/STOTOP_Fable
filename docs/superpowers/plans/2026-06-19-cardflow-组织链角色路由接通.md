# CardFlow 组织链/角色路由接通 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `OrgChain`/`RoleCodes`/`RoleNames` 在运行时与预演的求值上下文里被真实填充（从 card/request 的 org 沿 FParentId 上溯 + 查用户角色），使 `inOrgChain`/`roles.*` 路由条件由「恒 false」变为真正生效。

**Architecture:** 新建无状态 `OrgRoleContextResolver`：纯函数 `BuildOrgChain`（沿 FParentId 上溯放 FID 字符串、防环）+ DB 面 `ResolveAsync`（投影加载 active orgs → BuildOrgChain；查 SysUserRole JOIN SysRole → 角色编码/名称）。运行时 `ConditionEvaluationContextBuilder.BuildAsync` 与预演 `CardFlowPathPreviewService.BuildPreviewContextAsync` 共用 `ResolveAsync` 填三字段。无 schema/DI/构造器依赖改动。

**Tech Stack:** C# / .NET 10 / EF Core（InMemory 测试）/ xUnit。测试运行需 `--arch x64`。`STOTOP.Module.Task` 遮蔽 `System.Threading.Tasks.Task`，异步测试返回类型写 `global::System.Threading.Tasks.Task`。

**设计稿：** [2026-06-19-cardflow-组织链角色路由接通-design.md](../specs/2026-06-19-cardflow-组织链角色路由接通-design.md)

**提交纪律：** 在 master 上做（用户已授权）。每个 commit **只 `git add` 本计划新建/修改的文件**，不夹带工作树里其它未跟踪/已改文件（`_t6_*.json`/`.superpowers/`/并发会话 WIP 如 `FlowEngineService.cs`/`ExpenseApprovalE2ETests.cs` 等）。改 `FlowDefinitionService.cs` 不在本计划范围；本计划只碰下表文件。

---

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `src/STOTOP.Module.CardFlow/Services/OrgRoleContextResolver.cs` | 纯 `BuildOrgChain` + DB `ResolveAsync`，运行时/预演单一真源。 | **新建** |
| `src/STOTOP.Module.CardFlow/Services/ConditionEvaluationContextBuilder.cs` | `BuildAsync` 末尾调 `ResolveAsync` 填三字段。 | **修改** |
| `src/STOTOP.Module.CardFlow/Services/CardFlowPathPreviewService.cs` | `BuildPreviewContext`→实例 async `BuildPreviewContextAsync`，调 `ResolveAsync` 填三字段。 | **修改** |
| `tests/STOTOP.Module.CardFlow.Tests/Rules/OrgRoleContextResolverTests.cs` | 纯 `BuildOrgChain` 单测（4 case）。 | **新建** |
| `tests/STOTOP.Module.CardFlow.Tests/Rules/ConditionEvaluationContextBuilderTests.cs` | 追加运行时填充集成测试。 | **修改** |
| `tests/STOTOP.Module.CardFlow.Tests/Rules/CardFlowPathPreviewServiceTests.cs` | 追加预演 inOrgChain 命中集成测试。 | **修改** |

---

## Task 1: 纯函数 + DB 面 OrgRoleContextResolver

**Files:**
- Create: `tests/STOTOP.Module.CardFlow.Tests/Rules/OrgRoleContextResolverTests.cs`
- Create: `src/STOTOP.Module.CardFlow/Services/OrgRoleContextResolver.cs`

- [ ] **Step 1: 写失败测试（纯 BuildOrgChain）**

创建 `tests/STOTOP.Module.CardFlow.Tests/Rules/OrgRoleContextResolverTests.cs`：

```csharp
using STOTOP.Module.CardFlow.Services;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Rules;

public class OrgRoleContextResolverTests
{
    [Fact]
    public void BuildOrgChain_LinearHierarchy_WalksUpIncludingSelf()
    {
        var chain = OrgRoleContextResolver.BuildOrgChain(
            new[] { (3L, 2L), (2L, 1L), (1L, 0L) }, startOrgId: 3L);
        Assert.Equal(new[] { "3", "2", "1" }, chain);
    }

    [Fact]
    public void BuildOrgChain_StartNotInOrgs_ReturnsEmpty()
    {
        var chain = OrgRoleContextResolver.BuildOrgChain(
            new[] { (1L, 0L) }, startOrgId: 99L);
        Assert.Empty(chain);
    }

    [Fact]
    public void BuildOrgChain_CycleInParents_DoesNotLoopForever()
    {
        var chain = OrgRoleContextResolver.BuildOrgChain(
            new[] { (1L, 2L), (2L, 1L) }, startOrgId: 1L);
        Assert.Equal(new[] { "1", "2" }, chain);
    }

    [Fact]
    public void BuildOrgChain_SingleOrg_ReturnsSelf()
    {
        var chain = OrgRoleContextResolver.BuildOrgChain(
            new[] { (1L, 0L) }, startOrgId: 1L);
        Assert.Equal(new[] { "1" }, chain);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~OrgRoleContextResolverTests"`
Expected: 编译失败 / FAIL —— `OrgRoleContextResolver` 尚不存在。

- [ ] **Step 3: 写实现**

创建 `src/STOTOP.Module.CardFlow/Services/OrgRoleContextResolver.cs`：

```csharp
using Microsoft.EntityFrameworkCore;
using STOTOP.Infrastructure.Data;
using STOTOP.Module.System.Entities;

namespace STOTOP.Module.CardFlow.Services;

public sealed record OrgRoleContext(List<string> OrgChain, List<string> RoleCodes, List<string> RoleNames);

public static class OrgRoleContextResolver
{
    /// <summary>
    /// 从 startOrgId 沿 FParentId 上溯，含本组织，每级放 FID.ToString()。
    /// visited 防环；startOrgId 不在 orgs（停用/缺失）→ 空链。纯函数。
    /// </summary>
    public static List<string> BuildOrgChain(
        IReadOnlyCollection<(long Id, long ParentId)> orgs,
        long startOrgId)
    {
        var parentOf = new Dictionary<long, long>();
        foreach (var (id, parentId) in orgs)
            parentOf[id] = parentId;

        var chain = new List<string>();
        var visited = new HashSet<long>();
        var node = startOrgId;
        while (parentOf.TryGetValue(node, out var parent) && visited.Add(node))
        {
            chain.Add(node.ToString());
            node = parent;
        }
        return chain;
    }

    /// <summary>
    /// 加载 active 组织上溯 startOrgId 的 OrgChain；查 userId 的角色编码/名称。
    /// userId 空/≤0 → 角色空。运行时与预演共用。
    /// </summary>
    public static async Task<OrgRoleContext> ResolveAsync(
        STOTOPDbContext db, long orgId, long? userId, CancellationToken cancellationToken = default)
    {
        var orgRows = await db.Set<SysOrganization>()
            .Where(o => o.FStatus == 1)
            .Select(o => new { o.FID, o.FParentId })
            .ToListAsync(cancellationToken);
        var orgChain = BuildOrgChain(
            orgRows.Select(o => (o.FID, o.FParentId)).ToList(), orgId);

        var roleCodes = new List<string>();
        var roleNames = new List<string>();
        if (userId is long uid && uid > 0)
        {
            var roles = await db.Set<SysUserRole>()
                .Where(ur => ur.FUserId == uid)
                .Join(db.Set<SysRole>(), ur => ur.FRoleId, r => r.FID, (ur, r) => new { r.FCode, r.FName })
                .ToListAsync(cancellationToken);
            roleCodes = roles.Select(x => x.FCode).Where(c => !string.IsNullOrWhiteSpace(c)).ToList();
            roleNames = roles.Select(x => x.FName).Where(n => !string.IsNullOrWhiteSpace(n)).ToList();
        }

        return new OrgRoleContext(orgChain, roleCodes, roleNames);
    }
}
```

> ImplicitUsings 提供 `System.Linq`/`System.Collections.Generic`。三个显式 using 必需（EF Core、STOTOPDbContext、System 实体）。`ResolveAsync` 由 Task 2/3 集成测试覆盖。

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~OrgRoleContextResolverTests"`
Expected: PASS（4 个测试全绿）。

- [ ] **Step 5: 提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/OrgRoleContextResolver.cs tests/STOTOP.Module.CardFlow.Tests/Rules/OrgRoleContextResolverTests.cs
git commit -m "feat(cardflow): 抽 OrgRoleContextResolver(组织链上溯+角色查询)"
```

---

## Task 2: 运行时 BuildAsync 接线

**Files:**
- Modify: `tests/STOTOP.Module.CardFlow.Tests/Rules/ConditionEvaluationContextBuilderTests.cs`
- Modify: `src/STOTOP.Module.CardFlow/Services/ConditionEvaluationContextBuilder.cs`

- [ ] **Step 1: 写失败测试**

编辑 `tests/STOTOP.Module.CardFlow.Tests/Rules/ConditionEvaluationContextBuilderTests.cs`。文件顶部已有 `using STOTOP.Module.CardFlow.Entities;`——**新增** `using STOTOP.Module.System.Entities;`。在类内追加：

```csharp
    [Fact]
    public async global::System.Threading.Tasks.Task BuildAsync_ResolvesOrgChainAndRoles()
    {
        using var db = TestDbContextFactory.Create(nameof(BuildAsync_ResolvesOrgChainAndRoles));
        db.Set<SysOrganization>().AddRange(
            new SysOrganization { FID = 10, FParentId = 20, FCode = "LEAF", FName = "叶组织", FStatus = 1 },
            new SysOrganization { FID = 20, FParentId = 0, FCode = "ROOT", FName = "根组织", FStatus = 1 });
        db.Set<SysRole>().Add(new SysRole { FID = 100, FCode = "FIN", FName = "财务", FStatus = 1 });
        db.Set<SysUserRole>().Add(new SysUserRole { FID = 1, FUserId = 7, FRoleId = 100 });
        db.Set<CfCard>().Add(new CfCard
        {
            FID = 600, FOrgId = 10, FInitiatorId = 7, FCurrentRound = 1, FDataJson = "{}"
        });
        await db.SaveChangesAsync();

        var builder = new ConditionEvaluationContextBuilder(db);
        var context = await builder.BuildAsync(db.Set<CfCard>().Find(600L)!);

        Assert.Contains("10", context.OrgChain);
        Assert.Contains("20", context.OrgChain);   // 上溯到父组织
        Assert.Contains("FIN", context.RoleCodes);
        Assert.Contains("财务", context.RoleNames);
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~ConditionEvaluationContextBuilderTests.BuildAsync_ResolvesOrgChainAndRoles"`
Expected: FAIL —— 现 `BuildAsync` 不填 OrgChain/RoleCodes/RoleNames（恒空），`Assert.Contains` 失败。

- [ ] **Step 3: 接线 BuildAsync**

修改 `src/STOTOP.Module.CardFlow/Services/ConditionEvaluationContextBuilder.cs` 的 `BuildAsync`：在 `completedStageKey` 块（当前 [:50-57](../../../src/STOTOP.Module.CardFlow/Services/ConditionEvaluationContextBuilder.cs:50)）**之后、`return context;`（当前 :59）之前**插入：

```csharp
        var orgRole = await OrgRoleContextResolver.ResolveAsync(
            _dbContext, card.FOrgId, card.FInitiatorId, cancellationToken);
        context.OrgChain = orgRole.OrgChain;
        context.RoleCodes = orgRole.RoleCodes;
        context.RoleNames = orgRole.RoleNames;
```

`OrgRoleContextResolver` 同命名空间，无需新增 using。不改本方法其它部分。

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~ConditionEvaluationContextBuilderTests"`
Expected: PASS（新测试 + 既有 2 个 BuildAsync 测试全绿）。

- [ ] **Step 5: 提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/ConditionEvaluationContextBuilder.cs tests/STOTOP.Module.CardFlow.Tests/Rules/ConditionEvaluationContextBuilderTests.cs
git commit -m "fix(cardflow): 运行时 BuildAsync 填充 OrgChain/RoleCodes/RoleNames"
```

---

## Task 3: 预演 BuildPreviewContextAsync 接线

**Files:**
- Modify: `tests/STOTOP.Module.CardFlow.Tests/Rules/CardFlowPathPreviewServiceTests.cs`
- Modify: `src/STOTOP.Module.CardFlow/Services/CardFlowPathPreviewService.cs`

- [ ] **Step 1: 写失败测试**

编辑 `tests/STOTOP.Module.CardFlow.Tests/Rules/CardFlowPathPreviewServiceTests.cs`。**新增** `using STOTOP.Module.System.Entities;`。在 `CardFlowPathPreviewServiceTests` 类内追加测试 + 种子：

```csharp
    [Fact]
    public async global::System.Threading.Tasks.Task PreviewDraftVersion_InOrgChain_MatchesViaResolvedOrgChain()
    {
        using var db = TestDbContextFactory.Create(nameof(PreviewDraftVersion_InOrgChain_MatchesViaResolvedOrgChain));
        // 组织层级：叶 10 → 父 20
        db.Set<SysOrganization>().AddRange(
            new SysOrganization { FID = 10, FParentId = 20, FCode = "LEAF", FName = "叶", FStatus = 1 },
            new SysOrganization { FID = 20, FParentId = 0, FCode = "ROOT", FName = "根", FStatus = 1 });
        SeedInOrgChainFlow(db);
        await db.SaveChangesAsync();

        var service = new CardFlowPathPreviewService(db, new ConditionRuleEvaluator(), new AuditSnapshotPolicyService());

        // OrgId=10：org 链含祖先 20 → inOrgChain value=20 命中 vip 分支
        var hit = await service.PreviewDraftVersionAsync(900, new CardFlowPathPreviewRequest
        {
            DataJson = "{}",
            OrgId = 10
        });

        Assert.Equal("vip_org", hit.Steps[0].SelectedEdgeKey);
        Assert.Equal("vip", hit.Steps[1].StageKey);
    }

    private static void SeedInOrgChainFlow(STOTOP.Infrastructure.Data.STOTOPDbContext db)
    {
        db.Set<CfFlowDefinition>().Add(new CfFlowDefinition { FID = 900, FFlowName = "组织链路由", FFlowCode = "ORGCHAIN", FStatus = "draft", FOrgId = 1, FCreatedTime = DateTime.Now });
        db.Set<CfFlowVersion>().Add(new CfFlowVersion { FID = 901, FFlowDefinitionId = 900, FVersionNumber = 1, FStatus = "draft", FCreatedTime = DateTime.Now });
        db.Set<CfStageDefinition>().AddRange(
            new CfStageDefinition { FID = 90, FFlowVersionId = 901, FStageKey = "start", FStageName = "开始", FType = "human", FSortOrder = 1 },
            new CfStageDefinition { FID = 91, FFlowVersionId = 901, FStageKey = "vip", FStageName = "VIP", FType = "human", FSortOrder = 2 },
            new CfStageDefinition { FID = 92, FFlowVersionId = 901, FStageKey = "normal", FStageName = "普通", FType = "human", FSortOrder = 3 });
        db.Set<CfStageRouteRule>().AddRange(
            new CfStageRouteRule { FFlowVersionId = 901, FEdgeKey = "vip_org", FFromStageKey = "start", FToStageKey = "vip", FRouteName = "属于根组织链", FConditionJson = """{"field":"initiatorOrg.id","operator":"inOrgChain","value":20}""", FPriority = 1, FStatus = "active" },
            new CfStageRouteRule { FFlowVersionId = 901, FEdgeKey = "normal_default", FFromStageKey = "start", FToStageKey = "normal", FRouteName = "其他", FPriority = 99, FIsDefault = true, FStatus = "active" });
        // vip / normal 终端
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~CardFlowPathPreviewServiceTests.PreviewDraftVersion_InOrgChain_MatchesViaResolvedOrgChain"`
Expected: FAIL —— 现预演 `BuildPreviewContext` 不填 OrgChain（恒空），`inOrgChain value=20` 经 `IsInOrgChain` 在空链上判 false → 落默认分支 `normal_default`（`Steps[0].SelectedEdgeKey` 为 `normal_default` 而非 `vip_org`）。

- [ ] **Step 3: 改 BuildPreviewContext 为实例 async + 接线**

修改 `src/STOTOP.Module.CardFlow/Services/CardFlowPathPreviewService.cs`：

(a) 把 `BuildPreviewContext` 方法（当前 [:253-271](../../../src/STOTOP.Module.CardFlow/Services/CardFlowPathPreviewService.cs:253)）整体替换为实例 async：

```csharp
    private async Task<ConditionEvaluationContext> BuildPreviewContextAsync(
        CardFlowPathPreviewRequest request, CancellationToken cancellationToken)
    {
        var cardData = ParseObject(request.InitialDataJson);
        foreach (var pair in ParseObject(request.DataJson))
        {
            cardData[pair.Key] = pair.Value;
        }

        var context = ConditionContextFactory.Build(new ConditionContextInputs
        {
            CardData = cardData,
            SourceModule = request.SourceModule,
            SourceType = request.SourceType,
            SourceId = request.SourceId,
            InitiatorId = request.InitiatorId,
            OrgId = request.OrgId,
            HasCurrentStage = false
        });

        var orgRole = await OrgRoleContextResolver.ResolveAsync(
            _dbContext, request.OrgId ?? 0, request.InitiatorId, cancellationToken);
        context.OrgChain = orgRole.OrgChain;
        context.RoleCodes = orgRole.RoleCodes;
        context.RoleNames = orgRole.RoleNames;
        return context;
    }
```

(b) 改调用点（当前 [:71](../../../src/STOTOP.Module.CardFlow/Services/CardFlowPathPreviewService.cs:71)）`var context = BuildPreviewContext(request);` 为：

```csharp
        var context = await BuildPreviewContextAsync(request, cancellationToken);
```

`PreviewDraftVersionAsync` 已有 `cancellationToken` 形参；`OrgRoleContextResolver`/`_dbContext` 同类可用；`ParseObject`/`ToPlainValue` 静态辅助仍从实例方法可调，保持不变。

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~CardFlowPathPreviewServiceTests"`
Expected: PASS（新测试 + 既有预演测试全绿）。

- [ ] **Step 5: 提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/CardFlowPathPreviewService.cs tests/STOTOP.Module.CardFlow.Tests/Rules/CardFlowPathPreviewServiceTests.cs
git commit -m "fix(cardflow): 预演 BuildPreviewContextAsync 填充组织链/角色(预演inOrgChain真模拟)"
```

---

## Task 4: 收尾验证（全量模块单测 + 编译）

**Files:** 无新增改动（仅验证）。

- [ ] **Step 1: 全量模块单测**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: PASS，本计划相关测试（`OrgRoleContextResolverTests`/`ConditionEvaluationContextBuilderTests`/`CardFlowPathPreviewServiceTests`/`ConditionRuleEvaluatorTests`）全绿。

> 若末尾出现「测试主机进程崩溃」但计数为「失败 0」，属本机已知 socket teardown flake；`Unify.ShentongBatch*EventSourcesTests` 是真库共享并发致 flaky（DbUpdateException，与本改动无关），如出现重跑 `--filter "FullyQualifiedName~STOTOP.Module.CardFlow.Tests.Rules"` 单独确认 Rules 命名空间干净。

- [ ] **Step 2: 模块编译 0 错**

Run: `dotnet build src/STOTOP.Module.CardFlow/STOTOP.Module.CardFlow.csproj`
Expected: Build succeeded, 0 Error。

> 若主工作树因并发会话未提交 WIP 致无关编译错，改在 HEAD 临时 detached worktree 隔离 build 验证本变更（见 [[concurrent-sessions-share-worktree]]）。

- [ ] **Step 3: 确认提交干净**

Run: `git status --short`
Expected: 本计划的 6 处文件已 commit；剩余仅本计划之外的既有未跟踪/并发文件（与开工时一致）。

---

## 自检对照（spec coverage）

| Spec 目标/缺陷 | 对应任务 |
|---|---|
| 抽 `OrgRoleContextResolver`（纯 `BuildOrgChain` 上溯放 FID + 防环） | Task 1 |
| DB `ResolveAsync`（active orgs 上溯 + 角色查询） | Task 1（实现）+ Task 2/3（集成覆盖） |
| 运行时 `BuildAsync` 填三字段（用 card.FOrgId/FInitiatorId） | Task 2 |
| 预演 `BuildPreviewContextAsync` 填三字段（用 request.OrgId/InitiatorId） | Task 3 |
| `inOrgChain` 运行期+预演期真生效 | Task 2（字段填充）+ Task 3（预演 inOrgChain 命中端到端） |
| OrgChain 只放 FID / Relations 不做 / 前端不动 / 不加 DI 构造器依赖 | 设计落地，范围外未触及 |
| x64 全绿 + 编译 0 错 | Task 4 |
