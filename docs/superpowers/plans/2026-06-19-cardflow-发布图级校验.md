# CardFlow 发布图级校验 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 发布期对 active 路由图加图级校验 —— Kahn 阻断所有环 + 从入口 BFS 检测不可达节点，与同仓 `OrchestrationEngineService.ValidateDag` 口径对称，堵住「含环/含死节点的流程可发布上线」。

**Architecture:** 新建无 DbContext 的纯函数 `RouteGraphValidator`（Kahn 环检测优先、DAG 才查可达性，返回错误列表）。`FlowDefinitionService.ValidateRouteRulesAsync` 在现有 per-group 校验后、仅 rule-mode（有 active 规则）时构造图输入调它，有错即 `throw InvalidOperationException`。无 schema/迁移；只发布期、只 rule-mode 生效。

**Tech Stack:** C# / .NET 10 / EF Core（InMemory 测试）/ xUnit。测试运行需 `--arch x64`（团队约定：x86 testhost 在 socket 吃紧时 WSAENOBUFS 中止）。`STOTOP.Module.Task` 命名空间遮蔽 `System.Threading.Tasks.Task`，异步测试返回类型须写 `global::System.Threading.Tasks.Task`。

**设计稿：** [2026-06-19-cardflow-发布图级校验-design.md](../specs/2026-06-19-cardflow-发布图级校验-design.md)

**提交纪律：** 在 master 上做（用户已授权）。每个 commit **只 `git add` 本计划新建/修改的文件**，不夹带工作树里其它未跟踪/已改文件（`_t6_*.json`/`.superpowers/`/并发会话 WIP 等）。提交前 `git diff --stat <目标文件>` 确认目标文件干净（非并发 WIP）。

---

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `src/STOTOP.Module.CardFlow/Services/RouteGraphValidator.cs` | 纯函数：active 路由图 Kahn 环检测 + 入口可达性，返回错误列表。 | **新建** |
| `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs` | `ValidateRouteRulesAsync` 末尾、rule-mode 时调校验器、有错 throw。 | **修改** |
| `tests/STOTOP.Module.CardFlow.Tests/Rules/RouteGraphValidatorTests.cs` | 纯校验器单测（7 case）。 | **新建** |
| `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionPublishGraphTests.cs` | 发布期含环/不可达被拦的集成测试。 | **新建** |

> 仅 `PublishAsync`（[:225](../../../src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs:225)）调用 `ValidateRouteRulesAsync`；克隆走 `SaveRouteRulesAsync` 不受影响。

---

## Task 1: 纯函数 RouteGraphValidator

**Files:**
- Create: `tests/STOTOP.Module.CardFlow.Tests/Rules/RouteGraphValidatorTests.cs`
- Create: `src/STOTOP.Module.CardFlow/Services/RouteGraphValidator.cs`

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.Module.CardFlow.Tests/Rules/RouteGraphValidatorTests.cs`：

```csharp
using STOTOP.Module.CardFlow.Services;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Rules;

public class RouteGraphValidatorTests
{
    private static (string From, string To) E(string from, string to) => (from, to);

    [Fact]
    public void Validate_LinearDag_NoErrors()
    {
        var errors = RouteGraphValidator.Validate(
            new[] { "A", "B", "C" }, "A",
            new[] { E("A", "B"), E("B", "C") });
        Assert.Empty(errors);
    }

    [Fact]
    public void Validate_TwoNodeCycle_ReportsCycle()
    {
        var errors = RouteGraphValidator.Validate(
            new[] { "A", "B" }, "A",
            new[] { E("A", "B"), E("B", "A") });
        Assert.Single(errors);
        Assert.Contains("环", errors[0]);
        Assert.Contains("A", errors[0]);
        Assert.Contains("B", errors[0]);
    }

    [Fact]
    public void Validate_SelfLoop_ReportsCycle()
    {
        var errors = RouteGraphValidator.Validate(
            new[] { "A" }, "A",
            new[] { E("A", "A") });
        Assert.Single(errors);
        Assert.Contains("环", errors[0]);
        Assert.Contains("A", errors[0]);
    }

    [Fact]
    public void Validate_UnreachableNode_ReportsUnreachable()
    {
        var errors = RouteGraphValidator.Validate(
            new[] { "A", "B", "C" }, "A",
            new[] { E("A", "B") });   // C 孤立
        Assert.Single(errors);
        Assert.Contains("不可达", errors[0]);
        Assert.Contains("C", errors[0]);
    }

    [Fact]
    public void Validate_SingleNodeNoEdge_NoErrors()
    {
        var errors = RouteGraphValidator.Validate(
            new[] { "A" }, "A",
            new List<(string From, string To)>());
        Assert.Empty(errors);
    }

    [Fact]
    public void Validate_CycleAndIsolated_ReportsCycleOnly()
    {
        // 环 A<->B + 孤立 C：先报环，短路，不报不可达
        var errors = RouteGraphValidator.Validate(
            new[] { "A", "B", "C" }, "A",
            new[] { E("A", "B"), E("B", "A") });
        Assert.Single(errors);
        Assert.Contains("环", errors[0]);
        Assert.DoesNotContain("不可达", errors[0]);
    }

    [Fact]
    public void Validate_NullEntry_SkipsReachability()
    {
        // entry 为空 → 不查可达性（也不抛）；DAG 无环 → 无错
        var errors = RouteGraphValidator.Validate(
            new[] { "A", "B" }, null,
            new[] { E("A", "B") });
        Assert.Empty(errors);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~RouteGraphValidatorTests"`
Expected: 编译失败 / FAIL —— `RouteGraphValidator` 尚不存在。

- [ ] **Step 3: 写最小实现**

创建 `src/STOTOP.Module.CardFlow/Services/RouteGraphValidator.cs`：

```csharp
namespace STOTOP.Module.CardFlow.Services;

public static class RouteGraphValidator
{
    /// <summary>
    /// nodeKeys：全部 stage key（去空去重）。entryKey：首 sortOrder 节点 key。
    /// edges：active 路由规则 (from, to)。返回错误列表（空 = 通过）。
    /// 先 Kahn 环检测（阻断所有环、短路）；无环（DAG）才从入口 BFS 查可达性。
    /// 所有 key 比较大小写不敏感。
    /// </summary>
    public static IReadOnlyList<string> Validate(
        IReadOnlyCollection<string> nodeKeys,
        string? entryKey,
        IReadOnlyCollection<(string From, string To)> edges)
    {
        var nodes = nodeKeys
            .Where(k => !string.IsNullOrWhiteSpace(k))
            .ToHashSet(StringComparer.OrdinalIgnoreCase);
        if (nodes.Count == 0)
            return Array.Empty<string>();

        // 防御性过滤：只保留两端都在 nodes 里的边（来源/目标存在性由 ValidateRouteRulesAsync 既有校验先保证）
        var validEdges = edges
            .Where(e => !string.IsNullOrWhiteSpace(e.From) && !string.IsNullOrWhiteSpace(e.To)
                && nodes.Contains(e.From) && nodes.Contains(e.To))
            .ToList();

        var inDegree = nodes.ToDictionary(n => n, _ => 0, StringComparer.OrdinalIgnoreCase);
        var adj = nodes.ToDictionary(n => n, _ => new List<string>(), StringComparer.OrdinalIgnoreCase);
        foreach (var (from, to) in validEdges)
        {
            inDegree[to] += 1;
            adj[from].Add(to);
        }

        // ① Kahn 环检测（消耗 inDegree；结束后仍 >0 者即环及其下游）
        var queue = new Queue<string>(inDegree.Where(kv => kv.Value == 0).Select(kv => kv.Key));
        var visited = 0;
        while (queue.Count > 0)
        {
            var n = queue.Dequeue();
            visited++;
            foreach (var next in adj[n])
            {
                inDegree[next] -= 1;
                if (inDegree[next] == 0) queue.Enqueue(next);
            }
        }
        if (visited < nodes.Count)
        {
            var cyclic = inDegree.Where(kv => kv.Value > 0).Select(kv => kv.Key)
                .OrderBy(k => k, StringComparer.OrdinalIgnoreCase);
            return new List<string> { $"流程路由存在环，涉及节点：{string.Join("、", cyclic)}" };
        }

        // ② 从入口 BFS 可达性
        var errors = new List<string>();
        if (!string.IsNullOrWhiteSpace(entryKey) && nodes.Contains(entryKey))
        {
            var reachable = new HashSet<string>(StringComparer.OrdinalIgnoreCase) { entryKey };
            var bfs = new Queue<string>();
            bfs.Enqueue(entryKey);
            while (bfs.Count > 0)
            {
                foreach (var next in adj[bfs.Dequeue()])
                    if (reachable.Add(next)) bfs.Enqueue(next);
            }
            var unreachable = nodes.Where(n => !reachable.Contains(n))
                .OrderBy(k => k, StringComparer.OrdinalIgnoreCase).ToList();
            if (unreachable.Count > 0)
                errors.Add($"流程存在从起点不可达的节点：{string.Join("、", unreachable)}");
        }

        return errors;
    }
}
```

> 注：ImplicitUsings 提供 `System.Linq`/`System.Collections.Generic`，无需显式 using。

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~RouteGraphValidatorTests"`
Expected: PASS（7 个测试全绿）。

- [ ] **Step 5: 提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/RouteGraphValidator.cs tests/STOTOP.Module.CardFlow.Tests/Rules/RouteGraphValidatorTests.cs
git commit -m "feat(cardflow): 抽 RouteGraphValidator 纯函数(Kahn环检测+入口可达性)"
```

---

## Task 2: ValidateRouteRulesAsync 接线 + 发布集成测试

**Files:**
- Create: `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionPublishGraphTests.cs`
- Modify: `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs`

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionPublishGraphTests.cs`：

```csharp
using Microsoft.Extensions.Logging.Abstractions;
using STOTOP.Module.CardFlow.Entities;
using STOTOP.Module.CardFlow.Services;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Rules;

public class FlowDefinitionPublishGraphTests
{
    // 种一个含 active 规则的 rule-mode 流程草稿，返回流程定义 FID。
    private static (FlowDefinitionService Svc, long DefId) SeedFlow(
        STOTOP.Infrastructure.Data.STOTOPDbContext db,
        (string Key, int SortOrder)[] stages,
        (string From, string To)[] edges)
    {
        var def = new CfFlowDefinition { FFlowName = "图校验流程", FFlowCode = "GRAPH_" + System.Guid.NewGuid().ToString("N").Substring(0, 6), FStatus = "draft" };
        db.Set<CfFlowDefinition>().Add(def);
        db.SaveChanges();
        var version = new CfFlowVersion { FFlowDefinitionId = def.FID, FVersionNumber = 1, FStatus = "draft" };
        db.Set<CfFlowVersion>().Add(version);
        db.SaveChanges();
        foreach (var (key, sortOrder) in stages)
            db.Set<CfStageDefinition>().Add(new CfStageDefinition
            {
                FFlowVersionId = version.FID, FStageKey = key, FStageName = key.ToUpperInvariant(),
                FType = "human", FSortOrder = sortOrder
            });
        var i = 0;
        foreach (var (from, to) in edges)
            db.Set<CfStageRouteRule>().Add(new CfStageRouteRule
            {
                FFlowVersionId = version.FID, FEdgeKey = $"e{i++}", FFromStageKey = from, FToStageKey = to,
                FRouteName = $"{from}→{to}", FIsDefault = true, FPriority = 1, FStatus = "active"
            });
        db.SaveChanges();

        var svc = new FlowDefinitionService(db, NullLogger<FlowDefinitionService>.Instance);
        return (svc, def.FID);
    }

    [Fact]
    public async global::System.Threading.Tasks.Task Publish_RuleModeCycle_ThrowsWithCycleMessage()
    {
        using var db = TestDbContextFactory.Create(nameof(Publish_RuleModeCycle_ThrowsWithCycleMessage), 192);
        var (svc, defId) = SeedFlow(db,
            new[] { ("a", 1), ("b", 2) },
            new[] { ("a", "b"), ("b", "a") });   // 环

        var ex = await Assert.ThrowsAsync<System.InvalidOperationException>(() => svc.PublishAsync(defId, 1));
        Assert.Contains("环", ex.Message);
    }

    [Fact]
    public async global::System.Threading.Tasks.Task Publish_RuleModeUnreachable_ThrowsWithUnreachableMessage()
    {
        using var db = TestDbContextFactory.Create(nameof(Publish_RuleModeUnreachable_ThrowsWithUnreachableMessage), 192);
        var (svc, defId) = SeedFlow(db,
            new[] { ("a", 1), ("b", 2), ("c", 3) },
            new[] { ("a", "b") });   // c 不可达

        var ex = await Assert.ThrowsAsync<System.InvalidOperationException>(() => svc.PublishAsync(defId, 1));
        Assert.Contains("不可达", ex.Message);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~FlowDefinitionPublishGraphTests"`
Expected: 两个测试 FAIL —— 现 `ValidateRouteRulesAsync` 无图级校验，含环/不可达流程通过校验、`PublishAsync` 继续向后执行（不会抛「环」/「不可达」消息的 `InvalidOperationException`）。

> 注：含环/不可达流程的既有 per-group 校验（单默认/目标存在）均通过，故现状会越过 `ValidateRouteRulesAsync` 进入后续发布逻辑——测试断言「抛含环/不可达消息」当前不成立即为红。

- [ ] **Step 3: 接线 ValidateRouteRulesAsync**

修改 `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs` 的 `ValidateRouteRulesAsync`（当前 [:733-768](../../../src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs:733)），在方法末尾的 `foreach (var group in rules...)` 闭合大括号**之后、方法结束大括号之前**追加：

```csharp
        if (rules.Count > 0)
        {
            var nodeKeys = stages
                .Select(s => s.FStageKey)
                .Where(key => !string.IsNullOrWhiteSpace(key))
                .ToList();
            var entryKey = stages.FirstOrDefault()?.FStageKey;
            var edges = rules
                .Select(rule => (From: rule.FFromStageKey, To: rule.FToStageKey))
                .ToList();

            var graphErrors = RouteGraphValidator.Validate(nodeKeys, entryKey, edges);
            if (graphErrors.Count > 0)
                throw new InvalidOperationException(string.Join("；", graphErrors));
        }
```

> `rules` 是本方法已加载的 active 规则（[:739-741](../../../src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs:739)）；`stages` 是入参（已按 sortOrder 排序）。`RouteGraphValidator` 同命名空间，无需新增 using。不改本方法其它部分。

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~FlowDefinitionPublishGraphTests"`
Expected: PASS（两个测试绿：含环抛「环」、不可达抛「不可达」）。

- [ ] **Step 5: 跑既有发布/路由测试回归**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --no-build --filter "FullyQualifiedName~FlowDefinition"`
Expected: PASS（`FlowDefinitionOrgIsolationTests`/`FlowDefinitionStableKeyTests` 等不回归——它们的流程要么无 active 规则、要么是合法 DAG，不被图级校验误拦）。

> 若某既有测试因新图级校验失败，说明其种子是合法 DAG 却被误判，须停下核对（可能是 entry 或边构造理解有误），不要为迁就而放宽校验。

- [ ] **Step 6: 提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionPublishGraphTests.cs
git commit -m "fix(cardflow): 发布校验加图级环/不可达检测(rule-mode,阻断发布)"
```

---

## Task 3: 收尾验证（全量模块单测 + 编译）

**Files:** 无新增改动（仅验证）。

- [ ] **Step 1: 全量模块单测**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: PASS，本计划相关测试（`RouteGraphValidatorTests`/`FlowDefinitionPublishGraphTests`/`FlowDefinition*`）全绿。

> 若末尾出现「测试主机进程崩溃」但计数为「失败 0」，属本机已知 socket teardown flake；另注 `Unify.ShentongBatch*EventSourcesTests` 是真库共享并发致 flaky（DbUpdateException，与本改动无关），如出现重跑 `--filter "FullyQualifiedName~STOTOP.Module.CardFlow.Tests.Rules"` 单独确认 Rules 命名空间干净。

- [ ] **Step 2: 模块编译 0 错**

Run: `dotnet build src/STOTOP.Module.CardFlow/STOTOP.Module.CardFlow.csproj`
Expected: Build succeeded, 0 Error。

> 若主工作树因并发会话未提交 WIP 致无关编译错，改在 HEAD 临时 detached worktree 隔离 build 验证本变更（见 [[concurrent-sessions-share-worktree]]）。

- [ ] **Step 3: 确认提交干净**

Run: `git status --short`
Expected: 本计划的 4 处文件已 commit；剩余仅本计划之外的既有未跟踪/并发文件（与开工时一致）。

---

## 自检对照（spec coverage）

| Spec 目标/缺陷 | 对应任务 |
|---|---|
| 抽纯函数 `RouteGraphValidator`（Kahn 阻断所有环） | Task 1 |
| 从入口 BFS 可达性检测 | Task 1 |
| 有环先报环、短路（DAG 才查可达） | Task 1（`Validate_CycleAndIsolated_ReportsCycleOnly`） |
| entry 为空跳过可达性（防御） | Task 1（`Validate_NullEntry_SkipsReachability`） |
| `ValidateRouteRulesAsync` 仅 rule-mode 调校验器、有错 throw | Task 2 |
| 发布期含环/不可达被拦 | Task 2 集成测试 |
| 合法 DAG / 线性流不误拦 | Task 2 Step 5 回归（既有 FlowDefinition 测试） |
| 放弃终端检查 / 不引告警通道 / 不动运行时 / 不在克隆期 / 无迁移 | 范围外，未触及 |
| x64 全绿 + 编译 0 错 | Task 3 |
