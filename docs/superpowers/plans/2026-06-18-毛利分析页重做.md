# 毛利分析页（ProfitAnalysis）重做 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 对 `ProfitAnalysis.vue` 做 A 视觉债清理 + B KPI 条扁平化 + C 6 表合一（配置驱动单表），视觉对齐全站、模板去重，**行为 1:1 不变**。

**Architecture:** 单文件、顺序两任务（同文件不能并行）。Task 1 = A+B（`<style>` 块 + echarts 色板的纯视觉改动，低风险可视）；Task 2 = C（6 段 `<a-table>` → viewConfig 驱动的 1 个动态表，结构重构，风险隔离、可单独回退）。

**Tech Stack:** Vue 3 SFC + Ant Design Vue + ECharts + SCSS 设计令牌。无前端测试运行器——验证靠 `npm run build`（vite）+ `npx stylelint` + `rg` + 用户 live HMR 逐视角目视。

**Spec:** `docs/superpowers/specs/2026-06-18-毛利分析页重做-design.md`（§3/§4/§5 为权威依据）。

**铁律:** 不动取数 API、不动计费/口径、不动各视角列的业务含义与数据语义。

---

### Task 1: A 视觉债 + B KPI 扁平（纯视觉，`<style>` + echarts）

**Files:** Modify `web/src/views/express/report/ProfitAnalysis.vue`

- [ ] **Step 1: KPI 条扁平（`<style>` `.kpi-strip` 行 940）**

把 `background: linear-gradient(135deg, #f6f8fc 0%, #eef2f9 100%);` 改为:
```scss
  background: var(--bg-card);
  border: 1px solid var(--border);
```

- [ ] **Step 2: KPI 其余裸 hex/旧红 → 令牌（`<style>`）**

- `.kpi-label`（956）`color: #8c8c8c;` → `color: var(--text-3);`
- `.kpi-divider`（972）`background: #e0e0e0;` → `background: var(--border);`
- `.kpi-clickable:hover`（986）`background: rgba(255, 77, 79, 0.06);` → `background: var(--color-danger-light);`
- `.kpi-active`（990-991）`background: rgba(255, 77, 79, 0.1);` → `background: var(--color-danger-light);`；`box-shadow: inset 0 0 0 1px #ffa39e;` → `box-shadow: inset 0 0 0 1px var(--color-danger-border);`
- `.val-caution`（1001）`color: #d4a106;` → `color: var(--color-warning-text);`
- `.trend-view`（1012）`background: #fff;` → `background: var(--bg-card);`

- [ ] **Step 3: 删本页 `:deep(.ant-table)` 颜色覆盖，让全局中性样式接管（行 1024-1045）**

把整段改为（删表头灰底+weight、删 hover 旧蓝整条；合计行底改中性令牌、保留加粗；仅留 font-size/padding 密度微调）:
```scss
:deep(.ant-table) {
  .ant-table-thead > tr > th {
    font-size: 13px;
    padding: 8px 12px;
  }
  .ant-table-tbody > tr > td {
    font-size: 13px;
    padding: 6px 12px;
  }
  .ant-table-summary > tr > td,
  .ant-table-summary > tr > th {
    background: var(--bg-muted);
    font-weight: 600;
    font-size: 13px;
    padding: 8px 12px;
  }
}
```
（删掉了 thead 的 `background:#fafafa` 与 `font-weight:600`、整条 `tbody tr:hover td { background:#f0f7ff }`、summary 的 `#fafafa`→`var(--bg-muted)`。表头颜色/字重/hover 交由全局 `ant-override.scss` 中性样式。）

- [ ] **Step 4: echarts 趋势图换新板（`renderTrendChart` 行 788-798；canvas 用真 hex 是既定豁免，仅换值）**

- `xAxis.axisLabel.color`（788）`'#666'` → `'#5A6068'`
- `yAxis[0].axisLabel.color`（790）`'#999'` → `'#8A9099'`
- `yAxis[1].axisLabel.color`（791）`'#999'` → `'#8A9099'`
- 应收 `itemStyle.color`（794）`'#3A6FB0'` → `'#5B7290'`
- 成本 `itemStyle.color`（795）`'#E5484D'` → `'#D6584E'`
- 毛利 `itemStyle.color`（796）`'#2BA471'` → `'#3E9E6E'`；面积 `colorStops`：`'rgba(82,196,26,0.25)'` → `'rgba(62,158,110,0.22)'`、`'rgba(82,196,26,0.02)'` → `'rgba(62,158,110,0.02)'`
- 毛利率柱 `itemStyle.color`（797）`'#E6A700'` → `'#D49A2E'`

- [ ] **Step 5: 验证**

Run: `cd web && npx stylelint src/views/express/report/ProfitAnalysis.vue` → 0
Run: `rg "#[0-9a-fA-F]{3,6}" web/src/views/express/report/ProfitAnalysis.vue`
Expected: 仅命中 `renderTrendChart` 内 echarts 颜色（canvas 豁免，已是新板值 `#5A6068`/`#8A9099`/`#5B7290`/`#D6584E`/`#3E9E6E`/`#D49A2E`）；`<style>` 块与模板 0 命中。
Run: `cd web && npm run build 2>&1 | grep -iE "error|built in"` → `✓ built`

- [ ] **Step 6: 用户 live 看 KPI 条扁平 + 表格中性（表头/hover 不再旧蓝）+ 趋势图新色板**

- [ ] **Step 7: Commit**
```bash
git add web/src/views/express/report/ProfitAnalysis.vue
git commit -m "style(profit): A视觉债+B KPI扁平——去渐变/删本页表格旧覆盖让全局接管/图表换新板"
```

---

### Task 2: C 6 表合一（viewConfig 驱动单表，行为 1:1）

**Files:** Modify `web/src/views/express/report/ProfitAnalysis.vue`

- [ ] **Step 1: 给 6 个列数组 + provinceColumns 加 `format` 元（`<script>`）**

按下表给对应 `dataIndex` 的列定义**加 `format` 字段**（其余属性不动）。**未列出的列保持无 format**（纯文本，由 a-table 默认渲染，与现状 1:1——尤其 `waybillCount` 现在就是裸数字、不加格式）:

| dataIndex | format | 适用列数组 |
| --- | --- | --- |
| `totalWeight` / `avgWeight` | `weight` | client/shop/intermediary/salesman/weightSegment/region/province |
| `totalCharge` / `totalCost` / `avgPrice` / `downstreamRevenue` / `upstreamCost` / `commissionIncome` / `avgCommission` | `money` | 各自所在数组 |
| `profit` / `avgProfit` | `profit` | client/intermediary/weightSegment/region/province 等 |
| `profitRate` | `rate` | client/shop/intermediary/weightSegment/region/province |
| `clientType`（client）| `tagClient` | clientColumns |
| `clientType`（intermediary）| `tagIntermediary` | intermediaryColumns |

无 format（保持现状裸渲染）: `clientName`/`shopName`/`salesmanName`/`region`/`weightSegment`/`provinceName`/`networkPointId`/`chainLevel`/`waybillCount`。

- [ ] **Step 2: 加 viewConfig 注册表 + currentView（`<script>`，放在各 `xColumns`/`xData` 定义之后、`viewFetchMap` 附近）**

```ts
const PAGE50 = { pageSize: 50, showSizeChanger: true, showQuickJumper: true, showTotal: (t: number) => `共 ${t} 条` }
const TBL_Y = 'calc(100vh - 280px)'

const VIEW_CONFIGS: Record<string, {
  columns: any[]; rowKey: any; pagination: any; scroll: Record<string, any>;
  hasSummary?: boolean; hasExpand?: boolean;
}> = {
  client:        { columns: clientColumns,        rowKey: 'clientId',     pagination: PAGE50, scroll: { x: 1100, y: TBL_Y }, hasSummary: true },
  shop:          { columns: shopColumns,          rowKey: shopRowKey,     pagination: PAGE50, scroll: { x: 1000, y: TBL_Y } },
  intermediary:  { columns: intermediaryColumns,  rowKey: 'clientId',     pagination: PAGE50, scroll: { x: 1100, y: TBL_Y } },
  salesman:      { columns: salesmanColumns,      rowKey: 'salesmanId',   pagination: PAGE50, scroll: { x: 800,  y: TBL_Y } },
  weightSegment: { columns: weightSegmentColumns, rowKey: 'weightSegment',pagination: false,  scroll: { x: 900,  y: TBL_Y } },
  region:        { columns: regionColumns,        rowKey: 'region',       pagination: false,  scroll: { x: 1000, y: TBL_Y }, hasExpand: true },
}

const dataMap: Record<string, { value: any[] }> = {
  client: displayedClientData, shop: shopData, intermediary: intermediaryData,
  salesman: salesmanData, weightSegment: weightSegmentData, region: regionData,
}
const loadingMap: Record<string, { value: boolean }> = {
  client: clientLoading, shop: shopLoading, intermediary: intermediaryLoading,
  salesman: salesmanLoading, weightSegment: weightSegmentLoading, region: regionLoading,
}

const currentView = computed(() => {
  const cfg = VIEW_CONFIGS[activeView.value]
  if (!cfg) return null
  return { ...cfg, data: dataMap[activeView.value].value, loading: loadingMap[activeView.value].value }
})
```
（`displayedClientData` 已是「亏损过滤后」的 computed，client 用它即保留过滤行为；`shopRowKey` 为已存在的函数。）

- [ ] **Step 3: 模板——6 段 `<a-table>` 替换为 1 个动态表（`<template>` 行 89-376 整体替换）**

把 client/shop/intermediary/salesman/weightSegment/region 六段 `<a-table>` 删除，替换为:
```vue
<a-table
  v-if="activeView !== 'trend' && currentView"
  :columns="currentView.columns"
  :data-source="currentView.data"
  :loading="currentView.loading"
  :pagination="currentView.pagination"
  :row-key="currentView.rowKey"
  size="small"
  :scroll="currentView.scroll"
  :expanded-row-keys="currentView.hasExpand ? expandedRegionKeys : undefined"
  @expand="currentView.hasExpand ? handleRegionExpand : undefined"
>
  <template #bodyCell="{ column, record }">
    <template v-if="column.format === 'weight'">{{ (record[column.dataIndex] ?? 0).toFixed(2) }}</template>
    <template v-else-if="column.format === 'money'">¥{{ (record[column.dataIndex] ?? 0).toFixed(2) }}</template>
    <template v-else-if="column.format === 'profit'">
      <span :class="(record[column.dataIndex] ?? 0) >= 0 ? 'val-positive' : 'val-negative'">¥{{ (record[column.dataIndex] ?? 0).toFixed(2) }}</span>
    </template>
    <template v-else-if="column.format === 'rate'">
      <span :class="profitRateClass(record[column.dataIndex])">{{ formatRate(record[column.dataIndex]) }}</span>
    </template>
    <template v-else-if="column.format === 'tagClient'">
      <a-tag :color="record.clientType === 1 ? 'blue' : record.clientType === 2 ? 'green' : 'default'" size="small">{{ clientTypeLabel(record.clientType) }}</a-tag>
    </template>
    <template v-else-if="column.format === 'tagIntermediary'">
      <a-tag size="small">{{ record.clientType === 2 ? '业务代理' : record.clientType === 5 ? '承包区' : '其他' }}</a-tag>
    </template>
  </template>

  <template v-if="currentView.hasSummary" #summary>
    <a-table-summary fixed>
      <a-table-summary-row class="summary-row">
        <a-table-summary-cell :index="0">合计</a-table-summary-cell>
        <a-table-summary-cell :index="1" />
        <a-table-summary-cell :index="2" align="right">{{ formatNum(clientTotals.waybillCount) }}</a-table-summary-cell>
        <a-table-summary-cell :index="3" align="right">{{ clientTotals.totalWeight.toFixed(2) }}</a-table-summary-cell>
        <a-table-summary-cell :index="4" align="right">¥{{ clientTotals.totalCharge.toFixed(2) }}</a-table-summary-cell>
        <a-table-summary-cell :index="5" align="right">¥{{ clientTotals.totalCost.toFixed(2) }}</a-table-summary-cell>
        <a-table-summary-cell :index="6" align="right"><span :class="clientTotals.profit >= 0 ? 'val-positive' : 'val-negative'">¥{{ clientTotals.profit.toFixed(2) }}</span></a-table-summary-cell>
        <a-table-summary-cell :index="7" align="right"><span :class="profitRateClass(clientTotals.profitRate)">{{ formatRate(clientTotals.profitRate) }}</span></a-table-summary-cell>
        <a-table-summary-cell :index="8" align="right">¥{{ clientTotals.avgPrice.toFixed(2) }}</a-table-summary-cell>
        <a-table-summary-cell :index="9" align="right"><span :class="clientTotals.avgProfit >= 0 ? 'val-positive' : 'val-negative'">¥{{ clientTotals.avgProfit.toFixed(2) }}</span></a-table-summary-cell>
      </a-table-summary-row>
    </a-table-summary>
  </template>

  <template v-if="currentView.hasExpand" #expandedRowRender="{ record }">
    <a-table :columns="provinceColumns" :data-source="provinceMap[record.region] || []" :loading="provinceLoadingMap[record.region]" :pagination="false" row-key="provinceId" size="small">
      <template #bodyCell="{ column, record: pRow }">
        <template v-if="column.format === 'weight'">{{ (pRow[column.dataIndex] ?? 0).toFixed(2) }}</template>
        <template v-else-if="column.format === 'money'">¥{{ (pRow[column.dataIndex] ?? 0).toFixed(2) }}</template>
        <template v-else-if="column.format === 'profit'"><span :class="(pRow[column.dataIndex] ?? 0) >= 0 ? 'val-positive' : 'val-negative'">¥{{ (pRow[column.dataIndex] ?? 0).toFixed(2) }}</span></template>
        <template v-else-if="column.format === 'rate'"><span :class="profitRateClass(pRow[column.dataIndex])">{{ formatRate(pRow[column.dataIndex]) }}</span></template>
      </template>
    </a-table>
  </template>
</a-table>
```
趋势图块（`v-if="activeView === 'trend'"` 的 `.trend-view`，行 379-390）**原样保留**，紧接在动态表之后。

- [ ] **Step 4: 验证编译 + 全视角行为**

Run: `cd web && npm run build 2>&1 | grep -iE "error|built in"` → `✓ built`（重点看无 TS/模板编译错误）
Run: `cd web && npx stylelint src/views/express/report/ProfitAnalysis.vue` → 0

- [ ] **Step 5: 用户 live 逐视角验 1:1**

切换 7 个视角逐个确认与改前一致:业务对象（含**合计行**、**亏损过滤**点击、clientType 标签、默认按毛利降序）、报价、中间人（clientType 标签、计费记录数 KPI 标签）、业务员（提成列、毛利率显示 —）、重量段（不分页）、流向（**省份展开懒加载**、不分页）、趋势（图表）；以及**排序/导出/列宽/对齐/fixed** 不变。

- [ ] **Step 6: Commit**
```bash
git add web/src/views/express/report/ProfitAnalysis.vue
git commit -m "refactor(profit): 6 表视角合一为 viewConfig 驱动的动态单表(行为1:1,趋势图独立)"
```

---

## Self-Review（已执行）

- **Spec 覆盖**:§3 A 视觉债→Task1 Step1-4；§4 B KPI→Task1 Step1-2；§5 C 合并→Task2 全部。无遗漏。
- **1:1 保真要点**:`waybillCount` 等无 format 列保持裸渲染（现状本就无格式化，**不加 `num` 防止平白多出千分位**）；`displayedClientData` 保留亏损过滤；合计行仅 client；省份展开仅 region；趋势图原样。`weight/money/profit` 统一加 `?? 0` 仅防 null 崩溃、对真实非空数据显示不变。
- **占位符**:无 TBD/TODO；每步给文件+行号+确切代码。
- **命名一致**:`VIEW_CONFIGS`/`currentView`/`dataMap`/`loadingMap`/`PAGE50`/`TBL_Y` 跨步一致；`format` 值（weight/money/profit/rate/tagClient/tagIntermediary）在 Step1 定义、Step3 bodyCell 消费一致。
- **执行顺序**:同文件,Task1→Task2 顺序;Task2 可单独回退不影响 Task1。
