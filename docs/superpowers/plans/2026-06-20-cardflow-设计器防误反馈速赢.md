# CardFlow 设计器防误/反馈速赢 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (推荐，inline) 或 superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **注：前端无测试运行器**（无 vitest/jest），无 TDD。每任务=编辑+提交；验证集中在末任务（`vite build` + 用户 9001 live 逐项验）。改动全在 `<script>`，不碰 `<style>`/裸 hex。

**Goal:** 修 CardFlow 设计器 4 个行为类速赢——自动保存 pill 真实化、字段保存失败提示、删节点二次确认、离开页换 ant Modal.confirm。

**Architecture:** 纯前端 `<script>` 局部行为修复，3 个 Vue 文件：`FlowDefinitionEditPage.vue`（pill markDirty + goBack Modal.confirm）、`SchemaFieldEditor.vue`（字段保存 warning）、`StageDefinitionEditor.vue`（删节点 Modal.confirm）。不改 `useAutoSave`、保存/校验逻辑本身、`<style>`。

**Tech Stack:** Vue 3 (`<script setup>`) + Ant Design Vue（`message`/`Modal` 全局已注册）。验证 `vite build`（vue-tsc 基线本就红、非门禁）+ 用户 live。

**设计稿：** [2026-06-20-cardflow-设计器防误反馈速赢-design.md](../specs/2026-06-20-cardflow-设计器防误反馈速赢-design.md)

**提交纪律：** 在 master 上做（用户已授权）。每个 commit **只 `git add` 本计划修改的那个文件**，不夹带工作树里其它未跟踪/已改文件（`_t6_*.json`/`.superpowers/`/并发会话 WIP：`FlowEngineService.cs`、其它 `.vue` 等）。改前 `git diff --stat <file>` 确认目标文件干净（非并发 WIP）。

---

## File Structure

| 文件 | 改动 | 任务 |
|---|---|---|
| `web/src/views/cardflow/FlowDefinitionEditPage.vue` | #1 watcher 移位+markDirty；#6 goBack Modal.confirm；import 补 Modal | Task 1 |
| `web/src/components/cardflow/SchemaFieldEditor.vue` | #3 commitEditor 两处 message.warning；import message | Task 2 |
| `web/src/components/cardflow/StageDefinitionEditor.vue` | #4 removeStage→Modal.confirm + doRemoveStage；import Modal | Task 3 |

---

## Task 1: FlowDefinitionEditPage —— pill 真实化(#1) + 离开页 Modal.confirm(#6)

**Files:**
- Modify: `web/src/views/cardflow/FlowDefinitionEditPage.vue`

- [ ] **Step 1: import 补 Modal**

把第 19 行 `import { message } from 'ant-design-vue'` 改为：

```js
import { message, Modal } from 'ant-design-vue'
```

- [ ] **Step 2: #1 — watcher 移到 auto 之后 + markDirty**

把当前（约 [:223-230](../../../web/src/views/cardflow/FlowDefinitionEditPage.vue:223)）：

```js
const dirty = ref(false)
watch(() => state, () => { dirty.value = true }, { deep: true })

const auto = useAutoSave({
  intervalMs: 30_000,
  isDirty: () => dirty.value,
  save: () => silentSave(),
})
```

替换为（删掉旧 watcher 行，移到 auto 之后并补 `auto.markDirty()`）：

```js
const dirty = ref(false)

const auto = useAutoSave({
  intervalMs: 30_000,
  isDirty: () => dirty.value,
  save: () => silentSave(),
})

watch(() => state, () => { dirty.value = true; auto.markDirty() }, { deep: true })
```

- [ ] **Step 3: #6 — goBack 换 Modal.confirm**

把当前 `goBack`（约 [:1674-1680](../../../web/src/views/cardflow/FlowDefinitionEditPage.vue:1674)）：

```js
function goBack() {
  if (dirty.value) {
    const ok = window.confirm('有未保存的更改，确定离开？')
    if (!ok) return
  }
  router.push('/cardflow/definitions')
}
```

替换为：

```js
function goBack() {
  if (!dirty.value) {
    router.push('/cardflow/definitions')
    return
  }
  Modal.confirm({
    title: '有未保存的更改',
    content: '离开将丢失未保存的更改，确定离开吗？',
    okText: '确定离开',
    okType: 'danger',
    cancelText: '取消',
    onOk: () => router.push('/cardflow/definitions'),
  })
}
```

- [ ] **Step 4: 自检**

确认：`Modal` 已 import；旧 `watch(() => state, …)`（line 224）已删、新 watcher 在 `auto` 之后且含 `auto.markDirty()`；`goBack` 不再有 `window.confirm`；`saveStateText`/pill 模板未动。无 `<style>` 改动。

- [ ] **Step 5: 提交**

```bash
git diff --stat web/src/views/cardflow/FlowDefinitionEditPage.vue   # 确认仅本文件、且无并发 WIP
git add web/src/views/cardflow/FlowDefinitionEditPage.vue
git commit -m "fix(cardflow-web): 自动保存 pill 真实化(markDirty) + 离开页换 ant Modal.confirm"
```

---

## Task 2: SchemaFieldEditor —— 字段保存失败提示(#3)

**Files:**
- Modify: `web/src/components/cardflow/SchemaFieldEditor.vue`

- [ ] **Step 1: import message**

在 `<script setup>` 顶部 import 区（现有 `import { computed, ref, watch } from 'vue'` 等之后）加：

```js
import { message } from 'ant-design-vue'
```

- [ ] **Step 2: commitEditor 两处补提示**

把当前 `commitEditor`（约 [:133-143](../../../web/src/components/cardflow/SchemaFieldEditor.vue:133)）：

```js
function commitEditor() {
  if (editingIndex.value < 0 || !draft.value) return
  // 校验 key 唯一
  const key = (draft.value.key || '').trim()
  if (!key) { return }
  const dup = fields.value.some((f, i) => i !== editingIndex.value && f.key === key)
  if (dup) { return }
  fields.value[editingIndex.value] = clone(draft.value)
  emitUpdate()
  cancelEditor()
}
```

替换为：

```js
function commitEditor() {
  if (editingIndex.value < 0 || !draft.value) return   // 内部不变量，静默
  // 校验 key 唯一
  const key = (draft.value.key || '').trim()
  if (!key) { message.warning('请填写字段标识（key）'); return }
  const dup = fields.value.some((f, i) => i !== editingIndex.value && f.key === key)
  if (dup) { message.warning(`字段标识「${key}」已存在`); return }
  fields.value[editingIndex.value] = clone(draft.value)
  emitUpdate()
  cancelEditor()
}
```

- [ ] **Step 3: 自检**

确认：`message` 已 import；空 key / 重复 key 两路径各有 `message.warning` 后再 `return`；`editingIndex<0` 内部不变量那条保持静默；其余逻辑未动。

- [ ] **Step 4: 提交**

```bash
git diff --stat web/src/components/cardflow/SchemaFieldEditor.vue
git add web/src/components/cardflow/SchemaFieldEditor.vue
git commit -m "fix(cardflow-web): 字段编辑保存失败补 message.warning(空/重复 key 不再静默)"
```

---

## Task 3: StageDefinitionEditor —— 删节点二次确认(#4)

**Files:**
- Modify: `web/src/components/cardflow/StageDefinitionEditor.vue`

- [ ] **Step 1: import Modal**

在 `<script setup>` 顶部 import 区（现有 `import { computed, onMounted, ref, watch, nextTick } from 'vue'` 等之后）加：

```js
import { Modal } from 'ant-design-vue'
```

- [ ] **Step 2: removeStage 改为确认 + 执行拆分**

把当前 `removeStage`（约 [:197-205](../../../web/src/components/cardflow/StageDefinitionEditor.vue:197)）：

```js
function removeStage(idx: number) {
  stages.value.splice(idx, 1)
  if (selectedIndex.value === idx) {
    selectedIndex.value = -1
  } else if (selectedIndex.value > idx) {
    selectedIndex.value--
  }
  emitUpdate()
}
```

替换为：

```js
function removeStage(idx: number) {
  Modal.confirm({
    title: '删除节点',
    content: `确定删除节点「${stages.value[idx]?.name || '未命名节点'}」？该节点的路由/配置将一并移除。`,
    okText: '删除',
    okType: 'danger',
    cancelText: '取消',
    onOk: () => doRemoveStage(idx),
  })
}

function doRemoveStage(idx: number) {
  stages.value.splice(idx, 1)
  if (selectedIndex.value === idx) {
    selectedIndex.value = -1
  } else if (selectedIndex.value > idx) {
    selectedIndex.value--
  }
  emitUpdate()
}
```

模板删除按钮（约 :823 `@click.stop="removeStage(index)"`）**不变**——现在点它弹确认框。

- [ ] **Step 3: 自检**

确认：`Modal` 已 import；`removeStage` 现弹 `Modal.confirm`，`onOk` 调 `doRemoveStage`；原 splice/selectedIndex/emitUpdate 逻辑逐字搬进 `doRemoveStage`；模板按钮未动。

- [ ] **Step 4: 提交**

```bash
git diff --stat web/src/components/cardflow/StageDefinitionEditor.vue
git add web/src/components/cardflow/StageDefinitionEditor.vue
git commit -m "fix(cardflow-web): 删节点加 Modal.confirm 二次确认"
```

---

## Task 4: 验证（vite build + 用户 live）

**Files:** 无新增改动（仅验证）。

- [ ] **Step 1: vite build 绿**

Run: `cd web; npm run build`（或仓库约定的前端构建命令）
Expected: 构建成功，无本次 4 处改动引入的语法/类型/编译错。
（注：`vue-tsc -b` 基线本就大量报错、非门禁，以 `vite build` 成败为准；见 [[frontend-typecheck-baseline-red]]。若 build 因并发会话未提交的其它 `.vue` WIP 报无关错，定位是否本计划文件——非本计划文件的错属并发，记录但不修。）

- [ ] **Step 2: 确认改动文件 stylelint 0 / 无裸 hex 回潮**

本轮全在 `<script>`，理论不触发 stylelint。如仓库有 `npm run lint:style`，对 3 个改动文件跑一遍确认 0（或 `rg -n "#[0-9a-fA-F]{3,6}" <改动的 script 段>` 确认未新增裸 hex——预期无）。

- [ ] **Step 3: 确认提交干净**

Run: `git status --short`
Expected: 本计划 3 个 `.vue` 文件已 commit；剩余仅本计划之外的既有未跟踪/并发文件（与开工时一致）。

- [ ] **Step 4: 交用户 9001 live 逐项验**（我不自起 vite，避免双 Vue 冲突）

请用户在已登录的 9001 会话（HMR）逐项确认：
1. **pill**：编辑任意字段/节点 → 顶栏 pill 立刻变「未保存的更改」；30s 后或点「保存草稿」→ 回「已保存」。
2. **字段提示**：字段抽屉 key 留空 / 与他字段重复，点确认 → 弹黄色 warning，抽屉不静默关。
3. **删节点确认**：点节点删除按钮 → 弹「删除节点」确认框；取消不删、确认才删。
4. **离开确认**：有未保存更改点「返回」→ 弹 ant Modal（非浏览器原生框）；取消留页、确认离开。

> live 验出问题则回到对应 Task 修，再 build + 复验。

---

## 自检对照（spec coverage）

| Spec 目标 | 对应任务 |
|---|---|
| #1 pill 编辑后立即「未保存」（markDirty） | Task 1 Step 2 |
| #6 离开页 ant Modal.confirm | Task 1 Step 3 |
| #3 字段保存失败 message.warning（空/重复 key） | Task 2 |
| #4 删节点 Modal.confirm 二次确认 | Task 3 |
| 不动 useAutoSave/保存校验逻辑/`<style>`；onBeforeRouteLeave、#2、#5 非目标 | 范围外，未触及 |
| vite build 绿 + 用户 live 验四项 | Task 4 |
