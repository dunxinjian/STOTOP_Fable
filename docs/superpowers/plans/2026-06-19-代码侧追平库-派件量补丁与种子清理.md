# 代码侧追平库（派件量V51补丁 + 种子卫生清理）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 CardFlow 派件量功能在存量库补建（V51 补丁），并把代码侧种子（baseline.json / gzip blob / 死代码）清理到与现行库一致。

**Architecture:** ③ 复用幂等的 `MigrateV23` 通过新版本号 V51 在存量库重跑（V23 被网点质控历史占用）。④ 以库为准：脚本化清理 `baseline-reference-data.json`（删 16 条 oa:* + 补 3 行 CardFlow 权限）、`SystemSeeder` gzip 种子 blob（删 29 条废弃 SYS功能权限行）、`WorkflowSeeder` 图标、`BasicDataSeeder.EnsureOrgTypes`（补 3 个组织类型）。

**Tech Stack:** .NET 10 / EF Core / SQL Server；自定义 `MigrationRunner`（版本化）+ `SchemaAutoSync`；Python(pymssql) 只读核对（`Temp/dbq.py`）与 gzip blob 重建。

**前置事实（已核实）：**
- 现行库 `47.105.65.51/stotop` 可只读访问：`python Temp/dbq.py --out Temp/<名>.json "<SQL>"` 再 Read 文件（控制台中文乱码，必须 --out）。
- `MigrateV23`（CardFlowSeeder.cs:1501）完全幂等（建表/索引/规则3006/流程2264/版本2265/节点5033 全 IF NOT EXISTS）。
- gzip blob 解码逻辑：`SeederHelper.DecodeGzipBase64` = concat(SqlChunk0..12) → base64 解码 → gzip 解压 → UTF-8。项目**无编码器**，本计划自写。解码副本在 `Temp/blob_decoded.sql`。
- blob `SYS功能权限` 待删 = blob FID 不在库 FID 的行 = **29 行**（28 条 oa:* FID 500-522/525-529 + 1 条 `finance:amoeba:allocation` FID 1874）。组织/角色/用户/用户角色与库一致，不动。
- baseline.json 顶层 `{generatedAt, note, tables:[]}`；`tables` 13 项，每项 `{Name, RowCount, ExportMode, Columns, Rows}`，Rows 为按列名(中文)键的对象。SYS功能权限 表含 16 条 oa:*；SYS组织类型 已含 9 行（无需改）。
- `WorkflowSeeder.cs:41` `cardflow.start` 图标 `AuditOutlined`，库实为 `FileTextOutlined`（仅此一处改；line 53 `cardflow.apply` 库=AuditOutlined 不改）。该 INSERT 块被整表 IF NOT EXISTS 守卫（存量库死代码，仅影响全新库）。
- `BasicDataSeeder.cs:94` `EnsureOrgTypes` 硬编码 FID 1-6 且 `Any()` 守卫；缺 FID 7-9。
- 无前端测试运行器（记忆 frontend-typecheck-baseline-red）；`dotnet test` 须 `--arch x64`（记忆 dotnet-test-x64-socket）。本计划验证以"构建 + 启动后端幂等重跑 + 只读查库"为主。

**库侧精确值（实现时直接用）：**
- 权限 624：`F编码=cardflow:auto-plugin` `F名称=自动插件管理` `F类型=菜单` `F父ID=150` `F路由=/cardflow/auto-plugin` `F组件路径=cardflow/auto-plugin/AutoPluginList` `F图标=AppstoreOutlined` `F排序=8` `F是否可见=1` `F状态=1`
- 权限 629：`F编码=cardflow:auto-plugin-rules` `F名称=自动插件规则` `菜单` `父150` `/cardflow/auto-plugin/rules` `cardflow/auto-plugin/AutoPluginRuleList` `BlockOutlined` `排序9` `可见1` `状态1`
- 权限 2122：`F编码=cardflow:import:validation` `F名称=验证导入计算` `按钮` `父157` `路由=NULL` `组件=NULL` `图标=NULL` `排序3` `可见0` `状态1`
- 组织类型 7/8/9：`NETWORK_POINT 快递网点 层级3 可关联账套0 可切换0 图标apartment 排序7` / `FRANCHISE_AREA 承包区 层级4 0 0 location_city 8` / `LAST_MILE_STATION 末端驿站 层级4 0 0 store 9`（均 F是否启用=true）

---

## Task 1: ③ 派件量 V51 补丁（CardFlowSeeder 加步骤）

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs:72`（steps 列表，V50 之后追加 V51）

- [ ] **Step 1: 在步骤列表追加 V51**

把 CardFlowSeeder.cs 第 72 行 V50 那条 `new(50, ...MigrateV50),` 之后、`};`(73行) 之前，插入：

```csharp
            new(51, "派件量补丁：存量库补建 STG申通派件日明细+规则3006+流程2330（V23在存量库被网点质控占用未执行，复用幂等 MigrateV23）(2026-06-19)", MigrateV23),
```

不新增方法（直接复用现有 `MigrateV23`）。不改 `MigrateV23` 本体。

- [ ] **Step 2: 构建验证**

Run: `dotnet build src/STOTOP.WebAPI -c Debug`
Expected: 生成成功，0 错误（`ValidateSteps` 要求版本连续，V51 接 V50 合法）。

- [ ] **Step 3: 提交**

```bash
git add src/STOTOP.WebAPI/Data/Seeders/CardFlowSeeder.cs
git commit -m "feat(cardflow): 加 V51 派件量补丁(复用幂等MigrateV23,存量库V23被占未执行)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: ④c WorkflowSeeder 图标对齐库

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/WorkflowSeeder.cs:41`

- [ ] **Step 1: 改 cardflow.start 图标**

把第 41 行（`cardflow.start`）的 `'AuditOutlined'` 改为 `'FileTextOutlined'`：

```sql
    (0, 'cardflow.start', N'发起卡片流程', 'FileTextOutlined', 'cardflow', '/cardflow/upload', 'apply', NULL, 40, N'通过CardFlow发起业务流程');
```

⚠ 只改 `cardflow.start`（line 41）。`cardflow.apply`（line 53）库值就是 `AuditOutlined`，**保持不变**。

- [ ] **Step 2: 构建验证**

Run: `dotnet build src/STOTOP.WebAPI -c Debug`
Expected: 0 错误。

- [ ] **Step 3: 提交**

```bash
git add src/STOTOP.WebAPI/Data/Seeders/WorkflowSeeder.cs
git commit -m "fix(seed): WorkflowSeeder cardflow.start 图标对齐库 FileTextOutlined

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: ④d EnsureOrgTypes 补 7-9

**Files:**
- Modify: `src/STOTOP.WebAPI/Data/Seeders/BasicDataSeeder.cs:108`（`types` 列表 FID6 之后追加 7-9）

- [ ] **Step 1: 在 types 列表追加 3 个组织类型**

在 BasicDataSeeder.cs 第 108 行（`new() { FID = 6, FCode = "TEAM", ... }`）之后、`};`(109行) 之前，插入：

```csharp
            new() { FID = 7, FCode = "NETWORK_POINT",      FName = "快递网点", FLevel = 3, FCanBindAccountSet = false, FCanSwitch = false, FIcon = "apartment",     FSortOrder = 7, FIsEnabled = true, FCreateTime = now },
            new() { FID = 8, FCode = "FRANCHISE_AREA",     FName = "承包区",  FLevel = 4, FCanBindAccountSet = false, FCanSwitch = false, FIcon = "location_city", FSortOrder = 8, FIsEnabled = true, FCreateTime = now },
            new() { FID = 9, FCode = "LAST_MILE_STATION",  FName = "末端驿站", FLevel = 4, FCanBindAccountSet = false, FCanSwitch = false, FIcon = "store",         FSortOrder = 9, FIsEnabled = true, FCreateTime = now },
```

同时把方法上方注释 `幂等初始化 SYS组织类型 种子数据（6条）`(line 92) 改为 `（9条）`。

- [ ] **Step 2: 构建验证**

Run: `dotnet build src/STOTOP.WebAPI -c Debug`
Expected: 0 错误。

- [ ] **Step 3: 提交**

```bash
git add src/STOTOP.WebAPI/Data/Seeders/BasicDataSeeder.cs
git commit -m "fix(seed): EnsureOrgTypes 补快递网点/承包区/末端驿站(7-9)对齐库

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: ④a baseline.json 清理（脚本化）

**Files:**
- Create: `scripts/seed-maint/clean_baseline.py`
- Modify: `src/STOTOP.WebAPI/Data/Seeders/Baseline/baseline-reference-data.json`

- [ ] **Step 1: 写清理脚本**

创建 `scripts/seed-maint/clean_baseline.py`（删 SYS功能权限 表里 F编码 以 `oa:` 开头的行；从现库取 FID 624/629/2122 三行追加；重算 RowCount）：

```python
# -*- coding: utf-8 -*-
import json, os, sys
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..', 'Temp'))
import dbq  # 复用解密+连库

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..'))
BL = os.path.join(ROOT, 'src', 'STOTOP.WebAPI', 'Data', 'Seeders', 'Baseline', 'baseline-reference-data.json')

d = json.load(open(BL, encoding='utf-8'))
perm = next(t for t in d['tables'] if t['Name'] == 'SYS功能权限')
before = len(perm['Rows'])

# 1) 删 oa:* 行
perm['Rows'] = [r for r in perm['Rows'] if not str(r.get('F编码', '')).startswith('oa:')]
removed = before - len(perm['Rows'])

# 2) 从现库取 624/629/2122 三行(若 baseline 已无)，按 baseline 列对象追加
have = {r['FID'] for r in perm['Rows']}
need = [f for f in (624, 629, 2122) if f not in have]
if need:
    cols = [c['Name'] for c in perm['Columns']]
    collist = ','.join(f'[{c}]' for c in cols)
    rs = dbq.run(f"SELECT {collist} FROM [SYS功能权限] WHERE FID IN ({','.join(map(str,need))})")
    rset = rs[0]
    cidx = {n: i for i, n in enumerate(rset['columns'])}
    for row in rset['rows']:
        perm['Rows'].append({c: row[cidx[c]] for c in cols})

# 3) 排序 + 重算 RowCount
perm['Rows'].sort(key=lambda r: r['FID'])
perm['RowCount'] = len(perm['Rows'])

json.dump(d, open(BL, 'w', encoding='utf-8'), ensure_ascii=False, indent=2)
print(f"删 oa:* {removed} 行；补 {len(need)} 行；SYS功能权限 现 {perm['RowCount']} 行")
```

- [ ] **Step 2: 运行脚本并验证输出**

Run: `python scripts/seed-maint/clean_baseline.py`
Expected: 输出形如 `删 oa:* 16 行；补 N 行；SYS功能权限 现 M 行`。脚本动态判断缺哪几行（baseline 通常已含 624/629，故 N 多为 1=仅补 2122）。N/M 以脚本实际输出为准；权威校验在 Step 3（oa=0、624/629/2122 齐、RowCount 一致）。

- [ ] **Step 3: 校验 baseline 无 oa:* 且含 3 行**

Run:
```bash
python -c "import json,sys; sys.stdout.reconfigure(encoding='utf-8'); d=json.load(open('src/STOTOP.WebAPI/Data/Seeders/Baseline/baseline-reference-data.json',encoding='utf-8')); p=next(t for t in d['tables'] if t['Name']=='SYS功能权限'); fids={r['FID'] for r in p['Rows']}; oa=[r for r in p['Rows'] if str(r.get('F编码','')).startswith('oa:')]; print('oa行=',len(oa),'含624/629/2122=', {624,629,2122}<=fids, 'RowCount一致=', p['RowCount']==len(p['Rows']))"
```
Expected: `oa行= 0 含624/629/2122= True RowCount一致= True`

- [ ] **Step 4: 提交**

```bash
git add scripts/seed-maint/clean_baseline.py src/STOTOP.WebAPI/Data/Seeders/Baseline/baseline-reference-data.json
git commit -m "chore(seed): baseline.json 删16条oa:*死行+补CardFlow权限624/629/2122(以库为准)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: ④b gzip blob 彻底清理（删废弃 SYS功能权限 行）

**Files:**
- Create: `scripts/seed-maint/rebuild_blob.py`
- Modify: `src/STOTOP.WebAPI/Data/Seeders/SystemSeeder.cs`（替换 SqlChunk0..12 常量）

- [ ] **Step 1: 写 blob 重建脚本**

创建 `scripts/seed-maint/rebuild_blob.py`：提取 SqlChunk → 解码 → 删除 `SYS功能权限` 中 FID 不在现库的 INSERT → 重新 gzip+base64 切回 13 块 → 回写 SystemSeeder.cs。

```python
# -*- coding: utf-8 -*-
import re, os, sys, gzip, base64
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..', 'Temp'))
import dbq

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..'))
SS = os.path.join(ROOT, 'src', 'STOTOP.WebAPI', 'Data', 'Seeders', 'SystemSeeder.cs')

src = open(SS, encoding='utf-8-sig').read()
# 1) 提取 SqlChunk0..N 常量 (private const string SqlChunkN = "....";)
chunk_pat = re.compile(r'(SqlChunk(\d+)\s*=\s*")([^"]*)(";)')
chunks = {int(m.group(2)): m.group(3) for m in chunk_pat.finditer(src)}
n = len(chunks)
assert n >= 1, "未找到 SqlChunk 常量"
b64 = ''.join(chunks[i] for i in sorted(chunks))
sql = gzip.decompress(base64.b64decode(b64)).decode('utf-8')

# 2) 现库 SYS功能权限 FID 集合
dbfids = {r[0] for r in dbq.run("SELECT FID FROM [SYS功能权限]")[0]['rows']}

# 3) 删除 blob 中 FID 不在库 的 SYS功能权限 INSERT 语句（按到分号的完整语句匹配）
removed = []
def drop(m):
    fid = int(re.search(r'VALUES\s*\((\d+)', m.group(0)).group(1))
    if fid in dbfids:
        return m.group(0)
    removed.append(fid)
    return ''
new_sql = re.sub(r'INSERT INTO \[SYS功能权限\][^;]*;', drop, sql)
# 清理可能留下的连续空行
new_sql = re.sub(r'\n{3,}', '\n\n', new_sql)
print(f"删除 SYS功能权限 行 {len(removed)} 条: {sorted(removed)}")

# 4) 重新 gzip+base64，切成与原来相同数量(n)的块
nb64 = base64.b64encode(gzip.compress(new_sql.encode('utf-8'))).decode('ascii')
size = (len(nb64) + n - 1) // n
parts = [nb64[i*size:(i+1)*size] for i in range(n)]
while len(parts) < n:
    parts.append('')

# 5) 回写常量
def repl(m):
    idx = int(m.group(2))
    return f'{m.group(1)}{parts[idx]}{m.group(4)}'
out = chunk_pat.sub(repl, src)
open(SS, 'w', encoding='utf-8').write(out)
print(f"已回写 {n} 块。新 base64 长度 {len(nb64)}（原 {len(b64)}）")
```

- [ ] **Step 2: 运行脚本**

Run: `python scripts/seed-maint/rebuild_blob.py`
Expected: 输出 `删除 SYS功能权限 行 29 条: [500, 501, ..., 522, 525, ..., 529, 1874]` 与回写成功。

- [ ] **Step 3: 验证新 blob 往返正确 + 其余表段不变**

Run:
```bash
python -c "
import re,gzip,base64,sys
sys.stdout.reconfigure(encoding='utf-8')
src=open('src/STOTOP.WebAPI/Data/Seeders/SystemSeeder.cs',encoding='utf-8-sig').read()
chunks={int(m.group(2)):m.group(3) for m in re.finditer(r'SqlChunk(\d+)\s*=\s*\"([^\"]*)\"',src)}
sql=gzip.decompress(base64.b64decode(''.join(chunks[i] for i in sorted(chunks)))).decode('utf-8')
import collections
for t in ['SYS组织架构','SYS角色','SYS用户角色','SYS用户','SYS功能权限']:
    print(t, len(re.findall(r'INSERT INTO \['+t+r'\]', sql)))
print('oa:* 残留', len(re.findall(r\"N'oa:\", sql)))
"
```
Expected: `SYS组织架构 320 / SYS角色 5 / SYS用户角色 4 / SYS用户 500 / SYS功能权限 412 / oa:* 残留 0`（功能权限 441→412，删 29；其余表段不变）。

- [ ] **Step 4: 构建验证**

Run: `dotnet build src/STOTOP.WebAPI -c Debug`
Expected: 0 错误（base64 仅 A-Za-z0-9+/=，C# 字符串字面量安全）。

- [ ] **Step 5: 提交**

```bash
git add scripts/seed-maint/rebuild_blob.py src/STOTOP.WebAPI/Data/Seeders/SystemSeeder.cs
git commit -m "chore(seed): gzip种子blob删29条废弃SYS功能权限行(28 oa:*+finance:amoeba:allocation,以库为准)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 集成验证（现库幂等重跑 + 只读核对）

**Files:** 无（仅运行与核对）

- [ ] **Step 1: 启动后端触发迁移（Development）**

Run（后台）: `dotnet run --project src/STOTOP.WebAPI -c Debug`
等日志出现 `数据库迁移执行完成`（看 CardFlow 段应执行 V51；baseline 因哈希变化执行一次幂等对齐）。随后停止该进程。
Expected: 无 fail-fast 异常；CardFlow V51 执行；无 SchemaAutoSync 执行失败。

- [ ] **Step 2: 只读核对派件量三件套**

Run:
```bash
python Temp/dbq.py --out Temp/verify_paijian.json "
SELECT '表STG申通派件日明细' AS k, CASE WHEN OBJECT_ID(N'STG申通派件日明细') IS NULL THEN 0 ELSE 1 END AS v
UNION ALL SELECT '规则3006', COUNT(*) FROM [CF自动插件_规则] WHERE FID=3006
UNION ALL SELECT '卡片流程2264', COUNT(*) FROM [CF卡片流程] WHERE FID=2264
UNION ALL SELECT '流程版本2265', COUNT(*) FROM [CF流程版本] WHERE FID=2265
UNION ALL SELECT '节点5033', COUNT(*) FROM [CF流程节点] WHERE FID=5033
UNION ALL SELECT 'CardFlow最高版本', MAX(F版本号) FROM [SYS迁移历史] WHERE F模块='CardFlow'"
```
读 `Temp/verify_paijian.json`。Expected: 表=1、规则/流程/版本/节点 各=1、CardFlow 最高版本=51。

- [ ] **Step 3: 只读核对权限无 oa:* 复活 + 三权限在**

Run:
```bash
python Temp/dbq.py --out Temp/verify_perm.json "
SELECT 'oa:*行' AS k, COUNT(*) AS v FROM [SYS功能权限] WHERE F编码 LIKE 'oa:%'
UNION ALL SELECT '624/629/2122', COUNT(*) FROM [SYS功能权限] WHERE FID IN (624,629,2122)
UNION ALL SELECT 'cardflow.start图标对', COUNT(*) FROM [WF触发动作] WHERE F标识='cardflow.start' AND F图标='FileTextOutlined'
UNION ALL SELECT '组织类型数', COUNT(*) FROM [SYS组织类型]"
```
读 `Temp/verify_perm.json`。Expected: oa:*行=0、624/629/2122=3、cardflow.start图标对=1、组织类型数=9。

- [ ] **Step 4: 记录验证结论（无代码改动则无需提交）**

把 Step 2/3 结果记入 `Temp/三方差异比对报告.md` 的执行记录段（可选）。

---

## 备注（不在本计划范围）
- 规则3006 的 `excelColumn`/`F组织ID=192`/`fileNamePattern` 为占位，真正用派件文件导入前须按真实文件 row1 表头核对（另案）。
- blob 仍含 500 行真实用户（含密码哈希），与库一致故保留；"整库用户当种子"是独立安全隐患，另案处理。
- 全新库/隔离库验证（blob 改动的真正受益场景）：可在空库跑完整初始化，确认 SYS功能权限 无 oa:*/废弃、派件量三件套建出、组织类型 9 个、admin 权限正常。
