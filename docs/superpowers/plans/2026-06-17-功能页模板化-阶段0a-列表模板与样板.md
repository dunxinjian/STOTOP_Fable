# 列表页模板地基 + 样板页（阶段 0a）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务逐个实现。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 建立列表页类的核心共享件 `DataTable` 与全局密度兜底，并把样板页 `dormitory/BuildingManage` 迁移到「PageHeader(#toolbar)+DataTable+StatusTag」标准，作为后续所有列表页（~150+）迁移的可复制范本。

**Architecture:** 站在既有 `PATTERNS.md`/`TOKENS.md` 契约上，**复用优先于新建**——只新建一个 `DataTable.vue`（封装各页 100% 重复的 paginationConfig + 序号列 + 空态），其余复用已存在的 `PageHeader`/`StatusTag`/`EmptyState` 与全局 `.page-toolbar` 类；密度靠 `ConfigProvider` 全局兜底。承接 spec `docs/superpowers/specs/2026-06-17-功能页逐模块巡检与收敛-design.md`（方向① 克制收敛）。

**Tech Stack:** Vue 3.5 `<script setup>` + TypeScript + Ant Design Vue 4.2 + SCSS 令牌（`var(--token)`）+ Vite 8。组件经 `unplugin-vue-components` 自动注册（也兼容显式 import）。

---

## ⚠️ 项目现实：验证方式（无前端测试运行器）

本前端**没有单元测试运行器，且约定不引入测试框架**；`vue-tsc -b`（type-check）基线本就大量报错、**不可当绿灯门禁**。因此本计划每个任务的「验证」用以下手段（与上游 0616 那轮一致），**不写单元测试**：

- **构建绿**：`cd web && npm run build`（`vite build`，不跑 type-check）必须成功。
- **preview 渲染**：用 `preview_start` + `preview_snapshot`/`preview_screenshot` 打开样板页确认渲染与交互正常（分页/查询/状态标签）。
- **stylelint 计数**：`cd web && npx stylelint "<file>"` 对改动文件应 0 error（新代码禁裸 hex）。
- **grep 断言**：对照 PATTERNS 红线命令核验（如某写法已消除）。

> 提交前不得跳过 hook（本仓库当前无前端 pre-commit hook，stylelint 门禁是后续批次 0d 的任务）。

## 范围（本批次 = 0a）

**本批次做**：① 全局密度兜底 ② `DataTable.vue` ③ 样板页 `BuildingManage` 迁移 ④ `PATTERNS.md` 列表类迁移配方。

**不在本批次**（后续批次，spec 已含、各自带验证页）：
- 0b `StatFilterTabs.vue`（验证页：`vehicle/VehicleManage` 顶部 4 张统计卡 → 状态快筛）
- 0c `PageLayout.vue`（验证页：`cardflow/OrchestrationDetailPage` 的 flow variant）
- 0d stylelint 门禁（husky + lint-staged 只拦 staged + ECharts 22 文件白名单 + baseline 快照 1943）
- 0e `--avatar-palette-*` 令牌（消费方：`crm/CustomerManage` 的 BD 头像）
- 阶段1 各页面类批量迁移、阶段2 痛点页队列

---

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `web/src/components/DataTable.vue` | 列表页表格封装：分页默认值 + 序号列 + 内建空态 + 密度/边框约定，消灭各页重复样板 | 新建 |
| `web/src/App.vue` | 全局 ConfigProvider 增加 `component-size` 默认 small | 改 `:48` |
| `web/src/views/dormitory/BuildingManage.vue` | 样板列表页迁移到新标准 | 改 |
| `web/docs/PATTERNS.md` | 列表类骨架增 `DataTable` 契约 + 迁移配方 | 改 §一.1、§二 |

---

## Task 1：全局密度兜底（ConfigProvider component-size=small）

让 `size="small"` 成为真正的全局默认（现状 1571 处手写、漏写处回落 middle 致同页混用）。本任务只加兜底，不删存量手写（存量随各页迁移时清）。

**Files:**
- Modify: `web/src/App.vue:48`

- [ ] **Step 1：改 ConfigProvider**

把第 48 行：

```vue
  <a-config-provider :locale="zhCN" :theme="themeStore.antdTheme">
```

改为：

```vue
  <a-config-provider :locale="zhCN" :theme="themeStore.antdTheme" :component-size="'small'">
```

- [ ] **Step 2：构建验证**

Run: `cd web && npm run build`
Expected: 构建成功，无报错。

- [ ] **Step 3：preview 抽检密度**

用 `preview_start` 打开任一列表页（如 `/dormitory/buildings`），`preview_screenshot` 确认表格/按钮/输入为紧凑 small 档，布局未塌。

- [ ] **Step 4：提交**

```bash
git add web/src/App.vue
git commit -m "feat(ui): ConfigProvider 全局 component-size=small 兜底密度"
```

---

## Task 2：新建 `DataTable.vue`

封装列表页 100% 重复的：antd 分页对象（showSizeChanger/pageSizeOptions/showTotal「共 N 条」）、序号列与 index 计算、`@change` 回填、内建 `EmptyState` 空态、密度/边框约定。`bordered` 默认 `false`（克制收敛去边框），`size` 不显式传（继承 ConfigProvider 的 small）。

**Files:**
- Create: `web/src/components/DataTable.vue`

- [ ] **Step 1：创建组件文件**

```vue
<!--
  DataTable —— 列表页表格封装
  统一替代各页手写的 paginationConfig=computed + handleTableChange 回填 + 序号列计算 + #emptyText。
  - pagination：传 { pageIndex, pageSize, total } 响应式对象；翻页/改页长时就地回填该对象的
    pageIndex/pageSize 并 emit('change') 供页面重新取数；传 false 关闭分页。
  - indexColumn：默认在最左插入「序号」列并按当前分页计算行号。
  - 其余列的单元格渲染由父组件 #bodyCell 作用域插槽透传（序号列由本组件内部渲染）。
  - 空态默认 <EmptyState size="small">，可用 #emptyText 覆盖。
  - bordered 默认 false（克制收敛去边框）；size 不显式传，继承 ConfigProvider 全局 small。
-->
<template>
  <a-table
    :columns="mergedColumns"
    :data-source="dataSource"
    :loading="loading"
    :pagination="antPagination"
    :row-key="rowKey"
    :bordered="bordered"
    :scroll="scroll"
    @change="onChange"
  >
    <template #bodyCell="slotProps">
      <template v-if="slotProps.column.key === '__index__'">
        {{ indexText(slotProps.index) }}
      </template>
      <template v-else>
        <slot name="bodyCell" v-bind="slotProps" />
      </template>
    </template>
    <template #emptyText>
      <slot name="emptyText">
        <EmptyState size="small" :description="emptyText" />
      </slot>
    </template>
  </a-table>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import EmptyState from '@/components/EmptyState.vue'

interface PaginationModel {
  pageIndex: number
  pageSize: number
  total: number
}

const props = withDefaults(defineProps<{
  /** 列定义（不含序号列，序号列由 indexColumn 控制自动加） */
  columns: any[]
  /** 行数据 */
  dataSource: any[]
  /** 加载态 */
  loading?: boolean
  /** 分页模型；传 false 关闭分页 */
  pagination?: PaginationModel | false
  /** 行 key */
  rowKey?: string
  /** 横向/纵向滚动 */
  scroll?: Record<string, any>
  /** 是否在最左加序号列 */
  indexColumn?: boolean
  /** 是否带边框（默认 false，克制收敛去边框） */
  bordered?: boolean
  /** 空态描述文案 */
  emptyText?: string
}>(), {
  loading: false,
  pagination: () => ({ pageIndex: 1, pageSize: 20, total: 0 }),
  rowKey: 'id',
  scroll: undefined,
  indexColumn: true,
  bordered: false,
  emptyText: '暂无数据',
})

const emit = defineEmits<{
  /** 翻页/改页长后触发，供父组件重新取数 */
  (e: 'change'): void
}>()

const indexColDef = {
  title: '序号',
  dataIndex: '__index__',
  key: '__index__',
  width: 60,
  align: 'center' as const,
}

const mergedColumns = computed(() =>
  props.indexColumn ? [indexColDef, ...props.columns] : props.columns,
)

const antPagination = computed(() => {
  if (props.pagination === false) return false
  return {
    current: props.pagination.pageIndex,
    pageSize: props.pagination.pageSize,
    total: props.pagination.total,
    showSizeChanger: true,
    pageSizeOptions: ['10', '20', '50', '100'],
    showTotal: (t: number) => `共 ${t} 条`,
  }
})

function indexText(index: number) {
  if (props.pagination === false) return index + 1
  return (props.pagination.pageIndex - 1) * props.pagination.pageSize + index + 1
}

// 就地回填传入的响应式分页对象（同一引用），再通知父组件取数
function onChange(pag: any) {
  if (props.pagination === false) return
  props.pagination.pageIndex = pag.current
  props.pagination.pageSize = pag.pageSize
  emit('change')
}
</script>
```

- [ ] **Step 2：构建验证（组件能编译）**

Run: `cd web && npm run build`
Expected: 构建成功（DataTable 编译通过；此时尚无消费方，仅验证可编译）。

- [ ] **Step 3：stylelint 验证（无裸 hex）**

Run: `cd web && npx stylelint "src/components/DataTable.vue"`
Expected: 0 problems（本组件无 `<style>` 块、无裸 hex）。

- [ ] **Step 4：提交**

```bash
git add web/src/components/DataTable.vue
git commit -m "feat(ui): 新增 DataTable 列表表格封装（分页/序号列/空态/密度收敛）"
```

---

## Task 3：样板页 `BuildingManage` 迁移到新标准

把样板列表页迁到「`.page-toolbar` 全局类筛选条 + `DataTable` + `StatusTag`」，删掉本页的 `paginationConfig`/`handleTableChange`/序号列计算/`a-tag` 字面色/`a-table bordered`。**这是后续 ~150 个列表页的复制范本。**

**Files:**
- Modify: `web/src/views/dormitory/BuildingManage.vue`

- [ ] **Step 1：#toolbar 内联 flex 换成全局 `.page-toolbar` 类**

把模板第 9–43 行的 `<template #toolbar>` 整块替换为：

```vue
      <template #toolbar>
        <div class="page-toolbar">
          <div class="page-toolbar__group">
            <a-form-item label="关键词" style="margin-bottom:0">
              <a-input
                v-model:value="searchForm.keyword"
                placeholder="请输入编码/名称"
                allow-clear
                style="width: 280px"
                @keyup.enter="handleSearch"
              />
            </a-form-item>
            <a-form-item label="状态" style="margin-bottom:0">
              <a-select
                v-model:value="searchForm.status"
                placeholder="全部"
                allow-clear
                style="width: 120px"
                :options="[
                  { label: '启用', value: 1 },
                  { label: '停用', value: 0 },
                ]"
              />
            </a-form-item>
          </div>
          <div class="page-toolbar__filters">
            <a-button type="primary" @click="handleSearch">
              <template #icon><SearchOutlined /></template>查询
            </a-button>
            <a-button @click="handleReset">
              <template #icon><ReloadOutlined /></template>重置
            </a-button>
          </div>
        </div>
      </template>
```

- [ ] **Step 2：`a-card`+`a-table` 换成 `DataTable`**

把模板第 46–103 行（`<a-card>` 整块）替换为：

```vue
    <DataTable
      :columns="tableColumns"
      :data-source="tableData"
      :loading="loading"
      :pagination="pagination"
      :scroll="{ x: 1100 }"
      row-key="id"
      empty-text="暂无楼栋数据"
      @change="fetchBuildingList"
    >
      <template #bodyCell="{ column, record }">
        <template v-if="column.dataIndex === 'name'">
          <a-tooltip :title="record.name">{{ record.name }}</a-tooltip>
        </template>
        <template v-if="column.dataIndex === 'address'">
          <a-tooltip :title="record.address">{{ record.address }}</a-tooltip>
        </template>
        <template v-if="column.dataIndex === 'status'">
          <StatusTag :type="record.status === 1 ? 'success' : 'danger'" dot>
            {{ record.status === 1 ? '启用' : '停用' }}
          </StatusTag>
        </template>
        <template v-if="column.dataIndex === 'action'">
          <a-button type="link" size="small" @click="handleEdit(record)">
            <EditOutlined />编辑
          </a-button>
          <a-button type="link" size="small" @click="handleManageRooms(record)">
            <HomeOutlined />管理房间
          </a-button>
          <a-button
            type="link"
            size="small"
            :class="record.status === 1 ? 'btn-disable' : 'btn-enable'"
            @click="handleToggleStatus(record)"
          >
            {{ record.status === 1 ? '停用' : '启用' }}
          </a-button>
          <a-popconfirm
            title="确定删除该楼栋吗？"
            ok-text="确定"
            cancel-text="取消"
            @confirm="handleDelete(record)"
          >
            <a-button type="link" size="small" danger>
              <DeleteOutlined />删除
            </a-button>
          </a-popconfirm>
        </template>
      </template>
    </DataTable>
```

> 说明：序号列与翻页由 `DataTable` 内建，故 `bodyCell` 不再有 `index` 分支；状态由 `StatusTag`（`success`/`danger` 走令牌浅底+深字）替代 `a-tag` 字面色；去掉了 `a-card`、`bordered`、`@change=handleTableChange`、`:pagination=paginationConfig`。

- [ ] **Step 3：删序号列定义 + 清理脚本里的冗余**

在 `tableColumns`（第 243–254 行）中**删除序号列那一行**：

```ts
  { title: '序号', dataIndex: 'index', key: 'index', width: 60, align: 'center' as const },
```

删除 `paginationConfig`（第 271–278 行）与 `handleTableChange`（第 280–284 行）两段（已由 DataTable 内建）：

```ts
const paginationConfig = computed(() => ({
  current: pagination.pageIndex,
  pageSize: pagination.pageSize,
  total: pagination.total,
  showSizeChanger: true,
  pageSizeOptions: ['10', '20', '50', '100'],
  showTotal: (t: number) => `共 ${t} 条`,
}))

function handleTableChange(pag: any) {
  pagination.pageIndex = pag.current
  pagination.pageSize = pag.pageSize
  fetchBuildingList()
}
```

> 保留 `pagination`（reactive，DataTable 就地回填它）与 `computed`（若删后无其它使用，import 里去掉 `computed` 避免未用告警——本文件 `validateCode` 等不用 computed，删 import 中的 `computed`）。

- [ ] **Step 4：补齐 import（DataTable / StatusTag），去掉未用的 computed**

把脚本顶部 import（第 221、227–228 行附近）调整为：

```ts
import { ref, reactive, onMounted } from 'vue'
```

并在组件 import 区补：

```ts
import DataTable from '@/components/DataTable.vue'
import StatusTag from '@/components/StatusTag.vue'
```

（`PageHeader`、`EmptyState` 的 import 保留；`EmptyState` 仍被 DataTable 间接用，但本页模板已不直接用 `EmptyState`，若 lint 报未用则一并删除本页对 EmptyState 的 import。）

- [ ] **Step 5：构建验证**

Run: `cd web && npm run build`
Expected: 构建成功，无未定义/未用变量报错。

- [ ] **Step 6：stylelint 验证**

Run: `cd web && npx stylelint "src/views/dormitory/BuildingManage.vue"`
Expected: 0 problems。

- [ ] **Step 7：preview 行为验证**

`preview_start` → 打开 `/dormitory/buildings`：
- `preview_snapshot` 确认表格有「序号」列、状态列显示 StatusTag（启用绿/停用红、带圆点）、分页显示「共 N 条」。
- `preview_fill` 关键词 + `preview_click` 查询，`preview_snapshot` 确认筛选生效。
- `preview_click` 翻页，确认 `fetchBuildingList` 被触发、序号随页递增。
- `preview_screenshot` 留证。

- [ ] **Step 8：提交**

```bash
git add web/src/views/dormitory/BuildingManage.vue
git commit -m "refactor(ui): 样板页 BuildingManage 迁移到 DataTable+StatusTag+.page-toolbar 标准"
```

---

## Task 4：更新 `PATTERNS.md` 列表类契约 + 迁移配方

把样板跑通的写法回写进契约，作为后续列表页迁移的唯一参照。

**Files:**
- Modify: `web/docs/PATTERNS.md`（§一.1 列表页、§二 共享组件契约）

- [ ] **Step 1：§一.1 列表页骨架更新**

把「### 1. 列表页」骨架代码块更新为以 `DataTable` 为表格层（替换原 `<a-table>+<a-pagination>` 描述），并注明「序号列/分页/空态由 DataTable 内建」：

```
.page-container
├─ <PageHeader>（#actions 主操作；#toolbar 第二行筛选用 .page-toolbar/.page-toolbar__group/__filters）
└─ <DataTable>（内建：序号列 + 分页「共 N 条」+ #emptyText=EmptyState；状态列用 <StatusTag>）
```

- [ ] **Step 2：§二 增 DataTable 契约表**

在共享组件契约区追加：

```
### DataTable

| prop | 默认 | 说明 |
| --- | --- | --- |
| `columns` / `data-source` / `loading` | — | 透传 a-table（columns 不含序号列） |
| `pagination` | `{pageIndex,pageSize,total}` | 响应式分页对象；翻页就地回填并 emit `change`；传 `false` 关闭 |
| `index-column` | `true` | 最左自动加「序号」列并按分页计算行号 |
| `bordered` | `false` | 克制收敛去边框 |
| `row-key` / `scroll` / `empty-text` | `'id'` / — / `'暂无数据'` | 透传 / 内建空态文案 |

- 翻页：`@change` 触发后父组件重新取数；不再每页手写 `paginationConfig`/`handleTableChange`。
- 其余列单元格用父组件 `#bodyCell` 作用域插槽（序号列由组件内部渲染）。
- 密度：不显式传 `size`，继承 `ConfigProvider` 全局 `small`。
```

- [ ] **Step 3：迁移配方一句话**

在 §一.1 末尾加：

> **列表页迁移配方**：① #toolbar 内联样式 → `.page-toolbar`（`__group` 放筛选输入、`__filters` 放查询/重置，右推）；② `a-card`+`a-table` → `<DataTable>`，删本页 `paginationConfig`/`handleTableChange`/序号列定义/序号 bodyCell 分支；③ `a-tag :color` 字面色 → `<StatusTag :type>`；④ 去 `bordered`。样板：`dormitory/BuildingManage`。

- [ ] **Step 4：提交**

```bash
git add web/docs/PATTERNS.md
git commit -m "docs(ux): PATTERNS 列表页骨架增 DataTable 契约与迁移配方"
```

---

## 完成标准（本批次）

- `npm run build` 绿；`/dormitory/buildings` preview 渲染与交互（筛选/翻页/状态标签）正常。
- `BuildingManage.vue` 不再含 `paginationConfig`/`handleTableChange`/序号列定义/`a-tag :color` 字面色/`a-table bordered`。
- `DataTable.vue` 成为列表页表格唯一封装；`PATTERNS.md` 列表配方就绪，供阶段1 批量迁移引用。
- 后续批次 0b–0e 与阶段1/2 按 spec 继续。
