# CardFlow 脱敏链重构 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把卡片脱敏从「只在 active+v2 节点、按 schema denylist」重构为「单一权威 `CardRedactionService`：字段级敏感基线 + 粘附授权 + allowlist + 任意状态 + 最小访问门」，并统一前后端 maskPattern。

**Architecture:** 新建 `Services/Redaction/`（`FieldMasker` 共享打码器 + `ICardRedactionService` 单一脱敏权威）。`CardService.GetByIdAsync` 无条件调用脱敏服务并加访问门；`StageViewProfileResolver`/`CardPresentationResolver` 的打码统一委托 `FieldMasker`；明细聚合尊重列权限；前端补字段敏感位编辑、停止二次打码。

**Tech Stack:** .NET 10 / EF Core / xUnit（EFCore.InMemory，`--arch x64` 宿主）/ Vue 3 + TS（Ant Design Vue / Vant）。

**上游 spec：** [docs/superpowers/specs/2026-06-18-cardflow-脱敏链重构-design.md](../specs/2026-06-18-cardflow-脱敏链重构-design.md)（决策 D1-D5 已锁定）。

**通用命令：**
- 后端测试：`dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`（x86 testhost 会 WSAENOBUFS 中止，必须 x64）
- 后端编译：`dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj`
- 前端兜底：`cd web; npx vite build`（vue-tsc 基线本就红，不作门禁）
- 基线：当前 98 passed / 1 skipped。每个 Task 完成后该数字只增不减。

---

## 文件结构

**新建：**
- `src/STOTOP.Module.CardFlow/Services/Redaction/FieldMasker.cs` — 静态打码器，唯一打码规则真源（phone/idCard/bankCard/email/name/通用）。
- `src/STOTOP.Module.CardFlow/Services/Redaction/CardRedactionModels.cs` — 请求/结果模型 + ResolvedAccess。
- `src/STOTOP.Module.CardFlow/Services/Redaction/ICardRedactionService.cs` — 接口。
- `src/STOTOP.Module.CardFlow/Services/Redaction/CardRedactionService.cs` — 有效访问计算（基线+粘附+active）+ allowlist 脱敏。
- `tests/STOTOP.Module.CardFlow.Tests/Redaction/FieldMaskerTests.cs`
- `tests/STOTOP.Module.CardFlow.Tests/Redaction/CardRedactionServiceTests.cs`

**修改：**
- `src/STOTOP.Module.CardFlow/Dtos/Responses.cs` — `SchemaFieldDefinition` 加敏感位。
- `src/STOTOP.Module.CardFlow/Models/Schema/CardSchemaV2Models.cs` — `CardFieldDefinitionV2` 加敏感位。
- `src/STOTOP.Module.CardFlow/Services/StageViewProfileResolver.cs` — `MaskValue` 委托 `FieldMasker`。
- `src/STOTOP.Module.CardFlow/Services/CardPresentationResolver.cs` — `MaskValue` 委托 `FieldMasker`；聚合尊重列权限。
- `src/STOTOP.Module.CardFlow/Services/CardService.cs` — 访问门 + 无条件脱敏。
- `src/STOTOP.Module.CardFlow/Controllers/CardController.cs` — 传 `canViewAll`。
- `src/STOTOP.Module.CardFlow/Services/Interfaces/ICardService.cs` — `GetByIdAsync` 签名加 `canViewAll`。
- `src/STOTOP.Module.CardFlow/CardFlowModuleExtensions.cs` — 注册 `ICardRedactionService`。
- `web/src/types/cardflow.ts` — 字段类型加敏感位。
- `web/src/components/cardflow/SchemaFieldEditor.vue` — 敏感开关 + maskPattern 下拉。
- `web/src/components/cardflow/runtime/CardComponentRenderer.vue` — 停止二次打码。

---

## Task 1：字段定义加敏感位（模型 + 序列化测试）

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Dtos/Responses.cs:399-406`
- Modify: `src/STOTOP.Module.CardFlow/Models/Schema/CardSchemaV2Models.cs:23-38`
- Test: `tests/STOTOP.Module.CardFlow.Tests/Redaction/FieldMaskerTests.cs`（本任务先放一个序列化保障测试，FieldMasker 用例在 Task 2 加）

- [ ] **Step 1：写失败测试（v2 字段反序列化带敏感位）**

新建 `tests/STOTOP.Module.CardFlow.Tests/Redaction/SchemaSensitivityTests.cs`：

```csharp
using System.Text.Json;
using STOTOP.Module.CardFlow.Models.Schema;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Redaction;

public class SchemaSensitivityTests
{
    private static readonly JsonSerializerOptions Opts = new() { PropertyNameCaseInsensitive = true };

    [Fact]
    public void CardFieldDefinitionV2_DeserializesSensitiveAndMaskPattern()
    {
        const string json = """[{"key":"payeeAccountNo","label":"收款账号","type":"text","sensitive":true,"maskPattern":"bankCard"}]""";
        var fields = JsonSerializer.Deserialize<List<CardFieldDefinitionV2>>(json, Opts)!;
        Assert.True(fields[0].Sensitive);
        Assert.Equal("bankCard", fields[0].MaskPattern);
    }

    [Fact]
    public void CardFieldDefinitionV2_DefaultsNonSensitive()
    {
        const string json = """[{"key":"amount","label":"金额","type":"money"}]""";
        var fields = JsonSerializer.Deserialize<List<CardFieldDefinitionV2>>(json, Opts)!;
        Assert.False(fields[0].Sensitive);
        Assert.Null(fields[0].MaskPattern);
    }
}
```

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter SchemaSensitivityTests`
Expected: 编译失败 / FAIL（`CardFieldDefinitionV2` 无 `Sensitive`/`MaskPattern` 成员）。

- [ ] **Step 3：给 `CardFieldDefinitionV2` 加敏感位**

在 `CardSchemaV2Models.cs` 的 `CardFieldDefinitionV2` 末尾（`Options` 后）加：

```csharp
    public List<FieldOption> Options { get; set; } = new();
    public bool Sensitive { get; set; }
    public string? MaskPattern { get; set; }
```

- [ ] **Step 4：给 `SchemaFieldDefinition` 加敏感位（旁路扁平模型对齐）**

在 `Responses.cs` 的 `SchemaFieldDefinition` 末尾加：

```csharp
public class SchemaFieldDefinition
{
    public string Key { get; set; } = string.Empty;
    public string Label { get; set; } = string.Empty;
    public string Type { get; set; } = string.Empty;
    public bool Required { get; set; }
    public bool Readonly { get; set; }
    public bool Sensitive { get; set; }
    public string? MaskPattern { get; set; }
}
```

- [ ] **Step 5：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter SchemaSensitivityTests`
Expected: PASS（2 passed）。

- [ ] **Step 6：提交**

```bash
git add src/STOTOP.Module.CardFlow/Models/Schema/CardSchemaV2Models.cs src/STOTOP.Module.CardFlow/Dtos/Responses.cs tests/STOTOP.Module.CardFlow.Tests/Redaction/SchemaSensitivityTests.cs
git commit -m "feat(cardflow): 字段定义加 Sensitive/MaskPattern 敏感位（脱敏基线）"
```

---

## Task 2：`FieldMasker` 共享打码器（TDD）

**Files:**
- Create: `src/STOTOP.Module.CardFlow/Services/Redaction/FieldMasker.cs`
- Test: `tests/STOTOP.Module.CardFlow.Tests/Redaction/FieldMaskerTests.cs`

- [ ] **Step 1：写失败测试**

```csharp
using STOTOP.Module.CardFlow.Services.Redaction;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Redaction;

public class FieldMaskerTests
{
    [Theory]
    [InlineData("13800138000", "phone", "138****8000")]
    [InlineData("110101199003075678", "idCard", "1101**********5678")]
    [InlineData("6222021234567890123", "bankCard", "**** **** **** 0123")]
    [InlineData("zhangsan@corp.com", "email", "z***@corp.com")]
    [InlineData("张三丰", "name", "张**")]
    [InlineData("ABCDEFGH", null, "AB****GH")]
    [InlineData("ABC", null, "****")]
    public void Mask_AppliesPattern(string input, string? pattern, string expected)
    {
        Assert.Equal(expected, FieldMasker.Mask(input, pattern));
    }

    [Theory]
    [InlineData("")]
    [InlineData(null)]
    public void Mask_EmptyReturnsEmpty(string? input)
    {
        Assert.Equal(string.Empty, FieldMasker.Mask(input, "phone"));
    }

    [Fact]
    public void Mask_ShortPhoneFallsBackToGeneric()
    {
        // 长度不足 7，phone 模式退化为通用
        Assert.Equal("****", FieldMasker.Mask("123", "phone"));
    }
}
```

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter FieldMaskerTests`
Expected: 编译失败（`FieldMasker` 不存在）。

- [ ] **Step 3：实现 `FieldMasker`**

```csharp
namespace STOTOP.Module.CardFlow.Services.Redaction;

/// <summary>卡片字段打码的唯一真源，前后端口径以此为准。</summary>
public static class FieldMasker
{
    public static string Mask(string? value, string? pattern)
    {
        if (string.IsNullOrEmpty(value))
        {
            return string.Empty;
        }

        var v = value!;
        switch ((pattern ?? string.Empty).Trim().ToLowerInvariant())
        {
            case "phone":
                return v.Length >= 7 ? $"{v[..3]}****{v[^4..]}" : Generic(v);
            case "idcard":
                return v.Length >= 8 ? $"{v[..4]}**********{v[^4..]}" : Generic(v);
            case "bankcard":
                return v.Length >= 4 ? $"**** **** **** {v[^4..]}" : "****";
            case "email":
                var at = v.IndexOf('@');
                return at >= 1 ? $"{v[0]}***{v[at..]}" : Generic(v);
            case "name":
                return v.Length <= 1 ? v : $"{v[..1]}{new string('*', v.Length - 1)}";
            default:
                return Generic(v);
        }
    }

    private static string Generic(string v) => v.Length <= 4 ? "****" : $"{v[..2]}****{v[^2..]}";
}
```

- [ ] **Step 4：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter FieldMaskerTests`
Expected: PASS（全部）。

- [ ] **Step 5：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/Redaction/FieldMasker.cs tests/STOTOP.Module.CardFlow.Tests/Redaction/FieldMaskerTests.cs
git commit -m "feat(cardflow): 新增 FieldMasker 共享打码器（phone/idCard/bankCard/email/name/通用）"
```

---

## Task 3：两处 `MaskValue` 委托 `FieldMasker`（去重 + 接通 pattern）

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Services/StageViewProfileResolver.cs:226-243`
- Modify: `src/STOTOP.Module.CardFlow/Services/CardPresentationResolver.cs:549-558`
- Test: 既有 `StageViewProfileResolverTests` / `CardPresentationResolverTests` 回归（不新增）

> 说明：本任务只把通用打码逻辑收敛到 `FieldMasker`，行为对"无 pattern"场景不变（既有断言为 `NotEqual` 原值，仍成立）；`StageFieldAccessRule.MaskPattern` 的接通在 Task 5/6 经 `CardRedactionService` 实现，此处先打通调用。

- [ ] **Step 1：改 `StageViewProfileResolver.MaskValue` 接受 pattern 并委托**

`StageViewProfileResolver.cs` 顶部 using 增加：

```csharp
using STOTOP.Module.CardFlow.Services.Redaction;
```

把 `MaskValue`（:226-243）整体替换为：

```csharp
    private static JsonNode MaskValue(JsonNode? value, string? pattern = null)
    {
        if (value is null)
        {
            return string.Empty;
        }

        var text = value.GetValueKind() == JsonValueKind.String
            ? value.GetValue<string>()
            : value.ToJsonString();

        return FieldMasker.Mask(text, pattern);
    }
```

并把两个调用点（`RedactJson` :174、`RedactDetailJson` :202）改为传 pattern：

```csharp
                obj[key] = MaskValue(obj[key], rule.MaskPattern);
```

```csharp
                obj[columnKey] = MaskValue(obj[columnKey], rule.MaskPattern);
```

- [ ] **Step 2：改 `CardPresentationResolver.MaskValue` 委托**

`CardPresentationResolver.cs` 顶部 using 增加 `using STOTOP.Module.CardFlow.Services.Redaction;`，把 `MaskValue`（:549-558）替换为：

```csharp
    private static string MaskValue(JsonNode? value, string? pattern = null)
    {
        var text = ConvertNodeValue(value)?.ToString() ?? string.Empty;
        return FieldMasker.Mask(text, pattern);
    }
```

调用点 :251 与 :339 暂保持不传 pattern（默认通用），Task 7 会接 pattern：

```csharp
        return shouldMask ? MaskValue(value) : ConvertNodeValue(value);
```

- [ ] **Step 3：运行回归**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: PASS，通过数 ≥ 基线（98 + Task1/2 新增）。

- [ ] **Step 4：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/StageViewProfileResolver.cs src/STOTOP.Module.CardFlow/Services/CardPresentationResolver.cs
git commit -m "refactor(cardflow): 两处 MaskValue 收敛到 FieldMasker，打通 maskPattern 入参"
```

---

## Task 4：`CardRedactionService` 模型 + 有效访问计算（粘附授权，TDD）

**Files:**
- Create: `src/STOTOP.Module.CardFlow/Services/Redaction/CardRedactionModels.cs`
- Create: `src/STOTOP.Module.CardFlow/Services/Redaction/ICardRedactionService.cs`
- Create: `src/STOTOP.Module.CardFlow/Services/Redaction/CardRedactionService.cs`（本任务实现访问计算，allowlist 在 Task 5）
- Test: `tests/STOTOP.Module.CardFlow.Tests/Redaction/CardRedactionServiceTests.cs`

- [ ] **Step 1：写模型**

`CardRedactionModels.cs`：

```csharp
using STOTOP.Module.CardFlow.Entities;
using STOTOP.Module.CardFlow.Models.Approval;

namespace STOTOP.Module.CardFlow.Services.Redaction;

/// <summary>脱敏请求：调用方（CardService）已从 DB 解析好查看者关系与节点配置。</summary>
public sealed class CardRedactionRequest
{
    public required CfCard Card { get; init; }
    public string? CardSchemaJson { get; init; }
    public string? DetailSchemaJson { get; init; }
    public IReadOnlyCollection<CfCardDetail> Details { get; init; } = Array.Empty<CfCardDetail>();

    /// <summary>查看者是当前 active 节点处理人时，传该节点配置（节点视图对其权威）。</summary>
    public StageConfigEnvelope? ActiveStageConfig { get; init; }

    /// <summary>查看者历史上处理过（非当前 active）的节点配置，用于粘附授权（提权保留）。</summary>
    public IReadOnlyCollection<StageConfigEnvelope> HandledStageConfigs { get; init; }
        = Array.Empty<StageConfigEnvelope>();
}

public sealed class ResolvedAccess
{
    public string Access { get; set; } = "readonly"; // editable|readonly|masked|hidden
    public string? MaskPattern { get; set; }
}

public sealed class RedactedDetailRowResult
{
    public long Id { get; set; }
    public int SortOrder { get; set; }
    public string DetailTableKey { get; set; } = "default";
    public string DataJson { get; set; } = "{}";
}

public sealed class CardRedactionResult
{
    public Dictionary<string, ResolvedAccess> FieldAccess { get; set; } = new();
    public Dictionary<string, ResolvedAccess> DetailAccess { get; set; } = new();
    public string RedactedDataJson { get; set; } = "{}";
    public List<RedactedDetailRowResult> RedactedDetails { get; set; } = new();
}
```

`ICardRedactionService.cs`：

```csharp
namespace STOTOP.Module.CardFlow.Services.Redaction;

public interface ICardRedactionService
{
    CardRedactionResult Redact(CardRedactionRequest request);
}
```

- [ ] **Step 2：写失败测试（访问计算四场景）**

`CardRedactionServiceTests.cs`：

```csharp
using STOTOP.Module.CardFlow.Entities;
using STOTOP.Module.CardFlow.Models.Approval;
using STOTOP.Module.CardFlow.Models.Schema;
using STOTOP.Module.CardFlow.Services.Redaction;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Redaction;

public class CardRedactionServiceTests
{
    private const string Schema =
        """[{"key":"amount","type":"money"},{"key":"payeeAccountNo","type":"text","sensitive":true,"maskPattern":"bankCard"}]""";

    private static CfCard Card() => new()
    {
        FDataJson = """{"amount":88,"payeeAccountNo":"6222021234567890123","legacyNote":"x"}"""
    };

    [Fact]
    public void NoActiveNoGrant_SensitiveMasked_NonSensitiveReadonly()
    {
        var svc = new CardRedactionService();
        var result = svc.Redact(new CardRedactionRequest { Card = Card(), CardSchemaJson = Schema });

        Assert.Equal("readonly", result.FieldAccess["amount"].Access);
        Assert.Equal("masked", result.FieldAccess["payeeAccountNo"].Access);
        Assert.Equal("bankCard", result.FieldAccess["payeeAccountNo"].MaskPattern);
    }

    [Fact]
    public void StickyGrant_PastHandlerKeepsClearView()
    {
        var svc = new CardRedactionService();
        var handled = new StageConfigEnvelope
        {
            ViewProfile = new StageViewProfile
            {
                FieldAccess = new() { ["payeeAccountNo"] = new() { Access = "readonly" } }
            }
        };
        var result = svc.Redact(new CardRedactionRequest
        {
            Card = Card(),
            CardSchemaJson = Schema,
            HandledStageConfigs = new[] { handled }
        });

        // 曾经手过"放行该字段"的节点 → 完成后仍可见明文（readonly）
        Assert.Equal("readonly", result.FieldAccess["payeeAccountNo"].Access);
    }

    [Fact]
    public void ActiveAssignee_NodeConfigIsAuthoritative()
    {
        var svc = new CardRedactionService();
        var active = new StageConfigEnvelope
        {
            Version = 2,
            InputFields = new() { "amount" },
            ViewProfile = new StageViewProfile
            {
                FieldAccess = new() { ["payeeAccountNo"] = new() { Access = "editable" } }
            }
        };
        var result = svc.Redact(new CardRedactionRequest
        {
            Card = Card(),
            CardSchemaJson = Schema,
            ActiveStageConfig = active
        });

        Assert.Equal("editable", result.FieldAccess["amount"].Access);          // InputFields → editable
        Assert.Equal("editable", result.FieldAccess["payeeAccountNo"].Access);  // 节点放行
    }

    [Fact]
    public void ActiveAssignee_HiddenOverridesBaseline()
    {
        var svc = new CardRedactionService();
        var active = new StageConfigEnvelope
        {
            Version = 2,
            ViewProfile = new StageViewProfile
            {
                FieldAccess = new() { ["amount"] = new() { Access = "hidden" } }
            }
        };
        var result = svc.Redact(new CardRedactionRequest
        {
            Card = Card(),
            CardSchemaJson = Schema,
            ActiveStageConfig = active
        });

        Assert.Equal("hidden", result.FieldAccess["amount"].Access);            // 节点对 active 处理人权威，可限制
        Assert.Equal("masked", result.FieldAccess["payeeAccountNo"].Access);    // 未指定的敏感字段 → 基线 masked
    }
}
```

- [ ] **Step 3：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter CardRedactionServiceTests`
Expected: 编译失败（`CardRedactionService` 不存在）。

- [ ] **Step 4：实现 `CardRedactionService` 访问计算（allowlist 留 Task 5 占位）**

```csharp
using System.Text.Json;
using STOTOP.Module.CardFlow.Models.Approval;
using STOTOP.Module.CardFlow.Models.Schema;

namespace STOTOP.Module.CardFlow.Services.Redaction;

public sealed class CardRedactionService : ICardRedactionService
{
    private static readonly JsonSerializerOptions JsonOptions = new() { PropertyNameCaseInsensitive = true };

    public CardRedactionResult Redact(CardRedactionRequest request)
    {
        var fields = ReadFields(request.CardSchemaJson);
        var fieldAccess = ComputeFieldAccess(fields, request);

        return new CardRedactionResult
        {
            FieldAccess = fieldAccess,
            DetailAccess = new Dictionary<string, ResolvedAccess>(),
            RedactedDataJson = request.Card.FDataJson ?? "{}", // Task 5 接 allowlist
            RedactedDetails = new List<RedactedDetailRowResult>() // Task 5 接明细
        };
    }

    private static Dictionary<string, ResolvedAccess> ComputeFieldAccess(
        IReadOnlyCollection<CardFieldDefinitionV2> fields,
        CardRedactionRequest request)
    {
        var result = new Dictionary<string, ResolvedAccess>(StringComparer.Ordinal);
        foreach (var field in fields)
        {
            var key = field.Key;
            if (string.IsNullOrWhiteSpace(key))
            {
                continue;
            }

            if (request.ActiveStageConfig != null)
            {
                result[key] = ResolveActive(request.ActiveStageConfig, field);
                continue;
            }

            // 基线
            var rule = new ResolvedAccess
            {
                Access = field.Sensitive ? "masked" : "readonly",
                MaskPattern = field.MaskPattern
            };

            // 粘附授权：曾经手过"放行该字段"的节点 → 提到 readonly（明文可见，但非活动卡不可编辑）
            if (Rank(rule.Access) < Rank("readonly") && GrantedBySticky(request.HandledStageConfigs, key))
            {
                rule.Access = "readonly";
            }

            result[key] = rule;
        }

        return result;
    }

    private static ResolvedAccess ResolveActive(StageConfigEnvelope config, CardFieldDefinitionV2 field)
    {
        if (config.ViewProfile?.FieldAccess != null
            && config.ViewProfile.FieldAccess.TryGetValue(field.Key, out var r))
        {
            return new ResolvedAccess
            {
                Access = NormalizeAccess(r.Access),
                MaskPattern = r.MaskPattern ?? field.MaskPattern
            };
        }

        if (config.InputFields.Contains(field.Key))
        {
            return new ResolvedAccess { Access = "editable", MaskPattern = field.MaskPattern };
        }

        return new ResolvedAccess
        {
            Access = field.Sensitive ? "masked" : "readonly",
            MaskPattern = field.MaskPattern
        };
    }

    private static bool GrantedBySticky(
        IReadOnlyCollection<StageConfigEnvelope> handled,
        string key)
    {
        foreach (var config in handled)
        {
            if (config.InputFields.Contains(key))
            {
                return true;
            }

            if (config.ViewProfile?.FieldAccess != null
                && config.ViewProfile.FieldAccess.TryGetValue(key, out var r)
                && Rank(NormalizeAccess(r.Access)) >= Rank("readonly"))
            {
                return true;
            }
        }

        return false;
    }

    private static int Rank(string access) => access switch
    {
        "hidden" => 0,
        "masked" => 1,
        "readonly" => 2,
        "editable" => 3,
        "required" => 3,
        _ => 2
    };

    private static string NormalizeAccess(string? access) => access?.Trim().ToLowerInvariant() switch
    {
        "hidden" => "hidden",
        "masked" => "masked",
        "editable" => "editable",
        "required" => "editable",
        _ => "readonly"
    };

    private static List<CardFieldDefinitionV2> ReadFields(string? schemaJson)
    {
        if (string.IsNullOrWhiteSpace(schemaJson))
        {
            return new List<CardFieldDefinitionV2>();
        }

        try
        {
            using var document = JsonDocument.Parse(schemaJson);
            var root = document.RootElement;
            if (root.ValueKind == JsonValueKind.Array)
            {
                return JsonSerializer.Deserialize<List<CardFieldDefinitionV2>>(schemaJson, JsonOptions) ?? new();
            }

            if (root.ValueKind == JsonValueKind.Object)
            {
                var schema = JsonSerializer.Deserialize<CardSchemaV2>(schemaJson, JsonOptions);
                return schema?.Fields ?? new List<CardFieldDefinitionV2>();
            }
        }
        catch (JsonException)
        {
            // ignore，返回空
        }

        return new List<CardFieldDefinitionV2>();
    }
}
```

- [ ] **Step 5：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter CardRedactionServiceTests`
Expected: PASS（4 passed）。

- [ ] **Step 6：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/Redaction/ tests/STOTOP.Module.CardFlow.Tests/Redaction/CardRedactionServiceTests.cs
git commit -m "feat(cardflow): CardRedactionService 有效访问计算（基线+粘附授权+active 权威）"
```

---

## Task 5：`CardRedactionService` allowlist 脱敏（DataJson + 明细，TDD）

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Services/Redaction/CardRedactionService.cs`
- Test: `tests/STOTOP.Module.CardFlow.Tests/Redaction/CardRedactionServiceTests.cs`（追加）

- [ ] **Step 1：追加失败测试（allowlist 移除 schema 外键 + 明细脱敏）**

在 `CardRedactionServiceTests` 追加：

```csharp
    [Fact]
    public void Allowlist_DropsSchemaExternalKeys_AndMasksSensitive()
    {
        var svc = new CardRedactionService();
        var result = svc.Redact(new CardRedactionRequest { Card = Card(), CardSchemaJson = Schema });

        using var doc = System.Text.Json.JsonDocument.Parse(result.RedactedDataJson);
        var root = doc.RootElement;
        Assert.True(root.TryGetProperty("amount", out _));                 // schema 内非敏感 → 保留
        Assert.False(root.TryGetProperty("legacyNote", out _));            // schema 外 → 移除
        Assert.Equal("**** **** **** 0123",
            root.GetProperty("payeeAccountNo").GetString());              // 敏感 → bankCard 打码
    }

    [Fact]
    public void Allowlist_RedactsDetailRows()
    {
        var svc = new CardRedactionService();
        const string detailSchema = """[{"key":"invoiceNo","type":"text"},{"key":"idNo","type":"text","sensitive":true,"maskPattern":"idCard"}]""";
        var details = new List<CfCardDetail>
        {
            new() { FID = 1, FSortOrder = 0, FDetailTableKey = "default",
                    FDataJson = """{"invoiceNo":"INV-1","idNo":"110101199003075678","junk":"z"}""" }
        };
        var result = svc.Redact(new CardRedactionRequest
        {
            Card = Card(), CardSchemaJson = Schema, DetailSchemaJson = detailSchema, Details = details
        });

        using var doc = System.Text.Json.JsonDocument.Parse(result.RedactedDetails.Single().DataJson);
        var root = doc.RootElement;
        Assert.Equal("INV-1", root.GetProperty("invoiceNo").GetString());
        Assert.Equal("1101**********5678", root.GetProperty("idNo").GetString());
        Assert.False(root.TryGetProperty("junk", out _));                  // schema 外明细列移除
    }
```

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter CardRedactionServiceTests`
Expected: 2 个新用例 FAIL（当前 RedactedDataJson 是原值、RedactedDetails 为空）。

- [ ] **Step 3：实现 allowlist 脱敏 + 明细访问计算**

`CardRedactionService.cs` 顶部 using 增加：

```csharp
using System.Text.Json.Nodes;
using STOTOP.Module.CardFlow.Entities;
```

把 `Redact` 方法体替换为：

```csharp
    public CardRedactionResult Redact(CardRedactionRequest request)
    {
        var fields = ReadFields(request.CardSchemaJson);
        var fieldAccess = ComputeFieldAccess(fields, request);

        var detailFields = ReadFields(request.DetailSchemaJson);
        var detailAccess = ComputeDetailAccess(detailFields, request);

        return new CardRedactionResult
        {
            FieldAccess = fieldAccess,
            DetailAccess = detailAccess,
            RedactedDataJson = ApplyAllowlist(request.Card.FDataJson, fieldAccess),
            RedactedDetails = request.Details
                .OrderBy(d => d.FSortOrder)
                .Select(d => new RedactedDetailRowResult
                {
                    Id = d.FID,
                    SortOrder = d.FSortOrder,
                    DetailTableKey = NormalizeTable(d.FDetailTableKey),
                    DataJson = ApplyDetailAllowlist(d.FDataJson, NormalizeTable(d.FDetailTableKey), detailAccess)
                })
                .ToList()
        };
    }
```

在类内追加方法：

```csharp
    private static Dictionary<string, ResolvedAccess> ComputeDetailAccess(
        IReadOnlyCollection<CardFieldDefinitionV2> detailFields,
        CardRedactionRequest request)
    {
        // 明细列基线（按列敏感位）；明细暂不做粘附/active 区分（MVP，与字段同基线规则）。
        var result = new Dictionary<string, ResolvedAccess>(StringComparer.Ordinal);
        foreach (var col in detailFields)
        {
            if (string.IsNullOrWhiteSpace(col.Key))
            {
                continue;
            }

            var access = new ResolvedAccess
            {
                Access = col.Sensitive ? "masked" : "readonly",
                MaskPattern = col.MaskPattern
            };

            if (request.ActiveStageConfig?.ViewProfile?.DetailAccess != null
                && request.ActiveStageConfig.ViewProfile.DetailAccess.TryGetValue($"default.{col.Key}", out var r))
            {
                access.Access = NormalizeAccess(r.Access);
                access.MaskPattern = r.MaskPattern ?? col.MaskPattern;
            }

            result[$"default.{col.Key}"] = access;
        }

        return result;
    }

    private static string ApplyAllowlist(string? dataJson, Dictionary<string, ResolvedAccess> access)
    {
        var obj = ParseObject(dataJson);
        var output = new JsonObject();
        foreach (var (key, rule) in access)
        {
            if (rule.Access == "hidden" || !obj.TryGetPropertyValue(key, out var val))
            {
                continue;
            }

            output[key] = rule.Access == "masked"
                ? FieldMasker.Mask(NodeToText(val), rule.MaskPattern)
                : val?.DeepClone();
        }

        return output.ToJsonString();
    }

    private static string ApplyDetailAllowlist(
        string? dataJson, string tableKey, Dictionary<string, ResolvedAccess> detailAccess)
    {
        var obj = ParseObject(dataJson);
        var output = new JsonObject();
        var prefix = $"{tableKey}.";
        foreach (var (accessKey, rule) in detailAccess)
        {
            if (!accessKey.StartsWith(prefix, StringComparison.Ordinal))
            {
                continue;
            }

            var col = accessKey[prefix.Length..];
            if (rule.Access == "hidden" || !obj.TryGetPropertyValue(col, out var val))
            {
                continue;
            }

            output[col] = rule.Access == "masked"
                ? FieldMasker.Mask(NodeToText(val), rule.MaskPattern)
                : val?.DeepClone();
        }

        return output.ToJsonString();
    }

    private static string NormalizeTable(string? key)
        => string.IsNullOrWhiteSpace(key) ? "default" : key!;

    private static string NodeToText(JsonNode? value)
    {
        if (value is null)
        {
            return string.Empty;
        }

        return value.GetValueKind() == JsonValueKind.String
            ? value.GetValue<string>()
            : value.ToJsonString();
    }

    private static JsonObject ParseObject(string? json)
    {
        if (string.IsNullOrWhiteSpace(json))
        {
            return new JsonObject();
        }

        try
        {
            return JsonNode.Parse(json) as JsonObject ?? new JsonObject();
        }
        catch (JsonException)
        {
            return new JsonObject();
        }
    }
```

> 注意：明细 detailAccess 的 key 形如 `default.col`，与现网 `default.{key}` 口径一致（见 `StageViewProfileResolver.BuildDetailAccess`）。

- [ ] **Step 4：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter CardRedactionServiceTests`
Expected: PASS（6 passed）。

- [ ] **Step 5：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/Redaction/CardRedactionService.cs tests/STOTOP.Module.CardFlow.Tests/Redaction/CardRedactionServiceTests.cs
git commit -m "feat(cardflow): CardRedactionService allowlist 脱敏（移除 schema 外键 + 明细打码）"
```

---

## Task 6：接入 `CardService.GetByIdAsync`（访问门 + 无条件脱敏）+ DI

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Services/Interfaces/ICardService.cs`（`GetByIdAsync` 签名）
- Modify: `src/STOTOP.Module.CardFlow/Services/CardService.cs`（构造注入 + GetByIdAsync）
- Modify: `src/STOTOP.Module.CardFlow/Controllers/CardController.cs`（传 canViewAll）
- Modify: `src/STOTOP.Module.CardFlow/CardFlowModuleExtensions.cs`（注册）
- Test: `tests/STOTOP.Module.CardFlow.Tests/Redaction/CardRedactionServiceTests.cs`（已覆盖服务；本任务为集成接线，回归既有测试）

- [ ] **Step 1：DI 注册 `ICardRedactionService`**

`CardFlowModuleExtensions.cs` 在 `services.AddScoped<ICardSchemaService, CardSchemaService>();`（:53）后加（接口/实现均在 `Services.Redaction` 子命名空间，文件未 using 它，故全限定）：

```csharp
        services.AddScoped<Services.Redaction.ICardRedactionService, Services.Redaction.CardRedactionService>();
```

- [ ] **Step 2：改接口签名**

`ICardService.cs` 把 `GetByIdAsync` 改为：

```csharp
    Task<CardDetailDto?> GetByIdAsync(long id, long userId, bool canViewAll = false);
```

- [ ] **Step 3：构造注入脱敏服务**

`CardService.cs`：字段区加 `private readonly ICardRedactionService _redactionService;`；构造参数末尾加 `ICardRedactionService redactionService` 并赋值。顶部 using 加 `using STOTOP.Module.CardFlow.Services.Redaction;`。

- [ ] **Step 4：改 `GetByIdAsync` —— 访问门 + 无条件脱敏**

将签名改为 `public async Task<CardDetailDto?> GetByIdAsync(long id, long userId, bool canViewAll = false)`。

在加载 `assignees`（:155-157）之后、构造 `detail.StageInstances`（:159）之前，插入**访问门**：

```csharp
        // 最小卡片访问门：发起人 / 任意轮次处理人 / 管理员 否则拒绝（不暴露存在性）
        var isInitiator = card.FInitiatorId == userId;
        var isAssignee = assignees.Any(a => a.FUserId == userId);
        if (!canViewAll && !isInitiator && !isAssignee)
        {
            return null;
        }
```

把 active+v2 脱敏块（:194-228）整体替换为「先无条件脱敏，再仅 active+v2 时补 work-view」：

```csharp
        // 解析查看者处理过的节点配置（粘附授权）+ 当前 active 节点配置（节点视图权威）
        var viewerStageInstanceIds = assignees
            .Where(a => a.FUserId == userId)
            .Select(a => a.FStageInstanceId)
            .ToHashSet();
        var handledStageDefIds = stageInstances
            .Where(s => viewerStageInstanceIds.Contains(s.FID) && s.FStageDefinitionId != null)
            .Select(s => s.FStageDefinitionId!.Value)
            .Distinct()
            .ToList();

        var currentStage = stageInstances.FirstOrDefault(s => s.FID == card.FCurrentStageInstanceId && s.FStatus == "active");
        var viewerIsActiveAssignee = currentStage != null
            && assignees.Any(a => a.FStageInstanceId == currentStage.FID && a.FUserId == userId);

        var stageDefs = await _dbContext.Set<CfStageDefinition>()
            .AsNoTracking()
            .Where(s => handledStageDefIds.Contains(s.FID))
            .ToListAsync();
        var stageConfigs = stageDefs.ToDictionary(s => s.FID, s => _stageConfigParser.Parse(s.FInputFieldsJson));

        var flowVersion = await _dbContext.Set<CfFlowVersion>()
            .AsNoTracking()
            .FirstOrDefaultAsync(v => v.FID == card.FFlowVersionId);

        StageConfigEnvelope? activeConfig = null;
        var handledConfigs = new List<StageConfigEnvelope>();
        foreach (var (defId, cfg) in stageConfigs)
        {
            if (viewerIsActiveAssignee && currentStage?.FStageDefinitionId == defId)
            {
                activeConfig = cfg;
            }
            else
            {
                handledConfigs.Add(cfg);
            }
        }

        var redaction = _redactionService.Redact(new CardRedactionRequest
        {
            Card = card,
            CardSchemaJson = flowVersion?.FCardSchemaJson,
            DetailSchemaJson = flowVersion?.FDetailSchemaJson,
            Details = cardDetails,
            ActiveStageConfig = activeConfig,
            HandledStageConfigs = handledConfigs
        });

        detail.DataJson = redaction.RedactedDataJson;
        detail.Details = redaction.RedactedDetails.Select(row => new CardDetailRowDto
        {
            Id = row.Id,
            DetailTableKey = string.IsNullOrWhiteSpace(row.DetailTableKey) ? "default" : row.DetailTableKey,
            SortOrder = row.SortOrder,
            DataJson = row.DataJson
        }).ToList();

        // 仅当前 active 处理人查看 v2 节点时，补节点工作视图（sections/components/actions）
        if (viewerIsActiveAssignee && currentStage?.FStageDefinitionId != null)
        {
            var stageDefinition = await _dbContext.Set<CfStageDefinition>()
                .AsNoTracking()
                .FirstOrDefaultAsync(s => s.FID == currentStage.FStageDefinitionId.Value);
            var normalizedConfig = _stageConfigParser.Parse(stageDefinition?.FInputFieldsJson);
            if (stageDefinition != null && normalizedConfig.Version == 2)
            {
                var presentationRelations = await LoadPresentationRelationsAsync(card.FID);
                var presentationSnapshots = await LoadPresentationSnapshotsAsync(card, stageInstances);
                var resolved = _stageViewResolver.Resolve(
                    flowVersion?.FCardSchemaJson,
                    flowVersion?.FDetailSchemaJson,
                    stageDefinition,
                    card,
                    cardDetails,
                    userId,
                    normalizedConfig,
                    presentationRelations,
                    presentationSnapshots);
                detail.CurrentStageWorkView = ToStageWorkViewDto(resolved);
            }
        }
```

> 顶部 using 确认含 `using STOTOP.Module.CardFlow.Models.Approval;`（`StageConfigEnvelope`），如缺则补。

- [ ] **Step 5：控制器传 canViewAll**

`CardController.cs` 顶部 using 加 `using STOTOP.Module.System.Services;`；构造注入 `IAdminAuthorizationService adminService`（字段 `_adminService`）。把 `GetById`（:62-69）改为：

```csharp
    [HttpGet("{id}")]
    public async Task<ApiResult<CardDetailDto>> GetById(long id)
    {
        var result = await _cardService.GetByIdAsync(id, GetUserId(), _adminService.IsAdmin(User));
        if (result == null)
            return ApiResult<CardDetailDto>.Fail("卡片不存在");
        return ApiResult<CardDetailDto>.Success(result);
    }
```

> 拒绝态复用既有"卡片不存在"文案，不暴露存在性（spec §4.6）。

- [ ] **Step 6：编译 + 全量回归**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj`
Expected: 0 错误。
Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: PASS，通过数 ≥ 基线 + 本计划新增。

> 若 `ExpenseApprovalE2ETests` 因脱敏接入失败：核对其断言是否依赖未脱敏原值/无访问门；E2E 的查看者应是发起人或处理人（满足访问门），费用模板字段非敏感（基线 readonly，值不变）。如断言取 DataJson 原值且字段非敏感，应仍通过。

- [ ] **Step 7：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/Interfaces/ICardService.cs src/STOTOP.Module.CardFlow/Services/CardService.cs src/STOTOP.Module.CardFlow/Controllers/CardController.cs src/STOTOP.Module.CardFlow/CardFlowModuleExtensions.cs
git commit -m "feat(cardflow): GetByIdAsync 接入最小访问门 + 无条件脱敏（任意状态走 CardRedactionService）"
```

---

## Task 7：明细聚合尊重列权限（解 #17）

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Services/CardPresentationResolver.cs`（`ApplyAggregations`/`ComputeDefaultDetailSummary`/`SumDetailField` + 调用点）
- Test: `tests/STOTOP.Module.CardFlow.Tests/Presentation/CardPresentationResolverTests.cs`（追加）

- [ ] **Step 1：追加失败测试（masked/hidden 列不进默认聚合）**

在 `CardPresentationResolverTests` 追加一条用例：构造一个 `amount` 列被 `DetailAccess["default.amount"]=masked` 的请求，断言结果 `DetailSummary` 不含 `detailSum.amount`（或为 0/缺省）。参考既有测试构造 `CardPresentationResolveRequest` 的方式（同文件已有样板）。断言：

```csharp
        Assert.False(result.DetailSummary.ContainsKey("detailSum.amount"));
```

> 实现者：照搬本测试文件既有 `Resolve` 调用样板补 `DetailAccess`，避免重复构造代码。

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter CardPresentationResolverTests`
Expected: 新用例 FAIL（当前对 masked 列照样求和）。

- [ ] **Step 3：聚合前查列权限**

`ComputeDefaultDetailSummary` 改为接收并尊重 detailAccess：

```csharp
    private static void ComputeDefaultDetailSummary(
        IReadOnlyCollection<CfCardDetail> details,
        IDictionary<string, object?> detailSummary,
        IReadOnlyDictionary<string, StageDetailAccessRule> detailAccess)
    {
        if (IsColumnRestricted(detailAccess, "default", "amount"))
        {
            return;
        }

        if (!detailSummary.ContainsKey("detailSum.amount"))
        {
            detailSummary["detailSum.amount"] = SumDetailField(details, null, "amount");
        }
    }

    private static bool IsColumnRestricted(
        IReadOnlyDictionary<string, StageDetailAccessRule> detailAccess,
        string tableKey,
        string fieldKey)
    {
        if (detailAccess.TryGetValue($"{tableKey}.{fieldKey}", out var rule))
        {
            var a = rule.Access?.Trim().ToLowerInvariant();
            return a == "hidden" || a == "masked";
        }

        return false;
    }
```

`ApplyAggregations` 同样在 `detailSummary[sum.TargetKey]=...` 前加：

```csharp
            if (IsColumnRestricted(request.DetailAccess, tableKey, sum.FieldKey))
            {
                continue;
            }
```

> `ApplyAggregations` 需要拿到 `request.DetailAccess` —— 给它加 `IReadOnlyDictionary<string, StageDetailAccessRule> detailAccess` 参数，调用点（:264 `ResolveSummaryValue` 与主 `Resolve` 内）传 `request.DetailAccess`。`ComputeDefaultDetailSummary` 的两个调用点（:30、:263）同样传 `request.DetailAccess`。顶部 using 确认含 `using STOTOP.Module.CardFlow.Models.Schema;`。

- [ ] **Step 4：运行确认通过 + 全量回归**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: PASS，通过数 ≥ 上一 Task。

- [ ] **Step 5：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/CardPresentationResolver.cs tests/STOTOP.Module.CardFlow.Tests/Presentation/CardPresentationResolverTests.cs
git commit -m "fix(cardflow): 明细聚合尊重列权限，masked/hidden 列不进默认聚合（#17）"
```

---

## Task 8：前端 —— 敏感位编辑 + 停止二次打码

**Files:**
- Modify: `web/src/types/cardflow.ts`（字段类型加 `sensitive?`/`maskPattern?`）
- Modify: `web/src/components/cardflow/SchemaFieldEditor.vue`（敏感开关 + maskPattern 下拉）
- Modify: `web/src/components/cardflow/runtime/CardComponentRenderer.vue:150-157`（停止二次打码）

> 前端无测试运行器，验证以 `vite build` 兜底（memory：vue-tsc 基线红，不作门禁）。

- [ ] **Step 1：类型加敏感位**

`web/src/types/cardflow.ts` 找到卡片字段定义接口（`SchemaFieldDefinition` 或等价，含 `key/label/type/required`），加：

```typescript
  sensitive?: boolean
  maskPattern?: string | null
```

- [ ] **Step 2：`SchemaFieldEditor` 加敏感开关 + maskPattern**

在字段属性表单（必填/只读开关附近）加：

```vue
        <a-form-item label="敏感字段">
          <a-switch v-model:checked="model.sensitive" />
        </a-form-item>
        <a-form-item v-if="model.sensitive" label="脱敏样式">
          <a-select v-model:value="model.maskPattern" allow-clear placeholder="通用（前2后2）">
            <a-select-option value="phone">手机号（138****8000）</a-select-option>
            <a-select-option value="idCard">身份证（1101**********5678）</a-select-option>
            <a-select-option value="bankCard">银行卡（**** **** **** 0123）</a-select-option>
            <a-select-option value="email">邮箱（z***@corp.com）</a-select-option>
            <a-select-option value="name">姓名（张**）</a-select-option>
          </a-select>
        </a-form-item>
```

> 确认 `model` 是被编辑字段的响应式对象（本组件既有的字段编辑模型）；提交时 `sensitive`/`maskPattern` 随字段一起进 schema（commit 逻辑遍历的就是该 model，无需额外搬运）。

- [ ] **Step 3：运行态停止二次打码**

`CardComponentRenderer.vue` 的 `formatFieldValue`（:150-157）把 masked 分支改为**直接显示服务端值**（后端已打码）：

```typescript
function formatFieldValue(component: CardComponentRuntime): string {
  const value = fieldValue(component)
  const emptyText = component.props?.emptyText || '-'
  if (value === null || value === undefined || value === '') return emptyText

  // 脱敏由后端 FieldMasker 权威完成，前端不再二次打码，直接展示服务端值
  if (component.masked || component.access === 'masked') {
    return String(value)
  }
```

（保留其后的附件/日期等分支不变。`maskedValue` 函数可保留不删以免牵动其它引用，但 masked 分支不再调用它。）

- [ ] **Step 4：前端构建兜底**

Run: `cd web; npx vite build`
Expected: 构建成功（如有与本改动无关的既有红可忽略，但本三文件不得新增报错）。

- [ ] **Step 5：提交**

```bash
git add web/src/types/cardflow.ts web/src/components/cardflow/SchemaFieldEditor.vue web/src/components/cardflow/runtime/CardComponentRenderer.vue
git commit -m "feat(cardflow): 设计器字段敏感位编辑 + 运行态停止二次打码（后端权威）"
```

---

## Task 9：收尾验证

**Files:** 无代码改动（验证 + 必要时补救）

- [ ] **Step 1：schema 外键兼容性采样核查**

按 spec §6 风险①：采样在用流程（费用报销模板 FYBS_TEMPLATE 等）的卡片 `FDataJson` 键集 vs 其 `FCardSchemaJson` 字段集，确认无"前端依赖但 schema 未声明"的键被 allowlist 误删。

Run（开发库连接下，SQL 采样约 100 行即可，勿全量）：
```sql
SELECT TOP 100 c.FID, c.FFlowVersionId, c.FDataJson
FROM [CF卡片] c
WHERE c.FDataJson IS NOT NULL AND c.FDataJson <> '{}'
ORDER BY c.FID DESC;
```
人工比对若发现在用 schema 外键 → 记录并评估"补进 schema"或临时 passthrough；本计划默认结论是模板键全在 schema 内（无需补救）。

- [ ] **Step 2：列表路径确认（无需改）**

确认 `GetCardsAsync` 的 `CardListDto`（`CardService.cs:80-97`）不含任何字段值（仅 title/status/source 元信息）→ 列表路径无 DataJson 泄露，本轮不改。记录确认结论。

- [ ] **Step 3：全量回归 + 编译**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: PASS，通过数 = 基线 98 + 本计划新增（SchemaSensitivity 2 + FieldMasker 9 + CardRedaction 6 + Presentation 1 ≈ +18）/ 1 skipped。
Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj`
Expected: 0 错误。

- [ ] **Step 4：人工验收清单**

- 已完成卡片，非关联用户访问 → 返回"卡片不存在"（访问门）。
- 发起人/处理人查看已完成卡片 → 敏感字段 masked（除非其经手节点曾放行）。
- 财务节点处理人在卡片完成后查看 → 其放行的银行卡字段仍明文（粘附授权）。
- 设计器把某字段标敏感+maskPattern=phone → 该字段在卡片详情按手机号样式打码，且前端不再二次打码。

- [ ] **Step 5：最终提交（如有采样补救/文档更新）**

```bash
git add -A
git commit -m "chore(cardflow): 脱敏链重构收尾验证（schema外键采样 + 列表路径确认）"
```

---

## 自检（spec 覆盖）

- D1 字段级敏感基线 → Task 1（模型）+ Task 4（基线计算）。
- D2 schema 外键默认移除 → Task 5（allowlist）。
- D3 最小访问门 → Task 6（初始人/处理人/管理员）。
- D4 粘附授权 → Task 4（GrantedBySticky）。
- D5 MVP 仅 masked → Task 4（基线 masked，hidden 仅经节点配置）。
- #9 maskPattern 后端权威 + 前端停打码 → Task 2/3（后端）+ Task 8（前端）。
- #17 聚合尊重列权限 → Task 7。
- #7 硬前置（扁平字段可声明敏感）→ Task 1 + Task 8（编辑器）。
- 单一真源（消除两份 MaskValue）→ Task 3。
- 兼容性风险（schema 外键 / 列表路径）→ Task 9 验证。
