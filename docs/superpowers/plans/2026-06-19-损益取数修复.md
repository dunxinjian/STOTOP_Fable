# 损益取数修复（P1-2）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复「损益类科目取数硬匹配 `FCategory == "损益"`」导致的两张利润表损益恒 0、期末结账不结转损益。

**Architecture:** 新建 `Constants/FinAccountCategory` 作为「损益子类集合」单一真源（含大类名 `"损益"` 兼容历史脏数据），替换 3 处对损益类的判定/筛选；收入 vs 费用细分仍按 `FBalanceDirection`，不动；无数据迁移。

**Tech Stack:** .NET 10 / EF Core（InMemory 测试）/ xUnit。后端 C#。

> **背景（实现者必读）**：科目种子里损益科目的 `F类别` 全是子类名（营业收入/营业成本/期间费用…），大类名「损益」出现 0 次；而 `ReportService.cs:1021` 与 `AccountPeriodService.cs:192` 硬匹配 `== "损益"`，恒不命中 → 利润表损益金额全走 else 置 0、结账筛出的损益科目集合恒空（不生成结转凭证）。详见设计文档 `docs/superpowers/specs/2026-06-19-损益取数修复-design.md`。
>
> **测试约定（来自项目经验）**：① 运行测试加 `--arch x64`，规避 x86 testhost 在 socket 紧张时 WSAENOBUFS 中止；② `CalculateAccountAmounts` 不依赖 Repository，用 `new ReportService(null×12)` + 反射调用即可单测（避免构造 12 个依赖）；它是**同步**方法，测试方法用 `[Fact] public void`，不涉及 async。

---

### Task 1: 新建损益类别单一真源 `FinAccountCategory`

**Files:**
- Create: `src/STOTOP.Module.Finance/Constants/FinAccountCategory.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Accounts/FinAccountCategoryTests.cs`

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.Module.Finance.Tests/Accounts/FinAccountCategoryTests.cs`：

```csharp
using STOTOP.Module.Finance.Constants;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Accounts;

public class FinAccountCategoryTests
{
    [Theory]
    [InlineData("营业收入")]
    [InlineData("营业成本")]
    [InlineData("营业税金及附加")]
    [InlineData("期间费用")]
    [InlineData("其他收益")]
    [InlineData("其他损失")]
    [InlineData("所得税费用")]
    [InlineData("以前年度损益调整")]
    [InlineData("损益")] // 兼容早期以大类名落库的脏数据
    public void IsProfitLoss_returns_true_for_profit_loss_categories(string category)
    {
        Assert.True(FinAccountCategory.IsProfitLoss(category));
    }

    [Theory]
    [InlineData("流动资产")]
    [InlineData("非流动资产")]
    [InlineData("流动负债")]
    [InlineData("所有者权益")]
    [InlineData("成本")]
    [InlineData("")]
    [InlineData(null)]
    public void IsProfitLoss_returns_false_for_non_profit_loss(string? category)
    {
        Assert.False(FinAccountCategory.IsProfitLoss(category));
    }

    [Fact]
    public void ProfitLossCategories_contains_eight_subcategories_plus_umbrella()
    {
        Assert.Equal(9, FinAccountCategory.ProfitLossCategories.Length);
        Assert.Contains("损益", FinAccountCategory.ProfitLossCategories);
        Assert.Contains("营业收入", FinAccountCategory.ProfitLossCategories);
        Assert.Contains("以前年度损益调整", FinAccountCategory.ProfitLossCategories);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~FinAccountCategoryTests"`
Expected: 编译失败 —— `FinAccountCategory` 类型不存在（`CS0246`）。

- [ ] **Step 3: 写最小实现**

创建 `src/STOTOP.Module.Finance/Constants/FinAccountCategory.cs`：

```csharp
namespace STOTOP.Module.Finance.Constants;

/// <summary>
/// 会计科目「损益类」判定的单一真源。
///
/// 科目种子中损益科目的 FCategory 存的是子类名（营业收入/营业成本/期间费用…），
/// 大类名「损益」并不落库；但早期数据/兼容路径可能以「损益」落库，故集合中一并纳入。
/// 取数/结账处统一用此处判定，避免再出现硬编码字符串与前后端双真源。
/// </summary>
public static class FinAccountCategory
{
    /// <summary>
    /// 损益子类集合（含大类名「损益」兼容历史脏数据）。
    /// 用数组以便 EF Core 在 Where 中翻译为 SQL IN。
    /// </summary>
    public static readonly string[] ProfitLossCategories =
    {
        "损益", "营业收入", "营业成本", "营业税金及附加", "期间费用",
        "其他收益", "其他损失", "所得税费用", "以前年度损益调整"
    };

    /// <summary>判断某科目类别是否属于损益类。null/空返回 false。</summary>
    public static bool IsProfitLoss(string? category)
        => category != null && Array.IndexOf(ProfitLossCategories, category) >= 0;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~FinAccountCategoryTests"`
Expected: PASS（16 个用例全绿）。

- [ ] **Step 5: 提交**

```bash
git add "src/STOTOP.Module.Finance/Constants/FinAccountCategory.cs" "tests/STOTOP.Module.Finance.Tests/Accounts/FinAccountCategoryTests.cs"
git commit -m "feat(finance): 新增 FinAccountCategory 损益类别单一真源 + 纯函数测试

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 修复利润表取数（ReportService.CalculateAccountAmounts）

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/ReportService.cs:1021`
- Test: `tests/STOTOP.Module.Finance.Tests/Accounts/ReportProfitLossCalcTests.cs`

> 两张利润表（`GetProfitStatementAsync`、`GetSmallEnterpriseProfitStatementAsync`）都经 `CalculateAccountAmounts` 取数，故此一处修好两张表。该方法不依赖 Repository，用反射直测。

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.Module.Finance.Tests/Accounts/ReportProfitLossCalcTests.cs`：

```csharp
using System.Reflection;
using STOTOP.Module.Finance.Entities;
using STOTOP.Module.Finance.Services;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Accounts;

public class ReportProfitLossCalcTests
{
    // CalculateAccountAmounts 不碰 Repository，全 null 构造 + 反射调用即可。
    private static Dictionary<string, decimal> Invoke(List<FinVoucherEntry> entries, List<FinAccount> accounts)
    {
        var svc = new ReportService(null!, null!, null!, null!, null!, null!, null!, null!, null!, null!, null!, null!);
        var method = typeof(ReportService).GetMethod(
            "CalculateAccountAmounts", BindingFlags.NonPublic | BindingFlags.Instance)!;
        return (Dictionary<string, decimal>)method.Invoke(svc, new object[] { entries, accounts })!;
    }

    [Fact]
    public void Revenue_account_with_subcategory_name_is_no_longer_zeroed()
    {
        // 损益科目 F类别 存子类名「营业收入」——修复前 == "损益" 不命中 → 被置 0
        var revenue = new FinAccount { FCode = "5001", FName = "主营业务收入", FCategory = "营业收入", FBalanceDirection = "贷", FIsLeaf = 1 };
        var asset = new FinAccount { FCode = "1001", FName = "库存现金", FCategory = "流动资产", FBalanceDirection = "借", FIsLeaf = 1 };
        var entries = new List<FinVoucherEntry>
        {
            new() { FAccountCode = "5001", FCreditAmount = 1000m, FDebitAmount = 0m },
            new() { FAccountCode = "1001", FDebitAmount = 100m, FCreditAmount = 0m },
        };

        var result = Invoke(entries, new List<FinAccount> { revenue, asset });

        Assert.Equal(1000m, result["5001"]); // 收入类：贷 - 借
        Assert.Equal(0m, result["1001"]);    // 资产类不计入损益取数，保持 0
    }

    [Fact]
    public void Expense_account_with_subcategory_name_nets_debit_minus_credit()
    {
        var expense = new FinAccount { FCode = "5602", FName = "管理费用", FCategory = "期间费用", FBalanceDirection = "借", FIsLeaf = 1 };
        var entries = new List<FinVoucherEntry>
        {
            new() { FAccountCode = "5602", FDebitAmount = 300m, FCreditAmount = 50m },
        };

        var result = Invoke(entries, new List<FinAccount> { expense });

        Assert.Equal(250m, result["5602"]); // 费用类：借 - 贷
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~ReportProfitLossCalcTests"`
Expected: FAIL —— `result["5001"]` 实际为 `0`（当前 `FCategory == "损益"` 不命中子类名），断言 `1000m` 失败。

- [ ] **Step 3: 改实现**

在 `src/STOTOP.Module.Finance/Services/ReportService.cs:1021`，把：

```csharp
            if (account.FCategory == "损益")
```

改为：

```csharp
            if (FinAccountCategory.IsProfitLoss(account.FCategory))
```

并在文件顶部 using 区加入（若尚无）：

```csharp
using STOTOP.Module.Finance.Constants;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~ReportProfitLossCalcTests"`
Expected: PASS（2 个用例绿）。

- [ ] **Step 5: 提交**

```bash
git add "src/STOTOP.Module.Finance/Services/ReportService.cs" "tests/STOTOP.Module.Finance.Tests/Accounts/ReportProfitLossCalcTests.cs"
git commit -m "fix(finance): 利润表损益取数改用 FinAccountCategory.IsProfitLoss

修复 CalculateAccountAmounts 硬匹配 FCategory=='损益' 致子类名损益科目被置0、
两张利润表(GetProfitStatement/GetSmallEnterpriseProfitStatement)收入费用全0。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 修复期末结账损益筛选（AccountPeriodService）

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/AccountPeriodService.cs:192`

> 此处是 EF 查询 `.Where(...)`。改用 `ProfitLossCategories.Contains(a.FCategory)`，EF Core 翻译为 SQL `IN`。
>
> **不为此单写集成测试**，理由（经权衡，非遗漏）：① 损益判定集合的正确性已由 Task 1 纯函数测试覆盖；② `string[].Contains(column)` 是 EF Core 标准可翻译为 `IN` 的写法；③ 结账全流程需构造 12 个依赖 + 试算平衡前置 + 凭证 seed + 事件分发，集成测试成本与收益不成比例（YAGNI）。正确性由「判定测试 + 编译 + 全量回归（Task 5）」共同保证。

- [ ] **Step 1: 改实现**

在 `src/STOTOP.Module.Finance/Services/AccountPeriodService.cs:191-193`，把：

```csharp
        var profitAndLossAccounts = await _accountRepository.Query()
            .Where(a => a.FCategory == "损益" && a.FIsLeaf == 1 && a.FAccountSetId == accountSetId)
            .ToListAsync();
```

改为：

```csharp
        var profitAndLossAccounts = await _accountRepository.Query()
            .Where(a => FinAccountCategory.ProfitLossCategories.Contains(a.FCategory) && a.FIsLeaf == 1 && a.FAccountSetId == accountSetId)
            .ToListAsync();
```

并在文件顶部 using 区加入（若尚无）：

```csharp
using STOTOP.Module.Finance.Constants;
```

- [ ] **Step 2: 编译验证**

Run: `dotnet build src/STOTOP.Module.Finance`
Expected: 编译成功，0 错误（确认 EF 表达式中 `ProfitLossCategories.Contains(...)` 可编译）。

- [ ] **Step 3: 提交**

```bash
git add "src/STOTOP.Module.Finance/Services/AccountPeriodService.cs"
git commit -m "fix(finance): 期末结账损益科目筛选改用 FinAccountCategory.ProfitLossCategories

修复结账硬匹配 FCategory=='损益' 致损益末级集合恒空、不生成结转凭证、
本年利润3103恒0、12月转未分配利润(310405)连带失效。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 收敛 AccountService 损益子类到单一真源

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/AccountService.cs:87`

> `GetSubCategories("损益")` 现有分支用内联数组定义同一份损益子类，与新常量构成重复。改为引用单一真源，避免双真源漂移。内容已逐字核对一致（均为「损益」+8 子类）。

- [ ] **Step 1: 改实现**

在 `src/STOTOP.Module.Finance/Services/AccountService.cs` 的 `GetSubCategories` 方法中，把损益分支（第 87 行附近）：

```csharp
            // 包含 "损益" 本身：早期创建的损益科目 FCategory 直接存大类名
            "损益" => new[] { "损益", "营业收入", "营业成本", "期间费用", "其他收益", "其他损失", "所得税费用", "以前年度损益调整", "营业税金及附加" },
```

改为：

```csharp
            // 损益子类（含大类名"损益"兼容早期数据）统一收敛到 FinAccountCategory 单一真源
            "损益" => FinAccountCategory.ProfitLossCategories,
```

并在文件顶部 using 区加入（若尚无）：

```csharp
using STOTOP.Module.Finance.Constants;
```

- [ ] **Step 2: 编译 + 回归验证**

Run: `dotnet build src/STOTOP.Module.Finance`
Expected: 编译成功，0 错误。

> 说明：`GetSubCategories` 返回值用于 `GetTreeAsync` 的 `.Where(a => subCategories.Contains(a.FCategory))`（同样 EF `IN` 翻译），内容一致故行为不变。`GetSubCategories` 为 private static，正确性由「返回同一真源」结构保证 + 编译，无需单测。

- [ ] **Step 3: 提交**

```bash
git add "src/STOTOP.Module.Finance/Services/AccountService.cs"
git commit -m "refactor(finance): GetSubCategories 损益分支复用 FinAccountCategory 单一真源

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 全量构建 + 测试收尾验证

**Files:** 无（仅验证）

- [ ] **Step 1: 全量构建**

Run: `dotnet build src/STOTOP.Module.Finance`
Expected: 0 错误。

> 若报后端 dll 被占用（本地开发服务运行中锁定 dll），先停开发服务再 build，或在独立 worktree 构建（见项目经验）。

- [ ] **Step 2: 全量测试**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64`
Expected: 全绿——含本次新增 `FinAccountCategoryTests`（16）+ `ReportProfitLossCalcTests`（2），且既有测试不回归。

- [ ] **Step 3: 确认无残留硬编码**

Run: `git grep -n '== *"损益"' -- "src/STOTOP.Module.Finance/**/*.cs"`
Expected: 无输出（两处 `== "损益"` 判定已全部替换；阿米巴/迁移等其它含「损益」字样的代码不在此模式内，不应命中）。

---

## 完成判据

- 新增 `FinAccountCategory` 常量类，3 处损益判定/筛选全部改用它；`== "损益"` 判定零残留。
- `FinAccountCategoryTests` + `ReportProfitLossCalcTests` 全绿，证明：损益子类名科目取数非 0、资产类仍 0、历史「损益」脏数据被识别。
- 全量 `dotnet build` / `dotnet test` 通过，既有测试不回归。
- 收入/费用细分（按 `FBalanceDirection`）未改动；无数据迁移。
- 已结账历史期间的补结转不在本次范围（设计文档已注明，需运维反结账重结）。
