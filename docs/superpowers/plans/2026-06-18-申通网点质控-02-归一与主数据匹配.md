# 申通网点质控 · 统一质控模型 + 归一 + 主数据匹配 实现计划（Plan 2 / 3）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Plan 1 落地的 29 张 `STG申通_*` 暂存数据，归一成并入 `STOTOP.Module.Quality` 的统一质控模型（5 张 `QL申通_*` 表），并把脏网点名/脏员工名/工号解析到系统主数据（`ExpNetworkPoint`/`ExpSalesman`），未匹配挂「待认领」、补映射后可重跑回填。

**Architecture:** 新增 5 张 `QL申通_*` 表（事实 `承运商质量事件` + `员工日质量指标` + `网点日质量指标` + 配置 `质量问题字典` + `员工别名`；网点别名复用既有 `ExpNetworkPointAlias`）。归一为一个独立服务 `QualityUnificationService`：遍历各 STG申通_ 表 → 经 `MasterDataMatcher` 解析网点/员工 → 问题清单类落「质量事件」、汇总/员工类 upsert「日指标」；幂等可重跑。触发=手动 API + Hangfire 定时 Job。看板属 Plan 3。

**Tech Stack:** C# / .NET（`STOTOP.Module.Quality`）、EF Core（实体+Configuration 自动扫描）、SQL Server（stotop 开发库）、Hangfire（定时）。测试：xUnit `dotnet test ... --arch x64`，真库集成测试经 `TestSqlConnection`（Plan 1 已建）连 stotop，且**真库集成测试类统一 `[Collection("StotopRealDb")]` 串行**（Plan 1 已有该 collection，避免并行抢库）。

**关联：** spec `docs/superpowers/specs/2026-06-17-申通网点质控看板-design.md`（§6 统一质控模型、§6.6 主数据匹配）；Plan 1 `docs/superpowers/plans/2026-06-17-申通网点质控-01-导入接入层.md`（STG 源、TestSqlConnection、串行 collection）；约定见 [[stotop-cardflow-staging-import]]、[[stotop-data-conventions]]、[[stotop-architecture]]。

**本计划边界：** 只做「STG → QL申通_* 归一 + 主数据匹配」。看板三视角/查询 API/映射维护页 = Plan 3。二期 KPI 自算/派发/奖惩不在此。

**Plan 1 衔接现状：** Plan 1 工作在分支 `claude/romantic-mclean-658743`，master 的 FF 合并待并发会话清理主树后完成。**本 Plan 2 应在 Plan 1 已并入 master 后、于干净分支上执行**（执行前确认 `STG申通_*` 29 表与导入链已在 master）。

---

## 关键事实（来自代码探查，执行时复核）

- **Quality 模块建表**：现有 8 张 `QL*` 表由启动时 `DatabaseSeederAdapter.CreateMissingTables(ctx)`（`src/STOTOP.WebAPI/Data/DatabaseSeederAdapter.cs:489-677`）按 EF 模型自动建（排除 `STG/HangFire/__EF` 前缀；`QL申通_` 不被排除→会自动建）。**但该自动建表是否生成 `HasIndex` 的二级/唯一索引未确认 → Task 0 探明。**
- **实体规范**：`BaseEntity`（FID 由 DbContext 统一配 IDENTITY，实体不声明）；`IOrgScoped`（`FOrgId` 自动过滤/填充）；表名 `QL`+中文（Configuration `.ToTable`）；列名 `F`+中文（`HasColumnName`）。模板：`src/STOTOP.Module.Quality/Entities/QlException.cs` + `Configurations/QlExceptionConfiguration.cs`。配置自动扫描（Quality 程序集已在 `Program.cs:408` 注册），**无需手写 DbSet**。
- **STG 实体**：29 张 `StgShentong*`（`src/STOTOP.Module.CardFlow/Entities/`，均 `IStagingRecord`），`_db.Set<StgShentong*>()` 可查，IOrgScoped 自动过滤。系统字段：`F批次ID`、`F处理状态`(0未处理/1已处理/2失败)、`FIsRevoked`、`F归属网点编号`、`FOrgId`、`F创建时间`。业务列均 `string?`（日期/金额按字符串落地，归一需解析）。
- **网点主数据**：`ExpNetworkPoint`（`src/STOTOP.Module.Express/Entities/ExpNetworkPoint.cs`）：主键 `FCode`、`FShortName`/`FFullName`（**无 FName**）、`FOrgId`。**已有 `ExpNetworkPointAlias`（`FName`→`FNetworkPointCode`，IOrgScoped）+ `NetworkPointAliasController` CRUD** → 网点名匹配复用之。
- **员工主数据**：`ExpSalesman`（主键 `FEmployeeNo` 工号、`FName`、`FNetworkPointCode`、`FEmployeeId`→HR）；`HrEmployee.FName`/`SysUser.FName`。**无 salesman 别名表** → 本计划新建 `QL申通_员工别名`。`SalesmanService` 有模糊查询（工号/姓名/电话）可参考。
- **后台作业**：Hangfire（`Program.cs:385-394`）；`RecurringJob.AddOrUpdate<TJob>("id", j=>j.ExecuteAsync(), Cron.X)`（`Program.cs:626`）；Job 类样例 `src/STOTOP.WebAPI/Jobs/QualityOverdueCheckJob.cs`、`src/STOTOP.Module.Task/Jobs/PerformanceCalcJob.cs`（`[AutomaticRetry(Attempts=3)]`）。
- **组织上下文（脱离 HTTP）**：`IOrgContextAccessor.CurrentOrgId`（`src/STOTOP.Core/Services/IOrgContextAccessor.cs`）；Job/批次链显式 `orgAccessor.CurrentOrgId = orgId;`（见 `FlowEngineService.cs:233`），用完 `ClearOverride()`。
- **DI 注册**：`src/STOTOP.Module.Quality/QualityModuleExtensions.cs` 的 `AddQualityModule`，新服务 `services.AddScoped<I..,..>()` 加这里。

---

## 文件结构总览

**新增实体（Quality 模块，`src/STOTOP.Module.Quality/Entities/`）**
- `QlShentongQualityEvent.cs` → 表 `QL申通_承运商质量事件`（事实）
- `QlShentongEmployeeDailyMetric.cs` → `QL申通_员工日质量指标`
- `QlShentongNetworkDailyMetric.cs` → `QL申通_网点日质量指标`
- `QlShentongProblemDict.cs` → `QL申通_质量问题字典`（配置）
- `QlShentongEmployeeAlias.cs` → `QL申通_员工别名`

**新增 EF 配置（`src/STOTOP.Module.Quality/Configurations/`）**：上述 5 个 `*Configuration.cs`。

**新增服务（`src/STOTOP.Module.Quality/Services/Unification/`）**
- `IMasterDataMatcher.cs` / `MasterDataMatcher.cs` — 网点/员工解析（编码→名称→别名→启发式；未匹配返回 null + 原文）。
- `ISourceNormalizer.cs` + 各源 normalizer 或一个配置驱动的 `QualityUnificationService.cs` — STG → 质量事件/日指标。
- `IQualityUnificationService.cs` / `QualityUnificationService.cs` — 归一编排（遍历源、调 matcher、写 QL、置 STG 处理状态）。
- `IShentongSourceMap.cs` + `ShentongSourceMap.cs` — 每个 STG 源 → 域/问题类型/目标(事件|指标)/字段映射 的静态配置（扩展点，附录 A）。

**新增控制器/Job**
- `src/STOTOP.Module.Quality/Controllers/UnificationController.cs` — POST 触发归一、POST 重跑回填、别名 CRUD（员工别名）、待认领查询。
- `src/STOTOP.WebAPI/Jobs/ShentongUnificationJob.cs` + `Program.cs` 注册 RecurringJob。

**建表/种子**：按 Task 0 结论——或纯 EF 自动建 + 索引补丁 Seeder，或显式 `QualitySeeder`。

**测试（`tests/STOTOP.Module.Quality.Tests/` 或并入现有测试工程，Task 0 定）**：matcher 单测（InMemory/纯函数）+ 归一集成测试（连 stotop，`[Collection("StotopRealDb")]`）。

---

## Task 0：探明 Quality 建表机制与测试工程

**Files:** 只读 + 可能新增测试工程引用

- [ ] **Step 1: 确认 Quality 是否有专用测试工程**

查 `tests/` 下是否有 `STOTOP.Module.Quality.Tests`。若无，决定：新建该工程（引用 Quality/CardFlow/Express/HR/System/Infrastructure/WebAPI，参照 `tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj`，含 `TestSqlConnection` 与 `[Collection("StotopRealDb")]` 复用——可把 `TestSqlConnection.cs`/`RealDbCollection.cs` 提取共享或复制），或把 Quality 测试并入 CardFlow.Tests 工程（已具备真库基建）。**记录决定与命令**（`dotnet test <工程> --arch x64`）。

- [ ] **Step 2: 探明 QL 表 + 索引的建表机制（关键）**

读 `src/STOTOP.WebAPI/Data/DatabaseSeederAdapter.cs` 的 `CreateMissingTables`/`GenerateCreateTableSql`，确认：① `QL申通_` 前缀是否被排除（应否）；② 自动建表 SQL **是否包含 `builder.HasIndex` 定义的二级索引/唯一索引**（只建列+PK，还是也建索引？）。
- 若自动建表**含索引** → Phase A 仅需 实体 + Configuration（含 HasIndex），表与索引随启动自动建；测试用同一机制建表（见 Step 3）。
- 若自动建表**不含索引** → Phase A 额外加一个 `QualitySeeder`（仿 `CardFlowSeeder`）在自动建表后 `CREATE INDEX IF NOT EXISTS`（唯一去重索引尤其重要）。
**产出明确结论 A 或 B，后续 Phase A 按此分支。**

- [ ] **Step 3: 探明集成测试如何让 QL 表存在于 stotop**

确认测试中创建/确保 QL 表的途径：应用启动会建（`CreateMissingTables`），但测试需自足。找 `CreateMissingTables` 是否有可在测试调用的公开入口（或 `DatabaseSeederAdapter` 的公开方法）；若私有，方案：测试 setup 里对 `STOTOPDbContext` 调用等效的「按模型建缺失表」逻辑，或显式 `CREATE TABLE IF NOT EXISTS` DDL（与 Phase A 的 Seeder 共用）。**记录测试建表方案。**

- [ ] **Step 4: 基线**

跑选定测试工程一次（`dotnet test <工程> --arch x64`），记录基线绿。确认能连 stotop（`TestSqlConnection`）。

**报告 Task 0 结论**（测试工程、建表机制 A/B、测试建表方案、基线）后，再按结论执行 Phase A。

---

## Phase A：统一质控模型（5 张 QL申通_ 表）

> 每张表：实体（`BaseEntity, IOrgScoped`，不声明 FID，业务列按语义用 `string?`/`DateTime?`/`decimal?`/`int?`/`bool`）+ Configuration（`.ToTable` + `HasColumnName` + 索引）。建表按 Task 0 结论（自动建 or Seeder）。字段如下（依据 spec §6 + 真实 STG 列）。

### Task A1：`QL申通_质量问题字典`（先建，事件表外键引用它）

**Files:**
- Create: `src/STOTOP.Module.Quality/Entities/QlShentongProblemDict.cs`
- Create: `src/STOTOP.Module.Quality/Configurations/QlShentongProblemDictConfiguration.cs`
- Test: matcher/dict 单测（见 Phase B）

- [ ] **Step 1: 写实体**

```csharp
using STOTOP.Core.Models;
namespace STOTOP.Module.Quality.Entities;

/// <summary>质量问题类型字典：把各源原始问题类型映射成 域/大类/小类/严重度/是否考核/是否可归人。扩展点。</summary>
public class QlShentongProblemDict : BaseEntity, IOrgScoped
{
    public long FOrgId { get; set; }
    public string F承运商 { get; set; } = "申通";   // 预留多品牌；本期恒"申通"
    public string F质量域 { get; set; } = string.Empty;        // 物流信息/揽收时效/.../拦截渗透 共9域
    public string F来源问题类型原文 { get; set; } = string.Empty; // 如"未到件""虚假签收""破损"
    public string F问题类型编码 { get; set; } = string.Empty;   // 稳定编码，如 LOGI_NOT_ARRIVED
    public string F问题类型名称 { get; set; } = string.Empty;
    public string? F问题大类 { get; set; }
    public string? F问题小类 { get; set; }
    public int F默认严重度 { get; set; }      // 0低/1中/2高/3紧急（对齐 ExceptionPriority）
    public bool F是否考核 { get; set; }
    public bool F是否可归责到人 { get; set; }
    public int? F默认整改时限小时 { get; set; }
    public int F状态 { get; set; } = 1;
    public string? F备注 { get; set; }
}
```

- [ ] **Step 2: 写 Configuration**

照 `QlExceptionConfiguration` 模板：`builder.ToTable("QL申通_质量问题字典");` 各 `Property(...).HasColumnName("F...")`；`F质量域`/`F来源问题类型原文`/`F问题类型编码` 等 `HasMaxLength`；唯一索引 `HasIndex(e => new { e.FOrgId, e.F承运商, e.F质量域, e.F来源问题类型原文 }).IsUnique()`（同源同原文唯一）。

- [ ] **Step 3: 编译**

`dotnet build src/STOTOP.Module.Quality`（0 错误）。

- [ ] **Step 4: 提交**

```bash
git add src/STOTOP.Module.Quality/Entities/QlShentongProblemDict.cs src/STOTOP.Module.Quality/Configurations/QlShentongProblemDictConfiguration.cs
git commit -m "feat(quality): 新增 QL申通_质量问题字典 实体+配置"
```

### Task A2：`QL申通_承运商质量事件`（事实表）

**Files:**
- Create: `src/STOTOP.Module.Quality/Entities/QlShentongQualityEvent.cs`
- Create: `src/STOTOP.Module.Quality/Configurations/QlShentongQualityEventConfiguration.cs`

- [ ] **Step 1: 写实体**

```csharp
public class QlShentongQualityEvent : BaseEntity, IOrgScoped
{
    public long FOrgId { get; set; }
    public string F承运商 { get; set; } = "申通";
    public DateTime? F业务日期 { get; set; }
    public string? F统计年月 { get; set; }       // yyyyMM，便于按月汇总
    public string? F运单号 { get; set; }
    public string? F网点编码 { get; set; }        // 解析后；未匹配为 null
    public string? F网点名称 { get; set; }        // 原文（脏名也保留）
    public string? F员工工号 { get; set; }        // 解析后；未匹配为 null
    public string? F员工姓名原文 { get; set; }
    public long? F员工ID { get; set; }            // 解析到的 ExpSalesman/HrEmployee（可空）
    public string? F电商平台 { get; set; }
    public string F质量域 { get; set; } = string.Empty;
    public string? F问题类型编码 { get; set; }
    public string? F问题类型名称 { get; set; }
    public int F严重度 { get; set; }
    public bool F是否考核件 { get; set; }
    public decimal? F考核金额 { get; set; }
    public string? F责任方 { get; set; }          // 本网点/外部网点/总部/客户
    public string F来源STG表 { get; set; } = string.Empty;
    public long F来源行ID { get; set; }
    public long? F来源批次ID { get; set; }
    public string? F关键字段JSON { get; set; }
    public int F网点匹配状态 { get; set; }         // 0未匹配/1已匹配（编码或名称/别名）
    public int F员工匹配状态 { get; set; }         // 0未匹配/1工号/2别名/3启发式建议(待确认)/9不适用(无人维度)
    public bool F是否已提升异常单 { get; set; }    // 二期
    public long? F关联异常单ID { get; set; }       // 二期
    public DateTime F创建时间 { get; set; } = DateTime.Now;
}
```

- [ ] **Step 2: 写 Configuration**

`.ToTable("QL申通_承运商质量事件")`；各 `HasColumnName`；`F考核金额` `HasColumnType("decimal(18,2)")`；
- 幂等唯一索引（归一可重跑、按事件身份去重）：`HasIndex(e => new { e.FOrgId, e.F承运商, e.F来源STG表, e.F来源行ID }).IsUnique().HasDatabaseName("UX_QL申通_质量事件_源行");`（一条 STG 行最多产一条事件）。
- 查询索引：`(F承运商,F业务日期,F网点编码)`、`(F员工工号,F业务日期)`、`(F运单号)`、`(F质量域,F问题类型编码)`、`(F网点匹配状态)`/`(F员工匹配状态)`（待认领查询）。

- [ ] **Step 3-4: 编译 + 提交**

`dotnet build src/STOTOP.Module.Quality`；`git commit -m "feat(quality): 新增 QL申通_承运商质量事件 事实表实体+配置"`。

### Task A3：`QL申通_员工日质量指标`

**Files:** Create 实体 `QlShentongEmployeeDailyMetric.cs` + Configuration。

- [ ] **Step 1: 写实体**（键 `F承运商+F业务日期+F网点编码+F员工工号`）

```csharp
public class QlShentongEmployeeDailyMetric : BaseEntity, IOrgScoped
{
    public long FOrgId { get; set; }
    public string F承运商 { get; set; } = "申通";
    public DateTime F业务日期 { get; set; }
    public string? F统计年月 { get; set; }
    public string F网点编码 { get; set; } = string.Empty;
    public string F员工工号 { get; set; } = string.Empty;
    public string? F员工姓名原文 { get; set; }
    public long? F员工ID { get; set; }
    public int? F派件量 { get; set; }
    public int? F当日派签量 { get; set; }
    public decimal? F当日派签率 { get; set; }
    public int? F应上门量 { get; set; }
    public int? F未上门量 { get; set; }
    public decimal? F按需上门率 { get; set; }
    public int? F客诉发起量 { get; set; }
    public int? F工单定责量 { get; set; }
    public decimal? F客诉发起率 { get; set; }
    public int? F虚假签收数 { get; set; }
    public int? F照片质检不合格数 { get; set; }
    public int? F派送超时T0数 { get; set; }
    public int? F派送超时T1数 { get; set; }
    public int? F派送超时T2数 { get; set; }
    public int? F派送超时T3数 { get; set; }
    public int? F揽收不及时数 { get; set; }
    public int? F上传不及时数 { get; set; }
    public int? F问题件数 { get; set; }
    public int? F违规虚假电联 { get; set; }
    public int? F违规无效电联 { get; set; }
    public int? F违规双签 { get; set; }
    public int? F违规照片定位虚假 { get; set; }
    public int? F违规签收文本不规范 { get; set; }
    public int? F违规引导代收 { get; set; }
    public decimal? F回访真实率 { get; set; }
    public decimal? F考核金额合计 { get; set; }
    public long? F来源批次ID { get; set; }
    public DateTime F创建时间 { get; set; } = DateTime.Now;
}
```

- [ ] **Step 2: Configuration** — `.ToTable("QL申通_员工日质量指标")`；唯一索引 `HasIndex(e => new { e.FOrgId, e.F承运商, e.F业务日期, e.F网点编码, e.F员工工号 }).IsUnique();`（upsert 键）；率/金额 decimal 类型。
- [ ] **Step 3-4: 编译 + 提交**。

### Task A4：`QL申通_网点日质量指标`

**Files:** Create 实体 `QlShentongNetworkDailyMetric.cs` + Configuration。

- [ ] **Step 1: 写实体**（键 `F承运商+F业务日期+F网点编码`），承载只能到网点的域指标（按 spec §6.3 列）：

```csharp
public class QlShentongNetworkDailyMetric : BaseEntity, IOrgScoped
{
    public long FOrgId { get; set; }
    public string F承运商 { get; set; } = "申通";
    public DateTime F业务日期 { get; set; }
    public string? F统计年月 { get; set; }
    public string F网点编码 { get; set; } = string.Empty;
    public string? F网点名称 { get; set; }
    public string? F片区 { get; set; }
    public string? F省区 { get; set; }
    // 物流信息
    public decimal? F揽收上传不及时率 { get; set; }
    public decimal? F派件上传不及时率 { get; set; }
    public decimal? F签收上传不及时率 { get; set; }
    public decimal? F揽收缺失率 { get; set; }
    public decimal? F派件缺失率 { get; set; }
    public decimal? F到件缺失率 { get; set; }
    public decimal? F不准确率 { get; set; }
    public decimal? F到件不准确率 { get; set; }
    // 揽收
    public decimal? F及时揽收率 { get; set; }
    public int? F未及时揽收量 { get; set; }
    // 出仓
    public decimal? F一频次出仓及时率 { get; set; }
    public int? F未及时出仓量 { get; set; }
    public decimal? F出仓预估考核金额 { get; set; }
    // 交货滞留
    public decimal? F滞留率 { get; set; }
    public int? F考核滞留量 { get; set; }
    public decimal? F滞留预估考核金额 { get; set; }
    // 派送签收
    public decimal? F一阶段及时签收率 { get; set; }
    public decimal? F二阶段及时签收率 { get; set; }
    public decimal? F当天及时签收率 { get; set; }
    public decimal? F派送预估考核金额 { get; set; }
    public decimal? F有偿派费金额 { get; set; }
    public decimal? F预计返款金额 { get; set; }
    // 签收率考核
    public decimal? F48h签收率 { get; set; }
    public decimal? F签收率考核金额 { get; set; }
    // 积压遗失
    public int? F日均出港量 { get; set; }
    public int? F日均进港量 { get; set; }
    public decimal? F积压倍数 { get; set; }
    public int? F超3天积压量 { get; set; }
    public int? F超5天积压量 { get; set; }
    public int? F超7天积压量 { get; set; }
    public decimal? F遗失率ppm { get; set; }
    public int? F遗失量 { get; set; }
    // 投诉
    public int? F进港投诉量 { get; set; }
    public decimal? F进港投诉率 { get; set; }
    public decimal? F虚签投诉率 { get; set; }
    public int? F7日虚签投诉量 { get; set; }
    // 拦截
    public int? F应拦截量 { get; set; }
    public decimal? F拦截成功率 { get; set; }
    public decimal? F及时转出率 { get; set; }
    // 渗透建站
    public decimal? F自建渗透率 { get; set; }
    public decimal? F渗透率目标 { get; set; }
    public int? F建站待完成 { get; set; }
    public int? F喵柜激活格口数 { get; set; }
    public decimal? F考核金额合计 { get; set; }
    public long? F来源批次ID { get; set; }
    public DateTime F创建时间 { get; set; } = DateTime.Now;
}
```

- [ ] **Step 2: Configuration** — `.ToTable("QL申通_网点日质量指标")`；唯一索引 `HasIndex(e => new { e.FOrgId, e.F承运商, e.F业务日期, e.F网点编码 }).IsUnique();`；率/金额 decimal。
- [ ] **Step 3-4: 编译 + 提交**。

### Task A5：`QL申通_员工别名`（脏名→工号映射，主数据匹配兜底）

**Files:** Create 实体 `QlShentongEmployeeAlias.cs` + Configuration。

- [ ] **Step 1: 写实体**

```csharp
public class QlShentongEmployeeAlias : BaseEntity, IOrgScoped
{
    public long FOrgId { get; set; }
    public string F承运商 { get; set; } = "申通";
    public string F源姓名原文 { get; set; } = string.Empty;  // 脏名，如"城区屈梦幻400"
    public string? F源工号 { get; set; }                     // 若源同时有工号
    public string F员工工号 { get; set; } = string.Empty;    // → ExpSalesman.FEmployeeNo
    public long? F员工ID { get; set; }
    public int F状态 { get; set; } = 1;
    public string? F备注 { get; set; }
    public DateTime F创建时间 { get; set; } = DateTime.Now;
}
```

- [ ] **Step 2: Configuration** — `.ToTable("QL申通_员工别名")`；唯一索引 `HasIndex(e => new { e.FOrgId, e.F承运商, e.F源姓名原文 }).IsUnique();`。
- [ ] **Step 3-4: 编译 + 提交**。

### Task A6：建表落地 + 启动期建表（按 Task 0 结论）

**Files:** 按结论——`src/STOTOP.WebAPI/Data/Seeders/QualitySeeder.cs`（若需显式建表/索引）+ `Program.cs`（注册调用）；或仅验证自动建表。

- [ ] **Step 1: 落实建表**
  - 若 Task 0 结论 A（自动建表含索引）：无需 Seeder，仅写一个集成冒烟（Step 2）验证 5 表 + 唯一索引在 stotop 实建。
  - 若结论 B（自动建表不含索引）：新增 `QualitySeeder.Migrate(ctx)`（仿 `CardFlowSeeder`，`MigrationRunner` + `IF NOT EXISTS` 建表/`CREATE INDEX`），在 `Program.cs` 初始化管线（`CreateMissingTables` 之后）调用 `QualitySeeder.Migrate(ctx)`。

- [ ] **Step 2: 集成冒烟（连 stotop，`[SkippableFact]` + `[Collection("StotopRealDb")]`）**

测试：确保建表机制执行后，`sys.tables` 含 5 张 `QL申通_*` 表，且 `QL申通_承运商质量事件` 的唯一索引 `UX_QL申通_质量事件_源行`、各日指标唯一索引存在（查 `sys.indexes`）。

- [ ] **Step 3: 跑 + 提交**

`dotnet test <工程> --arch x64 --filter QlSchema`（绿）；`git commit -m "feat(quality): QL申通_* 5 表建表落地 + 冒烟验证"`。

---

## Phase B：主数据匹配（MasterDataMatcher）

### Task B1：网点解析（编码→简称/全称→别名）

**Files:**
- Create: `src/STOTOP.Module.Quality/Services/Unification/IMasterDataMatcher.cs`、`MasterDataMatcher.cs`
- Test: `<测试工程>/Unification/NetworkMatchTests.cs`（InMemory）

- [ ] **Step 1: 写失败测试（InMemory，种子 ExpNetworkPoint + ExpNetworkPointAlias）**

```csharp
[Fact]
public async Task ResolveNetwork_ByCode_Then_ByName_Then_Alias()
{
    using var db = TestDbContextFactory.Create();
    db.Add(new ExpNetworkPoint { FCode="320288", FShortName="太仓城区", FFullName="江苏太仓市城区公司", FOrgId=192 });
    db.Add(new ExpNetworkPointAlias { FName="江苏太仓市城区公司", FNetworkPointCode="320288", FOrgId=192 });
    await db.SaveChangesAsync();
    var m = new MasterDataMatcher(db);
    Assert.Equal("320288", (await m.ResolveNetworkAsync("320288", null, 192)).Code);          // 编码
    Assert.Equal("320288", (await m.ResolveNetworkAsync(null, "江苏太仓市城区公司", 192)).Code); // 全称/别名
    Assert.Null((await m.ResolveNetworkAsync(null, "查无此网点", 192)).Code);                   // 未匹配
}
```

- [ ] **Step 2: 跑确认失败**（无 `MasterDataMatcher`）。
- [ ] **Step 3: 实现网点解析**

```csharp
public record NetworkMatch(string? Code, string? NameRaw, int Status); // Status:0未匹配/1已匹配
public interface IMasterDataMatcher
{
    Task<NetworkMatch> ResolveNetworkAsync(string? code, string? name, long orgId, CancellationToken ct = default);
    Task<EmployeeMatch> ResolveEmployeeAsync(string? employeeNo, string? nameRaw, string? networkCode, long orgId, CancellationToken ct = default);
}
```
`ResolveNetworkAsync`：① `code` 非空且 `ExpNetworkPoint.FCode==code` 存在 → 命中；② `name` 精确等于 `FShortName`/`FFullName` → 命中其 FCode；③ 查 `ExpNetworkPointAlias.FName==name` → 命中其 FNetworkPointCode；④ 否则 `(null, name, 0)`。

- [ ] **Step 4: 跑确认通过**；**Step 5: 提交** `feat(quality): MasterDataMatcher 网点解析(编码/名称/别名)`。

### Task B2：员工解析（工号→别名→启发式建议）

**Files:** Modify `MasterDataMatcher.cs`；Test `<测试工程>/Unification/EmployeeMatchTests.cs`。

- [ ] **Step 1: 写失败测试**（种子 ExpSalesman + QL申通_员工别名）

```csharp
[Fact]
public async Task ResolveEmployee_ByNo_Then_Alias_Then_Heuristic()
{
    using var db = TestDbContextFactory.Create();
    db.Add(new ExpSalesman { FEmployeeNo="3202880036", FName="屈梦幻", FNetworkPointCode="320288", FEmployeeId=11 });
    db.Add(new QlShentongEmployeeAlias { F承运商="申通", F源姓名原文="城区屈梦幻400", F员工工号="3202880036", FOrgId=192 });
    await db.SaveChangesAsync();
    var m = new MasterDataMatcher(db);
    Assert.Equal(("3202880036",1), Tup(await m.ResolveEmployeeAsync("3202880036", null, "320288", 192)));     // 工号
    Assert.Equal(("3202880036",2), Tup(await m.ResolveEmployeeAsync(null, "城区屈梦幻400", "320288", 192)));   // 别名
    var h = await m.ResolveEmployeeAsync(null, "城区屈梦幻999无此人", "320288", 192);
    Assert.True(h.Status is 0 or 3);   // 未命中→0；或启发式候选→3(待确认,不自动绑定)
}
```

- [ ] **Step 2: 跑确认失败**。
- [ ] **Step 3: 实现员工解析**

```csharp
public record EmployeeMatch(string? EmployeeNo, long? EmployeeId, string? NameRaw, int Status);
// Status:0未匹配/1工号/2别名/3启发式候选(待确认)/9不适用
```
逻辑：① `employeeNo` 非空且 `ExpSalesman.FEmployeeNo==employeeNo`（同 orgId）→ (no, id,1)；② 查 `QlShentongEmployeeAlias.F源姓名原文==nameRaw`（承运商申通+orgId）→ 映射工号→ExpSalesman → (no,id,2)；③ 启发式：剥离脏名前缀(`城区/操作部/机动`)与数字/电话提取候选真实姓名，在 `networkCode` 网点内 `ExpSalesman.FName` 模糊匹配，**唯一命中才作候选 Status=3（不写工号、仅建议）**；④ 否则 `(null,null,nameRaw,0)`。脏名清洗用正则：去 `^城区|^操作部|机动`、去尾部数字串、去 11 位手机号。

- [ ] **Step 4: 跑确认通过**；**Step 5: 提交** `feat(quality): MasterDataMatcher 员工解析(工号/别名/启发式候选)`。

### Task B3：员工别名 CRUD + 网点别名复用（API）

**Files:** Create `src/STOTOP.Module.Quality/Controllers/UnificationController.cs`（先放别名/待认领端点）；DI 在 `QualityModuleExtensions` 注册 `IMasterDataMatcher`。

- [ ] **Step 1: 注册 DI** `services.AddScoped<IMasterDataMatcher, MasterDataMatcher>();`（`QualityModuleExtensions.AddQualityModule`）。
- [ ] **Step 2: 控制器端点**（最小）：`GET /quality/unify/pending-employees`（列未匹配/待确认的员工原文聚合）、`POST /quality/unify/employee-alias`（建/改 `QlShentongEmployeeAlias`）、`GET/POST /quality/unify/network-alias`（复用 `ExpNetworkPointAlias`，或直接复用 Express 的 `NetworkPointAliasController`——若已满足则不重复造，仅在前端 Plan 3 指向它）。绑定后由 Phase C 的「重跑回填」生效。
- [ ] **Step 3: 编译 + 提交** `feat(quality): 员工别名 CRUD + 待认领端点`。

---

## Phase C：归一作业（QualityUnificationService）

### Task C0：源映射配置 + 归一编排骨架（worked example）

**Files:**
- Create: `src/STOTOP.Module.Quality/Services/Unification/ShentongSourceMap.cs`（每源→域/目标/字段映射，附录 A）
- Create: `IQualityUnificationService.cs` / `QualityUnificationService.cs`
- Create: `ParseUtil.cs`（`TryDate`/`TryDecimal`/`TryInt`/`年月` 安全解析）
- Test: `<测试工程>/Unification/UnifyEventIntegrationTests.cs`（连 stotop，`[Collection("StotopRealDb")]`）

- [ ] **Step 1: ParseUtil**

```csharp
public static class ParseUtil
{
    public static DateTime? TryDate(string? s) => DateTime.TryParse(s, out var d) ? d : null;
    public static decimal? TryDecimal(string? s) => decimal.TryParse((s ?? "").TrimEnd('%'), out var v) ? v : null; // 处理"99.5%"取数值
    public static int? TryInt(string? s) => int.TryParse(s, out var v) ? v : (decimal.TryParse(s, out var dv) ? (int)dv : null);
    public static bool Truthy(string? s) => s is "是" or "1" or "true" or "True";
    public static string? Ym(DateTime? d) => d?.ToString("yyyyMM");
}
```

- [ ] **Step 2: 归一服务接口 + 事件落地（worked: 物流完整性明细 → 质量事件）**

```csharp
public interface IQualityUnificationService
{
    Task<UnifyResult> UnifyShentongAsync(long orgId, DateTime? from = null, DateTime? to = null, CancellationToken ct = default);
    Task<int> RematchUnresolvedAsync(long orgId, CancellationToken ct = default); // 别名补录后重跑回填
}
public record UnifyResult(int EventsUpserted, int EmployeeMetricUpserts, int NetworkMetricUpserts, int NetworkUnmatched, int EmployeeUnmatched);
```
worked：从 `StgShentongLogisticsCompleteness`（域=物流信息）读 `FIsRevoked==false` 行，逐行：
- 用 `ShentongSourceMap` 查该源的 域/问题类型来源列(`F问题类型`)；查/建 `QlShentongProblemDict`（按 域+原文，缺失则按默认严重度=1/是否考核=false 落一条待人工完善——或预置种子）。
- `matcher.ResolveNetworkAsync(网点编码列?, 网点名称列, orgId)`（本源有 `F网点名称`，无编码→走名称/别名）；`matcher.ResolveEmployeeAsync(签收员编号列, 签收员名称列, 网点码, orgId)`。
- upsert `QlShentongQualityEvent`（按唯一键 `来源STG表+来源行ID`）：填运单/网点(解析)/员工(解析)/平台/域/问题类型/严重度/匹配状态/关键字段JSON。
- 累计 unmatched 计数。

- [ ] **Step 3: 写集成测试**（先红：未实现 UnifyShentongAsync）

```csharp
[Fact]
public async Task Unify_LogisticsCompleteness_CreatesEvents_AndMatchesNetwork()
{
    // 安排：连 stotop；确保 QL 表存在(Task A6 机制)；用 Plan1 的 ExcelInputPlugin 导一批"未到件"到 STG申通_物流完整性明细(orgId=192)
    //      种子 ExpNetworkPoint(320288,江苏太仓市城区公司)
    // 行为：svc.UnifyShentongAsync(192)
    // 断言：QL申通_承运商质量事件 出现对应行数(=导入行)，F质量域="物流信息"，F问题类型名称="未到件"，
    //      F网点编码="320288"(名称匹配成功 F网点匹配状态=1)，F来源STG表/F来源行ID 正确；
    //      重复跑一次 → 行数不变(按 源行 唯一键幂等)
    // 清理：删本测试 orgId 的事件 + STG 行
}
```

- [ ] **Step 4: 实现至测试通过**（绑 DI `IQualityUnificationService`）；跑 `dotnet test <工程> --arch x64 --filter Unify_LogisticsCompleteness`。
- [ ] **Step 5: 提交** `feat(quality): 归一服务骨架 + 物流完整性→质量事件(worked) + 幂等`。

### Task C1：员工日指标 worked（小件员履约指标 → 员工日指标）

**Files:** Modify `QualityUnificationService.cs` + `ShentongSourceMap.cs`；Test `UnifyEmployeeMetricIntegrationTests.cs`。

- [ ] **Step 1: 写集成测试**（导 `小件员履约指标` 到 `StgShentongCourierFulfill` → unify → 断言 `QL申通_员工日质量指标` 按 工号×日 upsert，派签率/上门率/违规计数等字段落值；员工经别名/工号匹配 F员工ID 填充；重复跑幂等(唯一键 upsert)）。
- [ ] **Step 2: 跑确认失败 → 实现 metric upsert 路径**（汇总/员工类源：解析数值列→ upsert 指标，按唯一键 update-or-insert）。
- [ ] **Step 3: 跑通 + 提交** `feat(quality): 小件员履约→员工日指标(worked,upsert幂等)`。

### Task C2：网点日指标 worked（积压监控汇总 → 网点日指标）

**Files:** Modify 同上；Test `UnifyNetworkMetricIntegrationTests.cs`。

- [ ] **Step 1-3:** 导 `积压异常监控` 到 `StgShentongBacklogMonitor` → unify → 断言 `QL申通_网点日质量指标` 按 网点×日 upsert（积压倍数/遗失率ppm/进港投诉率等落值，网点按编码匹配）；幂等；提交 `feat(quality): 积压监控→网点日指标(worked)`。

### Task C3：覆盖其余 26 源（按附录 A 配方，事件类/指标类各依 worked）

> 每源在 `ShentongSourceMap` 加一条映射（域、目标[事件|员工指标|网点指标]、关键列映射、问题类型来源列/常量），归一服务据 map 通用处理；**不需要为每源写新代码**（map 驱动）。每源加一条「该源 unify 后目标表有数据」的轻量集成断言（或并入 Task D1 整批 e2e）。

附录 A 给出 29 源 → 目标 + 域 + 关键映射。逐源（或按域分批）补 map 条目 + 验证。

- [ ] C3 子任务（按域分批，如：物流及时准确/揽收/未出仓/交货滞留/派送考核/签收未达标/积压明细/疑似遗失/进港投诉/投诉账单/虚签/虚假签收/照片质检/履约率/送货上门/应拦截/渗透 → 事件或指标；物流信息3汇总/签收率/出仓/末端派送网点/交货滞留汇总/拦截汇总/揽收考核 → 网点指标）。每批：补 map + 集成断言 + 提交。

### Task C4：重跑回填 + 触发器（API + Hangfire）

**Files:** Modify `QualityUnificationService.RematchUnresolvedAsync`；`UnificationController`（POST 触发）；`src/STOTOP.WebAPI/Jobs/ShentongUnificationJob.cs` + `Program.cs` 注册。

- [ ] **Step 1: 重跑回填测试**：事件 `F网点匹配状态==0`/`F员工匹配状态∈{0,3}` 的，补 `ExpNetworkPointAlias`/`QlShentongEmployeeAlias` 后调 `RematchUnresolvedAsync` → 断言对应事件被重解析、匹配状态更新、工号/编码回填。
- [ ] **Step 2: 实现 RematchUnresolvedAsync**（仅重解析未匹配项，不重建事件）。
- [ ] **Step 3: 控制器**：`POST /quality/unify/run`（手动触发 UnifyShentongAsync，org 来自请求上下文）、`POST /quality/unify/rematch`。
- [ ] **Step 4: Hangfire Job**：`ShentongUnificationJob.ExecuteAsync()` 对各组织设 `IOrgContextAccessor.CurrentOrgId` 后调 UnifyShentongAsync；`Program.cs` `RecurringJob.AddOrUpdate<ShentongUnificationJob>("shentong-unify", j=>j.ExecuteAsync(), Cron.Daily())`。
- [ ] **Step 5: 编译 + 跑 + 提交** `feat(quality): 归一重跑回填 + 手动API + Hangfire 定时`。

---

## Phase D：整批归一 e2e 验收

### Task D1：导入→归一 全链路 e2e

**Files:** Test `<测试工程>/Unification/ShentongUnifyBatchE2ETests.cs`（`[Collection("StotopRealDb")]`）。

- [ ] **Step 1: e2e 测试**
  - 安排：连 stotop；建 QL 表；种子 `ExpNetworkPoint(320288...)` + 几个 `ExpSalesman`（取样例文件中真实工号）；用 Plan 1 的导入链把代表性样例文件导入对应 STG申通_ 表（复用 Plan 1 `ShentongQualityBatchE2ETests` 的导入封装）。
  - 行为：`UnifyShentongAsync(192)`。
  - 断言：① `QL申通_承运商质量事件` 覆盖事件类各域（抽样：物流完整性/虚假签收/进港投诉/照片质检 各有事件，问题类型名称正确）；② `QL申通_员工日质量指标`（小件员履约 60 行→60 员工指标，工号匹配）；③ `QL申通_网点日质量指标`（积压监控/出仓/交货滞留汇总等网点指标落值，网点 320288 匹配）；④ 主数据匹配：网点 320288 全匹配（F网点匹配状态=1）；有工号的事件 F员工匹配状态=1；脏名无工号的进「待认领」(0或3)；⑤ 幂等：再跑一次 UnifyShentongAsync，三表行数不变；⑥ 回填：给一个脏名补 `QlShentongEmployeeAlias` → `RematchUnresolvedAsync` → 该员工事件匹配状态变 2、工号回填。
  - 清理：删本测试 orgId 的 QL 数据 + STG 行 + 种子。
- [ ] **Step 2: 跑全量** `dotnet test <工程> --arch x64`（0 失败）。
- [ ] **Step 3: 提交** `test(quality): 申通导入→归一 e2e(事件/指标/匹配/幂等/回填)`。

### Task D2：问题类型字典种子

**Files:** `QualitySeeder` 或种子方法：预置 `QL申通_质量问题字典` 常见条目（9 域的高频问题类型 + 默认严重度/是否考核/是否可归人），避免归一时大量「默认条目」。

- [ ] **Step 1:** 依附录 A 的域→问题类型清单，种子约 30-50 条字典（如 物流信息:未到件/未揽收/未派件/到件晚于签收…；虚假签收:虚假签收/签收未收到…；投诉:破损/退回/派送地点错误…）。idempotent `IF NOT EXISTS`。
- [ ] **Step 2: 提交** `feat(quality): 质量问题字典种子(9域高频问题类型)`。

---

## 附录 A：29 源 → 归一目标映射（C3 据此逐源补 `ShentongSourceMap`）

> 列与实际 `StgShentong*` 实体属性一致（Plan 1 已建）。目标三类：**E**=质量事件、**EM**=员工日指标、**NM**=网点日指标。问题类型：事件类取「问题类型来源列」或常量。

| STG 源(实体) | 目标 | 域 | 网点列 | 员工列(工号/姓名) | 问题类型来源 | 日期列 |
|---|---|---|---|---|---|---|
| StgShentongLogisticsCompleteness | E | 物流信息 | F网点名称 | F签收员编号/F签收员名称 | F问题类型 | F统计日期 |
| StgShentongLogisticsTimeliness | E | 物流信息 | F网点名称 | F扫描员编号/F扫描员 | F问题类型 | F统计日期 |
| StgShentongPickupAnalysis | E(仅未及时) | 揽收时效 | F揽收所属网点 | F收件员 | 常量"揽收不及时"(F揽收及时标识=否) | F统计日期 |
| StgShentongOutboundMonitor | E | 出仓时效 | F应签所属网点 | F派件员 | 常量"未及时出仓" | F统计日期 |
| StgShentongHandoverDelay | E(仅滞留) | 交货滞留 | F揽收所属网点 | F揽收小件员名称 | 常量"交货滞留"(F交货滞留标识=是) | F业务日期 |
| StgShentongDeliveryAssess | E(仅延迟) | 派送签收时效 | F应签收所属网点名称 | F派件员姓名 | 常量"签收延迟"(T0-T3标识) | F统计日期 |
| StgShentongSignSubstandard | E | 派送签收时效 | F应签网点 | F业务员 | 常量"签收未达标" | F应签日期 |
| StgShentongBacklog | E | 积压与遗失 | F所属网点名称 | F业务员/F扫描员 | F问题件二级类型 | F业务日期 |
| StgShentongSuspectedLoss | E | 积压与遗失 | F扫描站点 | F业务员/F扫描操作人 | F问题件类型 | (3日轨迹中断触发时间) |
| StgShentongInboundComplaint | E | 投诉与赔付 | F所属网点名称 | F小件员编码/F小件员名称 | F投诉类型 | F统计日期 |
| StgShentongComplaintBill | E | 投诉与赔付 | F投诉网点 | (无) | F账单二级类型(理赔类型) | F账单生成时间; 金额→F考核金额 |
| StgShentongFakeSignComplaint | E | 虚假签收履约 | F所属网点名称 | F派件业务员id/F派件业务员名称 | F投诉类型 | F投诉日期; F预估考核金额→金额 |
| StgShentongFakeSign | E | 虚假签收履约 | F所属网点名称 | F派件业务员id/F派件业务员名称 | F投诉类型 | F投诉日期 |
| StgShentongPhotoQc | E(仅不合格) | 虚假签收履约 | F网点名称 | F小件员编码/F小件员名称 | F不合格类型(F是否质检合格=否) | F分区(日期) |
| StgShentongFulfillRate | E(仅失败) | 虚假签收履约 | (订单网点) | F小件员工号/F小件员名称 | F履约状态(履约失败) | F日期 |
| StgShentongHomeDelivery | E(仅失败/违规) | 虚假签收履约 | F承包区名称 | F业务员工号/F派送小件员名称 | F违规行为二级内容/F履约情况 | F统计日期 |
| StgShentongInterceptDetail | E | 拦截渗透 | F所属网点 | F派件小件员 | F拦截类型 | F统计日期; F预计考核金额→金额 |
| StgShentongPenetration | NM | 拦截渗透 | F网点编号 | — | — | F统计周期 |
| StgShentongInfoIndexTimely | NM | 物流信息 | F网点名称 | — | — | F统计日期 |
| StgShentongInfoIndexComplete | NM | 物流信息 | F网点名称 | — | — | F统计日期 |
| StgShentongInfoIndexAccurate | NM | 物流信息 | F网点名称 | — | — | F统计日期 |
| StgShentongSignRateAssess | NM | 派送签收时效 | F网点编号 | — | — | F日期; F总金额→签收率考核金额 |
| StgShentongOutboundAssess | NM | 出仓时效 | F所属网点编码 | — | — | F统计日期 |
| StgShentongCourierFulfill | EM | (员工综合) | F所属网点 | F所属小件员(脏名,无工号→别名/启发式) | — | (无日期→用导入批次日或当期) |
| StgShentongDeliveryNetSummary | NM | 派送签收时效 | F应签所属网点 | — | — | F统计日期 |
| StgShentongBacklogMonitor | NM | 积压与遗失 | F网点编码 | — | — | F统计日期 |
| StgShentongHandoverSummary | NM | 交货滞留 | F揽收所属网点编码 | — | — | F统计日期 |
| StgShentongInterceptSummary | NM | 拦截渗透 | F所属网点 | — | — | F统计日期 |
| StgShentongPickupAssess | NM | 揽收时效 | F揽收所属网点编码 | — | — | F统计日期 |

> 注：① 列名以实际实体属性为准（执行时核对，Plan 1 对斜杠/括号列做了去字符，如 `F违规行为二级内容`）；② `StgShentongCourierFulfill` 无日期列——用导入批次的统计期或当期年月（实现时定，落 `F业务日期` 取批次创建日）；③ 事件类「仅 X」表示只对满足条件的行产事件（如照片质检仅不合格、履约率仅失败），减少噪声。

---

## 自检（Self-Review）

- **spec §6 覆盖**：4 核心表→A1-A4；员工别名→A5；网点别名复用 ExpNetworkPointAlias（spec 原拟新建，探查发现已有→复用，记此偏差）；归一作业→C0-C4；§6.6 主数据匹配(网点编码→名称→别名 / 员工工号→别名→启发式 + 待认领 + 重跑回填)→B1-B3+C4；幂等→唯一键 upsert + e2e D1；问题字典→A1+D2。✅
- **占位扫描**：列映射以「附录 A + 实际实体属性」为真源（C3 据此），无 TODO；建表机制由 Task 0 定 A/B 两分支明确；测试均给真实断言。✅
- **类型/命名一致**：实体 `QlShentong*` ↔ 表 `QL申通_*`；matcher 返回 `NetworkMatch{Code,NameRaw,Status}`/`EmployeeMatch{EmployeeNo,EmployeeId,NameRaw,Status}`（B1/B2/C0/C4/D1 一致）；`UnifyShentongAsync`/`RematchUnresolvedAsync` 贯穿 C0/C4/D1；F网点匹配状态/F员工匹配状态 语义贯穿 A2/B/C/D。✅
- **依赖顺序**：Task 0→A(模型,建表)→B(matcher)→C(归一,依赖 A+B)→D(e2e)。C 的集成测试依赖 Plan 1 导入链（执行前确认 Plan 1 已在 master/分支可用）。✅
- **边界**：不做看板/查询API/维护页前端(Plan 3)、不做 KPI/派发/奖惩(二期)。✅
