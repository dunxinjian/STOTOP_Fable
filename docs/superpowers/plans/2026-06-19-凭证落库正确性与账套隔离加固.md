# 凭证落库正确性与账套隔离加固 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复手工凭证落库的账期/账套归零、写入非原子、跨账套越权三类 P0 缺陷，使凭证落库正确、原子、隔离。

**Architecture:** 后端权威。把「期间解析 / 结账校验 / 归属鉴权」三条决策抽成纯静态规则（`VoucherPostingRules`）做快速 TDD；再把规则接线进 `VoucherService` 的 Create/Update/SaveDraft/Copy/Reverse 与所有按 id 操作，并用显式数据库事务包裹写入；控制器补 `accountSetId` 请求头兜底与缺失的权限属性。

**Tech Stack:** .NET 10 / EF Core / xUnit / EF Core InMemory（集成测试）/ EF Core Sqlite（事务回滚测试）/ Vue 3 (前端配套)。

**对应 spec：** `docs/superpowers/specs/2026-06-19-凭证落库正确性与账套隔离加固-design.md`

---

## 文件结构

**新增（源码）**
- `src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs` — 纯静态规则：期间解析、结账校验、归属鉴权谓词。无 DbContext 依赖。

**修改（源码）**
- `src/STOTOP.Module.Finance/Services/VoucherService.cs` — 接线规则、事务包裹、按 id 操作加归属过滤、新增 `GetCurrentAccountSetId()`、UpdateAsync 分录补 `FOrgId/FDataScopeId`、事件移到 Commit 后。
- `src/STOTOP.Module.Finance/Controllers/VoucherController.cs` — `Create` 的 accountSetId 请求头兜底；补 10 个端点的 `[RequireAccountSetPermission]`。
- `web/src/views/finance/VoucherEntry.vue` — `watch(form.date)` 回写 `form.periodId`。

**新增（测试）**
- `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs` — 纯规则单测。
- `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTestHarness.cs` — 集成测试公共脚手架（构造全量 VoucherService）。
- `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs` — 簇 A 接线集成测试（InMemory）。
- `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceIsolationTests.cs` — 簇 C 越权隔离集成测试（InMemory）。
- `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTransactionTests.cs` — 簇 B 事务回滚测试（Sqlite）。

**修改（测试工程）**
- `tests/STOTOP.Module.Finance.Tests/STOTOP.Module.Finance.Tests.csproj` — 加 `Microsoft.AspNetCore.App` framework 引用（取 `DefaultHttpContext`）+ `Microsoft.EntityFrameworkCore.Sqlite` 包。

**测试运行约定（全程）：** `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64`（记忆：x86 testhost 在 socket 吃紧时会中止）。

---

## 阶段 0：测试工程脚手架

### Task 0.1：测试工程加 framework 引用与 Sqlite 包

**Files:**
- Modify: `tests/STOTOP.Module.Finance.Tests/STOTOP.Module.Finance.Tests.csproj`

- [ ] **Step 1: 加 framework 引用与 Sqlite 包**

在 `<ItemGroup>`（含 PackageReference 的那个）内追加 Sqlite 包：

```xml
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.8" />
```

在 `</Project>` 之前、ProjectReference 的 ItemGroup 之后，新增一个 ItemGroup 引用 AspNetCore 框架（用于在测试中构造 `DefaultHttpContext`）：

```xml
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
```

- [ ] **Step 2: 还原并编译，确认引用可用**

Run: `dotnet build tests/STOTOP.Module.Finance.Tests --arch x64`
Expected: 编译成功（0 error）。

- [ ] **Step 3: Commit**

```bash
git add tests/STOTOP.Module.Finance.Tests/STOTOP.Module.Finance.Tests.csproj
git commit -m "test(finance): 凭证测试工程加 AspNetCore framework 引用与 Sqlite 包"
```

---

## 阶段 1：纯规则 `VoucherPostingRules`（TDD 核心）

### Task 1.1：期间解析规则 `ResolvePeriod`

**Files:**
- Create: `src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs`

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs`：

```csharp
using STOTOP.Module.Finance.Entities;
using STOTOP.Module.Finance.Services;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Voucher;

public class VoucherPostingRulesTests
{
    private static List<FinAccountPeriod> Periods() => new()
    {
        new FinAccountPeriod { FID = 10, FAccountSetId = 1, FYear = 2026, FPeriodNo = 5,
            FStartDate = new DateTime(2026, 5, 1), FEndDate = new DateTime(2026, 5, 31), FIsClosed = 1 },
        new FinAccountPeriod { FID = 11, FAccountSetId = 1, FYear = 2026, FPeriodNo = 6,
            FStartDate = new DateTime(2026, 6, 1), FEndDate = new DateTime(2026, 6, 30), FIsClosed = 0 },
        new FinAccountPeriod { FID = 99, FAccountSetId = 2, FYear = 2026, FPeriodNo = 6,
            FStartDate = new DateTime(2026, 6, 1), FEndDate = new DateTime(2026, 6, 30), FIsClosed = 0 },
    };

    [Fact]
    public void ResolvePeriod_returns_matching_period_for_date_and_account_set()
    {
        var p = VoucherPostingRules.ResolvePeriod(Periods(), new DateTime(2026, 6, 15), accountSetId: 1);
        Assert.Equal(11, p.FID);
    }

    [Fact]
    public void ResolvePeriod_ignores_other_account_sets()
    {
        // 账套 2 的 6 月期间不能被账套 1 的查询命中
        var p = VoucherPostingRules.ResolvePeriod(Periods(), new DateTime(2026, 6, 15), accountSetId: 1);
        Assert.Equal(1, p.FAccountSetId);
    }

    [Fact]
    public void ResolvePeriod_throws_when_no_period_covers_date()
    {
        var ex = Assert.Throws<InvalidOperationException>(
            () => VoucherPostingRules.ResolvePeriod(Periods(), new DateTime(2026, 7, 1), accountSetId: 1));
        Assert.Contains("未找到", ex.Message);
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherPostingRulesTests"`
Expected: FAIL（`VoucherPostingRules` 不存在，编译错误）。

- [ ] **Step 3: 写最小实现**

创建 `src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs`：

```csharp
using STOTOP.Module.Finance.Entities;

namespace STOTOP.Module.Finance.Services;

/// <summary>
/// 凭证落库纯规则：期间解析、结账校验、归属鉴权。
/// 不依赖 DbContext，便于单元测试；VoucherService 负责取数后委派到这里。
/// </summary>
public static class VoucherPostingRules
{
    /// <summary>
    /// 按 日期 + 账套 在候选期间中定位唯一期间；查不到抛错（缺账期一律拒绝落库）。
    /// </summary>
    public static FinAccountPeriod ResolvePeriod(
        IEnumerable<FinAccountPeriod> candidates, DateTime date, long accountSetId)
    {
        var period = candidates
            .Where(p => p.FAccountSetId == accountSetId
                && p.FStartDate <= date
                && p.FEndDate >= date)
            .OrderByDescending(p => p.FStartDate)
            .FirstOrDefault();
        if (period == null)
            throw new InvalidOperationException(
                $"未找到账套 {accountSetId} 在 {date:yyyy-MM-dd} 对应的账期，请先建立该期间");
        return period;
    }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherPostingRulesTests"`
Expected: PASS（3 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs
git commit -m "feat(finance): 新增 VoucherPostingRules.ResolvePeriod 期间解析规则(TDD)"
```

### Task 1.2：结账校验规则 `EnsureOpenForPosting`

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs`

- [ ] **Step 1: 追加失败测试**

在 `VoucherPostingRulesTests` 类内追加：

```csharp
    [Fact]
    public void EnsureOpenForPosting_throws_when_period_closed()
    {
        var closed = new FinAccountPeriod { FID = 10, FIsClosed = 1, FYear = 2026, FPeriodNo = 5 };
        var ex = Assert.Throws<InvalidOperationException>(
            () => VoucherPostingRules.EnsureOpenForPosting(closed));
        Assert.Contains("已结账", ex.Message);
    }

    [Fact]
    public void EnsureOpenForPosting_passes_when_period_open()
    {
        var open = new FinAccountPeriod { FID = 11, FIsClosed = 0 };
        Assert.Null(Record.Exception(() => VoucherPostingRules.EnsureOpenForPosting(open)));
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherPostingRulesTests"`
Expected: FAIL（`EnsureOpenForPosting` 不存在）。

- [ ] **Step 3: 写最小实现**

在 `VoucherPostingRules` 类内追加方法：

```csharp
    /// <summary>
    /// 校验期间未结账，已结账则拒绝落库（FIsClosed: 1=已结账）。
    /// </summary>
    public static void EnsureOpenForPosting(FinAccountPeriod period)
    {
        if (period.FIsClosed == 1)
            throw new InvalidOperationException(
                $"{period.FYear}年第{period.FPeriodNo}期已结账，不能在该期间登记/修改凭证");
    }
```

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherPostingRulesTests"`
Expected: PASS（5 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs
git commit -m "feat(finance): 新增 VoucherPostingRules.EnsureOpenForPosting 结账校验规则(TDD)"
```

### Task 1.3：归属鉴权谓词 `IsAccessible`

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs`

- [ ] **Step 1: 追加失败测试**

在 `VoucherPostingRulesTests` 类内追加：

```csharp
    private static FinVoucher OwnedVoucher() => new() { FID = 1, FOrgId = 100, FAccountSetId = 7 };

    [Fact]
    public void IsAccessible_true_when_org_and_account_set_match()
        => Assert.True(VoucherPostingRules.IsAccessible(OwnedVoucher(), currentOrgId: 100, currentAccountSetId: 7));

    [Fact]
    public void IsAccessible_false_when_other_org()
        => Assert.False(VoucherPostingRules.IsAccessible(OwnedVoucher(), currentOrgId: 200, currentAccountSetId: 7));

    [Fact]
    public void IsAccessible_false_when_other_account_set_same_org()
        => Assert.False(VoucherPostingRules.IsAccessible(OwnedVoucher(), currentOrgId: 100, currentAccountSetId: 8));

    [Fact]
    public void IsAccessible_degrades_to_org_only_when_no_account_set_header()
        // currentAccountSetId==0 表示无请求头(后台任务)，仅校验组织
        => Assert.True(VoucherPostingRules.IsAccessible(OwnedVoucher(), currentOrgId: 100, currentAccountSetId: 0));

    [Fact]
    public void IsAccessible_degrades_to_unconstrained_when_no_org_context()
        // currentOrgId==0 表示无 HttpContext(后台任务)，不约束
        => Assert.True(VoucherPostingRules.IsAccessible(OwnedVoucher(), currentOrgId: 0, currentAccountSetId: 0));
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherPostingRulesTests"`
Expected: FAIL（`IsAccessible` 不存在）。

- [ ] **Step 3: 写最小实现**

在 `VoucherPostingRules` 类内追加方法：

```csharp
    /// <summary>
    /// 归属鉴权谓词：凭证是否对当前上下文可见/可操作。
    /// currentOrgId==0（无 HttpContext，后台任务）→ 不约束；
    /// currentAccountSetId==0（无 X-AccountSet-Id 请求头）→ 仅校验组织。
    /// </summary>
    public static bool IsAccessible(FinVoucher voucher, long currentOrgId, long currentAccountSetId)
        => (currentOrgId == 0 || voucher.FOrgId == currentOrgId)
        && (currentAccountSetId == 0 || voucher.FAccountSetId == currentAccountSetId);
```

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherPostingRulesTests"`
Expected: PASS（10 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherPostingRules.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherPostingRulesTests.cs
git commit -m "feat(finance): 新增 VoucherPostingRules.IsAccessible 归属鉴权谓词(TDD)"
```

---

## 阶段 2：簇 A 接线（期间/账套落库 + 结账校验）

### Task 2.1：集成测试脚手架 `VoucherServiceTestHarness`

**Files:**
- Create: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTestHarness.cs`

- [ ] **Step 1: 写脚手架（无独立测试，供后续任务复用）**

创建 `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTestHarness.cs`：

```csharp
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.Logging.Abstractions;
using System.Security.Claims;
using STOTOP.Infrastructure.Data;
using STOTOP.Infrastructure.Events;
using STOTOP.Infrastructure.Repositories;
using STOTOP.Module.Finance.Entities;
using STOTOP.Module.Finance.Services;

namespace STOTOP.Module.Finance.Tests.Voucher;

/// <summary>
/// 构造一个全量接线的 VoucherService 供集成测试使用。
/// 通过 DefaultHttpContext 注入 CurrentOrgId / X-AccountSet-Id 头 / 用户身份。
/// </summary>
public static class VoucherServiceTestHarness
{
    public sealed class NoOpEventDispatcher : IEventDispatcher
    {
        public Task PublishAsync<T>(T @event) where T : BusinessEvent => Task.CompletedTask;
    }

    public static IHttpContextAccessor HttpContext(long orgId, long accountSetId, long userId = 1, string userName = "tester")
    {
        var ctx = new DefaultHttpContext();
        ctx.Items["CurrentOrgId"] = orgId;
        if (accountSetId > 0)
            ctx.Request.Headers["X-AccountSet-Id"] = accountSetId.ToString();
        ctx.User = new ClaimsPrincipal(new ClaimsIdentity(new[]
        {
            new Claim(ClaimTypes.NameIdentifier, userId.ToString()),
            new Claim(ClaimTypes.Name, userName),
        }, "Test"));
        return new HttpContextAccessor { HttpContext = ctx };
    }

    public static VoucherService Build(STOTOPDbContext db, IHttpContextAccessor http)
    {
        var opLog = new OperationLogService(
            new Repository<FinOperationLog>(db), http, NullLogger<OperationLogService>.Instance);
        var changeTracking = new ChangeTrackingService(
            new Repository<FinChangeHistory>(db), http);
        return new VoucherService(
            new Repository<FinVoucher>(db),
            new Repository<FinVoucherEntry>(db),
            new Repository<FinAccount>(db),
            new Repository<FinAccountPeriod>(db),
            opLog,
            changeTracking,
            http,
            new NoOpEventDispatcher(),
            db);
    }

    public static FinAccount Account(long id, string code, string name, long accountSetId, long orgId)
        => new() { FID = id, FCode = code, FName = name, FAccountSetId = accountSetId, FOrgId = orgId, FEnableStatus = 1 };

    public static FinAccountPeriod Period(long id, int year, int periodNo, long accountSetId, int isClosed = 0)
        => new()
        {
            FID = id, FYear = year, FPeriodNo = periodNo, FAccountSetId = accountSetId, FIsClosed = isClosed,
            FStartDate = new DateTime(year, periodNo, 1),
            FEndDate = new DateTime(year, periodNo, DateTime.DaysInMonth(year, periodNo)),
        };
}
```

- [ ] **Step 2: 编译确认脚手架可用**

Run: `dotnet build tests/STOTOP.Module.Finance.Tests --arch x64`
Expected: 编译成功（若 `FinOperationLog`/`FinChangeHistory` 命名空间报错，确认 `using STOTOP.Module.Finance.Entities;` 已包含——它们应在该命名空间）。

- [ ] **Step 3: Commit**

```bash
git add tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTestHarness.cs
git commit -m "test(finance): 新增 VoucherServiceTestHarness 集成测试脚手架"
```

### Task 2.2：`ResolvePeriodAsync` + CreateAsync 接线（落库正确 + 结账拒绝 + 缺账期拒绝）

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/VoucherService.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs`

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs`：

```csharp
using STOTOP.Module.Finance.Dtos;
using STOTOP.Module.Finance.Entities;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Voucher;

public class VoucherServicePeriodTests
{
    private const long Org = 100;
    private const long AcctSet = 7;

    private static CreateVoucherRequest BalancedRequest(DateTime date) => new()
    {
        VoucherWord = "记",
        Date = date,
        PeriodId = 0, // 模拟前端不传期间
        Entries =
        {
            new CreateVoucherEntryRequest { LineNo = 1, Summary = "测试", AccountId = 1, DebitAmount = 100m },
            new CreateVoucherEntryRequest { LineNo = 2, Summary = "测试", AccountId = 2, CreditAmount = 100m },
        }
    };

    private static async Task SeedAsync(STOTOP.Infrastructure.Data.STOTOPDbContext db, int periodNo, int isClosed = 0)
    {
        db.Set<FinAccount>().AddRange(
            VoucherServiceTestHarness.Account(1, "1001", "库存现金", AcctSet, Org),
            VoucherServiceTestHarness.Account(2, "3001", "实收资本", AcctSet, Org));
        db.Set<FinAccountPeriod>().Add(
            VoucherServiceTestHarness.Period(11, 2026, periodNo, AcctSet, isClosed));
        await db.SaveChangesAsync();
    }

    [Fact]
    public async Task Create_resolves_period_and_account_set_from_date_when_periodId_zero()
    {
        await using var db = TestDbContextFactory.Create(nameof(Create_resolves_period_and_account_set_from_date_when_periodId_zero), Org);
        await SeedAsync(db, periodNo: 6);
        var http = VoucherServiceTestHarness.HttpContext(Org, AcctSet);
        var service = VoucherServiceTestHarness.Build(db, http);

        var dto = await service.CreateAsync(BalancedRequest(new DateTime(2026, 6, 15)), "tester", AcctSet);

        var saved = db.Set<FinVoucher>().Single(v => v.FID == dto.Id);
        Assert.Equal(11, saved.FPeriodId);       // 期间按日期解析
        Assert.Equal(AcctSet, saved.FAccountSetId); // 账套落库
        Assert.Equal(Org, saved.FOrgId);
    }

    [Fact]
    public async Task Create_rejects_when_no_period_for_date()
    {
        await using var db = TestDbContextFactory.Create(nameof(Create_rejects_when_no_period_for_date), Org);
        await SeedAsync(db, periodNo: 6);
        var http = VoucherServiceTestHarness.HttpContext(Org, AcctSet);
        var service = VoucherServiceTestHarness.Build(db, http);

        var ex = await Assert.ThrowsAsync<InvalidOperationException>(
            () => service.CreateAsync(BalancedRequest(new DateTime(2026, 7, 1)), "tester", AcctSet));
        Assert.Contains("未找到", ex.Message);
        Assert.Empty(db.Set<FinVoucher>()); // 拒绝落库
    }

    [Fact]
    public async Task Create_rejects_when_target_period_closed()
    {
        await using var db = TestDbContextFactory.Create(nameof(Create_rejects_when_target_period_closed), Org);
        await SeedAsync(db, periodNo: 6, isClosed: 1);
        var http = VoucherServiceTestHarness.HttpContext(Org, AcctSet);
        var service = VoucherServiceTestHarness.Build(db, http);

        var ex = await Assert.ThrowsAsync<InvalidOperationException>(
            () => service.CreateAsync(BalancedRequest(new DateTime(2026, 6, 15)), "tester", AcctSet));
        Assert.Contains("已结账", ex.Message);
        Assert.Empty(db.Set<FinVoucher>());
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServicePeriodTests"`
Expected: FAIL（当前 CreateAsync 不解析期间，`Create_resolves...` 断言 FPeriodId==11 失败为 0；结账/缺账期未拒绝）。

- [ ] **Step 3: 实现 `ResolvePeriodAsync` 并在 CreateAsync 接线**

在 `VoucherService.cs` 内新增私有方法（放在 `GetNextNumberAsync` 附近）：

```csharp
    /// <summary>
    /// 解析凭证期间：按 日期+账套 定位；查不到抛错。复用 VoucherPostingRules 纯规则。
    /// </summary>
    private async Task<FinAccountPeriod> ResolvePeriodAsync(DateTime date, long accountSetId)
    {
        var candidates = await _periodRepository.Query()
            .Where(p => p.FAccountSetId == accountSetId)
            .ToListAsync();
        return VoucherPostingRules.ResolvePeriod(candidates, date, accountSetId);
    }
```

在 `CreateAsync` 中，将原 `var nextNo = await GetNextNumberAsync(request.VoucherWord, request.PeriodId, accountSetId);` 这一段**之前**插入期间解析，并改用解析结果。具体改法：把方法体开头

```csharp
        ValidateVoucher(request);
        await ValidateEntriesAsync(request.Entries, enforceAuxContract);

        // 验证辅助核算JSON格式
        foreach (var entry in request.Entries)
            ValidateAuxiliaryJson(entry.AuxiliaryJson);

        var nextNo = await GetNextNumberAsync(request.VoucherWord, request.PeriodId, accountSetId);
```

替换为：

```csharp
        ValidateVoucher(request);
        await ValidateEntriesAsync(request.Entries, enforceAuxContract);

        // 验证辅助核算JSON格式
        foreach (var entry in request.Entries)
            ValidateAuxiliaryJson(entry.AuxiliaryJson);

        // 期间解析 + 结账校验（后端权威：PeriodId<=0 时按日期解析；已结账期拒绝）
        long periodId = request.PeriodId;
        if (periodId <= 0)
        {
            var period = await ResolvePeriodAsync(request.Date, accountSetId);
            VoucherPostingRules.EnsureOpenForPosting(period);
            periodId = period.FID;
        }

        var nextNo = await GetNextNumberAsync(request.VoucherWord, periodId, accountSetId);
```

并把后面 `FPeriodId = request.PeriodId,` 改为 `FPeriodId = periodId,`。

> 文件顶部已 `using Microsoft.EntityFrameworkCore;`（现有），`VoucherPostingRules` 同命名空间无需 using。

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServicePeriodTests"`
Expected: PASS（3 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherService.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs
git commit -m "feat(finance): CreateAsync 按日期解析期间并校验结账,缺账期/已结账拒绝落库(TDD)"
```

### Task 2.3：UpdateAsync 期间重解析 + 分录补 FOrgId/FDataScopeId

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/VoucherService.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs`

- [ ] **Step 1: 追加失败测试**

在 `VoucherServicePeriodTests` 类内追加：

```csharp
    [Fact]
    public async Task Update_reresolves_period_and_tags_entries_with_org()
    {
        await using var db = TestDbContextFactory.Create(nameof(Update_reresolves_period_and_tags_entries_with_org), Org);
        // 5月、6月两个开放期间
        db.Set<FinAccount>().AddRange(
            VoucherServiceTestHarness.Account(1, "1001", "库存现金", AcctSet, Org),
            VoucherServiceTestHarness.Account(2, "3001", "实收资本", AcctSet, Org));
        db.Set<FinAccountPeriod>().AddRange(
            VoucherServiceTestHarness.Period(10, 2026, 5, AcctSet),
            VoucherServiceTestHarness.Period(11, 2026, 6, AcctSet));
        await db.SaveChangesAsync();
        var http = VoucherServiceTestHarness.HttpContext(Org, AcctSet);
        var service = VoucherServiceTestHarness.Build(db, http);

        // 先在 5 月建单
        var created = await service.CreateAsync(BalancedRequest(new DateTime(2026, 5, 10)), "tester", AcctSet);
        Assert.Equal(10, db.Set<FinVoucher>().Single(v => v.FID == created.Id).FPeriodId);

        // 改日期到 6 月并更新
        var updateReq = BalancedRequest(new DateTime(2026, 6, 20));
        await service.UpdateAsync(created.Id, updateReq, "modifier");

        var saved = db.Set<FinVoucher>().Single(v => v.FID == created.Id);
        Assert.Equal(11, saved.FPeriodId); // 期间随日期重解析到 6 月
        var entries = db.Set<FinVoucherEntry>().Where(e => e.FVoucherId == created.Id).ToList();
        Assert.NotEmpty(entries);
        Assert.All(entries, e => Assert.Equal(Org, e.FOrgId)); // 分录补组织标记
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~Update_reresolves_period_and_tags_entries_with_org"`
Expected: FAIL（UpdateAsync 现状 `FPeriodId=request.PeriodId`=0；新分录 FOrgId 未设为 0 之外的值）。

- [ ] **Step 3: 实现 UpdateAsync 改造**

在 `UpdateAsync` 中：

1) 将 `voucher.FPeriodId = request.PeriodId;` 替换为期间重解析（放在原有结账校验之后、字段赋值处）：

```csharp
        // 期间随日期重解析（后端权威），并校验目标期间未结账
        long newPeriodId = request.PeriodId;
        if (newPeriodId <= 0)
        {
            var newPeriod = await ResolvePeriodAsync(request.Date, voucher.FAccountSetId);
            VoucherPostingRules.EnsureOpenForPosting(newPeriod);
            newPeriodId = newPeriod.FID;
        }
```

并把 `voucher.FPeriodId = request.PeriodId;` 改为 `voucher.FPeriodId = newPeriodId;`。

2) 在「添加新分录」的 `new FinVoucherEntry { ... }` 初始化块中补两个字段（与 CreateAsync 对齐）：

```csharp
                FOrgId = voucher.FOrgId,
                FDataScopeId = request.DataScopeId,
```

（加在 `FCreatedTime = DateTime.Now,` 之前。）

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServicePeriodTests"`
Expected: PASS（4 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherService.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs
git commit -m "feat(finance): UpdateAsync 期间随日期重解析+分录补 FOrgId/FDataScopeId(TDD)"
```

### Task 2.4：SaveDraft/Copy/Reverse 期间解析接线

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/VoucherService.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs`

- [ ] **Step 1: 追加失败测试**

在 `VoucherServicePeriodTests` 类内追加（覆盖 SaveDraft 解析期间、Reverse 落当前期）：

```csharp
    [Fact]
    public async Task SaveDraft_resolves_period_from_date()
    {
        await using var db = TestDbContextFactory.Create(nameof(SaveDraft_resolves_period_from_date), Org);
        await SeedAsync(db, periodNo: 6);
        var http = VoucherServiceTestHarness.HttpContext(Org, AcctSet);
        var service = VoucherServiceTestHarness.Build(db, http);

        var dto = await service.SaveDraftAsync(BalancedRequest(new DateTime(2026, 6, 15)), "tester", AcctSet);

        Assert.Equal(11, db.Set<FinVoucher>().Single(v => v.FID == dto.Id).FPeriodId);
        Assert.Equal(0, db.Set<FinVoucher>().Single(v => v.FID == dto.Id).FStatus); // 草稿
    }
```

> 注：`CopyAsync/ReverseAsync` 用 `DateTime.Today` 解析期间，单测依赖当天落在已建期间内，故不在单测固定断言其期间值；其正确性由真库回归覆盖（阶段 6）。本任务仅确保 Copy/Reverse 在「当天无对应期间」时按规则抛错不崩库。

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~SaveDraft_resolves_period_from_date"`
Expected: FAIL（SaveDraftAsync 现状用 request.PeriodId=0）。

- [ ] **Step 3: 实现 SaveDraft/Copy/Reverse 接线**

1) `SaveDraftAsync`：把开头

```csharp
        var nextNo = await GetNextNumberAsync(request.VoucherWord, request.PeriodId, accountSetId);
```

替换为（草稿解析期间但不做结账硬拒绝）：

```csharp
        long periodId = request.PeriodId;
        if (periodId <= 0)
        {
            var period = await ResolvePeriodAsync(request.Date, accountSetId);
            periodId = period.FID;
        }
        var nextNo = await GetNextNumberAsync(request.VoucherWord, periodId, accountSetId);
```

并把 `FPeriodId = request.PeriodId,` 改为 `FPeriodId = periodId,`。

2) `CopyAsync`：把

```csharp
        var nextNo = await GetNextNumberAsync(source.FVoucherWord, source.FPeriodId, source.FAccountSetId);
```

替换为（按今天解析期间 + 结账校验）：

```csharp
        var targetPeriod = await ResolvePeriodAsync(DateTime.Today, source.FAccountSetId);
        VoucherPostingRules.EnsureOpenForPosting(targetPeriod);
        var nextNo = await GetNextNumberAsync(source.FVoucherWord, targetPeriod.FID, source.FAccountSetId);
```

并把新凭证初始化里的 `FPeriodId = source.FPeriodId,` 改为 `FPeriodId = targetPeriod.FID,`。

3) `ReverseAsync`：同样替换其

```csharp
        var nextNo = await GetNextNumberAsync(source.FVoucherWord, source.FPeriodId, source.FAccountSetId);
```

为：

```csharp
        var targetPeriod = await ResolvePeriodAsync(DateTime.Today, source.FAccountSetId);
        VoucherPostingRules.EnsureOpenForPosting(targetPeriod);
        var nextNo = await GetNextNumberAsync(source.FVoucherWord, targetPeriod.FID, source.FAccountSetId);
```

并把新凭证初始化里的 `FPeriodId = source.FPeriodId,` 改为 `FPeriodId = targetPeriod.FID,`。

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServicePeriodTests"`
Expected: PASS（5 passed）。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherService.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServicePeriodTests.cs
git commit -m "feat(finance): SaveDraft/Copy/Reverse 按日期解析期间,复制冲销落当前开放期(TDD)"
```

### Task 2.5：控制器 Create 的 accountSetId 请求头兜底

**Files:**
- Modify: `src/STOTOP.Module.Finance/Controllers/VoucherController.cs`

- [ ] **Step 1: 实现请求头兜底**

在 `VoucherController` 类内新增私有方法：

```csharp
    /// <summary>
    /// 解析当前账套：优先用 query 参数，其次回退到 X-AccountSet-Id 请求头
    /// （权限过滤器读取同一请求头，避免 action 拿到 0 导致 FAccountSetId 落 0）。
    /// </summary>
    private long ResolveAccountSetId(long accountSetId)
    {
        if (accountSetId > 0) return accountSetId;
        var header = Request.Headers["X-AccountSet-Id"].FirstOrDefault();
        return long.TryParse(header, out var id) ? id : 0;
    }
```

在 `Create` action 内，把

```csharp
            var creator = User.FindFirst(ClaimTypes.Name)?.Value ?? "system";
            // 手动录入凭证：强校验科目辅助核算契约(声明的维度必须带齐, E2)
            var result = await _voucherService.CreateAsync(request, creator, accountSetId, enforceAuxContract: true);
```

改为：

```csharp
            var creator = User.FindFirst(ClaimTypes.Name)?.Value ?? "system";
            var effectiveAccountSetId = ResolveAccountSetId(accountSetId);
            // 手动录入凭证：强校验科目辅助核算契约(声明的维度必须带齐, E2)
            var result = await _voucherService.CreateAsync(request, creator, effectiveAccountSetId, enforceAuxContract: true);
```

同样在 `SaveDraft` action 内，把 `accountSetId` 传参改为 `ResolveAccountSetId(accountSetId)`。

- [ ] **Step 2: 编译确认**

Run: `dotnet build src/STOTOP.Module.Finance --arch x64`
Expected: 编译成功（0 error）。

- [ ] **Step 3: Commit**

```bash
git add src/STOTOP.Module.Finance/Controllers/VoucherController.cs
git commit -m "fix(finance): 凭证 Create/SaveDraft 的 accountSetId 请求头兜底,避免落 0"
```

---

## 阶段 3：簇 C 越权隔离 + 权限属性

### Task 3.1：按 id 操作统一加归属过滤

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/VoucherService.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceIsolationTests.cs`

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceIsolationTests.cs`：

```csharp
using Microsoft.EntityFrameworkCore;
using STOTOP.Module.Finance.Entities;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Voucher;

public class VoucherServiceIsolationTests
{
    // 库里有一张属于 组织100/账套7 的凭证（FOrgId 显式=100，不会被 SaveChanges 自动回写，因为非 0）
    private static async Task<long> SeedForeignVoucherAsync(STOTOP.Infrastructure.Data.STOTOPDbContext db)
    {
        var v = new FinVoucher
        {
            FID = 500, FVoucherWord = "记", FVoucherNo = 1, FDate = new DateTime(2026, 6, 1),
            FPeriodId = 11, FStatus = 1, FAccountSetId = 7, FOrgId = 100, FCreator = "owner"
        };
        db.Set<FinVoucher>().Add(v);
        db.Set<FinVoucherEntry>().AddRange(
            new FinVoucherEntry { FID = 1, FVoucherId = 500, FLineNo = 1, FAccountId = 1, FDebitAmount = 100m, FOrgId = 100 },
            new FinVoucherEntry { FID = 2, FVoucherId = 500, FLineNo = 2, FAccountId = 2, FCreditAmount = 100m, FOrgId = 100 });
        await db.SaveChangesAsync();
        return v.FID;
    }

    // 账套维度无全局过滤器：GetById 走 Query() 组织过滤已挡住跨组织，真实缺口是跨账套。
    [Fact]
    public async Task GetById_returns_null_for_other_account_set()
    {
        await using var db = TestDbContextFactory.Create(nameof(GetById_returns_null_for_other_account_set), orgId: 100);
        var id = await SeedForeignVoucherAsync(db);
        // 同组织100（全局过滤器放行），但当前账套 8 ≠ 凭证账套 7
        var http = VoucherServiceTestHarness.HttpContext(orgId: 100, accountSetId: 8);
        var service = VoucherServiceTestHarness.Build(db, http);

        var dto = await service.GetByIdAsync(id);

        Assert.Null(dto); // 越账套：视为不存在
    }

    // Delete 走 GetByIdAsync(FindAsync 绕过全局过滤器)：跨账套缺口。
    [Fact]
    public async Task Delete_rejects_other_account_set_same_org()
    {
        await using var db = TestDbContextFactory.Create(nameof(Delete_rejects_other_account_set_same_org), orgId: 100);
        var id = await SeedForeignVoucherAsync(db);
        var http = VoucherServiceTestHarness.HttpContext(orgId: 100, accountSetId: 8);
        var service = VoucherServiceTestHarness.Build(db, http);

        var ok = await service.DeleteAsync(id);

        Assert.False(ok); // 越账套：视为不存在
        Assert.Single(db.Set<FinVoucher>()); // 未删除（同组织，全局过滤器下可见）
    }

    // Audit 走 GetByIdAsync(FindAsync 绕过全局过滤器)：跨组织缺口。
    [Fact]
    public async Task Audit_rejects_other_org()
    {
        await using var db = TestDbContextFactory.Create(nameof(Audit_rejects_other_org), orgId: 999);
        var id = await SeedForeignVoucherAsync(db);
        // 攻击者上下文：组织 999，与凭证组织 100 不符
        var http = VoucherServiceTestHarness.HttpContext(orgId: 999, accountSetId: 7);
        var service = VoucherServiceTestHarness.Build(db, http);

        var ok = await service.AuditAsync(id, "attacker");

        Assert.False(ok);
        // 跨组织读取需 IgnoreQueryFilters（当前 org=999 过滤器会隐藏 org=100 的凭证）
        Assert.Equal(1, db.Set<FinVoucher>().IgnoreQueryFilters().Single().FStatus); // 仍待审核，未被审核
    }
}
```

> 完整文件即上述代码（已含闭合 `}`）。三个测试分别覆盖：GetById 跨账套、Delete 跨账套、Audit 跨组织——对应 FindAsync 绕过过滤器与账套无过滤两类缺口。

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServiceIsolationTests"`
Expected: FAIL（现状 GetById 不校验账套→返回他账套凭证；Delete 跨账套删除成功；Audit 经 FindAsync 跨组织审核成功）。

- [ ] **Step 3: 实现归属过滤**

在 `VoucherService.cs` 内：

1) 在 `GetCurrentUserId()` 附近新增：

```csharp
    private long GetCurrentAccountSetId()
    {
        var header = _httpContextAccessor.HttpContext?.Request.Headers["X-AccountSet-Id"].FirstOrDefault();
        return long.TryParse(header, out var id) ? id : 0;
    }

    /// <summary>
    /// 按 id 取凭证（含分录），并施加组织/账套归属过滤。无权或不存在均返回 null。
    /// </summary>
    private async Task<FinVoucher?> GetOwnedVoucherAsync(long id, bool includeEntries = false)
    {
        var query = _voucherRepository.Query();
        if (includeEntries) query = query.Include(v => v.Entries);
        var voucher = await query.AsTracking().FirstOrDefaultAsync(v => v.FID == id);
        if (voucher == null) return null;
        var orgId = GetCurrentOrgId();
        var accountSetId = GetCurrentAccountSetId();
        return VoucherPostingRules.IsAccessible(voucher, orgId, accountSetId) ? voucher : null;
    }
```

2) 在以下方法中，把对 `_voucherRepository.GetByIdAsync(id)` 或 `Query().FirstOrDefaultAsync(v => v.FID == id)` 的取数替换为 `GetOwnedVoucherAsync(id, includeEntries: <按需>)`，并保持原有 null 返回语义：

- `GetByIdAsync(long id)`：开头改为 `var voucher = await GetOwnedVoucherAsync(id, includeEntries: true);`（删除原 `_voucherRepository.Query().Include(...).FirstOrDefaultAsync(...)`）。
- `AuditAsync` / `UnAuditAsync` / `DeleteAsync`：把 `var voucher = await _voucherRepository.GetByIdAsync(id);` 改为 `var voucher = await GetOwnedVoucherAsync(id);`。
- `UpdateAsync`：把开头 `var voucher = await _voucherRepository.Query().Include(v => v.Entries).AsTracking().FirstOrDefaultAsync(v => v.FID == id);` 改为 `var voucher = await GetOwnedVoucherAsync(id, includeEntries: true);`。
- `CompleteRecordAsync`：把开头取数改为 `var voucher = await GetOwnedVoucherAsync(id, includeEntries: true);`。
- `CopyAsync` / `ReverseAsync`：把 `var source = await _voucherRepository.Query().Include(v => v.Entries).FirstOrDefaultAsync(v => v.FID == voucherId);` 改为 `var source = await GetOwnedVoucherAsync(voucherId, includeEntries: true);`。
- `BatchAuditAsync`：循环内把 `var voucher = await _voucherRepository.GetByIdAsync(id);` 改为 `var voucher = await GetOwnedVoucherAsync(id);`（取不到 `continue`，自然计入跳过）。

> 这些方法原本都已处理 `voucher == null`（返回 false/Fail/null），归属不符时复用同一分支即可，无需新增语义。

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServiceIsolationTests"`
Expected: PASS（3 passed）。

- [ ] **Step 5: 跑全量凭证测试防回归**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~Voucher"`
Expected: PASS（含 Period/PostingRules/Isolation/Validation 全绿）。

- [ ] **Step 6: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherService.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceIsolationTests.cs
git commit -m "fix(finance): 凭证按id操作统一加组织/账套归属过滤,堵死跨账套IDOR(TDD)"
```

### Task 3.2：控制器补缺失的权限属性

**Files:**
- Modify: `src/STOTOP.Module.Finance/Controllers/VoucherController.cs`

- [ ] **Step 1: 补权限属性**

为以下 action 各加一行 `[RequireAccountSetPermission(...)]`（加在 `[HttpXxx(...)]` 之下、方法签名之上）：

```csharp
    // SaveDraft (draft)
    [RequireAccountSetPermission(AccountSetPermissions.VoucherCreate)]

    // GetDrafts (drafts)
    [RequireAccountSetPermission(AccountSetPermissions.VoucherView)]

    // ReorderNumbers (reorder/{periodId})
    [RequireAccountSetPermission(AccountSetPermissions.VoucherEdit)]

    // GetNextNumber (next-number)
    [RequireAccountSetPermission(AccountSetPermissions.VoucherView)]

    // GetPendingAuditCount (pending-count)
    [RequireAccountSetPermission(AccountSetPermissions.VoucherView)]

    // Copy (copy/{id})
    [RequireAccountSetPermission(AccountSetPermissions.VoucherCreate)]

    // Reverse (reverse/{id})
    [RequireAccountSetPermission(AccountSetPermissions.VoucherCreate)]

    // CheckGap (check-gap)
    [RequireAccountSetPermission(AccountSetPermissions.VoucherView)]

    // ExportTemplate (export-template)
    [RequireAccountSetPermission(AccountSetPermissions.VoucherView)]

    // CompleteRecord ({id}/complete-record)
    [RequireAccountSetPermission(AccountSetPermissions.VoucherCreate)]
```

> 文件已 `using STOTOP.Module.Finance.Filters;` 与 `using STOTOP.Module.Finance.Services;`（含 AccountSetPermissions 常量类所在命名空间 `STOTOP.Module.Finance`）。若 `AccountSetPermissions` 解析不到，在文件顶部补 `using STOTOP.Module.Finance;`。
> 注意：`Copy`/`Reverse`/`CheckGap`/`ReorderNumbers`/`CompleteRecord`/`NextNumber`/`pending-count` 这些端点的 action 参数里没有 `accountSetId`，过滤器会从 `X-AccountSet-Id` 请求头取（前端已注入），故权限校验可正常工作。

- [ ] **Step 2: 编译确认**

Run: `dotnet build src/STOTOP.Module.Finance --arch x64`
Expected: 编译成功（0 error）。

- [ ] **Step 3: Commit**

```bash
git add src/STOTOP.Module.Finance/Controllers/VoucherController.cs
git commit -m "fix(finance): 补凭证 Copy/Reverse/Draft/Reorder/NextNumber 等10端点的账套权限属性"
```

---

## 阶段 4：簇 B 写入事务原子化

### Task 4.1：写入方法用显式事务包裹 + 事件移到 Commit 后

**Files:**
- Modify: `src/STOTOP.Module.Finance/Services/VoucherService.cs`
- Test: `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTransactionTests.cs`

- [ ] **Step 1: 写失败测试（Sqlite 真事务）**

创建 `tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTransactionTests.cs`：

```csharp
using Microsoft.Data.Sqlite;
using Microsoft.EntityFrameworkCore;
using STOTOP.Core.Interfaces;
using STOTOP.Core.Services;
using STOTOP.Infrastructure.Data;
using STOTOP.Module.Finance.Dtos;
using STOTOP.Module.Finance.Entities;
using Xunit;

namespace STOTOP.Module.Finance.Tests.Voucher;

public class VoucherServiceTransactionTests
{
    private const long Org = 100;
    private const long AcctSet = 7;

    // 包装真实分录仓储，在第 N 次 AddAsync 时抛错，模拟“分录写入中途失败”
    private sealed class ThrowOnNthEntryRepository : IRepository<FinVoucherEntry>
    {
        private readonly IRepository<FinVoucherEntry> _inner;
        private readonly int _throwOn;
        private int _count;
        public ThrowOnNthEntryRepository(IRepository<FinVoucherEntry> inner, int throwOn) { _inner = inner; _throwOn = throwOn; }
        public Task<FinVoucherEntry> AddAsync(FinVoucherEntry entity)
        {
            if (++_count == _throwOn) throw new InvalidOperationException("模拟分录写入失败");
            return _inner.AddAsync(entity);
        }
        public Task<FinVoucherEntry?> GetByIdAsync(long id) => _inner.GetByIdAsync(id);
        public Task<List<FinVoucherEntry>> GetAllAsync() => _inner.GetAllAsync();
        public Task UpdateAsync(FinVoucherEntry entity) => _inner.UpdateAsync(entity);
        public Task DeleteAsync(long id) => _inner.DeleteAsync(id);
        public IQueryable<FinVoucherEntry> Query() => _inner.Query();
    }

    private static STOTOPDbContext CreateSqliteDb(SqliteConnection conn)
    {
        STOTOPDbContext.RegisterModuleAssembly(typeof(FinAmoebaPLTemplate).Assembly);
        var options = new DbContextOptionsBuilder<STOTOPDbContext>()
            .UseSqlite(conn)
            .Options;
        var db = new STOTOPDbContext(options, new SqliteOrgAccessor(Org));
        db.Database.EnsureCreated();
        return db;
    }

    private sealed class SqliteOrgAccessor(long orgId) : IOrgContextAccessor
    {
        public long? CurrentOrgId { get; set; } = orgId;
    }

    [Fact]
    public async Task Create_rolls_back_header_when_entry_write_fails()
    {
        using var conn = new SqliteConnection("DataSource=:memory:");
        conn.Open();
        await using var db = CreateSqliteDb(conn);

        db.Set<FinAccount>().AddRange(
            VoucherServiceTestHarness.Account(1, "1001", "库存现金", AcctSet, Org),
            VoucherServiceTestHarness.Account(2, "3001", "实收资本", AcctSet, Org));
        db.Set<FinAccountPeriod>().Add(VoucherServiceTestHarness.Period(11, 2026, 6, AcctSet));
        await db.SaveChangesAsync();

        var http = VoucherServiceTestHarness.HttpContext(Org, AcctSet);
        // 用会在第 2 条分录抛错的仓储替换 entryRepository
        var opLog = new STOTOP.Module.Finance.Services.OperationLogService(
            new STOTOP.Infrastructure.Repositories.Repository<FinOperationLog>(db), http,
            Microsoft.Extensions.Logging.Abstractions.NullLogger<STOTOP.Module.Finance.Services.OperationLogService>.Instance);
        var changeTracking = new STOTOP.Module.Finance.Services.ChangeTrackingService(
            new STOTOP.Infrastructure.Repositories.Repository<FinChangeHistory>(db), http);
        var service = new STOTOP.Module.Finance.Services.VoucherService(
            new STOTOP.Infrastructure.Repositories.Repository<FinVoucher>(db),
            new ThrowOnNthEntryRepository(new STOTOP.Infrastructure.Repositories.Repository<FinVoucherEntry>(db), throwOn: 2),
            new STOTOP.Infrastructure.Repositories.Repository<FinAccount>(db),
            new STOTOP.Infrastructure.Repositories.Repository<FinAccountPeriod>(db),
            opLog, changeTracking, http,
            new VoucherServiceTestHarness.NoOpEventDispatcher(), db);

        var request = new CreateVoucherRequest
        {
            VoucherWord = "记", Date = new DateTime(2026, 6, 15), PeriodId = 0,
            Entries =
            {
                new CreateVoucherEntryRequest { LineNo = 1, Summary = "t", AccountId = 1, DebitAmount = 100m },
                new CreateVoucherEntryRequest { LineNo = 2, Summary = "t", AccountId = 2, CreditAmount = 100m },
            }
        };

        await Assert.ThrowsAsync<InvalidOperationException>(() => service.CreateAsync(request, "tester", AcctSet));

        // 关键断言：分录写入失败后，凭证头不得残留（事务整体回滚）
        Assert.Empty(db.Set<FinVoucher>());
        Assert.Empty(db.Set<FinVoucherEntry>());
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServiceTransactionTests"`
Expected: FAIL（当前无事务，凭证头已 SaveChanges 提交，`Assert.Empty(FinVoucher)` 失败——库里残留 1 张脏头）。
> 若 `EnsureCreated()` 因模型含 SQL Server 专属配置而抛错，则记录具体报错交由执行者处理（多为某列默认值/计算列），不属本测试逻辑问题。

- [ ] **Step 3: 实现事务包裹**

在 `CreateAsync` 中，把「`await _voucherRepository.AddAsync(voucher);` 起，到 `var createdVoucher = await GetByIdAsync(...)` 与 `_operationLogService.LogAsync(...)`」这段写入用事务包裹，并把事件派发移到事务提交之后。改造模板：

```csharp
        await using var tx = await _context.Database.BeginTransactionAsync();
        try
        {
            await _voucherRepository.AddAsync(voucher);

            foreach (var entryRequest in request.Entries)
            {
                var account = await _accountRepository.GetByIdAsync(entryRequest.AccountId);
                var entry = new FinVoucherEntry { /* ...原有字段不变... */ };
                await _entryRepository.AddAsync(entry);
            }

            await _operationLogService.LogAsync(
                accountSetId, "凭证", "新增",
                $"新增凭证 {voucher.FVoucherWord}{voucher.FVoucherNo}",
                voucher.FID, $"{voucher.FVoucherWord}{voucher.FVoucherNo}");

            await tx.CommitAsync();
        }
        catch
        {
            await tx.RollbackAsync();
            throw;
        }

        var createdVoucher = await GetByIdAsync(voucher.FID) ?? throw new InvalidOperationException("创建凭证失败");

        // 凭证状态为待审核时发布事件（移到事务提交之后）
        if (voucher.FStatus == 1)
        {
            var currentUserId = GetCurrentUserId();
            await _eventDispatcher.PublishAsync(new VoucherPendingAuditEvent { /* ...原有字段不变... */ });
        }

        return createdVoucher;
```

对 `UpdateAsync`、`SaveDraftAsync`、`CopyAsync`、`ReverseAsync` 同样用 `BeginTransactionAsync` 把「头 + 全部分录 + 操作日志/变更追踪」包成单一事务，`catch` 回滚后 rethrow，读回/返回放提交之后。

> 前提（已在 spec 确认）：DbContext 与 Repository 均 Scoped，同请求共享同一 DbContext，多次 SaveChanges 落入同一显式事务，可整体回滚。

- [ ] **Step 4: 运行确认通过**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~VoucherServiceTransactionTests"`
Expected: PASS（1 passed；回滚后库内无脏凭证）。

- [ ] **Step 5: 跑全量凭证测试防回归**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64 --filter "FullyQualifiedName~Voucher"`
Expected: PASS（全绿）。

- [ ] **Step 6: Commit**

```bash
git add src/STOTOP.Module.Finance/Services/VoucherService.cs tests/STOTOP.Module.Finance.Tests/Voucher/VoucherServiceTransactionTests.cs
git commit -m "fix(finance): 凭证 Create/Update/Draft/Copy/Reverse 写入用显式事务,失败整体回滚,事件移到提交后(TDD)"
```

---

## 阶段 5：前端配套（A6 日期→期间回写）

### Task 5.1：`VoucherEntry.vue` 按日期回写 form.periodId

**Files:**
- Modify: `web/src/views/finance/VoucherEntry.vue`

- [ ] **Step 1: 实现日期→期间回写**

在 `VoucherEntry.vue` 的 `<script setup>` 中，定位已有的 `watch(() => form.value.date, ...)`（约 786 行，更新 currentYear/currentPeriod 的那个）。在其回调体末尾追加按 `periodList` 匹配回写 `form.periodId` 的逻辑：

```ts
    // 按日期在已加载的期间列表中定位并回写 periodId（用于取号预览与"期"显示）
    const matched = periodList.value.find(p => {
      const start = new Date(p.startDate)
      const end = new Date(p.endDate)
      return date >= start && date <= end
    })
    form.value.periodId = matched ? matched.id : null
```

> `periodList` 元素结构为 `{ id, startDate, endDate, isClosed }`（见 `loadPeriods`）。`form.periodId` 现有类型为 `number | null`，赋 null 兼容。

- [ ] **Step 2: 前端校验（type-check + build 兜底）**

Run: `cd web; npx vue-tsc --noEmit -p tsconfig.json 2>&1 | Select-String "VoucherEntry"`
Expected: 与本文件相关的新增报错为 0（注意：仓库 type-check 基线本就大量报错，只看 VoucherEntry.vue 是否引入新错）。

Run: `cd web; npx vite build`
Expected: build 成功。

- [ ] **Step 3: Commit**

```bash
git add web/src/views/finance/VoucherEntry.vue
git commit -m "fix(finance-web): 凭证录入按日期回写 periodId,修正取号预览与期间显示"
```

---

## 阶段 6：整体回归与真库验证

### Task 6.1：全量后端测试 + 构建

**Files:** 无（验证任务）

- [ ] **Step 1: 跑 Finance 模块全量测试**

Run: `dotnet test tests/STOTOP.Module.Finance.Tests --arch x64`
Expected: 全绿（含既有 Amoeba/Accounts/Voucher 测试，确认无回归）。

- [ ] **Step 2: 构建受影响 src 项目**

Run: `dotnet build src/STOTOP.Module.Finance --arch x64`
Expected: 0 error。

- [ ] **Step 3: 回归 CardFlow 自动凭证（Bridge 兼容）**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: 全绿（确认 CardFlowVoucherBridge 走 CreateAsync 传合法 periodId 不受新逻辑影响）。

### Task 6.2：真库回归清单（手工，对照 spec §5）

**Files:** 无（手工验证，按记忆 `shentong-hq-tx-import-test` 的测试库连法/JWT 登录）

- [ ] **Step 1: 建单→可见**：在某开放期建一张手工凭证，确认列表「按账期」筛选可见，库内 `FPeriodId/FAccountSetId/FOrgId` 均非 0。
- [ ] **Step 2: 凭证号按期重置**：在两个不同月份各建单，确认凭证号各自从 1 起、互不连号；断号检查能覆盖。
- [ ] **Step 3: 结账保护**：将某期间结账后，尝试在该期建单/把已有凭证日期改入该期 → 后端拒绝。
- [ ] **Step 4: 跨月冲销落当前期**：对上月已审核凭证冲销，确认红字单 `FDate=今天`、`FPeriodId=当前开放期`。
- [ ] **Step 5: 越权验证**：用仅对账套 A 有权的账号，构造调用 B 账套某凭证 id 的 GET/审核/删除请求 → 返回「不存在/无权」，数据不变。
- [ ] **Step 6: 记录结果**：把验证结论补记到 spec 或本计划末尾，必要时更新记忆。

---

## 自检（写计划者已核对）

- **spec 覆盖**：簇 A（ResolvePeriod/结账校验/accountSetId 兜底/UpdateAsync 期间重解析+分录补 FOrgId/Copy-Reverse 落当前期/前端回写）= 阶段 1-2、5；簇 B（事务）= 阶段 4；簇 C（IDOR 归属过滤 + 10 端点权限）= 阶段 3。spec §6 八条验收均有对应任务（落库正确=2.2；凭证号按期=6.2-2；结账保护=2.2/2.3；复制冲销落当前期=2.4；事务回滚=4.1；越权拒绝=3.1/3.2；分录补 FOrgId=2.3；测试通过=6.1）。
- **不含项**：外币（簇 D）、职责分离、冲销前置校验均未出现在任务中（符合 spec §4.3/§5 范围）。
- **类型一致性**：`VoucherPostingRules.ResolvePeriod/EnsureOpenForPosting/IsAccessible` 签名在规则定义任务与 service 接线任务中一致；`GetOwnedVoucherAsync(id, includeEntries)` 签名在定义与各调用点一致；harness `Build(db, http)`/`HttpContext(orgId, accountSetId,...)`/`Period(...)`/`Account(...)` 在各测试中一致。
- **占位扫描**：无 TBD/TODO；每个改码步骤含完整代码或精确的「将 X 替换为 Y」指令。事务包裹任务用 `/* ...原有字段不变... */` 指代不变的实体初始化块，属「保持原样」明确指令而非占位。
