# 工作台 CardFlow 卡片活动时间轴 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在工作台右栏 `WorkHubDetail` 为 CardFlow 工作项内嵌一条"审批进度 × 操作日志"去重合并、按时间升序串联的活动时间轴，减少跳完整页。

**Architecture:** 纯函数 `cardActivity.ts` 负责把 stageInstances/auditTrail/actionLog 合并去重为 `ActivityEvent[]`；`CardActivityTimeline.vue` 按 cardId 取数（并行 + 小缓存）并渲染时间轴 + 当前待审 footer；`WorkHubDetail.vue` 仅对 `source==='cardflow'` 工作项嵌入它。

**Tech Stack:** Vue 3 `<script setup>` + Ant Design Vue + dayjs；CardFlow API（getCard/getCardLogs）。

## 验证方式（本仓库特例）

无前端单测运行器、且约定不引入测试框架（见记忆「前端 type-check 基线红」）。每个任务以下替代 TDD：
- **编译门禁**：`cd web && npm run build`（vite/esbuild）。
- **样式门禁**（改 `<style>` 的任务）：`cd web && npm run lint:style`（stylelint 禁裸 hex；既有历史告警不计，只要本次不新增 error）。
- **手动验证**：preview 工作流（用户已登录浏览器）。

命令默认在 `web/` 目录执行。提交信息以 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 结尾。当前分支 master（已获用户同意在 master 提交）；每个任务只 `git add` 本任务文件，勿 `git add -A`。

---

### Task 1: 合并去重纯函数 cardActivity.ts

**Files:**
- Create: `web/src/components/workhub/cardActivity.ts`

> 字段名依据 `web/src/types/cardflow.ts`：`StageInstanceDto`(stageName/type/status/round/activatedTime/completedTime/opinion/assignees) · `AssigneeDto`(userName/status/opinion/completedTime) · `CardFlowRuntimeAuditDto`(snapshotType/stageName/title/reason/eventTime) · `ActionLogDto`(actionType/operatorName/operationTime/opinion)。

- [ ] **Step 1: 写纯函数模块**

```ts
import type {
  StageInstanceDto,
  CardFlowRuntimeAuditDto,
  ActionLogDto,
} from '@/types/cardflow'

export type ActivityKind = 'op' | 'decision' | 'node-enter' | 'system'

export interface ActivityEvent {
  time: string
  kind: ActivityKind
  title: string
  actor?: string
  node?: string
  opinion?: string
  status?: string
}

export interface PendingStage {
  stageName: string
  assignees: string[]
}

// 操作日志中属于"审批决策"的动作类型——决策统一由审批节点出，故在操作日志里过滤掉以去重。
// 若后端新增其它决策类动作，加到此集合即可。
export const DECISION_ACTION_TYPES = new Set(['approve', 'reject'])

const ACTION_TEXT: Record<string, string> = {
  create: '创建草稿',
  submit: '提交审批',
  approve: '审批通过',
  reject: '退回',
  withdraw: '撤回',
  resubmit: '重新提交',
  void: '废除',
  resume: '恢复',
  countersign: '加签',
  transfer: '转交',
  cc: '抄送',
  urge: '催办',
  autoComplete: '自动完成',
}

export function actionText(action: string): string {
  return ACTION_TEXT[action] || action
}

const STATUS_LABEL: Record<string, string> = {
  approved: '已通过',
  rejected: '已退回',
  processing: '处理中',
  pending: '待处理',
  skipped: '已跳过',
}

export function statusLabel(status: string): string {
  return STATUS_LABEL[status] || status
}

function ts(v?: string | null): number {
  if (!v) return NaN
  const t = new Date(v).getTime()
  return Number.isNaN(t) ? NaN : t
}

/** 合并三源为去重的活动事件，按时间升序（发起在前、最新在后） */
export function buildActivityEvents(
  stageInstances: StageInstanceDto[] = [],
  auditTrail: CardFlowRuntimeAuditDto[] = [],
  logs: ActionLogDto[] = [],
): ActivityEvent[] {
  const events: ActivityEvent[] = []

  // 1. 操作事件（排除 approve/reject，避免与节点决策重复）
  for (const log of logs) {
    if (DECISION_ACTION_TYPES.has(log.actionType)) continue
    events.push({
      time: log.operationTime,
      kind: 'op',
      title: actionText(log.actionType),
      actor: log.operatorName || '系统',
      opinion: log.opinion ?? undefined,
    })
  }

  // 2/3. 节点进入 + 审批决策
  for (const s of stageInstances) {
    if (s.activatedTime) {
      events.push({
        time: s.activatedTime,
        kind: 'node-enter',
        title: `进入「${s.stageName}」`,
        node: s.stageName,
      })
    }
    const decided = (s.assignees || []).filter((a) => a.completedTime)
    if (decided.length > 0) {
      for (const a of decided) {
        events.push({
          time: a.completedTime as string,
          kind: 'decision',
          title: statusLabel(a.status),
          actor: a.userName,
          node: s.stageName,
          status: a.status,
          opinion: a.opinion ?? undefined,
        })
      }
    } else if (s.completedTime) {
      // 自动节点（无 assignees）：以节点完成兜底，不丢自动完成
      events.push({
        time: s.completedTime,
        kind: 'decision',
        title: s.status === 'approved' ? '自动完成' : statusLabel(s.status),
        node: s.stageName,
        status: s.status,
        opinion: s.opinion ?? undefined,
      })
    }
  }

  // 4. 系统轨迹（条件流转 / 动态审批）
  for (const a of auditTrail) {
    events.push({
      time: a.eventTime,
      kind: 'system',
      title: a.title || a.reason,
      node: a.stageName ?? undefined,
    })
  }

  // 5. 升序排序，丢弃无有效时间
  return events
    .filter((e) => !Number.isNaN(ts(e.time)))
    .sort((a, b) => ts(a.time) - ts(b.time))
}

/** 当前待审节点（pending/processing），用于底部高亮 footer */
export function buildPendingStages(stageInstances: StageInstanceDto[] = []): PendingStage[] {
  return stageInstances
    .filter((s) => s.status === 'pending' || s.status === 'processing')
    .map((s) => ({
      stageName: s.stageName,
      assignees: (s.assignees || [])
        .filter((a) => !a.completedTime)
        .map((a) => a.userName)
        .filter(Boolean),
    }))
}
```

- [ ] **Step 2: 编译门禁**

Run: `cd web && npm run build`
Expected: 构建成功。注意：此文件此刻无 importer，vite tree-shaking 不会编译它（build 通过≠校验到它），真正编译校验在 Task 2 被 import 时发生；本步仅确认未引入回归，并肉眼核对类型导入与字段名。

- [ ] **Step 3: Commit**

```bash
git add web/src/components/workhub/cardActivity.ts
git commit -m "feat(workhub): 新增 cardActivity 合并去重纯函数(审批进度×操作日志→活动事件)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: CardActivityTimeline 组件

**Files:**
- Create: `web/src/components/workhub/CardActivityTimeline.vue`

- [ ] **Step 1: 写组件 `<script setup>` + `<template>` + `<style>`**

```vue
<script setup lang="ts">
/**
 * CardActivityTimeline.vue — CardFlow 卡片活动时间轴
 *
 * 按 cardId 并行拉取卡片详情（审批进度）与操作日志，合并去重为统一时间轴，
 * 底部高亮当前待审节点。供工作台右栏 WorkHubDetail 内嵌（source='cardflow'）。
 */
import { ref, watch } from 'vue'
import {
  CheckOutlined,
  CloseOutlined,
  ClockCircleOutlined,
  ArrowDownOutlined,
  BranchesOutlined,
  FlagOutlined,
} from '@ant-design/icons-vue'
import dayjs from 'dayjs'
import { getCard, getCardLogs } from '@/api/cardflow'
import type { CardDetailDto, ActionLogDto } from '@/types/cardflow'
import {
  buildActivityEvents,
  buildPendingStages,
  type ActivityEvent,
  type PendingStage,
} from './cardActivity'

const props = defineProps<{ cardId: number }>()

const loading = ref(false)
const events = ref<ActivityEvent[]>([])
const pendingStages = ref<PendingStage[]>([])

// 模块级小缓存：避免 J/K 来回切重复拉（预览态可接受短暂陈旧）
const cache = new Map<number, { events: ActivityEvent[]; pending: PendingStage[] }>()

async function load(id: number) {
  if (!id) {
    events.value = []
    pendingStages.value = []
    return
  }
  const cached = cache.get(id)
  if (cached) {
    events.value = cached.events
    pendingStages.value = cached.pending
    return
  }
  loading.value = true
  try {
    const [detailRes, logsRes] = await Promise.all([
      getCard(id).catch(() => null),
      getCardLogs(id).catch(() => [] as ActionLogDto[]),
    ])
    const detail = detailRes as CardDetailDto | null
    const evs = buildActivityEvents(
      detail?.stageInstances || [],
      detail?.auditTrail || [],
      (logsRes as ActionLogDto[]) || [],
    )
    const pend = buildPendingStages(detail?.stageInstances || [])
    events.value = evs
    pendingStages.value = pend
    cache.set(id, { events: evs, pending: pend })
  } finally {
    loading.value = false
  }
}

function fmtTime(v?: string) {
  return v ? dayjs(v).format('MM-DD HH:mm') : ''
}

function iconFor(ev: ActivityEvent) {
  if (ev.kind === 'decision') {
    return ev.status === 'rejected' ? CloseOutlined : CheckOutlined
  }
  if (ev.kind === 'node-enter') return ArrowDownOutlined
  if (ev.kind === 'system') return BranchesOutlined
  return FlagOutlined
}

watch(() => props.cardId, (id) => load(id), { immediate: true })
</script>

<template>
  <div class="card-activity">
    <div class="ca-section-label">活动 · 审批进度与操作日志</div>

    <div v-if="loading" class="ca-state"><a-spin size="small" /></div>

    <template v-else>
      <div v-if="events.length === 0 && pendingStages.length === 0" class="ca-state">
        暂无活动记录
      </div>

      <div v-else class="ca-timeline">
        <div
          v-for="(ev, idx) in events"
          :key="idx"
          class="ca-item"
          :class="`ca-item--${ev.kind}`"
        >
          <span class="ca-dot" :class="ev.status ? `ca-dot--${ev.status}` : ''"></span>
          <div class="ca-body">
            <div class="ca-line">
              <component :is="iconFor(ev)" class="ca-icon" />
              <span class="ca-title">{{ ev.title }}</span>
              <span v-if="ev.actor" class="ca-actor">· {{ ev.actor }}</span>
              <span v-if="ev.node && ev.kind === 'decision'" class="ca-node">（{{ ev.node }}）</span>
              <span v-if="ev.kind === 'system'" class="ca-sys-tag">系统</span>
            </div>
            <div v-if="ev.opinion" class="ca-opinion">意见：{{ ev.opinion }}</div>
            <div class="ca-time">{{ fmtTime(ev.time) }}</div>
          </div>
        </div>

        <!-- 当前待审 -->
        <div
          v-for="(p, idx) in pendingStages"
          :key="`p-${idx}`"
          class="ca-item ca-item--pending"
        >
          <span class="ca-dot ca-dot--pending-now"></span>
          <div class="ca-body">
            <div class="ca-line">
              <ClockCircleOutlined class="ca-icon ca-icon--pending" />
              <span class="ca-title ca-title--pending">等待「{{ p.stageName }}」审批</span>
            </div>
            <div v-if="p.assignees.length" class="ca-pending-assignees">
              审批人：{{ p.assignees.join('、') }}
            </div>
          </div>
        </div>
      </div>
    </template>
  </div>
</template>

<style scoped>
.card-activity {
  padding: 4px 0;
}
.ca-section-label {
  font-size: 12px;
  color: var(--text-3);
  margin: 0 0 8px;
}
.ca-state {
  padding: 16px 0;
  text-align: center;
  color: var(--text-3);
  font-size: 13px;
}
.ca-timeline {
  position: relative;
  padding-left: 18px;
}
.ca-timeline::before {
  content: '';
  position: absolute;
  left: 4px;
  top: 4px;
  bottom: 10px;
  width: 1.5px;
  background: var(--border);
}
.ca-item {
  position: relative;
  margin-bottom: 14px;
}
.ca-item:last-child {
  margin-bottom: 0;
}
.ca-dot {
  position: absolute;
  left: -18px;
  top: 3px;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--bg-card);
  border: 2px solid var(--border-strong);
}
.ca-dot--approved {
  background: var(--color-success);
  border-color: var(--color-success);
}
.ca-dot--rejected {
  background: var(--color-danger);
  border-color: var(--color-danger);
}
.ca-dot--pending-now {
  background: var(--color-info);
  border-color: var(--color-info);
}
.ca-body {
  min-width: 0;
}
.ca-line {
  display: flex;
  align-items: center;
  gap: 5px;
  flex-wrap: wrap;
  font-size: 13px;
  color: var(--text-1);
}
.ca-icon {
  font-size: 13px;
  color: var(--text-3);
}
.ca-actor {
  color: var(--text-2);
}
.ca-node {
  color: var(--text-3);
  font-size: 12px;
}
.ca-sys-tag {
  font-size: 11px;
  color: var(--text-3);
  background: var(--bg-muted);
  border-radius: var(--radius-md);
  padding: 0 5px;
}
.ca-opinion {
  font-size: 12px;
  color: var(--text-2);
  margin-top: 3px;
  padding: 4px 8px;
  background: var(--bg-muted);
  border-radius: var(--radius-md);
}
.ca-time {
  font-size: 11px;
  color: var(--text-3);
  margin-top: 2px;
}
.ca-item--system .ca-title,
.ca-item--node-enter .ca-title {
  color: var(--text-2);
}
.ca-title--pending {
  color: var(--color-info);
  font-weight: 500;
}
.ca-icon--pending {
  color: var(--color-info);
}
.ca-pending-assignees {
  font-size: 12px;
  color: var(--text-2);
  margin-top: 1px;
}
</style>
```

- [ ] **Step 2: 编译 + 样式门禁**

Run: `cd web && npm run build && npm run lint:style`
Expected: 构建成功（此处 cardActivity.ts 被 import，二者真正编译）；stylelint 对本文件无 error（全用令牌、无裸 hex）。

- [ ] **Step 3: Commit**

```bash
git add web/src/components/workhub/CardActivityTimeline.vue
git commit -m "feat(workhub): 新增 CardActivityTimeline 组件(按 cardId 取数+合并时间轴+待审 footer+缓存)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: WorkHubDetail 集成

**Files:**
- Modify: `web/src/views/workhub/WorkHubDetail.vue`

- [ ] **Step 1: 引入组件 + cardId computed**

在 `<script setup>` 的 import 区（`useWorkHub` import 之后）加入：

```ts
import CardActivityTimeline from '@/components/workhub/CardActivityTimeline.vue'
```

在 `workItem` computed 之后新增 cardId 计算（仅 cardflow 工作项有效）：

```ts
const cardId = computed<number | null>(() => {
  if (workItem.value?.source !== 'cardflow') return null
  const raw = (workItem.value.metadata as any)?.cardId
  const n = Number(raw)
  return Number.isFinite(n) && n > 0 ? n : null
})
```

- [ ] **Step 2: 模板插入时间轴，cardflow 时替代通用元数据区**

将现有"元数据"块（`<div v-if="metadataEntries.length > 0" class="wi-metadata">…</div>`）的开头条件改为同时排除 cardflow：

```html
<div v-if="metadataEntries.length > 0 && !cardId" class="wi-metadata">
```

并在该元数据块**之后、`<!-- 查看完整页面链接 -->` 之前**插入活动时间轴：

```html
<!-- CardFlow 活动时间轴（审批进度 × 操作日志合并） -->
<CardActivityTimeline v-if="cardId" :card-id="cardId" />
```

「查看完整页面链接」块（`<div v-if="workItem.detailRoute" class="wi-link-row">…</div>`）与底部操作按钮区保持不变（兜底跳完整页）。

- [ ] **Step 3: 编译 + 样式门禁**

Run: `cd web && npm run build && npm run lint:style`
Expected: 构建成功；stylelint 对本文件无新增 error（本任务未加新样式）。

- [ ] **Step 4: Commit**

```bash
git add web/src/views/workhub/WorkHubDetail.vue
git commit -m "feat(workhub): 右栏对 CardFlow 工作项内嵌活动时间轴,替代通用元数据区

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 集成手动验证（preview）

**Files:** 无（仅验证）

- [ ] **Step 1: 准备**

确认 dev 服务在跑（前端 9001 已 HMR 本次改动），登录进入工作台 `/workhub`，确保列表有 CardFlow审批 工作项。

- [ ] **Step 2: 验证时间轴**

核对点：
- 点 CardFlow 卡 → 右栏出现「活动 · 审批进度与操作日志」时间轴：发起/提交（op）→ 进入节点 → 审批决策（含节点名 + 意见）→ 系统条件流转（带「系统」标）→ 底部高亮"等待「节点」审批 · 审批人"。
- **通过/驳回不重复出现**（操作日志的 approve/reject 已被过滤，决策只来自节点）。
- 非 cardflow 工作项右栏仍显示通用元数据（未受影响）。

- [ ] **Step 3: 验证取数/缓存/降级**

核对点：
- J/K 切到不同卡 → 时间轴更新；来回切回已看过的卡 → 命中缓存不再请求（网络面板核对仅首次有 `GET /cardflow/cards/{id}` 与 `/logs`）。
- 故意断网或某接口失败 → 不白屏（loading 收起后显示已得到的部分或「暂无活动记录」）。
- 「查看完整页面」仍可跳完整页；底部 通过/驳回 操作正常。

- [ ] **Step 4: 截图留证**

preview 截图右栏时间轴，附完成说明。

---

## Self-Review

**Spec coverage：**
- 去重合并模型（op 过滤 approve/reject、节点进入、节点决策、自动节点兜底、系统轨迹、升序、待审 footer）→ Task 1 ✓
- 组件（按 cardId 并行取数 + 缓存 + 载入/空/错误态 + 渲染）→ Task 2 ✓
- WorkHubDetail 集成（cardId computed、cardflow 替代元数据区、查看完整页面兜底）→ Task 3 ✓
- 令牌/无裸 hex、复用语义 → Task 2 样式 ✓
- 验证（含去重不重复、缓存、降级）→ Task 4 ✓

**Placeholder scan：** 无 TBD/TODO；每个代码步骤含完整代码。

**Type consistency：** `cardActivity.ts` 导出 `buildActivityEvents/buildPendingStages/ActivityEvent/PendingStage/DECISION_ACTION_TYPES/actionText/statusLabel`；Task 2 import 的是 `buildActivityEvents/buildPendingStages/ActivityEvent/PendingStage`，名称一致。`getCard/getCardLogs` 与 `web/src/api/cardflow.ts:219/264` 一致；DTO 字段（stageInstances/auditTrail/assignees.userName/completedTime/auditTrail.eventTime/actionType/operationTime）与 `types/cardflow.ts` 一致。`cardId` 来自 `metadata.cardId`（后端 WorkHubService 注入）。

**风险：** `actionType` 决策集合 `{approve, reject}`；如后端新增决策类动作需补 `DECISION_ACTION_TYPES`。自动节点无 assignees 的完成已在 Task 1 规则兜底。
