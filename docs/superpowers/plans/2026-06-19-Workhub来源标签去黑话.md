# Workhub 来源标签去黑话 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Workhub 工作台的列表卡片、详情、筛选 chip 不再出现 `oa`/`cardflow` 等框架黑话，统一改为按业务类型（费用报销/凭证复核/质量异常/合同到期/任务/通知/积分审批…）展示，并消除 oa 与 cardflow 共用同一蓝色的撞色问题。

**Architecture:** 后端在 `WorkItemDto` 上新增 `BizTypeKey` / `BizTypeLabel` 两个字段，由一个 **纯函数** `WorkHubService.ResolveBizType(WorkItemDto)` 统一派生（走 xUnit TDD），在多源 item 合并后用一个共享的 `EnrichBizTypes` 步骤回填。前端把当前散落在 3 个组件里的 `source → 标签/颜色/图标` 映射表退役，统一渲染后端给的 `bizTypeLabel`，并按 `bizTypeKey` 上色（彻底拆开 oa/cardflow 撞色）。**不改 `source` 字段本身**（详情页时间轴 `cardId` 计算依赖 `source==='cardflow'`，动了会失效）。

**Tech Stack:** 后端 .NET 10 / C# / xUnit 2.9（`dotnet test --arch x64`）；前端 Vue 3 + Pinia + Ant Design Vue（`npm run build` 即 vite build；**无前端单测运行器，前端任务靠 build + 预览验证，勿引入测试框架**）。

---

## 关键现状坐标（来自代码勘察，执行时按此定位；行号可能因改动漂移，以邻近代码为准）

**后端**
- `src/STOTOP.WebAPI/Dtos/WorkHub/WorkItemDto.cs`：`WorkItemDto` L3-21（`Source` L6、`Category` L7、`Metadata` 末尾）；`WorkHubStatsDto` L59-68。
- `src/STOTOP.WebAPI/Services/WorkHubService.cs`：8 个来源映射处 `MapWfWorkItemToDto` L85-117（L113 把 `wf.BizType` 写进 `Metadata["bizType"]`）、`GetCardFlowTodosFromServiceAsync` L492-533（`Source="cardflow"` L504、流程名 `todo.FlowName` L510/L528）、quality L356-392、task L395-425、notification L428-460、contract L463-489、points L535-566、finance L569-601；多源合并后统计 Count 在 L237-246（with-stats 路径）与 L305-314（stats-only 路径）。
- 测试现状：**WorkHub 零测试**；`tests/STOTOP.Module.CardFlow.Tests` 已 `ProjectReference` 了 `STOTOP.WebAPI.csproj`（证明 WebAPI 可被测试工程引用）。本计划新建独立工程 `tests/STOTOP.WebAPI.Tests`（语义正确的归属）。

**前端**（根目录 `web/`）
- `web/src/api/workhub.ts`：`WorkItem` interface L35-60（`source` 字面量联合 L37、`category` L38）。
- `web/src/views/workhub/WorkItemCard.vue`：`sourceConfig` L328-339（oa L329、cardflow L333）；消费 `sourceLabel/sourceColor/sourceIcon` L341-343 → 模板 chip L33-35、左图标 L25-27。**注意 L40/L46/L82-101 有 `item.source==='oa'` 的富渲染分支、L104 `isDataImport = source==='datacenter'`——这些是按 source 的渲染逻辑，保留不动，只改标签来源。**
- `web/src/views/workhub/WorkHubDetail.vue`：`sourceConfig` L94-106（oa L96、cardflow L97、datacenter='CardFlow' L100）；消费 `workItemSourceConfig` L138-141 → 模板 `.wi-source-tag` L328-337。**L131-136 `cardId` 依赖 `source!=='cardflow'`，L379 时间轴依赖之——不要动 source。**
- `web/src/views/workhub/WorkHubCenter.vue`：`statsItems` chip L62-69（key='oa' label='审批' count=stats.approval）；混合列表两张表 `sourceColorMap` L179-188、`sourceLabelMap` L189-198（**无 cardflow 键 → L452 兜底显示英文 "cardflow"**）；chip 渲染 L505-518、筛选 `toggleSourceFilter` L256-267。
- `web/src/stores/theme.ts`：`--biz-approval` ≈ `#5B7290` 定义 L310，被 oa+cardflow 共用；其余 biz token L307-312。
- 构建：`web/package.json` scripts `build`=`vite build`、`type-check`=`vue-tsc -b`（**基线红，只看新增错误，不当门禁**）。

---

## Task 1: 后端 DTO 增加业务类型字段

**Files:**
- Modify: `src/STOTOP.WebAPI/Dtos/WorkHub/WorkItemDto.cs`（`Source`/`Category` 之后）

- [ ] **Step 1: 在 `WorkItemDto` 增加两个字段**

在 `WorkItemDto` 内 `public string Category { get; set; }`（约 L7）之后插入：

```csharp
    /// <summary>业务类型键（用于前端上色/分组，如 "voucher"/"quality"/"flow:费用报销"）。</summary>
    public string BizTypeKey { get; set; } = "approval";

    /// <summary>业务类型中文标签（用户可见，如 "凭证复核"/"质量异常"/"费用报销"）。</summary>
    public string BizTypeLabel { get; set; } = "审批";
```

- [ ] **Step 2: 构建验证编译通过**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj -c Debug`
Expected: Build succeeded, 0 Error。

- [ ] **Step 3: Commit**

```bash
git add src/STOTOP.WebAPI/Dtos/WorkHub/WorkItemDto.cs
git commit -m "feat(workhub): WorkItemDto 增加 BizTypeKey/BizTypeLabel 字段"
```

---

## Task 2: 新建 WebAPI 测试工程

**Files:**
- Create: `tests/STOTOP.WebAPI.Tests/STOTOP.WebAPI.Tests.csproj`
- Modify: `src/STOTOP.WebAPI/STOTOP.WebAPI.csproj`（加 InternalsVisibleTo）
- Modify: 解决方案 `.sln`（加入新工程）

- [ ] **Step 1: 创建测试工程 csproj**（包版本对齐 CardFlow.Tests）

创建 `tests/STOTOP.WebAPI.Tests/STOTOP.WebAPI.Tests.csproj`：

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\STOTOP.WebAPI\STOTOP.WebAPI.csproj" />
  </ItemGroup>

</Project>
```

- [ ] **Step 2: 让 WebAPI 对测试工程开放 internal 成员**

在 `src/STOTOP.WebAPI/STOTOP.WebAPI.csproj` 的第一个 `</PropertyGroup>` 之后加一个 ItemGroup：

```xml
  <ItemGroup>
    <InternalsVisibleTo Include="STOTOP.WebAPI.Tests" />
  </ItemGroup>
```

- [ ] **Step 3: 把测试工程加入解决方案**

先确认解决方案文件名：

Run: `ls *.sln`
Expected: 输出形如 `STOTOP.sln`（以实际为准，下命令替换之）。

Run: `dotnet sln STOTOP.sln add tests/STOTOP.WebAPI.Tests/STOTOP.WebAPI.Tests.csproj`
Expected: `Project ... added to the solution.`

- [ ] **Step 4: 还原 + 编译测试工程**

Run: `dotnet build tests/STOTOP.WebAPI.Tests/STOTOP.WebAPI.Tests.csproj -c Debug`
Expected: Build succeeded, 0 Error（此时工程空、无测试，仅验证引用链通）。

- [ ] **Step 5: Commit**

```bash
git add tests/STOTOP.WebAPI.Tests/ src/STOTOP.WebAPI/STOTOP.WebAPI.csproj STOTOP.sln
git commit -m "test(workhub): 新建 STOTOP.WebAPI.Tests 工程 + InternalsVisibleTo"
```

---

## Task 3: 为 ResolveBizType 写失败测试

**Files:**
- Create: `tests/STOTOP.WebAPI.Tests/WorkHub/ResolveBizTypeTests.cs`

业务类型派生规则（本任务测试即规格）：

| source | category | 取数 | → BizTypeKey | → BizTypeLabel |
|---|---|---|---|---|
| cardflow / datacenter | 任意 | `Metadata["flowName"]` | `flow:{流程名}` | 流程名（如 "费用报销"） |
| cardflow / datacenter | 任意 | flowName 缺失 | `approval` | `审批` |
| finance | 任意 | — | `voucher` | `凭证复核` |
| quality | 任意 | — | `quality` | `质量异常` |
| contract | 任意 | — | `contract` | `合同到期` |
| points | 任意 | — | `points` | `积分审批` |
| task | `notification` | — | `notification` | `通知` |
| task | 其它 | — | `task` | `任务` |
| oa / workflow / 未知 | 任意 | `Metadata["bizType"]` | `wf:{F业务类型}` | F业务类型 |
| oa / workflow / 未知 | 任意 | bizType 缺失 | `approval` | `审批` |

- [ ] **Step 1: 写失败测试**

创建 `tests/STOTOP.WebAPI.Tests/WorkHub/ResolveBizTypeTests.cs`：

```csharp
using STOTOP.WebAPI.Dtos.WorkHub;
using STOTOP.WebAPI.Services;
using Xunit;

namespace STOTOP.WebAPI.Tests.WorkHub;

public class ResolveBizTypeTests
{
    private static WorkItemDto Item(string source, string category,
        Dictionary<string, object>? metadata = null) =>
        new() { Source = source, Category = category, Metadata = metadata };

    [Fact]
    public void CardFlow_WithFlowName_UsesFlowNameAsLabel()
    {
        var (key, label) = WorkHubService.ResolveBizType(
            Item("cardflow", "approval", new() { ["flowName"] = "费用报销" }));
        Assert.Equal("flow:费用报销", key);
        Assert.Equal("费用报销", label);
    }

    [Fact]
    public void CardFlow_WithoutFlowName_FallsBackToApproval()
    {
        var (key, label) = WorkHubService.ResolveBizType(Item("cardflow", "approval"));
        Assert.Equal("approval", key);
        Assert.Equal("审批", label);
    }

    [Theory]
    [InlineData("finance", "approval", "voucher", "凭证复核")]
    [InlineData("quality", "alert", "quality", "质量异常")]
    [InlineData("contract", "reminder", "contract", "合同到期")]
    [InlineData("points", "approval", "points", "积分审批")]
    [InlineData("task", "notification", "notification", "通知")]
    [InlineData("task", "task", "task", "任务")]
    public void KnownSources_MapToBizType(string source, string category, string expectKey, string expectLabel)
    {
        var (key, label) = WorkHubService.ResolveBizType(Item(source, category));
        Assert.Equal(expectKey, key);
        Assert.Equal(expectLabel, label);
    }

    [Fact]
    public void Wf_WithBizType_UsesBizTypeMetadata()
    {
        var (key, label) = WorkHubService.ResolveBizType(
            Item("workflow", "approval", new() { ["bizType"] = "用印申请" }));
        Assert.Equal("wf:用印申请", key);
        Assert.Equal("用印申请", label);
    }

    [Fact]
    public void UnknownSource_WithoutMetadata_FallsBackToApproval()
    {
        var (key, label) = WorkHubService.ResolveBizType(Item("oa", "approval"));
        Assert.Equal("approval", key);
        Assert.Equal("审批", label);
    }
}
```

> 注：若 `WorkItemDto.Metadata` 实际类型不是 `Dictionary<string, object>?`（执行时打开 DTO 文件确认其声明），把上面 `Item()` 与 InlineData 的 metadata 构造改成其真实类型；规则与断言不变。

- [ ] **Step 2: 运行测试，确认因方法不存在而失败**

Run: `dotnet test tests/STOTOP.WebAPI.Tests/STOTOP.WebAPI.Tests.csproj --arch x64`
Expected: 编译失败 / FAIL —— `WorkHubService` 不含 `ResolveBizType`（方法尚未实现）。

---

## Task 4: 实现 ResolveBizType 纯函数

**Files:**
- Modify: `src/STOTOP.WebAPI/Services/WorkHubService.cs`（类内新增 internal static 方法）

- [ ] **Step 1: 实现方法**

在 `WorkHubService` 类内（建议紧挨现有 `MapWf*` 私有静态方法附近）新增：

```csharp
    /// <summary>
    /// 由统一 DTO 派生「业务类型」（用户可见标签 + 上色键），不依赖 source 框架黑话。
    /// 纯函数，便于单测。
    /// </summary>
    internal static (string Key, string Label) ResolveBizType(WorkItemDto item)
    {
        string? Meta(string k) =>
            item.Metadata != null && item.Metadata.TryGetValue(k, out var v) ? v?.ToString() : null;

        switch (item.Source)
        {
            case "cardflow":
            case "datacenter":
                var flow = Meta("flowName");
                return string.IsNullOrWhiteSpace(flow) ? ("approval", "审批") : ("flow:" + flow, flow!);
            case "finance":
                return ("voucher", "凭证复核");
            case "quality":
                return ("quality", "质量异常");
            case "contract":
                return ("contract", "合同到期");
            case "points":
                return ("points", "积分审批");
            case "task":
                return item.Category == "notification" ? ("notification", "通知") : ("task", "任务");
            default:
                var biz = Meta("bizType");
                return string.IsNullOrWhiteSpace(biz) ? ("approval", "审批") : ("wf:" + biz, biz!);
        }
    }
```

- [ ] **Step 2: 运行测试，确认全部通过**

Run: `dotnet test tests/STOTOP.WebAPI.Tests/STOTOP.WebAPI.Tests.csproj --arch x64`
Expected: PASS（Passed! - Failed: 0）。

- [ ] **Step 3: Commit**

```bash
git add src/STOTOP.WebAPI/Services/WorkHubService.cs tests/STOTOP.WebAPI.Tests/WorkHub/ResolveBizTypeTests.cs
git commit -m "feat(workhub): ResolveBizType 纯函数派生业务类型(TDD)"
```

---

## Task 5: 在多源合并后回填业务类型

**Files:**
- Modify: `src/STOTOP.WebAPI/Services/WorkHubService.cs`

- [ ] **Step 1: 确认 cardflow item 带 flowName 元数据**

打开 `GetCardFlowTodosFromServiceAsync`（约 L492-533）。确认每个 cardflow `WorkItemDto` 的 `Metadata` 写入了流程名。若已有形如 `Metadata = new() { ["cardId"]=..., ["cardNumber"]=..., ["flowName"]=todo.FlowName }` 则无需改；若 `flowName` 未入 Metadata，在该处补：`Metadata["flowName"] = todo.FlowName;`（`todo.FlowName` 见 L510/L528）。

- [ ] **Step 2: 新增共享回填方法**

在 `WorkHubService` 类内新增：

```csharp
    /// <summary>对合并后的工作项批量回填业务类型字段。</summary>
    private static void EnrichBizTypes(IEnumerable<WorkItemDto> items)
    {
        foreach (var item in items)
        {
            var (key, label) = ResolveBizType(item);
            item.BizTypeKey = key;
            item.BizTypeLabel = label;
        }
    }
```

- [ ] **Step 3: 在两条装配路径调用回填**

在 with-stats 路径（统计 Count 之前，约 L237 `allItems` 已就绪处）插入一行：

```csharp
        EnrichBizTypes(allItems);
```

在 stats-only 路径若同样向前端返回 items（约 L305 附近 `allItems` 就绪处）也插入同一行。
（定位法：搜 `allItems` 的赋值/最终列表变量名，在其完成、被 `.Count(...)` 或被返回之前调用 `EnrichBizTypes`。若两路径共用同一个返回 items 的私有方法，则只在该方法内调用一次即可，避免重复。）

- [ ] **Step 4: 编译 + 跑测试**

Run: `dotnet build src/STOTOP.WebAPI/STOTOP.WebAPI.csproj -c Debug`
Expected: Build succeeded, 0 Error。

Run: `dotnet test tests/STOTOP.WebAPI.Tests/STOTOP.WebAPI.Tests.csproj --arch x64`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/STOTOP.WebAPI/Services/WorkHubService.cs
git commit -m "feat(workhub): 多源合并后回填 BizType + cardflow flowName 入元数据"
```

---

## Task 6: 前端类型同步

**Files:**
- Modify: `web/src/api/workhub.ts`（`WorkItem` interface，约 L35-60）

- [ ] **Step 1: 给 WorkItem 接口加字段**

在 `WorkItem` 接口内 `category` 字段（约 L38）之后加：

```ts
  /** 业务类型键（后端派生，用于上色/分组，如 'voucher' | 'quality' | 'flow:费用报销'） */
  bizTypeKey: string
  /** 业务类型中文标签（用户可见，如 '凭证复核' | '质量异常' | '费用报销'） */
  bizTypeLabel: string
```

- [ ] **Step 2: 构建验证（不破坏类型）**

Run: `cd web && npm run build`
Expected: vite build 成功产出 dist；如有报错只接受与本改动无关的既有基线错误（参照改动前基线）。

- [ ] **Step 3: Commit**

```bash
git add web/src/api/workhub.ts
git commit -m "feat(workhub-web): WorkItem 类型增加 bizTypeKey/bizTypeLabel"
```

---

## Task 7: 新增前端业务类型样式映射（统一上色，拆撞色）

**Files:**
- Create: `web/src/views/workhub/bizType.ts`

- [ ] **Step 1: 建立 bizTypeKey → 颜色/图标 映射（单一真源，三组件共用）**

创建 `web/src/views/workhub/bizType.ts`：

```ts
import {
  AuditOutlined,
  FileTextOutlined,
  WarningOutlined,
  BellOutlined,
  CheckSquareOutlined,
  TrophyOutlined,
  ScheduleOutlined,
} from '@ant-design/icons-vue'
import type { Component } from 'vue'

interface BizTypeStyle {
  color: string
  icon: Component
}

// 按业务类型上色，彻底拆开旧 oa/cardflow 共用的 --biz-approval。
// 颜色复用 theme.ts 已有的业务 token；前缀型 key（flow:xxx / wf:xxx）归入审批族。
const STYLE_BY_KEY: Record<string, BizTypeStyle> = {
  approval: { color: 'var(--biz-approval)', icon: AuditOutlined },
  voucher: { color: 'var(--biz-finance)', icon: FileTextOutlined },
  quality: { color: 'var(--biz-quality)', icon: WarningOutlined },
  contract: { color: 'var(--biz-contract)', icon: ScheduleOutlined },
  points: { color: 'var(--biz-points)', icon: TrophyOutlined },
  task: { color: 'var(--biz-task)', icon: CheckSquareOutlined },
  notification: { color: 'var(--biz-notification)', icon: BellOutlined },
}

const FALLBACK: BizTypeStyle = { color: 'var(--biz-approval)', icon: AuditOutlined }

/** 前缀型 key（flow:费用报销 / wf:用印申请）归并到审批族取色，但标签仍用后端给的细粒度文案。 */
export function bizTypeStyle(bizTypeKey: string | undefined): BizTypeStyle {
  if (!bizTypeKey) return FALLBACK
  if (bizTypeKey.startsWith('flow:') || bizTypeKey.startsWith('wf:')) return STYLE_BY_KEY.approval
  return STYLE_BY_KEY[bizTypeKey] ?? FALLBACK
}
```

> 执行前确认 `web/src/stores/theme.ts` L307-312 里实际存在的 biz token 名（如 `--biz-finance`/`--biz-quality`/`--biz-contract`/`--biz-points`/`--biz-task`/`--biz-notification`）。若某个 token 不存在，在 theme.ts 同处补一个（取与现有色系协调、且与 `--biz-approval` 蓝可区分的值）或退化到 `--biz-approval`。**至少保证 voucher（凭证复核）与 approval（审批）取不同色**——这是拆撞色的核心目的。

- [ ] **Step 2: 构建验证**

Run: `cd web && npm run build`
Expected: 成功（仅基线无关错误）。

- [ ] **Step 3: Commit**

```bash
git add web/src/views/workhub/bizType.ts
git commit -m "feat(workhub-web): 新增 bizType 样式单一真源(拆 oa/cardflow 撞色)"
```

---

## Task 8: WorkItemCard 卡片改渲染业务类型

**Files:**
- Modify: `web/src/views/workhub/WorkItemCard.vue`（`sourceConfig` L328-339、消费 L341-343、模板 L25-35）

- [ ] **Step 1: 用 bizType 取代 source 标签/颜色/图标**

在 `<script setup>` 引入：`import { bizTypeStyle } from './bizType'`。

把 `sourceLabel/sourceColor/sourceIcon`（L341-343）三个计算属性改为基于 bizType：

```ts
const bizLabel = computed(() => props.item.bizTypeLabel || '审批')
const bizStyle = computed(() => bizTypeStyle(props.item.bizTypeKey))
const bizColor = computed(() => bizStyle.value.color)
const bizIcon = computed(() => bizStyle.value.icon)
```

模板中：
- chip（L33-35）：`{{ sourceLabel }}` → `{{ bizLabel }}`，`:style` 里的 `sourceColor` → `bizColor`。
- 左侧图标（L25-27）：`<component :is="sourceIcon" />` → `<component :is="bizIcon" />`，颜色 `sourceColor` → `bizColor`。

删除已无引用的 `sourceConfig`（L328-339）及 `sourceLabel/sourceColor/sourceIcon` 旧定义。

> **保留不动**：L40/L46/L82-101 中 `item.source === 'oa'` 的当前审批节点/审批模式/流程编号富渲染分支，以及 L104 `isDataImport = item.source === 'datacenter'`——这些按 source 的渲染逻辑与"去标签"无关，保持原样（source 值未变）。

- [ ] **Step 2: 构建验证**

Run: `cd web && npm run build`
Expected: 成功；无 `sourceLabel/sourceColor/sourceIcon/sourceConfig` 未定义残留引用报错。

- [ ] **Step 3: Commit**

```bash
git add web/src/views/workhub/WorkItemCard.vue
git commit -m "refactor(workhub-web): 卡片改渲染业务类型标签(退役 source 映射)"
```

---

## Task 9: WorkHubDetail 详情改渲染业务类型

**Files:**
- Modify: `web/src/views/workhub/WorkHubDetail.vue`（`sourceConfig` L94-106、消费 `workItemSourceConfig` L138-141、模板 `.wi-source-tag` L328-337）

- [ ] **Step 1: 用 bizType 取代来源标签**

引入：`import { bizTypeStyle } from './bizType'`。

把 `workItemSourceConfig`（L138-141）改为：

```ts
const workItemBizConfig = computed(() => ({
  label: workItem.value?.bizTypeLabel || '审批',
  ...bizTypeStyle(workItem.value?.bizTypeKey),
}))
```

模板 `.wi-source-tag`（L328-337）中 `workItemSourceConfig.label/color/icon` → `workItemBizConfig.label/color/icon`。删除已无引用的 `sourceConfig`（L94-106）。

> **保留不动**：L131-136 `cardId` 计算（依赖 `workItem.value?.source !== 'cardflow'`）与 L379 `CardActivityTimeline` 渲染条件——它们读 `source`，本任务不触碰 `source`。

- [ ] **Step 2: 构建验证**

Run: `cd web && npm run build`
Expected: 成功；无 `sourceConfig/workItemSourceConfig` 残留引用报错。

- [ ] **Step 3: Commit**

```bash
git add web/src/views/workhub/WorkHubDetail.vue
git commit -m "refactor(workhub-web): 详情改渲染业务类型标签(保留 source 供时间轴)"
```

---

## Task 10: WorkHubCenter 混合列表标签去黑话

**Files:**
- Modify: `web/src/views/workhub/WorkHubCenter.vue`（混合列表两张表 `sourceColorMap` L179-188、`sourceLabelMap` L189-198、avatar 消费 L448-453）

本任务只处理「全部」Tab 混合列表 avatar 用的两张映射表（它们直接产生 'OA' 文案与 cardflow 英文兜底）。**筛选 chip（statsItems L62-69）的来源筛选机制不在本计划范围内**（属"按业务实质聚类"，黄色项），本任务仅去黑话、不改筛选维度。

- [ ] **Step 1: avatar 改用业务类型**

引入：`import { bizTypeStyle } from './bizType'`。

把 avatar 渲染（L448-453）原先读 `sourceColorMap[source]` / `sourceLabelMap[source] ?? item.data.source` 的逻辑，改为读条目自身的业务类型：
- 颜色：`bizTypeStyle(item.data.bizTypeKey).color`
- 文案首字/标签：`item.data.bizTypeLabel`（取首字或整词，按现有 avatar 展示方式保持一致）

删除已无引用的 `sourceColorMap`（L179-188）与 `sourceLabelMap`（L189-198）。

> 若混合列表项的数据形状不是 `item.data`（执行时按该文件实际渲染项结构取 `bizTypeKey/bizTypeLabel`）。

- [ ] **Step 2: 顺手修 chip 文案中的框架黑话（仅文案，不改 key/筛选）**

检查 `statsItems`（L62-69）：把任何仍显示框架名的 `label` 改成业务语义（现状 `key:'oa'` 的 label 已是 '审批'，确认其余 chip label 无 'OA'/'CardFlow' 字样即可；如有则改）。**不改 `key` 值**（筛选逻辑依赖它）。

- [ ] **Step 3: 构建验证**

Run: `cd web && npm run build`
Expected: 成功；无 `sourceColorMap/sourceLabelMap` 残留引用报错。

- [ ] **Step 4: Commit**

```bash
git add web/src/views/workhub/WorkHubCenter.vue
git commit -m "refactor(workhub-web): 混合列表标签去黑话(退役 source 映射表)"
```

---

## Task 11: 端到端预览验证

**Files:** 无（仅运行验证）

- [ ] **Step 1: 起后端 + 前端开发服务**

按项目约定启动（参照 `scripts/dev` 工具链 / restart-dev skill；端口 9000/9001）。确保后端单进程、前端 vite 起来。

- [ ] **Step 2: 登录 + 切到有数据的组织，进 `/workhub`**

用有 Workhub 待办数据的组织（如太仓美申 org192）。肉眼核对：
- 中栏卡片、右栏详情、全部 Tab 混合列表的来源标签**均为业务类型中文**（费用报销/凭证复核/质量异常/合同到期/任务/通知/积分审批…），**不再出现 "OA审批" "CardFlow审批" "CardFlow" 或英文 "cardflow"**。
- 凭证复核（voucher）与一般审批（approval）**颜色可区分**，oa/cardflow 不再撞同一蓝。
- 点开一条 cardflow 单据，右栏 `CardActivityTimeline` 时间轴**仍正常渲染**（验证未误伤 source）。

- [ ] **Step 3: 截图留证 + 收尾说明**

记录验证截图/结论。若发现某来源 `bizTypeLabel` 为空或仍显示英文，回到后端 `ResolveBizType`/各 `Map*` 的 metadata 补齐对应取数后重验。

---

## Self-Review（写计划后自检结论）

- **Spec 覆盖**：绿色项"oa/cardflow 合并去标签"=后端统一派生业务类型 + 前端三处映射表退役（Task 4/5/8/9/10）；"撞色"=Task 7 拆 voucher/approval 色 + 按 bizTypeKey 上色。✅
- **占位符**：无 TBD；少数"执行时确认"点（Metadata 实际类型、biz token 实名、混合列表项结构、.sln 文件名）均给了定位法与回退策略，非空泛占位。✅
- **类型一致**：后端 `BizTypeKey/BizTypeLabel`（Task1）↔ 测试断言（Task3）↔ 回填 `EnrichBizTypes`/`ResolveBizType`（Task4/5）↔ 前端 `bizTypeKey/bizTypeLabel`（Task6）↔ `bizTypeStyle()`（Task7）↔ 三组件消费（Task8/9/10）命名贯通一致。✅
- **风险**：`oa` 来源疑为后端已不产出的幽灵——本设计对任意 source 都有 default 回退（不依赖 oa 真实存在），故不阻塞；Task11 预览即可暴露真实来源分布。

## 范围外（留给后续计划）
- 按业务实质聚类、智能批处理、真批量、新增业务类型 tab/筛选维度（黄/红色项）。
- WorkItemDto 业务类型维度的 stats Count（本计划未改 `WorkHubStatsDto`）。
