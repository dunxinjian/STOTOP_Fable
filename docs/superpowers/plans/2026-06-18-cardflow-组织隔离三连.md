# CardFlow 组织隔离三连修复 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 CardFlow 克隆/模板链三处组织隔离缺陷——克隆一律落当前组织、模板真正持久化 `FOrgId=0`、`GetTemplatesAsync` 只返全局模板——并堵掉以他组织私有流程为源的越权。

**Architecture:** 新增通用、默认关的 `STOTOPDbContext.SuppressOrgIdFill()` 作用域（仅模板创建用）；把克隆主体抽为共享私有 `CloneInternalAsync`，公开克隆改为源读取隔离 + 落当前组织（忽略请求体 OrgId），`SaveAsTemplate` 新建分支走 `asGlobalTemplate:true` 在抑制作用域内置 0；`GetTemplatesAsync` 加 `FOrgId==0`。

**Tech Stack:** .NET 10 / EF Core / xUnit（EFCore.InMemory，`--arch x64` 宿主）。

**上游 spec：** [docs/superpowers/specs/2026-06-18-cardflow-组织隔离三连-design.md](../specs/2026-06-18-cardflow-组织隔离三连-design.md)（D1-D3 已锁定）。

**已核实事实：**
- 仅 `CfFlowDefinition : BaseEntity, IOrgScoped`；`CfFlowVersion`/`CfStageDefinition`/`CfStageRouteRule`/`CfDynamicStagePolicy` 非 IOrgScoped → `FillOrgId` 不碰它们，抑制只需罩 def 的一次 SaveChanges。
- `FlowDefinitionService` 注入具体 `STOTOPDbContext`。
- `STOTOPDbContext(DbContextOptions<STOTOPDbContext> options, IOrgContextAccessor? orgContextAccessor = null)`；`IOrgContextAccessor { long? CurrentOrgId { get; set; } }`（`STOTOP.Core.Services`）。全局过滤器按 `CurrentOrgId` 过滤、为 null 时不过滤。
- `TestDbContextFactory.Create(string)` 现不注入 accessor（FillOrgId no-op）。

**通用命令：**
- 测试：`dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`（x86 宿主会 WSAENOBUFS 中止，必带 x64）
- 模块编译：`dotnet build src/STOTOP.Module.CardFlow/STOTOP.Module.CardFlow.csproj`（dev 后端占 WebAPI DLL，用模块编译）
- 共享 master：仅 `git add` 本任务文件，勿 `git add -A`；若并发提交把暂存文件裹入，核实内容后报告该 SHA，勿 amend/force。

---

## 文件结构

**修改：**
- `src/STOTOP.Infrastructure/Data/STOTOPDbContext.cs` — 加 `SuppressOrgIdFill()` + `FillOrgIdForNewEntities` 抑制早返回。
- `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs` — `CloneInternalAsync` 抽取、公开 `CloneFlowDefinitionAsync` 改、`SaveAsTemplateAsync` 新建分支改、`GetTemplatesAsync` 加过滤。
- `tests/STOTOP.Module.CardFlow.Tests/TestDbContextFactory.cs` — 加注入假 accessor 的重载。

**新增：**
- `tests/STOTOP.Module.CardFlow.Tests/TestOrgContextAccessor.cs` — 假 `IOrgContextAccessor`。
- `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs` — 组织隔离测试。

---

## Task 1：测试基建——假 accessor + 工厂重载

**Files:**
- Create: `tests/STOTOP.Module.CardFlow.Tests/TestOrgContextAccessor.cs`
- Modify: `tests/STOTOP.Module.CardFlow.Tests/TestDbContextFactory.cs`

- [ ] **Step 1：写假 accessor**

`TestOrgContextAccessor.cs`：
```csharp
using STOTOP.Core.Services;

namespace STOTOP.Module.CardFlow.Tests;

/// <summary>测试用组织上下文：可设当前组织以驱动全局过滤器与 FillOrgId。</summary>
public sealed class TestOrgContextAccessor : IOrgContextAccessor
{
    public TestOrgContextAccessor(long? currentOrgId = null) => CurrentOrgId = currentOrgId;
    public long? CurrentOrgId { get; set; }
}
```

- [ ] **Step 2：加工厂重载**

在 `TestDbContextFactory.cs` 现有 `Create(string databaseName)` 旁加一个带组织的重载（复用已有 `RegisterModuleAssembly` 调用与 options 构造；核对现有方法体把同样的 `RegisterModuleAssembly` 序列 + `UseInMemoryDatabase` 选项复制进来）：
```csharp
    public static STOTOPDbContext Create(string databaseName, long? currentOrgId)
    {
        STOTOPDbContext.RegisterModuleAssembly(typeof(CfCard).Assembly);
        STOTOPDbContext.RegisterModuleAssembly(typeof(OaExpenseRequest).Assembly);
        STOTOPDbContext.RegisterModuleAssembly(typeof(SysUser).Assembly);
        STOTOPDbContext.RegisterModuleAssembly(typeof(FinVoucher).Assembly);
        STOTOPDbContext.RegisterModuleAssembly(typeof(ExpSalesman).Assembly);
        STOTOPDbContext.RegisterModuleAssembly(typeof(QlException).Assembly);

        var options = new DbContextOptionsBuilder<STOTOPDbContext>()
            .UseInMemoryDatabase(databaseName)
            .Options;
        return new STOTOPDbContext(options, new TestOrgContextAccessor(currentOrgId));
    }
```
> 实现者：照现有 `Create(string)` 方法体逐行对齐 `RegisterModuleAssembly`/options（上面按当前已知内容写出；若现有方法体已有差异以现有为准，只在末尾把 `new STOTOPDbContext(options)` 换成带 accessor 的构造）。

- [ ] **Step 3：编译测试项目**

Run: `dotnet build tests/STOTOP.Module.CardFlow.Tests/STOTOP.Module.CardFlow.Tests.csproj`
Expected: 0 错误。

- [ ] **Step 4：提交**

```bash
git add tests/STOTOP.Module.CardFlow.Tests/TestOrgContextAccessor.cs tests/STOTOP.Module.CardFlow.Tests/TestDbContextFactory.cs
git commit -m "test(cardflow): 测试基建——假 IOrgContextAccessor + 带组织的 DbContext 工厂重载"
```

---

## Task 2：基建——`SuppressOrgIdFill()` 抑制作用域

**Files:**
- Modify: `src/STOTOP.Infrastructure/Data/STOTOPDbContext.cs`
- Test: `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs`（本任务先建文件 + 1 个直测 DbContext 的用例）

- [ ] **Step 1：写失败测试（抑制生效 / 不抑制仍填充）**

新建 `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs`：
```csharp
using Microsoft.EntityFrameworkCore;
using STOTOP.Module.CardFlow.Entities;
using Xunit;

namespace STOTOP.Module.CardFlow.Tests.Rules;

public class FlowDefinitionOrgIsolationTests
{
    [Fact]
    public void SuppressOrgIdFill_KeepsOrgZero_WhileNormalAddGetsCurrentOrg()
    {
        using var db = TestDbContextFactory.Create(nameof(SuppressOrgIdFill_KeepsOrgZero_WhileNormalAddGetsCurrentOrg), 192);

        // 抑制作用域内：FOrgId=0 应保持 0
        using (db.SuppressOrgIdFill())
        {
            db.Set<CfFlowDefinition>().Add(new CfFlowDefinition { FFlowName = "T", FFlowCode = "TPL_A", FOrgId = 0, FStatus = "published" });
            db.SaveChanges();
        }
        var tpl = db.Set<CfFlowDefinition>().IgnoreQueryFilters().Single(x => x.FFlowCode == "TPL_A");
        Assert.Equal(0, tpl.FOrgId);

        // 非抑制：FOrgId=0 被 FillOrgId 填成当前组织 192
        db.Set<CfFlowDefinition>().Add(new CfFlowDefinition { FFlowName = "N", FFlowCode = "NORMAL_A", FOrgId = 0, FStatus = "draft" });
        db.SaveChanges();
        var normal = db.Set<CfFlowDefinition>().IgnoreQueryFilters().Single(x => x.FFlowCode == "NORMAL_A");
        Assert.Equal(192, normal.FOrgId);
    }
}
```

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter FlowDefinitionOrgIsolationTests`
Expected: 编译失败（`SuppressOrgIdFill` 不存在）。

- [ ] **Step 3：实现抑制开关**

在 `STOTOPDbContext.cs` 类内（字段区，`_orgContextAccessor` 附近）加字段 + 方法；并在 `FillOrgIdForNewEntities()` 方法体**第一行**加早返回：
```csharp
    private bool _suppressOrgIdFill;

    /// <summary>在返回的作用域内，SaveChanges 不对 FOrgId==0 的新 IOrgScoped 实体自动填充当前组织。
    /// 仅用于「有意创建全局(FOrgId=0)记录」这类受控内部场景（如保存为全局模板）。</summary>
    public IDisposable SuppressOrgIdFill()
    {
        _suppressOrgIdFill = true;
        return new SuppressOrgIdFillScope(() => _suppressOrgIdFill = false);
    }

    private sealed class SuppressOrgIdFillScope : IDisposable
    {
        private readonly Action _onDispose;
        public SuppressOrgIdFillScope(Action onDispose) => _onDispose = onDispose;
        public void Dispose() => _onDispose();
    }
```
在 `private void FillOrgIdForNewEntities()` 方法体最开头加：
```csharp
        if (_suppressOrgIdFill)
        {
            return;
        }
```

- [ ] **Step 4：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter FlowDefinitionOrgIsolationTests`
Expected: PASS（1 passed）。

- [ ] **Step 5：编译 Infra + 模块**

Run: `dotnet build src/STOTOP.Infrastructure/STOTOP.Infrastructure.csproj`
Expected: 0 错误。

- [ ] **Step 6：提交**

```bash
git add src/STOTOP.Infrastructure/Data/STOTOPDbContext.cs tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs
git commit -m "feat(infra): STOTOPDbContext.SuppressOrgIdFill 作用域(创建全局FOrgId=0记录用,默认关)"
```

---

## Task 3：`GetTemplatesAsync` 只返全局模板

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs`（`GetTemplatesAsync`）
- Test: `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs`（追加）

- [ ] **Step 1：追加失败测试**

在 `FlowDefinitionOrgIsolationTests` 追加（`FlowDefinitionService` 构造仅需 `(STOTOPDbContext, ILogger)`；用 `NullLogger`）：
```csharp
    [Fact]
    public async Task GetTemplatesAsync_ReturnsOnlyGlobalTemplates()
    {
        using var db = TestDbContextFactory.Create(nameof(GetTemplatesAsync_ReturnsOnlyGlobalTemplates), null);
        db.Set<CfFlowDefinition>().AddRange(
            new CfFlowDefinition { FFlowName = "全局模板", FFlowCode = "G1", FOrgId = 0, FIsTemplate = true, FStatus = "published" },
            new CfFlowDefinition { FFlowName = "组织伪模板", FFlowCode = "O1", FOrgId = 192, FIsTemplate = true, FStatus = "published" },
            new CfFlowDefinition { FFlowName = "全局非模板", FFlowCode = "G2", FOrgId = 0, FIsTemplate = false, FStatus = "published" });
        await db.SaveChangesAsync();

        var svc = new STOTOP.Module.CardFlow.Services.FlowDefinitionService(
            db, Microsoft.Extensions.Logging.Abstractions.NullLogger<STOTOP.Module.CardFlow.Services.FlowDefinitionService>.Instance);
        var templates = await svc.GetTemplatesAsync();

        Assert.Single(templates);
        Assert.Equal("G1", templates[0].FlowCode);
    }
```
> 实现者：若 `FlowDefinitionService` 构造签名不同（核 `FlowDefinitionService.cs:16`），按真实签名构造（已知为 `(STOTOPDbContext dbContext, ILogger<FlowDefinitionService> logger)`）。

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter GetTemplatesAsync_ReturnsOnlyGlobalTemplates`
Expected: FAIL（当前返回 2 条，含 org192 伪模板）。

- [ ] **Step 3：加 FOrgId==0 过滤**

在 `GetTemplatesAsync` 的 `.Where(...)` 改为：
```csharp
            .Where(x => x.FIsTemplate && x.FStatus == "published" && x.FOrgId == 0)
```

- [ ] **Step 4：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter GetTemplatesAsync_ReturnsOnlyGlobalTemplates`
Expected: PASS。

- [ ] **Step 5：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs
git commit -m "fix(cardflow): GetTemplatesAsync 仅返 FOrgId=0 全局模板，堵组织绑定伪模板跨租户泄露"
```

---

## Task 4：克隆收敛 `CloneInternalAsync` + 源读取隔离 + 忽略请求体 OrgId

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs`（`CloneFlowDefinitionAsync` 重构 + 新增私有 `CloneInternalAsync`）
- Test: `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs`（追加）

- [ ] **Step 1：追加失败测试**

在 `FlowDefinitionOrgIsolationTests` 追加（验证忽略请求体 OrgId + 源读取隔离）：
```csharp
    [Fact]
    public async Task CloneFlowDefinitionAsync_IgnoresRequestOrgId()
    {
        using var db = TestDbContextFactory.Create(nameof(CloneFlowDefinitionAsync_IgnoresRequestOrgId), null); // accessor=null → FillOrgId no-op → FOrgId 保持 0
        db.Set<CfFlowDefinition>().Add(new CfFlowDefinition { FID = 0, FFlowName = "源", FFlowCode = "SRC1", FOrgId = 0, FStatus = "published" });
        await db.SaveChangesAsync();
        var srcId = db.Set<CfFlowDefinition>().IgnoreQueryFilters().Single(x => x.FFlowCode == "SRC1").FID;

        var svc = new STOTOP.Module.CardFlow.Services.FlowDefinitionService(
            db, Microsoft.Extensions.Logging.Abstractions.NullLogger<STOTOP.Module.CardFlow.Services.FlowDefinitionService>.Instance);
        var dto = await svc.CloneFlowDefinitionAsync(srcId,
            new STOTOP.Module.CardFlow.Dtos.CloneFlowDefinitionRequest { FlowName = "克隆", FlowCode = "CLONE1", OrgId = 999 }, operatorId: 1);

        var cloned = db.Set<CfFlowDefinition>().IgnoreQueryFilters().Single(x => x.FFlowCode == "CLONE1");
        Assert.NotEqual(999, cloned.FOrgId); // 请求体 OrgId=999 被忽略
        Assert.Equal(0, cloned.FOrgId);      // accessor=null 下保持 0（prod 下 FillOrgId 会填当前组织，见 Task 5）
    }

    [Fact]
    public async Task CloneFlowDefinitionAsync_OtherOrgPrivateSource_NotFound()
    {
        using var db = TestDbContextFactory.Create(nameof(CloneFlowDefinitionAsync_OtherOrgPrivateSource_NotFound), 192);
        // 他组织(888)私有流程；当前组织过滤器(192)读不到，且非 FOrgId=0 模板 → 源不存在
        using (db.SuppressOrgIdFill())
        {
            db.Set<CfFlowDefinition>().Add(new CfFlowDefinition { FFlowName = "他组织私有", FFlowCode = "OTHER1", FOrgId = 888, FStatus = "published" });
            db.SaveChanges();
        }
        var otherId = db.Set<CfFlowDefinition>().IgnoreQueryFilters().Single(x => x.FFlowCode == "OTHER1").FID;

        var svc = new STOTOP.Module.CardFlow.Services.FlowDefinitionService(
            db, Microsoft.Extensions.Logging.Abstractions.NullLogger<STOTOP.Module.CardFlow.Services.FlowDefinitionService>.Instance);
        await Assert.ThrowsAsync<InvalidOperationException>(() => svc.CloneFlowDefinitionAsync(otherId,
            new STOTOP.Module.CardFlow.Dtos.CloneFlowDefinitionRequest { FlowName = "x", FlowCode = "x" }, 1));
    }
```

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~CloneFlowDefinitionAsync"`
Expected: FAIL（IgnoresRequestOrgId 现得 999；OtherOrgPrivateSource 现因 `IgnoreQueryFilters` 读到他组织源 → 不抛）。

- [ ] **Step 3：重构 `CloneFlowDefinitionAsync` + 新增 `CloneInternalAsync`**

把现有 `public async Task<FlowDefinitionDto> CloneFlowDefinitionAsync(long sourceDefinitionId, CloneFlowDefinitionRequest request, long operatorId)` 改为：源读取隔离 + 委托内部方法（**不再读 `request.OrgId`**）：
```csharp
    public async Task<FlowDefinitionDto> CloneFlowDefinitionAsync(
        long sourceDefinitionId,
        CloneFlowDefinitionRequest request,
        long operatorId)
    {
        // 源读取隔离：先按当前组织过滤读；读不到再回退全局模板(FOrgId=0)。不可读他组织私有流程。
        // request.OrgId 忽略——克隆一律落当前组织，目标组织由服务端 CurrentOrgId(FillOrgId) 决定。
        var sourceDefinition = await _dbContext.Set<CfFlowDefinition>()
                .FirstOrDefaultAsync(x => x.FID == sourceDefinitionId)
            ?? await _dbContext.Set<CfFlowDefinition>()
                .IgnoreQueryFilters()
                .FirstOrDefaultAsync(x => x.FID == sourceDefinitionId && x.FOrgId == 0)
            ?? throw new InvalidOperationException("源流程定义不存在");

        var newDefinition = await CloneInternalAsync(
            sourceDefinition, request.FlowName, request.FlowCode, request.Description,
            asGlobalTemplate: false, operatorId);

        _logger.LogInformation("Cloned flow definition {SourceId} -> {NewId} by operator {OperatorId}",
            sourceDefinitionId, newDefinition.FID, operatorId);
        return MapToDto(newDefinition);
    }

    /// <summary>克隆主体：建定义 + 复制当前发布版本/节点/路由/动态策略。
    /// asGlobalTemplate=true 时把定义建为全局(FOrgId=0)并抑制自动组织填充；否则落当前组织(FillOrgId)。</summary>
    private async Task<CfFlowDefinition> CloneInternalAsync(
        CfFlowDefinition sourceDefinition, string flowName, string flowCode, string? description,
        bool asGlobalTemplate, long operatorId)
    {
        var newDefinition = new CfFlowDefinition
        {
            FFlowName = flowName,
            FFlowCode = flowCode,
            FDescription = description ?? sourceDefinition.FDescription,
            FStatus = "draft",
            FNumberTemplate = sourceDefinition.FNumberTemplate,
            FTitleTemplate = sourceDefinition.FTitleTemplate,
            FAllowedRolesJson = sourceDefinition.FAllowedRolesJson,
            FFlowGroupId = null,
            FTriggerConfigJson = sourceDefinition.FTriggerConfigJson,
            FAccountSetId = sourceDefinition.FAccountSetId,
            FOrgId = 0, // 非模板时由 FillOrgId 填当前组织；模板时在抑制作用域内保持 0
            FCreatorId = operatorId,
            FCreatedTime = DateTime.Now
        };

        _dbContext.Set<CfFlowDefinition>().Add(newDefinition);
        if (asGlobalTemplate)
        {
            using (_dbContext.SuppressOrgIdFill())
            {
                await _dbContext.SaveChangesAsync(); // FOrgId 保持 0
            }
        }
        else
        {
            await _dbContext.SaveChangesAsync(); // FillOrgId 填当前组织
        }

        var sourceVersion = await _dbContext.Set<CfFlowVersion>()
            .FirstOrDefaultAsync(x => x.FFlowDefinitionId == sourceDefinition.FID && x.FIsCurrentVersion);
        if (sourceVersion != null)
        {
            var newVersion = new CfFlowVersion
            {
                FFlowDefinitionId = newDefinition.FID,
                FVersionNumber = 1,
                FStatus = "draft",
                FCardSchemaJson = sourceVersion.FCardSchemaJson,
                FDetailSchemaJson = sourceVersion.FDetailSchemaJson,
                FFlowSettingsJson = sourceVersion.FFlowSettingsJson,
                FCreatorId = operatorId,
                FCreatedTime = DateTime.Now,
                FIsCurrentVersion = false
            };
            _dbContext.Set<CfFlowVersion>().Add(newVersion);
            await _dbContext.SaveChangesAsync();

            var sourceStages = await _dbContext.Set<CfStageDefinition>()
                .Where(s => s.FFlowVersionId == sourceVersion.FID)
                .OrderBy(s => s.FSortOrder)
                .ToListAsync();
            var usedStageKeys = new HashSet<string>(StringComparer.OrdinalIgnoreCase);
            foreach (var src in sourceStages)
            {
                _dbContext.Set<CfStageDefinition>().Add(new CfStageDefinition
                {
                    FFlowVersionId = newVersion.FID,
                    FStageKey = EnsureStageKey(src.FStageKey, src.FSortOrder, src.FStageName, usedStageKeys),
                    FSortOrder = src.FSortOrder,
                    FStageName = src.FStageName,
                    FType = src.FType,
                    FApprovalMode = src.FApprovalMode,
                    FAssigneeStrategy = src.FAssigneeStrategy,
                    FAssigneeConfigJson = src.FAssigneeConfigJson,
                    FConditionJson = src.FConditionJson,
                    FInputFieldsJson = src.FInputFieldsJson,
                    F处理粒度 = src.F处理粒度,
                    F插件注册ID = src.F插件注册ID,
                    F插件规则ID = src.F插件规则ID,
                    FFailurePolicyJson = src.FFailurePolicyJson,
                    FCcConfigJson = src.FCcConfigJson,
                    FTimeoutHours = src.FTimeoutHours,
                    FPriorityTemplate = src.FPriorityTemplate
                });
            }
            await _dbContext.SaveChangesAsync();
            await CloneRouteRulesAsync(sourceVersion.FID, newVersion.FID);
            await CloneDynamicPoliciesAsync(sourceVersion.FID, newVersion.FID);
            await _dbContext.SaveChangesAsync();
        }

        return newDefinition;
    }
```
> 说明：上面的版本/节点/路由复制是把原 `CloneFlowDefinitionAsync` 的 957-1010 段**原样移入** `CloneInternalAsync`（仅去掉重复、并用入参 `sourceDefinition` 替代原 `sourceDefinition` 变量）。核对原方法体字段集与 `EnsureStageKey` 调用一致。

- [ ] **Step 4：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~CloneFlowDefinitionAsync"`
Expected: PASS（2 passed）。

- [ ] **Step 5：模块编译 + 全量回归**

Run: `dotnet build src/STOTOP.Module.CardFlow/STOTOP.Module.CardFlow.csproj`（0 错误）
Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`（绿；忽略需真 SQL Server 的集成测试环境性失败）

- [ ] **Step 6：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs
git commit -m "fix(cardflow): 克隆收敛 CloneInternalAsync——源读取隔离+忽略请求体OrgId+一律落当前组织(堵跨组织写越权)"
```

---

## Task 5：`SaveAsTemplateAsync` 新建分支走 `asGlobalTemplate:true`

**Files:**
- Modify: `src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs`（`SaveAsTemplateAsync` 的 `else` 新建分支）
- Test: `tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs`（追加，用带组织 accessor）

- [ ] **Step 1：追加失败测试**

```csharp
    [Fact]
    public async Task SaveAsTemplate_PersistsGlobalOrgZero_DespiteOrgFill()
    {
        using var db = TestDbContextFactory.Create(nameof(SaveAsTemplate_PersistsGlobalOrgZero_DespiteOrgFill), 192);
        // 源：当前组织(192)的已发布流程 + 当前发布版本
        var src = new CfFlowDefinition { FFlowName = "报销", FFlowCode = "EXP_SRC", FStatus = "published" }; // FOrgId 由 FillOrgId 填 192
        db.Set<CfFlowDefinition>().Add(src);
        await db.SaveChangesAsync();
        db.Set<CfFlowVersion>().Add(new CfFlowVersion { FFlowDefinitionId = src.FID, FVersionNumber = 1, FStatus = "published", FIsCurrentVersion = true });
        await db.SaveChangesAsync();

        var svc = new STOTOP.Module.CardFlow.Services.FlowDefinitionService(
            db, Microsoft.Extensions.Logging.Abstractions.NullLogger<STOTOP.Module.CardFlow.Services.FlowDefinitionService>.Instance);
        var dto = await svc.SaveAsTemplateAsync(src.FID, operatorId: 1);

        var tpl = db.Set<CfFlowDefinition>().IgnoreQueryFilters().Single(x => x.FFlowCode == "EXP_SRC" && x.FIsTemplate);
        Assert.Equal(0, tpl.FOrgId);          // 模板真正落全局 0（抑制生效，未被 FillOrgId 盖成 192、未撞唯一索引 500）
        Assert.True(tpl.FIsTemplate);
        Assert.Equal("published", tpl.FStatus);
    }

    [Fact]
    public async Task Clone_NonTemplate_GetsCurrentOrg()
    {
        using var db = TestDbContextFactory.Create(nameof(Clone_NonTemplate_GetsCurrentOrg), 192);
        var src = new CfFlowDefinition { FFlowName = "源", FFlowCode = "SRC_ORG", FStatus = "published" };
        db.Set<CfFlowDefinition>().Add(src);
        await db.SaveChangesAsync();

        var svc = new STOTOP.Module.CardFlow.Services.FlowDefinitionService(
            db, Microsoft.Extensions.Logging.Abstractions.NullLogger<STOTOP.Module.CardFlow.Services.FlowDefinitionService>.Instance);
        await svc.CloneFlowDefinitionAsync(src.FID,
            new STOTOP.Module.CardFlow.Dtos.CloneFlowDefinitionRequest { FlowName = "克隆", FlowCode = "CLONE_ORG", OrgId = 999 }, 1);

        var cloned = db.Set<CfFlowDefinition>().IgnoreQueryFilters().Single(x => x.FFlowCode == "CLONE_ORG");
        Assert.Equal(192, cloned.FOrgId); // 普通克隆经 FillOrgId 落当前组织，请求体 999 无效
    }
```
> 注：`EXP_SRC` 源是 org192；模板复用同 FFlowCode 但 FOrgId=0 → `(FFlowCode,FOrgId)` 唯一索引不冲突（192 vs 0）。InMemory 不强制唯一索引，但断言 FOrgId=0 即证抑制路径正确（prod 下正是靠 0≠192 避免冲突）。

- [ ] **Step 2：运行确认失败**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~SaveAsTemplate_PersistsGlobalOrgZero|FullyQualifiedName~Clone_NonTemplate"`
Expected: `SaveAsTemplate_PersistsGlobalOrgZero` FAIL（现走 CloneFlowDefinitionAsync(OrgId=0)→FillOrgId 盖成 192 → tpl.FOrgId==192）。`Clone_NonTemplate` 应已 PASS（Task 4 已落当前组织）。

- [ ] **Step 3：改 `SaveAsTemplateAsync` 新建分支**

把 `SaveAsTemplateAsync` 的 `else`（新建模板）分支里 `var result = await CloneFlowDefinitionAsync(sourceDefinitionId, request, operatorId);`（及其前的 `request` 构造）替换为直接调内部方法 + 后处理用实体：
```csharp
        else
        {
            // 新建全局模板：在抑制作用域内以 FOrgId=0 创建（CloneInternalAsync 内部处理）
            var newTemplate = await CloneInternalAsync(
                source, source.FFlowName, source.FFlowCode, source.FDescription,
                asGlobalTemplate: true, operatorId);

            newTemplate.FStatus = "published";
            newTemplate.FIsTemplate = true;

            var templateVersion = await _dbContext.Set<CfFlowVersion>()
                .IgnoreQueryFilters()
                .FirstOrDefaultAsync(x => x.FFlowDefinitionId == newTemplate.FID);
            if (templateVersion != null)
            {
                templateVersion.FStatus = "published";
                templateVersion.FIsCurrentVersion = true;
                templateVersion.FPublishTime = DateTime.Now;
            }
            await _dbContext.SaveChangesAsync();

            _logger.LogInformation("Created new template {TemplateId} from source {SourceId} by operator {OperatorId}", newTemplate.FID, sourceDefinitionId, operatorId);
            return MapToDto(newTemplate);
        }
```
> 删除该分支内原先构造 `CloneFlowDefinitionRequest{OrgId=0}` + 调 `CloneFlowDefinitionAsync` + 用 `IgnoreQueryFilters().FirstAsync(result.Id)` 重新取定义的代码（现直接持有 `newTemplate` 实体）。`existingTemplate`（更新已存在模板）分支保持不变。

- [ ] **Step 4：运行确认通过**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64 --filter "FullyQualifiedName~SaveAsTemplate_PersistsGlobalOrgZero|FullyQualifiedName~Clone_NonTemplate"`
Expected: PASS（2 passed）。

- [ ] **Step 5：模块编译 + 全量回归**

Run: `dotnet build src/STOTOP.Module.CardFlow/STOTOP.Module.CardFlow.csproj`（0 错误）
Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`（绿）

- [ ] **Step 6：提交**

```bash
git add src/STOTOP.Module.CardFlow/Services/FlowDefinitionService.cs tests/STOTOP.Module.CardFlow.Tests/Rules/FlowDefinitionOrgIsolationTests.cs
git commit -m "fix(cardflow): SaveAsTemplate 新建分支经抑制作用域落 FOrgId=0,恢复 save-as-template(原 FillOrgId 覆写致 500)"
```

---

## Task 6：收尾验证

- [ ] **Step 1：全量回归 + 编译**

Run: `dotnet test tests/STOTOP.Module.CardFlow.Tests --arch x64`
Expected: 绿（既有 + 本计划新增 ~6 用例；需真 SQL Server 的集成测试若因无连接/host 环境失败属预存，与本改动无关——确认失败项均为 SQL Server 集成测试）。
Run: `dotnet build src/STOTOP.Module.CardFlow/STOTOP.Module.CardFlow.csproj` 与 `dotnet build src/STOTOP.Infrastructure/STOTOP.Infrastructure.csproj`
Expected: 0 错误。

- [ ] **Step 2：人工验收清单（记录确认，不必跑应用）**
- save-as-template：当前组织流程 → 生成 FOrgId=0 模板，不再 500。
- 从模板创建列表：只见全局模板，不见他组织伪模板。
- 克隆：落当前组织;以他组织私有流程 id 为源 → "源不存在"。

- [ ] **Step 3：如有收尾改动提交**（通常无）

---

## 自检（spec 覆盖）
- D1 克隆落当前组织 + 忽略 request.OrgId → Task 4。
- D2 SuppressOrgIdFill + 模板 FOrgId=0 → Task 2（基建）+ Task 5（应用）。
- D3 源读取隔离 → Task 4。
- #2 GetTemplates 限 FOrgId=0 → Task 3。
- 测试基建（假 accessor）→ Task 1。
- 非目标（不加 RequirePermission / 不删 OrgId 字段）→ 计划未触碰，符合。
